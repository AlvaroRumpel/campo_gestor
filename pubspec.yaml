name: campo_gestor
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # Cupertino icons (Flutter template default — kept).
  cupertino_icons: ^1.0.8

  # ─── State Management ───
  # Plan specified Riverpod 2.x but the full stack (build_runner 2.14+, json_serializable 6.13+,
  # freezed 3.x) requires analyzer/source_gen/build versions incompatible with riverpod 2.x tooling.
  # Resolution: flutter_riverpod 3.x (runtime) + riverpod_annotation 4.x + riverpod_generator 4.x.
  # No Riverpod code exists in Phase 0 so no API migration needed.
  flutter_riverpod: ">=3.0.0 <4.0.0"
  riverpod_annotation: ">=4.0.0 <5.0.0"

  # ─── Navigation (web-friendly per CLAUDE.md; latest stable) ───
  go_router: ^17.2.0

  # ─── Backend client ───
  supabase_flutter: ^2.12.0

  # ─── Data classes & serialization (Freezed 3.x per RESEARCH.md Standard Stack) ───
  freezed_annotation: ^3.0.0
  json_annotation: ^4.11.0

  # ─── Local storage (Hive/Isar PROHIBITED in MVP — see CLAUDE.md What NOT to Use) ───
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.5.0

  # ─── UI helpers ───
  flutter_svg: ^2.2.0
  data_table_2: ^2.7.0

  # ─── Web URL strategy (usePathUrlStrategy — Pitfall 7) ───
  flutter_web_plugins:
    sdk: flutter

  # ─── Localization (pt-BR per D-13) ───
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # ─── Codegen (per D-10 — pipeline configured but unused until Phase 1) ───
  build_runner: ^2.14.0
  freezed: ^3.2.0
  json_serializable: ^6.13.0
  riverpod_generator: ">=4.0.0 <5.0.0"

  # ─── Lints ───
  # NOTE: custom_lint + riverpod_lint are incompatible with flutter_riverpod 3.x + freezed 3.x +
  # json_serializable 6.13.x due to transitive analyzer/freezed_annotation version conflicts.
  # Deferred until the ecosystem aligns. Riverpod rules enforced via flutter_lints + code review.
  # custom_lint: ^0.6.0   # BLOCKED: see deviation note in 00-03-SUMMARY.md
  # riverpod_lint: ^2.3.0 # BLOCKED: depends on custom_lint

  # ─── Mocking ───
  mocktail: ^1.0.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Redesign fonts (bundled — google_fonts runtime fetch prohibited on web).
  fonts:
    - family: Archivo
      fonts:
        - asset: assets/fonts/Archivo-400.ttf
          weight: 400
        - asset: assets/fonts/Archivo-500.ttf
          weight: 500
        - asset: assets/fonts/Archivo-600.ttf
          weight: 600
        - asset: assets/fonts/Archivo-700.ttf
          weight: 700
    - family: IBMPlexMono
      fonts:
        - asset: assets/fonts/IBMPlexMono-400.ttf
          weight: 400
        - asset: assets/fonts/IBMPlexMono-500.ttf
          weight: 500
        - asset: assets/fonts/IBMPlexMono-600.ttf
          weight: 600
        - asset: assets/fonts/IBMPlexMono-700.ttf
          weight: 700
