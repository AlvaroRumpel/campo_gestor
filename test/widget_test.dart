// Placeholder widget test — the default Flutter counter app has been replaced
// by CampoGestorApp. Full widget tests will be added in Phase 1 screens.
// Integration smoke test lives in integration_test/app_smoke_test.dart.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — no widget tests in Phase 0', () {
    // intentionally empty
  });
}
