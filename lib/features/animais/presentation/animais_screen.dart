import 'package:flutter/material.dart';

class AnimaisScreen extends StatelessWidget {
  const AnimaisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Animais', style: TextStyle(fontSize: 24)),
    );
  }
}
