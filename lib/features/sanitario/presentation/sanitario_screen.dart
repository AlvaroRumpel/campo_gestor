import 'package:flutter/material.dart';

class SanitarioScreen extends StatelessWidget {
  const SanitarioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sanitário', style: TextStyle(fontSize: 24)),
    );
  }
}
