import 'package:flutter/material.dart';

class ReproducaoScreen extends StatelessWidget {
  const ReproducaoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Reprodução', style: TextStyle(fontSize: 24)),
    );
  }
}
