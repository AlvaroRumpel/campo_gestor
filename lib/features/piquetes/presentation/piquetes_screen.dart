import 'package:flutter/material.dart';

class PiquetesScreen extends StatelessWidget {
  const PiquetesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Piquetes', style: TextStyle(fontSize: 24)),
    );
  }
}
