---
phase: quick-260813-v19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/piquetes/presentation/piquetes_lotes_header.dart
  - lib/features/piquetes/presentation/piquetes_board_view.dart
  - lib/features/piquetes/presentation/piquetes_screen.dart
  - lib/features/lotes/presentation/lotes_table_view.dart
  - lib/features/lotes/presentation/mover_lote_dialog.dart
  - test/widget/piquetes_board_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Em >=1024px, a aba Piquetes de /piquetes mostra um quadro com uma coluna por piquete, não a lista de cards mobile."
    - "O header de cada coluna mostra nome, ha, 'UA atual / capacidade' em mono (vermelho quando acima), UA/ha e uma barra fina de semáforo; piquete acima da capacidade tem o header em dangerContainer com ícone de aviso."
    - "Cards de lote são arrastáveis entre colunas quando o usuário pode editar; soltar num piquete diferente abre o fluxo de mover lote já existente com o piquete de destino pré-selecionado."
    - "Durante o arrasto, toda coluna elegível mostra uma drop-zone tracejada com o preview 'fica X / Y UA' colorido pelo semáforo de lotação."
    - "Clicar (sem arrastar) num card abre o LoteDetailPanel de 380px existente e o quadro continua visível ao lado."
    - "Abaixo de 1024px a aba Piquetes continua byte a byte a lista de _PaddockCard atual."
  artifacts:
    - lib/features/piquetes/presentation/piquetes_lotes_header.dart
    - lib/features/piquetes/presentation/piquetes_board_view.dart
    - test/widget/piquetes_board_test.dart
  key_links:
    - "PiquetesScreen -> LayoutBuilder(Breakpoints.rail) -> Row(PiquetesBoardView | LotesTableView, LoteDetailPanel)"
    - "PiquetesBoardView.onMoveLot -> MoverLoteDialog(initialPaddock:) -> LoteRepository.moveLot (RPC move_lot_to_paddock já existente)"
    - "PiquetesLotesHeader reusado por LotesTableView e PiquetesBoardView — um único header de tela para as duas abas desktop"
    - "PiquetesBoardView -> paddockListProvider + loteWithPaddockListByPropertyProvider + animalListByPropertyProvider (derivados na tela, zero provider novo)"
  prohibitions:
    - "Nenhum package novo: drag-and-drop usa Draggable/DragTarget do próprio Flutter (pubspec.yaml e pubspec.lock sem diff)."
    - "Nenhum método novo em LoteRepository, PaddockRepository ou AnimalRepository."
    - "Nenhum arquivo de teste existente pode ser editado (git diff --exit-code sobre eles precisa passar)."
    - "Nenhum literal de cor fora de AppColors, exceto `Colors.transparent`."
    - "Nenhuma mudança de comportamento abaixo de 1024px, em nenhuma das duas abas."
---

<objective>
Levar a aba **Piquetes** da tela `/piquetes` para o quadro (kanban) desktop aprovado no mockup "2c Piquetes — quadro": a partir de `Breakpoints.rail` (1024px) a lista de cards vira um quadro de colunas (uma por piquete) com cards de lote arrastáveis, e soltar um card em outra coluna dispara o fluxo de mover lote que já existe.

Purpose: fechar a última aba que ainda cai no layout mobile em telas largas e transformar "mover lote" — hoje 4 toques via painel → diálogo → picker → confirmar — num gesto direto, com o efeito na lotação visível **antes** de soltar.
Output: `piquetes_lotes_header.dart` (header compartilhado extraído), `piquetes_board_view.dart` (o quadro), o branch desktop unificado em `piquetes_screen.dart`, um parâmetro opcional em `mover_lote_dialog.dart` e `test/widget/piquetes_board_test.dart`.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Tela a estender (já tem o `LayoutBuilder` e o branch desktop da aba Lotes — quick 260813-ugd):
@lib/features/piquetes/presentation/piquetes_screen.dart

Padrões desktop do 260813-ugd a reusar tal e qual:
@lib/features/lotes/presentation/lotes_table_view.dart
@lib/features/lotes/presentation/lote_detail_panel.dart

Fluxo de mover lote existente (o único caminho de escrita deste task):
@lib/features/lotes/presentation/mover_lote_dialog.dart
@lib/features/lotes/data/lote_repository.dart

Dados e tema:
@lib/features/piquetes/data/piquete_model.dart
@lib/features/animais/data/animal_model.dart
@lib/features/animais/data/animal_constants.dart
@lib/core/theme/app_colors.dart
@lib/core/theme/breakpoints.dart
@lib/core/widgets/ui.dart

Teste-molde (harness `setSurfaceSize` + overrides de provider + router stub):
@test/widget/lotes_desktop_test.dart
</context>

<planner_assumptions>
Levantamento feito antes de planejar — decisões que o executor **não** precisa reabrir:

| Item | Fonte / decisão |
|---|---|
| UA e capacidade por piquete | Soma de `calcTotalUa` dos lotes da coluna (`animalsByLot`) vs `paddock.uaCapacity`. Mesma regra `ratio >= 1.0` do `_PaddockCard` — nada de provider novo. |
| Barra de semáforo fina | `CapacityBar(height: 4)` de `ui.dart` — já usa `AppColors.capacityColor` internamente. Zero widget novo. |
| Header da tela (título/subtítulo/segmented) | Hoje está **embutido** em `LotesTableView`. Extraído para `PiquetesLotesHeader` e reusado pelas duas abas desktop, em vez de duplicar ~55 linhas no quadro. |
| CTA do header no quadro | O header ganha um `action` **opcional**. `LotesTableView` continua passando o "Novo lote"; o quadro passa `null` e mantém o FAB "Piquete" atual — a aba Piquetes cria piquete, não lote. Toggle Quadro/Tabela: **não implementado** (o segmented Piquetes/Lotes já é o único eixo de troca; a aba Lotes é a tabela). |
| Confirmação ao soltar | O fluxo atual **exige** confirmação (`Confirmar movimentação`). Em vez de duplicar o try/catch + as 5 invalidações do `MoverLoteDialog._submit` no quadro, o diálogo existente ganha um `initialPaddock` opcional e abre já pré-selecionado. Diff de 6 linhas no diálogo, zero lógica de escrita nova. |
| Dashed drop-zone | Sem package: `CustomPainter` de ~15 linhas com `Path.computeMetrics()/extractPath` sobre um `RRect`. |
| Clique vs. arrasto no web | `Draggable` usa `ImmediateMultiDragGestureRecognizer`, que só vence a arena quando o ponteiro se move — um clique parado cai no `InkWell` do card. O teste 2 trava esse comportamento. |
| Gate de permissão | Card só vira `Draggable` quando `canEdit` (mesmo gate `veterinarian` da tela). Sem `canEdit` o quadro é somente leitura. |
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Extrair PiquetesLotesHeader do LotesTableView</name>
  <files>lib/features/piquetes/presentation/piquetes_lotes_header.dart, lib/features/lotes/presentation/lotes_table_view.dart</files>
  <action>
Criar `lib/features/piquetes/presentation/piquetes_lotes_header.dart` com `PiquetesLotesHeader` (`StatelessWidget`), movendo para lá **exatamente** as duas primeiras faixas do `build` de `LotesTableView` (o `Padding(fromLTRB(16, 14, 16, 10))` do título/subtítulo/CTA e o `Padding(fromLTRB(16, 0, 16, 10))` dos chips do segmented), sem mudar um pixel do resultado renderizado.

Parâmetros: `paddocks` (`List<Paddock>`), `animalsByLot` (`Map<String, List<Animal>>`), `paddockCount` (`int`), `lotCount` (`int`), `showLots` (`bool`), `onShowLotsChanged` (`ValueChanged<bool>`) e `action` (`Widget?`, opcional).

Corpo: `Column(crossAxisAlignment: stretch, mainAxisSize: min)` com

1. Faixa do título: título `'Piquetes e lotes'` 24/w600 num `Expanded` + subtítulo `'{uaOcupada} / {capacidadeTotal} UA · {ha} ha · {uaPorHa} UA/ha'` em `monoStyle(size: 13, color: AppColors.textSecondary)`. Os agregados são calculados aqui, com a mesma aritmética que hoje está no topo do `build` de `LotesTableView`: `uaOcupada` = soma de `calcTotalUa` sobre os values de `animalsByLot`; `capacidadeTotal` e `haTotal` = folds de `paddocks`; `uaPorHa` = `haTotal > 0 ? uaOcupada / haTotal : 0`. Helper local `_fmt1` (1 casa, vírgula decimal), igual ao dos outros arquivos. À direita, quando `action != null`, um `SizedBox(width: 12)` + o próprio `action`.
2. Faixa do segmented: `Row` com dois `AtfScopeChip` (`label: 'Piquetes'`, `count: paddockCount`, `selected: !showLots`) e (`label: 'Lotes'`, `count: lotCount`, `selected: showLots`), separados por `SizedBox(width: 8)`, chamando `onShowLotsChanged(false)` / `onShowLotsChanged(true)`. `AtfScopeChip` continua vindo do mesmo import de hoje (`reproducao_table_view.dart`).

Em `lotes_table_view.dart`: substituir as duas faixas pelo novo widget —
`PiquetesLotesHeader(paddocks: widget.paddocks, animalsByLot: widget.animalsByLot, paddockCount: widget.paddockCount, lotCount: widget.lotCount, showLots: widget.showLots, onShowLotsChanged: widget.onShowLotsChanged, action: widget.canEdit ? FilledButton.icon(onPressed: widget.onCreate, icon: const Icon(Icons.add, size: 20), label: const Text('Novo lote')) : null)`.
A assinatura pública de `LotesTableView` **não muda** (mesmos parâmetros, mesma ordem) — só o miolo do `build`. Remover de `lotes_table_view.dart` o cálculo dos agregados (`uaOcupada`, `capacidadeTotal`, `haTotal`, `uaPorHa`), que passou para o header, e o import de `AtfScopeChip` se ficar sem uso. Manter o `_fmt1` local só se ainda houver uso.

Nenhum literal de cor fora de `AppColors`. Todo número em `monoStyle`.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos lib/features/piquetes/presentation/piquetes_lotes_header.dart lib/features/lotes/presentation/lotes_table_view.dart</automated>
    <automated>[ "$(grep -c 'AtfScopeChip' lib/features/piquetes/presentation/piquetes_lotes_header.dart)" -ge 2 ]</automated>
    <automated>[ "$(grep -c 'PiquetesLotesHeader' lib/features/lotes/presentation/lotes_table_view.dart)" -ge 1 ]</automated>
    <automated>flutter test test/widget/lotes_desktop_test.dart</automated>
  </verify>
  <done>`piquetes_lotes_header.dart` exporta `PiquetesLotesHeader` com `action` opcional, `LotesTableView` o consome sem mudar a própria assinatura, e `lotes_desktop_test.dart` (que valida o subtítulo mono e o segmented) continua verde sem edição.</done>
</task>

<task type="auto">
  <name>Task 2: PiquetesBoardView — quadro de colunas com drag-and-drop</name>
  <files>lib/features/piquetes/presentation/piquetes_board_view.dart</files>
  <action>
Criar `PiquetesBoardView` (`StatefulWidget`; o único estado é o lote em arrasto). Widget puro de apresentação: **não** lê `ref`, **não** conhece repositório nem diálogo — recebe dados prontos e devolve callbacks.

Parâmetros: `paddocks` (`List<Paddock>`), `lots` (`List<LotWithPaddockCount>`), `animalsByLot` (`Map<String, List<Animal>>`, já filtrado por `deletedAt == null`), `onShowLotsChanged` (`ValueChanged<bool>`), `selectedLotId` (`String?`), `onSelectLot` (`ValueChanged<String?>`), `onMoveLot` (`void Function(LotWithPaddockCount lot, Paddock target)`) e `canEdit` (`bool`).

Estrutura: `ColoredBox(color: AppColors.surface)` → `Column(crossAxisAlignment: stretch)` com
- `PiquetesLotesHeader(paddocks:, animalsByLot:, paddockCount: paddocks.length, lotCount: lots.length, showLots: false, onShowLotsChanged:, action: null)` — o CTA fica com o FAB "Piquete" da tela;
- `Expanded(Padding(fromLTRB(16, 4, 16, 12), Row(crossAxisAlignment: stretch, children: [...])))` com um `Expanded(_PaddockColumn(...))` por piquete e `SizedBox(width: 12)` entre eles (flex igual, gap 12, conforme o mockup);
- quando `paddocks` está vazio, o `EmptyState` que a tela já usa na aba Piquetes (`Icons.fence_outlined`, mesmo título e mensagem) no lugar do `Row`.

Estado: `LotWithPaddockCount? _dragging`, setado em `onDragStarted` e zerado em `onDragEnd` do `Draggable`.

**`_PaddockColumn`** (privado) recebe `paddock`, os lotes daquele piquete (`lots.where((l) => l.lot.paddockId == paddock.id)`, ordenados por UA desc e nome como desempate), `animalsByLot`, `dragging`, `selectedLotId`, `onSelectLot`, `onMoveLot`, `canEdit` e os dois callbacks de drag. Renderiza `Container(decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.circular(14), border: Border.all(color: AppColors.divider)))` com `Column(crossAxisAlignment: stretch)`:

1. **Header da coluna** — `Container(padding: EdgeInsets.all(10))` com `color: over ? AppColors.dangerContainer : AppColors.surfaceVariant` e cantos superiores arredondados em 14 (`borderRadius: vertical(top: Radius.circular(14))`), onde `ua` = soma de `calcTotalUa` dos lotes da coluna, `ratio` = `paddock.uaCapacity > 0 ? ua / paddock.uaCapacity : 0`, `over` = `ratio >= 1.0`:
   - `Row`: `Expanded(Text(paddock.name, 15/w700, ellipsis, color: over ? AppColors.onDangerContainer : AppColors.ink))` + `Text('{ha} ha', monoStyle(11.5, color: AppColors.textSecondary))` + quando `over`, `SizedBox(width: 4)` + `Icon(Icons.warning_amber_rounded, size: 16, color: AppColors.danger)`.
   - `SizedBox(height: 6)` + `Row`: `Text('{ua} / {capacidade} UA', monoStyle(12.5, w600, color: over ? AppColors.danger : AppColors.ink))` + `Spacer` + `Text('{uaHa} UA/ha', monoStyle(11.5, color: AppColors.textSecondary))`, com `uaHa` = `paddock.areaHa > 0 ? ua / paddock.areaHa : 0`.
   - `SizedBox(height: 8)` + `CapacityBar(current: ua, capacity: paddock.uaCapacity, height: 4)`.
   Helper local `_fmt1` para todos esses números.

2. **Corpo** — `Expanded(DragTarget<LotWithPaddockCount>(...))`:
   - `onWillAcceptWithDetails: (d) => d.data.lot.paddockId != paddock.id`
   - `onAcceptWithDetails: (d) => onMoveLot(d.data, paddock)`
   - `builder`: `ListView(key: ValueKey('board-drop-${paddock.id}'), padding: EdgeInsets.all(8), children: [...cards, if (showDropZone) dropZone])`. A `key` é o alvo do teste de arrasto — não removê-la. O `ListView` preenche o `Expanded` inteiro, então a coluna aceita drop também quando está vazia.
   - `showDropZone` = `dragging != null && dragging.lot.paddockId != paddock.id`.

3. **Drop-zone** (`_DropZone`, privado) — `Container(height: 56)` desenhado por `CustomPaint(painter: _DashedBorderPainter(color: previewColor, radius: 12))`, centro com `Text('fica {uaResultante} / {capacidade} UA', monoStyle(12.5, w600, color: previewColor))`, onde `uaResultante` = UA da coluna + UA do lote arrastado e `previewColor` = `AppColors.capacityColor(capacidade > 0 ? uaResultante / capacidade : 0)` — verde quando cabe, laranja perto do limite, vermelho quando estoura.

4. **`_DashedBorderPainter`** — pinta um `RRect` tracejado: `Path()..addRRect(...)`, e para cada `metric` em `path.computeMetrics()` percorrer o comprimento extraindo traços de 6px a cada 10px (`canvas.drawPath(metric.extractPath(d, d + 6), paint)`), `paint` com `style: PaintingStyle.stroke`, `strokeWidth: 1.5` e a cor recebida. `shouldRepaint` compara a cor.

5. **Card do lote** (`_LotCard`, privado) — `Container(margin: EdgeInsets.only(bottom: 8), decoration: BoxDecoration(color: AppColors.surfaceVariant, borderRadius: BorderRadius.circular(12), border: selected ? Border.all(color: AppColors.primary, width: 2) : null))` com `InkWell(borderRadius: 12, onTap: () => onSelectLot(item.lot.id))` sobre `Padding(all: 10)` → `Row`: `Icon(Icons.drag_indicator, size: 18, color: AppColors.textTertiary)` + `SizedBox(width: 6)` + `Expanded(Column(crossAxisAlignment: start, children: [Text(nome, 14/w700, ellipsis), SizedBox(height: 2), Text('{n} an. · {ua} UA', monoStyle(12, color: AppColors.textSecondary))]))`, com `n` = `animalsByLot[item.lot.id]?.length ?? 0` e `ua` = `calcTotalUa` desses animais.

6. **Arrasto** — quando `canEdit`, cada card é envolvido em
   `Draggable<LotWithPaddockCount>(data: item, onDragStarted:, onDragEnd:, feedback: Material(color: Colors.transparent, elevation: 6, borderRadius: BorderRadius.circular(12), child: SizedBox(width: 240, child: card)), childWhenDragging: Opacity(opacity: 0.35, child: card), child: card)`.
   Sem `canEdit`, o card é renderizado direto, sem `Draggable` — quadro somente leitura. Não usar `LongPressDraggable`: no web o arrasto com mouse precisa ser imediato, e o clique continua chegando ao `InkWell` porque o reconhecedor de arrasto só vence a arena quando o ponteiro se move.

Nenhum package novo. Nenhum literal de cor fora de `AppColors` — a única exceção é o `Colors.transparent` do `Material` de feedback. Todo número em `monoStyle`.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos lib/features/piquetes/presentation/piquetes_board_view.dart</automated>
    <automated>[ "$(grep -c 'Draggable<LotWithPaddockCount>\|DragTarget<LotWithPaddockCount>' lib/features/piquetes/presentation/piquetes_board_view.dart)" -ge 2 ]</automated>
    <automated>[ "$(grep -c "board-drop-" lib/features/piquetes/presentation/piquetes_board_view.dart)" -ge 1 ]</automated>
    <automated>[ "$(grep -v '^\s*//' lib/features/piquetes/presentation/piquetes_board_view.dart | grep -oE '(^|[^A-Za-z.])Colors\.[A-Za-z]+|Color\(0x' | grep -cv 'Colors\.transparent')" -eq 0 ]</automated>
    <automated>git diff --exit-code -- pubspec.yaml pubspec.lock</automated>
  </verify>
  <done>`piquetes_board_view.dart` compila limpo, exporta `PiquetesBoardView` sem depender de `ref`/repositório, usa `Draggable`/`DragTarget` nativos com a key de drop por piquete, não introduz package nem cor literal.</done>
</task>

<task type="auto">
  <name>Task 3: Ligar o quadro na PiquetesScreen, pré-selecionar o piquete no MoverLoteDialog e testar</name>
  <files>lib/features/piquetes/presentation/piquetes_screen.dart, lib/features/lotes/presentation/mover_lote_dialog.dart, test/widget/piquetes_board_test.dart</files>
  <action>
**(a) `mover_lote_dialog.dart`** — adicionar o parâmetro opcional `final Paddock? initialPaddock;` ao construtor (importando `../../piquetes/data/piquete_model.dart`) e um `initState` que faz `_selectedPaddockId = widget.initialPaddock?.id;` e `_selectedPaddockName = widget.initialPaddock?.name;`. Nada mais muda: o `_PaddockPickerList` já destaca o `selectedPaddockId` recebido e o botão `Confirmar movimentação` já habilita quando há seleção, então o diálogo abre funcionando como confirmação do destino. Como o parâmetro é opcional, todas as chamadas atuais e `mover_lote_dialog_test.dart` seguem válidas sem edição.

**(b) `piquetes_screen.dart`** — unificar o branch desktop. Hoje `showDesktopLots = isDesktop && _showLots`; passa a ser **`isDesktop`** para as duas abas: renomear `_buildDesktopLots` para `_buildDesktop` e, dentro dele, trocar o lado esquerdo do `Row` por
`Expanded(child: _showLots ? LotesTableView(...) : PiquetesBoardView(paddocks:, lots:, animalsByLot:, onShowLotsChanged: (v) => setState(() => _showLots = v), selectedLotId: _selectedLotId, onSelectLot: (id) => setState(() => _selectedLotId = id), onMoveLot: (item, target) => _onDropLot(context, item, target), canEdit: canEdit))`.
O `LoteDetailPanel` do lado direito, o cálculo de `animalsByLot`, `overloadedPaddockIds`, `lastApplicationByLot` e o `selected` permanecem exatamente como estão — o painel passa a servir as duas abas. O `Column` mobile (segmented de 42px + lista de `_PaddockCard` / `LotesListView`) fica intocado, e o gate do FAB (`canEdit && !_showLots`) também: na aba Piquetes desktop o FAB "Piquete" continua sendo o caminho de criação.

Adicionar o método privado `_onDropLot(BuildContext context, LotWithPaddockCount item, Paddock target)`: abre `showAdaptiveForm<Map<String, String>>(context: context, builder: (_) => MoverLoteDialog(lot: item.lot, activeAnimalCount: item.activeAnimalCount, initialPaddock: target))` e, quando o resultado não for nulo e o widget ainda estiver montado, invalida `loteWithPaddockListByPropertyProvider`, `animalListByPropertyProvider` e `paddockListProvider`, e mostra o `SnackBar` `'Lote movido para {paddockName}'` — o mesmo texto de `LoteDetailPanel._onMover`. Toda a escrita, o tratamento de erro e as invalidações de `loteByIdProvider`/`loteListByPaddockProvider` continuam dentro do diálogo: a tela **não** pode chamar o repositório de lotes para mover — o único caminho de escrita é o diálogo (há um gate de verificação sobre isso).

**(c) `test/widget/piquetes_board_test.dart`** — arquivo novo, harness e fixtures copiados de `lotes_desktop_test.dart` (mesmo `ProviderScope` de overrides, mesmo `GoRouter` stub, mesmos `_pumpDesktop`/`_pumpMobile`), com **dois** piquetes: `pad-1` "Piquete 1" (8,5 ha, capacidade 12) com `lot-a` (2 vacas) e `lot-b` (1 terneiro), e `pad-2` "Piquete 2" (5,0 ha, capacidade 6) **sem lotes** — a coluna vazia é o alvo do arrasto. `paddockListProvider` retorna os dois.

Testes (mínimo 5, todos na aba Piquetes, que é a default — sem tocar no segmented):
1. `1440x900`: `find.byType(PiquetesBoardView)` findsOneWidget; os nomes dos dois piquetes aparecem; `find.byIcon(Icons.drag_indicator)` findsNWidgets(2) (um por lote).
2. Clique simples em `find.text('Lote A')` → `find.byType(LoteDetailPanel)` findsOneWidget e `find.byType(PiquetesBoardView)` continua findsOneWidget (o clique não é engolido pelo `Draggable`).
3. Arrasto: `final gesture = await tester.startGesture(tester.getCenter(find.text('Lote A')));` → `await gesture.moveBy(const Offset(0, 24)); await tester.pump();` → `await gesture.moveTo(tester.getCenter(find.byKey(const ValueKey('board-drop-pad-2')))); await tester.pump();` → nesse ponto `find.textContaining('fica')` findsWidgets (a drop-zone com preview) → `await gesture.up(); await tester.pumpAndSettle();` → `find.byType(MoverLoteDialog)` findsOneWidget e `find.text('Confirmar movimentação')` findsOneWidget. **Não** tocar em Confirmar (não há Supabase no teste).
4. Sem permissão: mesma árvore com `memberPropertiesProvider` retornando `role: 'proprietario'` → `find.byType(Draggable<LotWithPaddockCount>)` findsNothing e o quadro continua renderizando (`PiquetesBoardView` findsOneWidget).
5. `800x600`: `find.byType(PiquetesBoardView)` findsNothing e a lista mobile continua (`find.text('hectares')` findsWidgets, label que só existe no `_PaddockCard`).
6. Segmented do header compartilhado no quadro: em `1440x900`, tocar no chip `'Lotes'` troca para `find.byType(LotesTableView)` findsOneWidget e `PiquetesBoardView` findsNothing.

**Não editar nenhum arquivo de teste existente.** Se `piquetes_screen_test.dart` ou `lotes_desktop_test.dart` quebrarem, a causa é regressão — corrigir o código de produção.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos</automated>
    <automated>flutter test</automated>
    <automated>git diff --exit-code -- test/widget/piquetes_screen_test.dart test/widget/lotes_desktop_test.dart test/widget/mover_lote_dialog_test.dart test/widget/lote_detail_screen_test.dart test/widget/lote_form_dialog_test.dart</automated>
    <automated>[ "$(grep -c 'testWidgets(' test/widget/piquetes_board_test.dart)" -ge 6 ]</automated>
    <automated>[ "$(grep -c 'PiquetesBoardView' lib/features/piquetes/presentation/piquetes_screen.dart)" -ge 1 ]</automated>
    <automated>[ "$(grep -c 'initialPaddock' lib/features/lotes/presentation/mover_lote_dialog.dart)" -ge 2 ]</automated>
    <automated>[ "$(grep -F '.moveLot(' lib/features/piquetes/presentation/piquetes_screen.dart | grep -cv onMoveLot)" -eq 0 ]</automated>
  </verify>
  <done>`flutter analyze --no-fatal-infos` limpo, `flutter test` verde no suite inteiro, nenhum teste pré-existente modificado, o quadro aparece em 1440x900 na aba Piquetes com clique→painel e arrasto→diálogo pré-selecionado, e 800x600 continua na lista atual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → Supabase (PostgREST/RPC) | Único caminho de escrita: `move_lot_to_paddock`, já existente e protegido por RLS. Este task não abre caminho novo. |
| Interação de arrasto → mutação | Novo gesto que dispara uma mutação; a autorização real continua no servidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v19-01 | Elevation of Privilege | `PiquetesBoardView` drag | medium | mitigate | Card só vira `Draggable` quando `canEdit` (papel `veterinarian`); o RPC `move_lot_to_paddock` + RLS continuam sendo a autoridade final. Teste 4 trava o gate. |
| T-v19-02 | Tampering | Movimentação acidental por arrasto | low | mitigate | Soltar não escreve: abre o `MoverLoteDialog` existente com o destino pré-selecionado e exige `Confirmar movimentação`. Teste 3 trava esse contrato. |
| T-v19-03 | Denial of Service | Colunas por piquete | low | accept | O quadro renderiza uma coluna por piquete da propriedade; a lista já é carregada pela tela hoje, sem query adicional. |
| T-v19-SC | Tampering | npm/pub installs | high | mitigate | Nenhum package novo — `git diff --exit-code -- pubspec.yaml pubspec.lock` é gate da Task 2. Sem instalação, sem superfície de supply-chain. |
</threat_model>

<verification>
1. `flutter analyze --no-fatal-infos` sem erros nem warnings novos.
2. `flutter test` verde no suite inteiro (inclui `piquetes_screen_test.dart`, `lotes_desktop_test.dart` e `mover_lote_dialog_test.dart` sem edição).
3. `git diff --exit-code` limpo sobre os testes pré-existentes e sobre `pubspec.yaml`/`pubspec.lock`.
4. `test/widget/piquetes_board_test.dart` com >= 5 `testWidgets`, cobrindo quadro em 1440x900, clique→painel, arrasto→drop-zone→diálogo, gate de permissão e lista mobile intacta em 800x600.
5. Nenhum literal de cor fora de `AppColors` nos arquivos novos (exceto `Colors.transparent`).
</verification>

<success_criteria>
- Em >= 1024px a aba Piquetes renderiza `PiquetesBoardView` com uma coluna por piquete, header de semáforo e cards de lote.
- Arrastar um card para outra coluna mostra a drop-zone tracejada com o preview de lotação e, ao soltar, abre o `MoverLoteDialog` com o piquete de destino já selecionado.
- Clicar num card abre o `LoteDetailPanel` de 380px existente.
- Abaixo de 1024px nada muda em nenhuma das duas abas.
- Zero package novo, zero método novo de repositório, zero teste existente editado.
</success_criteria>

<output>
Create `.planning/quick/260813-v19-piquetes-em-quadro-kanban-desktop-coluna/260813-v19-SUMMARY.md` when done
</output>
