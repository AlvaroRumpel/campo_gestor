---
phase: quick-260814-d79
plan: 01
subsystem: ui
tags: [flutter, riverpod, design-system, gastos]

requires: []
provides:
  - "GastosPropertyScreen desktop (>=Breakpoints.rail) recomposto no molde mestre-detalhe de Animais/Reprodução/Lotes"
affects: [gastos, ui-consistency]

tech-stack:
  added: []
  patterns:
    - "Painel de detalhe ancorado: Container(width: 380, border-left: AppColors.divider), molde LoteDetailPanel/AnimalDetailPanel"
    - "_PeriodChip: clone visual de _ScopeChip (animais_table_view.dart) para filtros de período pill em vez de dropdown no desktop"
    - "Corpo full-bleed: sem padding lateral, border-top separando do header, ColoredBox(surface) único cobrindo tabela e EmptyState"
    - "Rodapé de tabela: 'N lançamentos · R$ total' mono alinhado à direita (molde AnimaisTableView), sem rótulo TOTAL"

key-files:
  created: []
  modified:
    - lib/features/gastos/presentation/gastos_property_screen.dart
    - test/widget/gastos_property_screen_test.dart

key-decisions:
  - "Fundo do painel é AppColors.background (osso), não AppColors.surface — o token 'AppColors.bg' citado no briefing não existe, e fundo branco faria os SectionCard internos perderem definição"
  - "_buildPeriodDropdown foi mantido no arquivo (usado só por _buildMobile); os 4 chips substituem apenas o caminho desktop"

patterns-established:
  - "Painel de detalhe ancorado à borda direita (380px + border-left) é o molde padrão de toda tela mestre-detalhe da aplicação"

requirements-completed: [GAST-01]

coverage:
  - id: D1
    description: "Painel de resumo ancorado à borda direita em >=1024, 380px de largura, borda esquerda de 1px, sem gap/padding sobrando"
    requirement: "GAST-01"
    verification:
      - kind: automated_ui
        ref: "test/widget/gastos_property_screen_test.dart#1440x900: tabela + painel ancorado de 380px renderizam, total do rodapé bate com o subtítulo do header"
        status: pass
    human_judgment: false
  - id: D2
    description: "Filtro de período no desktop é uma linha de 4 chips pill que refiltra a tabela; dropdown continua ativo em <1024"
    requirement: "GAST-01"
    verification:
      - kind: automated_ui
        ref: "test/widget/gastos_property_screen_test.dart#1440x900: chips de período substituem o dropdown e refiltram a tabela"
        status: pass
      - kind: automated_ui
        ref: "test/widget/gastos_property_screen_test.dart#800x600: sem tabela (nenhum cabeçalho DATA), ExpenseListItemCard presente — caminho mobile intacto"
        status: pass
    human_judgment: false
  - id: D3
    description: "Corpo desktop full-bleed com border-top separando do header; rodapé da tabela mostra 'N lançamentos · R$ total' mono à direita"
    requirement: "GAST-01"
    verification:
      - kind: automated_ui
        ref: "test/widget/gastos_property_screen_test.dart#1440x900: tabela + painel ancorado de 380px renderizam, total do rodapé bate com o subtítulo do header"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-14
status: complete
---

# Quick Task 260814-d79 Summary

**GastosPropertyScreen desktop recomposto no molde mestre-detalhe (painel 380px ancorado, chips de período, corpo full-bleed) sem tocar em providers, cálculos ou no caminho mobile**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2/2 completed
- **Files modified:** 2

## Accomplishments
- Painel direito agora é um `Container(width: 380)` com borda esquerda de 1px, ancorado à borda direita da tela — sem `SizedBox`/`Padding` soltos ao redor, igual a `LoteDetailPanel`/`AnimalDetailPanel`
- Dropdown de período no desktop trocado por 4 `_PeriodChip` (clone visual de `_ScopeChip`), que refiltram a tabela de verdade (coberto por teste que troca para "Mês passado" e verifica `EmptyState`)
- Corpo full-bleed: tabela encosta na borda esquerda, separada do header por `border-top`, com `ColoredBox(surface)` único cobrindo tanto a tabela quanto o `EmptyState`
- Rodapé da tabela extraiu `_countLabel` (reusado no subtítulo do header) e passou a mostrar `'N lançamentos · R$ total'` mono à direita, sem o rótulo `TOTAL`
- Caminho mobile (`_buildMobile`, `_buildPeriodDropdown`) permanece intocado — teste dedicado confirma dropdown presente e chips ausentes em <1024

## Task Commits

1. **Task 1: Recompor o desktop de GastosPropertyScreen no molde mestre-detalhe** - `c529372` (feat)
2. **Task 2: Atualizar o teste de widget para os seletores novos** - `bb9debe` (test)

## Files Created/Modified
- `lib/features/gastos/presentation/gastos_property_screen.dart` - painel ancorado 380px, `_PeriodChip`, corpo full-bleed, `_countLabel`/rodapé com contagem
- `test/widget/gastos_property_screen_test.dart` - seletores atualizados para o layout novo (painel por key, chips, rodapé sem "TOTAL"), novo teste de refiltragem por chip, asserção extra no caminho mobile

## Decisions Made
- Fundo do painel é `AppColors.background` (osso), não `surface` — desvio deliberado do briefing (que citava um token inexistente `AppColors.bg`), necessário porque o conteúdo é uma pilha de `SectionCard` brancos que perderiam definição sobre fundo branco
- `_buildPeriodDropdown` foi mantido no arquivo por ser usado exclusivamente por `_buildMobile`; nenhuma duplicação de lógica de filtro foi criada — chips e dropdown chamam o mesmo `setState(() => _preset = ...)`

## Deviations from Plan

None - plan executado como escrito. A única divergência da descrição textual do briefing (cor do painel, token `AppColors.bg`) já estava antecipada e resolvida pelo próprio PLAN.md (ver `_buildPanel` action, item 1), não é uma decisão nova do executor.

## Issues Encountered
- Worktree veio sem código gerado (freezed/riverpod/json_serializable). `flutter pub get && flutter pub run build_runner build --delete-conflicting-outputs` foi necessário antes de `flutter analyze` funcionar (conforme instrução de dispatch) — não é um desvio do plano, é setup de ambiente.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `GastosPropertyScreen` desktop agora segue o mesmo molde visual de Animais/Reprodução/Lotes; nenhuma mudança pendente identificada para esta tela.
- `flutter analyze --no-fatal-infos` limpo nos arquivos tocados; suíte completa (`flutter test`, 422 testes) verde.

---
*Quick task: 260814-d79*
*Completed: 2026-08-14*
