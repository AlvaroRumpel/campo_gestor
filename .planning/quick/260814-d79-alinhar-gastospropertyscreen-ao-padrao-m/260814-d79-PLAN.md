---
phase: quick-260814-d79
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/gastos/presentation/gastos_property_screen.dart
  - test/widget/gastos_property_screen_test.dart
autonomous: true
requirements: [GAST-01]

must_haves:
  truths:
    - "Em >=1024 o painel de resumo encosta na borda direita da tela com 380px de largura e uma borda esquerda de 1px; não sobra gap nem padding entre a tabela e o painel."
    - "O filtro de período no desktop é uma linha de 4 chips pill (Mês atual / Mês passado / Últimos 3 meses / Ano); trocar de chip refiltra a tabela como o dropdown fazia."
    - "O corpo desktop é full-bleed: a tabela branca encosta na borda esquerda e é separada do header por uma borda superior de 1px."
    - "O rodapé da tabela mostra 'N lançamentos · R$ total' em mono alinhado à direita, no mesmo molde do rodapé de Animais, com o total ainda visível."
    - "Em <1024 a tela continua idêntica: dropdown de período e ExpenseListItemCard, sem chips."
    - "Nenhuma query/provider muda — os mesmos valores aparecem antes e depois do ajuste."
  artifacts:
    - lib/features/gastos/presentation/gastos_property_screen.dart
    - test/widget/gastos_property_screen_test.dart
  key_links:
    - "`_buildPanel` passa a devolver o Container ancorado (width 380 + border-left) em vez de um Column solto: quem chama não pode reintroduzir SizedBox/Padding em volta, senão a âncora à borda direita quebra."
    - "ColoredBox(surface) duplicado: o Expanded do corpo passa a pintar a superfície, então `_buildTable` perde o seu — se ficarem os dois, tudo bem visualmente, mas se nenhum ficar o EmptyState perde o fundo branco."
    - "`_buildPeriodDropdown` continua existindo só para o caminho mobile: apagá-lo junto com a troca por chips quebra a faixa <1024."
---

<objective>
Alinhar o desktop (>=`Breakpoints.rail`) de `GastosPropertyScreen` ao padrão mestre-detalhe já usado em Animais / Reprodução / Lotes. Feedback do usuário em produção: "gastos ficou fora do padrão das outras abas".

Purpose: consistência visual entre abas — a tela já mostra os dados certos, só está composta fora do molde.
Output: `gastos_property_screen.dart` com painel de 380px ancorado, chips de período e corpo full-bleed; teste da tela atualizado.

Escopo fechado: apresentação apenas. Zero mudança em providers, cálculos, rotas ou no caminho mobile.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@lib/features/gastos/presentation/gastos_property_screen.dart
@lib/features/animais/presentation/animais_table_view.dart
@lib/features/lotes/presentation/lote_detail_panel.dart
@lib/core/theme/app_colors.dart
@test/widget/gastos_property_screen_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompor o desktop de GastosPropertyScreen no molde mestre-detalhe</name>
  <files>lib/features/gastos/presentation/gastos_property_screen.dart</files>
  <action>
Ajustar exatamente as três divergências abaixo em `_buildDesktop` e nos helpers que ela usa. Não redesenhar header, colunas da tabela, conteúdo do painel nem o caminho mobile.

**(1) Painel ancorado de 380px.** Em `_buildDesktop`, remover o `SizedBox(width: 14)` e o `SizedBox(width: 330, child: SingleChildScrollView(...))`; passar a chamar `_buildPanel(items, totalUa)` direto como filho do `Row`. Mudar `_buildPanel` para devolver, no lugar do `Column` solto, um `Container(width: 380, decoration: const BoxDecoration(color: ..., border: Border(left: BorderSide(color: AppColors.divider))), child: SingleChildScrollView(padding: const EdgeInsets.all(12), child: <o Column atual, inalterado>))`. Molde literal: `LoteDetailPanel` / `AnimalDetailPanel` (mesmo width, mesma borda esquerda). Diferença deliberada de uma coisa só: a cor de fundo é `AppColors.background` (osso), não `AppColors.surface` — o conteúdo deste painel é uma pilha de `SectionCard` brancos, que sobre branco perde a definição. O token citado no briefing como `AppColors.bg` não existe; o correto é `AppColors.background`. Adicionar `key: const ValueKey('gastos-painel')` nesse Container (o teste mede a largura por essa key). Os cards internos e seus espaçadores ficam como estão.

**(2) Chips de período no lugar do dropdown (só no desktop).** Criar no fim do arquivo um `_PeriodChip` privado, clone visual do `_ScopeChip` de `animais_table_view.dart`: `InkWell` com `borderRadius: BorderRadius.circular(999)` sobre `Container(height: 34, padding: EdgeInsets.symmetric(horizontal: 12))`; selecionado → fundo `AppColors.positiveChipBg`, sem borda, texto `AppColors.primaryDarkText` `w600`; não selecionado → fundo `AppColors.surface`, `Border.all(color: AppColors.chipBorder)`, texto `AppColors.ink` `w400`; `fontSize: 13` nos dois casos. Seguir o widget existente, não a descrição aproximada do briefing ("fundo verde/texto claro") — "padrão das outras abas" quer dizer o `_ScopeChip` real. Em `_buildDesktop`, trocar o `Padding` que hoje envolve `_buildPeriodDropdown` por um `Padding(padding: const EdgeInsets.fromLTRB(16, 0, 16, 10), child: Row(children: [...]))` com um chip por valor de `ExpensePeriodPreset` exceto `personalizado` (4 chips), separados por `SizedBox(width: 8)`, cada um com `onTap: () => setState(() => _preset = preset)` e `selected: _preset == preset`. `_buildPeriodDropdown` **permanece no arquivo** — `_buildMobile` continua usando-o sem alteração.

**(3) Corpo full-bleed com border-top.** Em `_buildDesktop`, apagar o `Padding(fromLTRB(16, 0, 16, 16))` que envolve o `Row` do corpo e envolver o `Row` num `DecoratedBox(decoration: const BoxDecoration(border: Border(top: BorderSide(color: AppColors.divider))))` dentro do `Expanded`. O `Row` mantém `crossAxisAlignment: CrossAxisAlignment.stretch` e fica com dois filhos: `Expanded(child: ColoredBox(color: AppColors.surface, child: <EmptyState ou _buildTable>))` e o painel. Como esse `ColoredBox` passa a pintar a superfície inteira (incluindo o estado vazio, que hoje fica sobre o fundo osso), remover o `ColoredBox` interno de `_buildTable` e devolver o `Column` direto.

**Rodapé da tabela.** Extrair um helper `String _countLabel(int count)` com o `Intl.plural` que hoje está inline no subtítulo do header e usá-lo nos dois lugares. `_buildTableFooter` passa a receber `(int count, double total)` e o `Container` (minHeight 34, `EdgeInsets.symmetric(horizontal: 16)`, `color: AppColors.surfaceVariant`) passa a ter `alignment: Alignment.centerRight` e um único `Text('${_countLabel(count)} · ${formatCurrencyBrl(total)}', style: monoStyle(size: 11.5, color: AppColors.textSecondary))` — molde literal do rodapé de `AnimaisTableView`. Some o rótulo `'TOTAL'` + `Spacer`; o total continua visível na mesma linha. `_buildTable` repassa `count` (já calculado em `_buildDesktop` como `itemCount(periodItems)` — passe-o adiante em vez de recalcular).

Nenhuma cor literal: tudo sai de `AppColors`. Nenhuma alteração em `_buildMobile`, nos providers, em `_openNewExpenseFlow`/`_openEditDialog`/`_deleteExpense` ou nas larguras de coluna.
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/campo_gestor && flutter analyze --no-fatal-infos lib/features/gastos/presentation/gastos_property_screen.dart</automated>
  </verify>
  <done>`flutter analyze --no-fatal-infos` limpo no arquivo; painel é um `Container(width: 380)` com `Border(left:)`; 4 `_PeriodChip` no desktop e `_buildPeriodDropdown` ainda referenciado por `_buildMobile`; corpo sem `Padding` externo e com `Border(top:)`; rodapé sem a string `'TOTAL'`.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar o teste de widget para os seletores novos</name>
  <files>test/widget/gastos_property_screen_test.dart</files>
  <action>
Atualizar `test/widget/gastos_property_screen_test.dart` para o layout novo, preservando o comportamento coberto. Fixtures, harness (`_pumpDesktop`/`_pumpMobile`, 1440x900 e 800x600) e overrides de provider ficam como estão.

- Primeiro teste desktop: a asserção `find.text('TOTAL')` deixa de valer (o rótulo sumiu do rodapé). Trocar por `expect(find.textContaining('lançamentos'), findsNWidgets(2))` — subtítulo do header e rodapé da tabela, que agora carregam o mesmo texto plural. Manter `expect(find.textContaining('R\$ 350,00'), findsAtLeastNWidgets(2))` e atualizar o nome/comentário do teste (hoje diz "painel de 330px").
- Acrescentar ao mesmo teste desktop a checagem da âncora: `expect(tester.getSize(find.byKey(const ValueKey('gastos-painel'))).width, 380)`.
- Novo teste desktop dos chips: `expect(find.text('Mês atual'), findsOneWidget)`, `expect(find.text('Ano'), findsOneWidget)`, `expect(find.byType(DropdownButton<ExpensePeriodPreset>), findsNothing)`. Depois `await tester.tap(find.text('Mês passado')); await tester.pumpAndSettle();` e verificar que a tabela some para o mês passado com estas fixtures (todas no mês corrente) — `expect(find.byType(EmptyState), findsOneWidget)`. Isso prova que o chip refiltra, não só pinta.
- Teste mobile: acrescentar `expect(find.byType(DropdownButton<ExpensePeriodPreset>), findsOneWidget)` e `expect(find.text('Mês passado'), findsNothing)` (o dropdown fechado só renderiza o item selecionado), garantindo que <1024 ficou intacto.
- Importar `ExpensePeriodPreset` de `package:campo_gestor/features/gastos/data/expense_calculations.dart`.

Registrar no SUMMARY quais asserções mudaram e por quê (o rótulo `'TOTAL'` foi absorvido pelo rodapé com contagem).
  </action>
  <verify>
    <automated>cd /f/_geral/Projetos/campo_gestor && flutter test test/widget/gastos_property_screen_test.dart</automated>
  </verify>
  <done>Todos os testes do arquivo passam; suíte completa (`flutter test`) verde; nenhum outro teste precisou de alteração (se algum precisar, documentar no SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Mudança puramente de composição de widgets; nenhuma entrada nova, nenhuma query, nenhuma escrita. Os dados continuam vindo dos mesmos providers já sujeitos a RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d79-01 | Information Disclosure | `_buildDesktopHeader` / painel | low | accept | O botão "Novo gasto" segue guardado por `canManageExpenses` e a exclusão por `canManage`; nenhum gate é tocado por este plano. |
| T-d79-SC | Tampering | instalação de pacotes | low | accept | Nenhum pacote novo — só widgets do Flutter e tokens de `AppColors`. |
</threat_model>

<verification>
```bash
cd /f/_geral/Projetos/campo_gestor
flutter analyze --no-fatal-infos
flutter test
```
Manual (desktop >=1024, `/gastos`): o painel encosta na direita sem folga; os chips trocam o período; a tabela encosta na esquerda com uma linha separando-a do header; o rodapé mostra "N lançamentos · R$ X". Em <1024 nada mudou.
</verification>

<success_criteria>
- `flutter analyze --no-fatal-infos` sem novos avisos.
- `flutter test` verde (suíte completa).
- Apenas 2 arquivos alterados.
- Nenhuma cor literal fora de `AppColors`; nenhuma mudança em providers, cálculos ou no caminho mobile.
</success_criteria>

<output>
Create `.planning/quick/260814-d79-alinhar-gastospropertyscreen-ao-padrao-m/260814-d79-SUMMARY.md` when done
</output>
