---
phase: quick-260814-f2v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260814_09_multitenant_hardening.sql
  - lib/core/widgets/ui.dart
  - lib/features/animais/presentation/animais_screen.dart
  - lib/features/animais/presentation/animal_detail_screen.dart
  - lib/features/dashboard/presentation/dashboard_screen.dart
  - lib/features/gastos/presentation/gastos_property_screen.dart
  - lib/features/gastos/presentation/gastos_screen.dart
  - lib/features/lotes/presentation/lotes_list_view.dart
  - lib/features/lotes/presentation/lote_detail_screen.dart
  - lib/features/lotes/presentation/_lots_section.dart
  - lib/features/piquetes/presentation/paddock_detail_screen.dart
  - lib/features/piquetes/presentation/piquetes_screen.dart
  - lib/features/propriedades/presentation/propriedades_screen.dart
  - lib/features/reproducao/presentation/animal_reproductive_history_section.dart
  - lib/features/reproducao/presentation/atf_animal_selection_screen.dart
  - lib/features/reproducao/presentation/atf_detail_screen.dart
  - lib/features/reproducao/presentation/reproducao_screen.dart
  - lib/features/sanitario/presentation/aplicacao_detail_screen.dart
  - lib/features/sanitario/presentation/registrar_aplicacao_screen.dart
  - lib/features/sanitario/presentation/sanitario_screen.dart
  - lib/features/sanitario/presentation/sanitary_history_section.dart
  - lib/core/widgets/property_selector.dart
  - test/widget/animais_screen_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Um veterinário membro de duas propriedades não consegue mover animal/lote/piquete/dose/gasto entre propriedades por PATCH direto (property_id imutável)"
    - "Nenhum usuário consegue se auto-inserir em property_members via PostgREST (takeover de tenant fechado)"
    - "Todo animal tem lot_id — o banco recusa NULL, alinhado ao modelo Dart (Animal.lotId é String não-nullable)"
    - "animals.category só aceita as 7 categorias de kCategories; animal com ATF ativa não pode virar categoria inelegível"
    - "Baixa via PATCH direto em animals.deleted_at desativa a membership ATF (não é mais no-op silencioso)"
    - "Em qualquer estado de erro de rede o usuário vê 'Tentar novamente' e o retry refaz a requisição"
    - "Trocar a propriedade ativa limpa filtros e seleções das telas montadas — nenhum id de outra propriedade sobrevive à troca"
  artifacts:
    - supabase/migrations/20260814_09_multitenant_hardening.sql
    - "ErrorRetry em lib/core/widgets/ui.dart"
  key_links:
    - "enforce_property_id_immutable() ligado por CREATE TRIGGER às 6 tabelas com property_id (paddocks, animals, lots, doses, expenses, property_members)"
    - "ref.listen(currentPropertyProvider) → setState de reset nas 4 telas com filtro local"
    - "cada ErrorRetry.onRetry → ref.invalidate do provider exato cujo AsyncValue produziu o erro"
  prohibitions:
    - "NÃO rodar supabase db push / MCP apply_migration — deploy é manual do usuário"
    - "NÃO rodar supabase test db nem escrever novos arquivos pgTAP (sem stack local)"
    - "NÃO editar migrations já aplicadas — correções são forward-only (precedente 20260812_06)"
---

<objective>
Fechar 7 findings de review: 5 furos de isolamento multi-tenant / integridade no Postgres (uma migration nova, forward-only) e 2 lacunas de UX de campo no Flutter (reset de filtros na troca de propriedade, retry em erro de rede).

Purpose: o app é multi-tenant por RLS; três dos findings permitem cruzar a fronteira de propriedade por PATCH direto no PostgREST, contornando os RPCs. Os dois findings de Flutter são de campo: filtro preso de outra propriedade mostra dado errado, e erro de rede sem retry obriga a sair e voltar da tela em 4G ruim.
Output: 1 arquivo SQL novo (não aplicado), 1 widget compartilhado `ErrorRetry`, ~25 sites de erro com retry, reset de estado efêmero em 4 telas, testes verdes.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Fontes de verdade a ler antes de editar (não re-ler o que já está citado abaixo):
@supabase/migrations/20260716_04_animal_lot_property_trigger.sql
@supabase/migrations/20260804_05_reproductive_module.sql
@lib/features/gastos/presentation/paddock_expense_summary_card.dart
@lib/core/providers/current_property_provider.dart
</context>

<planner_evidence>
Fatos verificados durante o planning — o executor não precisa reconfirmar:

- `self_insert_membership` está em `20260508_02_property_paddock.sql:61-67`. Único INSERT em `property_members` no repo é o do RPC `create_property_with_membership` (`20260509_03_create_property_rpc.sql:22`), que é `SECURITY DEFINER` (linha 11). No Dart, `property_members` só aparece em leitura (`lib/features/auth/data/property_repository.dart:42`) e num comentário desatualizado (`lib/features/propriedades/data/propriedade_repository.dart:28-30`). Nenhum caminho legítimo depende da policy.
- `animals.lot_id` foi criado nullable em `20260514_03_lots_animals.sql:58`. O modelo Dart já assume não-nulo: `Animal.lotId` é `String` (ver `lib/features/animais/data/animal_model.g.dart:12`) e o único write vem do RPC `create_lot_with_animals` (`20260514_03:222-223`), que sempre preenche.
- Todas as 6 tabelas alvo têm `property_id uuid NOT NULL`: paddocks (`20260508_02:74`), lots (`20260508_02:119`), animals (`20260514_03:13`), doses (`20260810_06:27`), expenses (`20260813_07:25`), property_members. `sanitary_applications` também tem, mas já está coberta por `trg_snapshot_immutable` (imutabilidade total pós-INSERT) — **não incluir**.
- Categorias válidas (strings exatas, de `lib/features/animais/data/animal_constants.dart:4-12`): `vaca`, `novilha`, `terneiro`, `terneira`, `touro`, `boi`, `novilho`.
- Predicado de elegibilidade ATF já existe em `20260804_05_reproductive_module.sql:143` — `NOT IN ('vaca', 'novilha')`. Reusar o mesmo par, não re-derivar.
- `deactivate_atf_membership_on_baixa()` (`20260804_05:212-231`) já tem `SET search_path = public`; falta só `SECURITY DEFINER`.
- Padrão de retry de referência: `lib/features/gastos/presentation/paddock_expense_summary_card.dart:50-57` (`IconButton` → `ref.invalidate(provider)`). Formato de ListTile, específico daquele card — **não** refatorar para `ErrorRetry`.
- `ui.dart` já expõe primitivas do redesign (`StatusChip`, `EmptyState`, `WarningBanner`, ...) — `ErrorRetry` entra ao lado delas.
- Campos de estado efêmero: `animais_screen.dart:54-55` (`_lotId`/`_paddockId`) + `_selectedAnimalIdProvider` (NotifierProvider top-level, linha 35); `sanitario_screen.dart:64,67` (`_lotFilterId`/`_animalFilterId`); `piquetes_screen.dart:41` (`_selectedLotId`); `reproducao_screen.dart:34` (`_selectedAtfId`).
- **Armadilha**: `sanitario_screen.dart:95-96` inicializa `_lotFilterId`/`_animalFilterId` a partir de query params da rota. O listener de reset não pode disparar no primeiro resolve do provider, senão apaga o deep link.
</planner_evidence>

<tasks>

<task type="auto">
  <name>Task 1: Migration de hardening multi-tenant (5 fixes, forward-only, não aplicada)</name>
  <files>supabase/migrations/20260814_09_multitenant_hardening.sql</files>
  <action>
Criar UM arquivo novo com este nome exato. Não editar nenhuma migration existente (precedente forward-only de `20260812_06`). Cabeçalho comentado explicando que é hardening cross-fase vindo de review, com referência aos arquivos/linhas de origem listados em `<planner_evidence>`.

Conteúdo, nesta ordem:

1. **Fechar takeover de tenant**: `DROP POLICY IF EXISTS "self_insert_membership" ON property_members;`. Comentar que a policy virou obsoleta quando `create_property_with_membership` passou a `SECURITY DEFINER` e a inserir a membership sozinho, e que nenhum caminho Dart insere direto.

2. **Animal sempre em lote**: bloco `DO` que conta `animals` com `lot_id IS NULL` (incluindo soft-deleted) e, se > 0, `RAISE EXCEPTION` reportando a contagem — o `SET NOT NULL` sozinho falharia com mensagem genérica e o usuário aplica isso à mão em PROD, então a mensagem precisa dizer quantas linhas corrigir. Em seguida `ALTER TABLE animals ALTER COLUMN lot_id SET NOT NULL;`. Depois `CREATE OR REPLACE FUNCTION enforce_animal_lot_same_property()` idêntica à de `20260716_04` **exceto** por remover a condição `NEW.lot_id IS NOT NULL AND` do IF externo. Preservar o resto do escopo (`TG_OP = 'INSERT' OR NEW.lot_id IS DISTINCT FROM OLD.lot_id OR NEW.property_id IS DISTINCT FROM OLD.property_id`) — é o que impede a baixa de re-disparar a validação. Não recriar o trigger: `CREATE OR REPLACE FUNCTION` mantém o binding de `trg_animals_lot_same_property`. Ajustar o comentário do corpo para refletir que lot_id agora é obrigatório.

3. **property_id imutável**: uma função genérica `enforce_property_id_immutable()` (`LANGUAGE plpgsql`, `SET search_path = public`) que faz `RAISE EXCEPTION` com `ERRCODE = '23514'` quando `NEW.property_id IS DISTINCT FROM OLD.property_id`, citando `TG_TABLE_NAME`, o valor antigo e o novo. Ligar com um `CREATE TRIGGER ... BEFORE UPDATE OF property_id ON <t> FOR EACH ROW WHEN (NEW.property_id IS DISTINCT FROM OLD.property_id) EXECUTE FUNCTION enforce_property_id_immutable()` para cada uma de: `paddocks`, `animals`, `lots`, `doses`, `expenses`, `property_members`. Nome do trigger: `trg_<tabela>_property_id_immutable`. Preceder cada um de `DROP TRIGGER IF EXISTS` para a migration ser re-executável. Não incluir `sanitary_applications` (já coberta por `trg_snapshot_immutable`) — deixar isso escrito num comentário para o próximo leitor não achar que é esquecimento. Fecha o gap marcado como deferred em `20260716_04:36` e `20260717_04:38-39`.

4. **Categoria válida e estável**: `ALTER TABLE animals ADD CONSTRAINT animals_category_check CHECK (category IN (...))` com as 7 strings exatas de `<planner_evidence>`. Depois `enforce_animal_category_atf_guard()` + `CREATE TRIGGER trg_animals_category_atf_guard BEFORE UPDATE OF category ON animals FOR EACH ROW WHEN (NEW.category IS DISTINCT FROM OLD.category)`: se existe `animal_atf_memberships` com `animal_id = NEW.id AND active = true` e `NEW.category NOT IN ('vaca', 'novilha')`, `RAISE EXCEPTION` explicando que é preciso remover o animal do ATF antes de trocar a categoria. Usar o mesmo par `('vaca','novilha')` do `20260804_05:143` e comentar a referência para manter fonte única.

5. **Baixa desativa membership em qualquer caminho**: `CREATE OR REPLACE FUNCTION deactivate_atf_membership_on_baixa()` com corpo idêntico ao de `20260804_05:212-231` mais `SECURITY DEFINER`. Comentar a causa: `animal_atf_memberships` tem FORCE RLS e nenhuma policy de UPDATE, então rodando como invoker o UPDATE filtra 0 linhas silenciosamente quando a baixa chega por PATCH direto em `animals.deleted_at`. Trigger `trg_animals_baixa_deactivates_atf` continua ligado — não recriar.

PROIBIDO nesta task: rodar `supabase db push`, `supabase migration up`, `supabase test db` ou qualquer MCP `apply_migration`. O deploy é manual do usuário. Escrever o arquivo e parar.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/campo_gestor" && M=supabase/migrations/20260814_09_multitenant_hardening.sql && B=$(grep -v '^[[:space:]]*--' "$M") && for t in 'DROP POLICY IF EXISTS' 'ALTER COLUMN lot_id SET NOT NULL' 'enforce_property_id_immutable' 'animals_category_check' 'SECURITY DEFINER' 'deactivate_atf_membership_on_baixa'; do echo "$B" | grep -q "$t" || { echo "FALTA: $t"; exit 1; }; done && N=$(echo "$B" | grep -c 'CREATE TRIGGER') && [ "$N" -ge 7 ] && ! echo "$B" | grep -q 'sanitary_applications' && echo "OK triggers=$N"</automated>
  </verify>
  <done>Arquivo existe com os 5 fixes; ≥7 `CREATE TRIGGER` (6 de imutabilidade + o guard de categoria); nenhum `CREATE TRIGGER` em `sanitary_applications`; nenhuma migration existente modificada (`git status` mostra só o arquivo novo); nada aplicado ao banco.</done>
</task>

<task type="auto">
  <name>Task 2: ErrorRetry compartilhado e retry em todo estado de erro de rede</name>
  <files>lib/core/widgets/ui.dart, lib/core/widgets/property_selector.dart, lib/features/animais/presentation/animais_screen.dart, lib/features/animais/presentation/animal_detail_screen.dart, lib/features/dashboard/presentation/dashboard_screen.dart, lib/features/gastos/presentation/gastos_property_screen.dart, lib/features/gastos/presentation/gastos_screen.dart, lib/features/lotes/presentation/lotes_list_view.dart, lib/features/lotes/presentation/lote_detail_screen.dart, lib/features/lotes/presentation/_lots_section.dart, lib/features/piquetes/presentation/paddock_detail_screen.dart, lib/features/piquetes/presentation/piquetes_screen.dart, lib/features/propriedades/presentation/propriedades_screen.dart, lib/features/reproducao/presentation/animal_reproductive_history_section.dart, lib/features/reproducao/presentation/atf_animal_selection_screen.dart, lib/features/reproducao/presentation/atf_detail_screen.dart, lib/features/reproducao/presentation/reproducao_screen.dart, lib/features/sanitario/presentation/aplicacao_detail_screen.dart, lib/features/sanitario/presentation/registrar_aplicacao_screen.dart, lib/features/sanitario/presentation/sanitario_screen.dart, lib/features/sanitario/presentation/sanitary_history_section.dart</files>
  <action>
Adicionar `ErrorRetry` em `lib/core/widgets/ui.dart`, ao lado de `EmptyState`/`WarningBanner` e no mesmo estilo (StatelessWidget, doc comment curto em pt-BR, tipografia via `Theme.of(context).textTheme`, cor de erro via `Theme.of(context).colorScheme.error`): recebe `String message` e `VoidCallback onRetry`, renderiza a mensagem mais um `TextButton.icon` com `Icons.refresh` e label `Tentar novamente`. `Column` com `mainAxisSize: MainAxisSize.min` e **sem** `Center` interno — vários call sites já estão dentro de `Center`/`Column` próprios e um `Center` embutido quebraria o layout deles.

Depois substituir o texto de erro estático nos 25 sites abaixo (arquivo:linha de referência — as linhas movem conforme você edita, localize pelo `error: (`):

property_selector.dart:167 · animais_screen.dart:147 · animal_detail_screen.dart:117 · dashboard_screen.dart:381,480,635 · gastos_property_screen.dart:119 · gastos_screen.dart:206 · lotes_list_view.dart:28 · lote_detail_screen.dart:65,350 · _lots_section.dart:42 · paddock_detail_screen.dart:47 · piquetes_screen.dart:109 · propriedades_screen.dart:42 · animal_reproductive_history_section.dart:82 · atf_animal_selection_screen.dart:257 · atf_detail_screen.dart:53 · reproducao_screen.dart:75 · aplicacao_detail_screen.dart:45 · registrar_aplicacao_screen.dart:419 · sanitario_screen.dart:344,545 · sanitary_history_section.dart:57,131

Regras de substituição:
- Preservar a mensagem em pt-BR que já está lá (ela nomeia o que falhou). Só se a mensagem atual for genérica demais para o contexto, torná-la específica.
- `onRetry` deve invalidar o provider exato cujo `AsyncValue` produziu aquele branch de erro — ler o `ref.watch(...)` correspondente logo acima do `.when(`. Se o widget observa mais de um provider e o `.when` é de um só, invalidar só esse. Não usar `ref.invalidate` em provider adivinhado.
- Remover `const` de expressões que deixam de ser constantes.
- Se `ref` não estiver em escopo no site (widget `StatelessWidget` recebendo o `AsyncValue` por parâmetro), converter o widget para `ConsumerWidget`/`ConsumerStatefulWidget` — é a convenção usada em todo o projeto — em vez de passar callback por construtor.
- Onde o erro está dentro de um `Scaffold` de tela de detalhe (`animal_detail_screen`, `atf_detail_screen`, `aplicacao_detail_screen`), manter o `Scaffold`/`AppBar` e trocar só o corpo.

Fora de escopo, deliberadamente (não tocar): `paddock_expense_summary_card.dart:50` (já tem retry, formato de ListTile); `mover_animal_dialog.dart:171` e `mover_lote_dialog.dart:198` (erro inline em picker de diálogo — o usuário fecha e reabre); `_lots_section.dart:139` e `piquetes_screen.dart:560` (os branches `error:` retornam `String`/`List`, não widget).
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/campo_gestor" && N=$(grep -rn 'ErrorRetry(' --include=*.dart lib/ | grep -c ':') && [ "$N" -ge 24 ] && flutter analyze --no-pub 2>&1 | tail -3 && flutter test 2>&1 | tail -5</automated>
  </verify>
  <done>`ErrorRetry` existe em `ui.dart`; ≥24 usos em `lib/`; `flutter analyze` sem issues; `flutter test` verde (≥360 testes); nenhum dos 5 sites fora de escopo alterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resetar filtros e seleções ao trocar de propriedade</name>
  <files>lib/features/animais/presentation/animais_screen.dart, lib/features/sanitario/presentation/sanitario_screen.dart, lib/features/piquetes/presentation/piquetes_screen.dart, lib/features/reproducao/presentation/reproducao_screen.dart, test/widget/animais_screen_test.dart</files>
  <behavior>
    - Com propriedade A ativa e `_lotId`/`_paddockId` setados, trocar para propriedade B zera ambos e a lista volta a mostrar todos os animais.
    - Trocar para B também limpa a seleção mestre-detalhe (`_selectedAnimalIdProvider` volta a null).
    - O primeiro resolve de `currentPropertyProvider` (null → A) NÃO limpa nada — os filtros de deep link do `SanitarioScreen` (`?lote=`/`?animal=`, inicializados em `initState`) sobrevivem.
  </behavior>
  <action>
No `build()` de cada uma das 4 telas (todas já são `ConsumerState`), adicionar um `ref.listen(currentPropertyProvider, ...)` no topo, antes de qualquer `ref.watch`.

Guarda obrigatória: só resetar quando o id anterior **e** o novo forem não-nulos e diferentes — `final prevId = prev?.value?.id; final nextId = next.value?.id; if (prevId == null || nextId == null || prevId == nextId) return;`. Sem isso o primeiro resolve (prev null → A) dispara um reset e apaga os filtros de deep link do `SanitarioScreen` inicializados em `initState` (linhas 95-96). Escrever essa razão como comentário curto no listener de `sanitario_screen.dart`, já que é lá que a regressão apareceria.

O que cada tela limpa dentro do `setState`:
- `animais_screen.dart`: `_lotId` e `_paddockId` (não mexer em `_query`/`_category`/`_showArchived`/`_sortDescending` — não carregam id de propriedade), e no mesmo callback `ref.invalidate(_selectedAnimalIdProvider)` fora do `setState`.
- `sanitario_screen.dart`: `_lotFilterId` e `_animalFilterId`.
- `piquetes_screen.dart`: `_selectedLotId`.
- `reproducao_screen.dart`: `_selectedAtfId`.

Manter `_selectedAnimalIdProvider` como `NotifierProvider` (não converter para autoDispose — a tela continua montada durante a troca, autoDispose não dispararia).

Teste: em `test/widget/animais_screen_test.dart`, um teste novo que monta a tela com propriedade A, aplica filtro de lote, troca a propriedade ativa e afirma que o filtro voltou para "todos". Seguir a convenção de override já usada no arquivo (o projeto sobrescreve `memberPropertiesProvider` em vez de `currentPropertyProvider` — ver decisão 04-01 no STATE.md); se sobrescrever `currentPropertyProvider` direto for mais barato aqui, é aceitável desde que o teste realmente exercite a transição A→B.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/campo_gestor" && N=$(grep -rln 'ref.listen(currentPropertyProvider' lib/features/animais/presentation/animais_screen.dart lib/features/sanitario/presentation/sanitario_screen.dart lib/features/piquetes/presentation/piquetes_screen.dart lib/features/reproducao/presentation/reproducao_screen.dart | wc -l) && [ "$N" -eq 4 ] && flutter analyze --no-pub 2>&1 | tail -3 && flutter test 2>&1 | tail -5</automated>
  </verify>
  <done>As 4 telas têm o listener com a guarda de prev/next não-nulos e diferentes; teste novo em `animais_screen_test.dart` passa; `flutter test` verde inteiro (nenhum teste de deep link do sanitário quebrou); `flutter analyze` limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → PostgREST | Qualquer usuário autenticado pode emitir PATCH/POST arbitrário em qualquer tabela exposta; os RPCs são caminho voluntário, não obrigatório |
| propriedade A ↔ propriedade B | Um veterinário membro de duas propriedades passa `is_member_of()` nas duas — a fronteira é a coluna `property_id` da linha, não a sessão |
| UI ↔ propriedade ativa | Estado efêmero do widget guarda ids da propriedade anterior após a troca |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-f2v-01 | Elevation of Privilege | policy `self_insert_membership` em `property_members` | critical | mitigate | Task 1.1 — DROP da policy; único INSERT legítimo é o RPC SECURITY DEFINER |
| T-f2v-02 | Tampering | `PATCH /<t>?id=eq.X {"property_id": "<outra propriedade>"}` em paddocks/animals/lots/doses/expenses/property_members | high | mitigate | Task 1.3 — trigger `enforce_property_id_immutable` nas 6 tabelas, independente do caminho de acesso |
| T-f2v-03 | Tampering | `animals.lot_id` nullable + skip de NULL no trigger de coerência | high | mitigate | Task 1.2 — SET NOT NULL e remoção do skip, fechando a janela em que o animal escapa da validação lote↔propriedade |
| T-f2v-04 | Tampering | `animals.category` sem CHECK; troca de categoria com ATF ativa | medium | mitigate | Task 1.4 — CHECK das 7 categorias + trigger de guarda ATF |
| T-f2v-05 | Repudiation | `deactivate_atf_membership_on_baixa` como invoker → UPDATE no-op sob FORCE RLS | high | mitigate | Task 1.5 — recriar como SECURITY DEFINER com `search_path` fixo; animal baixado deixa de constar como ATF ativa |
| T-f2v-06 | Information Disclosure | filtros/seleção de UI retendo ids da propriedade anterior após a troca | medium | mitigate | Task 3 — reset via `ref.listen(currentPropertyProvider)`; dados em si já são barrados por RLS, o risco é decisão operacional sobre tela enganosa |
| T-f2v-07 | Denial of Service | erro de rede transitório sem retry deixa a tela morta em campo | low | mitigate | Task 2 — `ErrorRetry` com invalidação do provider |
| T-f2v-SC | Tampering | instalação de pacotes | low | accept | Nenhuma dependência nova em nenhuma das 3 tasks |
</threat_model>

<verification>
1. `git status` mostra exatamente 1 arquivo novo em `supabase/migrations/` e nenhuma migration existente modificada.
2. `flutter analyze` sem issues.
3. `flutter test` verde, com pelo menos 1 teste novo.
4. Nenhum comando de deploy de banco executado (sem `db push`, sem `apply_migration`, sem `test db`) — a SUMMARY deve registrar que a migration está pendente de aplicação manual pelo usuário.
</verification>

<success_criteria>
- Os 5 fixes de banco estão num único arquivo SQL novo, forward-only, não aplicado.
- `ErrorRetry` existe uma vez em `ui.dart` e é usado em ≥24 sites; nenhum estado de erro de lista/tela ficou sem "Tentar novamente".
- As 4 telas com filtro local zeram estado efêmero na troca de propriedade sem quebrar o deep link do sanitário.
- `flutter analyze` limpo e suite de testes verde.
</success_criteria>

<output>
Create `.planning/quick/260814-f2v-corrigir-findings-do-review-seguranca-mu/260814-f2v-SUMMARY.md` when done.

Na SUMMARY, registrar explicitamente: **`20260814_09_multitenant_hardening.sql` está pendente de aplicação manual** (o usuário faz o push), e que o `ALTER COLUMN lot_id SET NOT NULL` e o `ADD CONSTRAINT animals_category_check` abortam a transação se PROD tiver linhas violando — o bloco `DO` de pré-checagem reporta a contagem.
</output>
</content>
</invoke>
