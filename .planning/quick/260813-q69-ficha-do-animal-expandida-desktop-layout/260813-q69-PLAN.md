---
phase: quick
plan: 260813-q69
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/animais/presentation/animal_detail_screen.dart
  - test/widget/animal_detail_desktop_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Em >=1024px a ficha do animal mostra header verde horizontal compacto (hero #N + meta + stats + ações à direita) e um corpo de duas colunas: contexto 340px à esquerda, timeline à direita limitada a ~760px"
    - "Em <1024px a ficha renderiza exatamente o layout atual — test/widget/animal_detail_screen_test.dart passa sem uma única edição"
    - "As ações desktop (Editar / Mover / dar baixa) abrem os MESMOS diálogos da ficha atual e continuam atrás do mesmo gate canEdit (veterinarian)"
    - "A timeline desktop é o mesmo AnimalTimelineCard + AnimalTimelineFilterChips já compartilhados — nenhuma segunda implementação de eventos"
    - "Nenhum card exibe dado que não existe no modelo: sem custo sanitário, sem previsão de parto"
    - "flutter analyze limpo e toda a suíte de testes verde"
  artifacts:
    - lib/features/animais/presentation/animal_detail_screen.dart
    - test/widget/animal_detail_desktop_test.dart
  key_links:
    - "AnimalDetailScreen -> Breakpoints.rail (fonte única do corte 1024px, sem número mágico)"
    - "_WideHeader / coluna de contexto -> os mesmos _onEdit/_onMover/_onBaixa do State (mesmos diálogos, mesmo invalidate)"
    - "badge de status repro -> animalReproStatusByPropertyProvider (mesma fonte que AnimaisTableView e AnimalDetailPanel)"
    - "card Reprodução -> reproductiveHistoryByAnimalProvider (o mesmo provider que a timeline já observa — zero request extra)"
    - "_StateCard reusado com parâmetro de orientação pelas duas larguras (uma implementação de EC + observação)"
  prohibitions:
    - "Nenhuma mudança de comportamento ou de árvore de widgets abaixo de 1024px"
    - "Não editar test/widget/animal_detail_screen_test.dart"
    - "Não criar card de custo nem de previsão de parto — esses dados não existem em nenhum modelo/provider (mesma regra do quick task p10)"
    - "Não duplicar a timeline nem os diálogos de ação"
    - "Nenhuma cor literal fora de AppColors no arquivo tocado"
    - "Não adicionar dependência nova"
---

<objective>
Layout desktop da ficha do animal: a partir de 1024px a mesma rota de detalhe ganha
header verde horizontal compacto e corpo de duas colunas — contexto 340px (estado
corporal + reprodução) à esquerda, timeline à direita limitada a ~760px.

Purpose: em desktop a ficha atual é uma coluna mobile esticada — o hero de 44px e os
tiles glass ocupam a largura toda e a timeline, que é o conteúdo real da tela, fica
espremida embaixo da dobra. O veterinário abre a ficha para ler histórico, não para ler
o cabeçalho.
Output: branch desktop dentro de `animal_detail_screen.dart` (header horizontal, coluna
de contexto, coluna de timeline) reusando timeline, cards e diálogos existentes, mais um
teste de widget cobrindo os dois lados do corte de 1024px.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@lib/core/theme/breakpoints.dart
@lib/core/theme/app_colors.dart
@lib/core/widgets/ui.dart
@lib/core/widgets/campo_app_bar.dart
@lib/features/animais/presentation/animal_detail_screen.dart
@lib/features/animais/presentation/animal_timeline.dart
@lib/features/animais/presentation/animal_detail_panel.dart
@lib/features/reproducao/data/dg_summary.dart
@lib/features/reproducao/data/atf_model.dart
@test/widget/animal_detail_screen_test.dart
@test/widget/animais_desktop_test.dart
</context>

<planner_findings>
Levantado na leitura, para o executor não redescobrir:

- `DetailAppBar` (campo_app_bar.dart) JÁ é a "linha superior" pedida no spec: back +
  `parentLabel` "Animais" à esquerda, `contextPill` (FarmContextPill da fazenda) à
  direita. Ela já está montada no `Scaffold` da ficha para os três estados
  (loading/error/data). Nada a fazer nessa linha — o header novo é só a linha principal.
- `_onEdit` / `_onMover` / `_onBaixa` e o gate `_canEdit` já existem no
  `_AnimalDetailScreenState`. O branch desktop recebe as callbacks por parâmetro.
- `AnimalTimelineFilterChips` + `AnimalTimelineCard` (animal_timeline.dart) já são
  compartilhados com o painel desktop de 380px, e `AnimalTimelineCard` já traz o
  "Ver histórico completo (N)" quando trunca (`collapsedCount`, default 10). O desktop
  usa o default.
- `animalReproStatusByPropertyProvider` (atf_repository.dart) é a fonte única de status
  repro por animal (`AnimalReproStatus.label` / `.chipKind` em dg_summary.dart), já usada
  por `AnimaisTableView` e `AnimalDetailPanel`. Default quando ausente do mapa:
  `AnimalReproStatus.foraDoAtf`.
- `ReproductiveHistoryEntry` (atf_model.dart) expõe exatamente: `atfName`, `atfActive`,
  `inseminationDate`, `implantationDate`, `bullName?`, `lastDgResult?`, `lastDgDate?`,
  `dgRecords`. A lista vem ordenada por `inseminationDate` desc.
- **Custo por animal não existe.** `expenses` (Phase 7) é por piquete e
  `SanitaryApplication` não carrega valor monetário — grep confirmou. O card de custo
  fica de fora, sem placeholder e sem "—". Mesma regra para previsão de parto: não há
  constante de gestação em nenhum modelo.
- `animal_detail_screen.dart` tem hoje UMA cor hexadecimal literal (a borda do botão de
  baixa em `_ActionBar`); `AnimalDetailPanel` já resolveu o mesmo caso com o token
  `AppColors.danger` + `withValues(alpha: 0.30)`. Task 1 alinha isso para o gate de cor
  poder ser absoluto no arquivo.
- Os testes existentes rodam a 800px (ou 360px) de largura, ou seja, **nunca** entram no
  branch desktop. É isso que mantém `animal_detail_screen_test.dart` verde sem edição —
  qualquer provider novo observado precisa ficar DENTRO de widget que só é construído
  acima de `Breakpoints.rail`.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: branch desktop da ficha (header horizontal + duas colunas)</name>
  <files>lib/features/animais/presentation/animal_detail_screen.dart</files>
  <read_first>
    - lib/features/animais/presentation/animal_detail_panel.dart (composição de referência: header verde, StatusChip de repro, botões claros sobre verde, reuso de AnimalTimelineCard)
    - lib/core/widgets/ui.dart (SectionCard, EcMeter, StatusChip, OverlineLabel, GlassTile)
    - lib/core/theme/breakpoints.dart (Breakpoints.rail = o corte)
  </read_first>
  <action>
No `data:` do `animalAsync.when`, trocar o `body:` por um `LayoutBuilder`. Abaixo de
`Breakpoints.rail`, retornar EXATAMENTE o `ListView` de hoje (mesma árvore, mesma ordem,
mesmos widgets) — nada de refatorar o caminho mobile. A partir de `Breakpoints.rail`,
retornar a árvore desktop nova. O `appBar: DetailAppBar(...)` continua igual nas duas
larguras e nos três estados.

Árvore desktop (widget privado com `key: const Key('ficha-desktop')` na raiz):

1. `_WideHeader` — `ConsumerWidget`, `Container` com `AppColors.primary`, sem
   `borderRadius` (o header agora encosta nas bordas), padding horizontal 20 / vertical 14.
   Conteúdo em uma linha só, `crossAxisAlignment: center`:
   - Bloco de identidade em `Expanded`: `#${animal.number}` com `monoStyle(size: 52,
     weight: FontWeight.w600, color: AppColors.onGreen, height: 1.05)`; abaixo
     "Categoria · Raça" (omitir " · Raça" quando `breed` for nulo/vazio) em
     `AppColors.onGreen` 16/w600; abaixo a linha de subtítulo em
     `AppColors.onGreenSecondary` 13.5 com "EC X/5" (só quando `bodyCondition != null`,
     valor em `monoStyle`) e "na fazenda desde dd/MM/yyyy" (data em `monoStyle`),
     separados por " · " — reusar o `_dateFmt` já existente no arquivo.
   - `StatusChip` do status reprodutivo, lendo
     `ref.watch(animalReproStatusByPropertyProvider).asData?.value` e caindo em
     `AnimalReproStatus.foraDoAtf` quando o animal não está no mapa (mesma expressão do
     `AnimalDetailPanel`). Este `ref.watch` NÃO pode subir para o `build` do State — ele
     só pode existir dentro de widget construído acima do corte, senão os testes
     existentes (que não fazem override desse provider) passam a tocá-lo.
   - Stats inline: UA (`kUaWeights[animal.category] ?? 0.0` formatado por `_fmtUa`, mono),
     Lote e Piquete lidos de `loteWithPaddockByIdProvider(animal.lotId)` com "—" quando
     ainda não resolveu ou falhou. Rótulo em `OverlineLabel`-style sobre verde
     (`AppColors.onGreenSecondary`), valor em `AppColors.onGreen`. Lote e Piquete
     continuam navegáveis (`context.go(AppRoutes.loteDetail(...))` e
     `/piquetes/{paddockId}`) como no header mobile.
   - Ações à direita, só quando `canEdit`: "Editar" `FilledButton.icon` claro
     (`backgroundColor: AppColors.onGreen`, `foregroundColor: AppColors.primary`),
     "Mover" `OutlinedButton.icon` claro (`side` com `AppColors.onGreen` em alpha ~0.45,
     `foregroundColor: AppColors.onGreen`) e o ícone de baixa (`Icons.arrow_downward`
     dentro de `Tooltip('Dar baixa')`, `foregroundColor: AppColors.onGreen`) — Mover e
     baixa só quando `animal.deletedAt == null`, igual à `_ActionBar`. As três chamam as
     callbacks `_onEdit`/`_onMover`/`_onBaixa` recebidas por parâmetro; não instanciar
     diálogo nenhum aqui.
   - Contra overflow a 1024: bloco de identidade em `Expanded`, e stats + ações num
     `Wrap(alignment: WrapAlignment.end, spacing: 16, runSpacing: 8,
     crossAxisAlignment: WrapCrossAlignment.center)`, para quebrarem em vez de estourar.

2. Corpo: `Expanded(child: Row(crossAxisAlignment: CrossAxisAlignment.start, ...))` com
   duas colunas que rolam de forma independente:
   - Esquerda: `SizedBox(width: 340, child: SingleChildScrollView(padding: 16))` com, em
     ordem — `_BaixaBanner` (só quando `animal.deletedAt != null`, widget existente),
     `_StateCard` e o card de reprodução novo, separados por 12px.
   - Direita: `Expanded` com `SingleChildScrollView(padding: 16)` +
     `Align(alignment: Alignment.topLeft, child: ConstrainedBox(BoxConstraints(maxWidth: 760)))`
     contendo `AnimalTimelineFilterChips` (mesmo `_filter` do State) + 10px +
     `AnimalTimelineCard` (mesmo `_showAllEvents`/`onShowAll`, `collapsedCount` default).
     Reusar os dois widgets de `animal_timeline.dart` — não escrever renderização de
     evento nova.

3. `_StateCard`: ganha um parâmetro booleano de orientação (default = comportamento
   atual). Na variante vertical, empilhar em `Column`: `OverlineLabel('Estado corporal')`,
   `EcMeter` (ou "—"), `Divider`, `OverlineLabel('Observação')`, texto da observação (ou
   "—"). Nenhum texto, nenhum rótulo e nenhum estilo mudam — só o eixo. É proibido
   duplicar EC/observação num widget novo.

4. Card de reprodução: `SectionCard(title: 'Reprodução')` alimentado por
   `ref.watch(reproductiveHistoryByAnimalProvider(animal.id))` — o MESMO provider que a
   timeline já observa, então não custa request nenhum. Conteúdo: o `StatusChip` do
   status repro; e, do entry escolhido (o primeiro com `atfActive == true`, ou o primeiro
   da lista quando nenhum estiver ativo, já que ela vem ordenada por inseminação desc),
   as linhas "ATF <atfName>" com sufixo "ciclo ativo"/"ciclo encerrado" conforme
   `atfActive`, "insem. dd/MM/yyyy" (mono), "touro: <bullName>" apenas quando houver
   nome, e "última DG dd/MM/yyyy" (mono) quando `lastDgDate != null`, senão
   "aguardando DG". Lista vazia: "Sem histórico reprodutivo." em
   `AppColors.textSecondary`. Enquanto `isLoading`, não renderizar spinner próprio —
   mostrar o card com o badge e o texto de vazio é suficiente. **Não** re-mapear
   `DgResult` para rótulo aqui: quem comunica prenhe/vazia/duvidosa é o badge, que já vem
   pronto de `AnimalReproStatus`.

5. Substituir a única cor hexadecimal literal do arquivo (borda do botão de baixa em
   `_ActionBar`) pelo token equivalente, exatamente como `AnimalDetailPanel` faz:
   `AppColors.danger` com `withValues(alpha: 0.30)`. Depois disso o arquivo inteiro passa
   a viver só de tokens de `AppColors`.

Proibido neste task: mexer no `ListView` mobile, criar arquivo novo em `lib/`, editar
`animal_timeline.dart`, `ui.dart` ou qualquer teste existente.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos && flutter test test/widget/animal_detail_screen_test.dart</automated>
    <automated>git diff --exit-code test/widget/animal_detail_screen_test.dart</automated>
    <automated>[ "$(grep -v '^\s*//' lib/features/animais/presentation/animal_detail_screen.dart | grep -c 'Color(0x')" -eq 0 ]</automated>
    <automated>[ "$(grep -c 'class _Timeline' lib/features/animais/presentation/animal_detail_screen.dart)" -eq 0 ]</automated>
    <automated>[ "$(grep -c 'Breakpoints.rail' lib/features/animais/presentation/animal_detail_screen.dart)" -ge 1 ]</automated>
  </verify>
  <done>`flutter analyze` sem erros nem warnings; os 20 testes de `animal_detail_screen_test.dart` passam com o arquivo de teste byte-a-byte inalterado (git diff limpo); o arquivo não tem mais nenhuma cor hexadecimal literal; o corte de largura sai de `Breakpoints.rail` e não de um número solto; nenhuma classe de timeline nova.</done>
</task>

<task type="auto">
  <name>Task 2: teste de widget dos dois lados do corte de 1024px</name>
  <files>test/widget/animal_detail_desktop_test.dart</files>
  <read_first>
    - test/widget/animais_desktop_test.dart (padrão de teste desktop deste projeto: `tester.binding.setSurfaceSize` + `addTearDown(() => tester.binding.setSurfaceSize(null))`)
    - test/widget/animal_detail_screen_test.dart (harness de overrides da ficha: animalByIdProvider, memberPropertiesProvider, reproductiveHistoryByAnimalProvider, sanitaryHistoryByAnimalProvider, loteWithPaddockByIdProvider)
  </read_first>
  <action>
Arquivo de teste NOVO — não tocar em `animal_detail_screen_test.dart`.

Montar um `_buildScreen` local copiando o harness de overrides da ficha e adicionando
`animalReproStatusByPropertyProvider` (mapa fixo, ex.: o animal como `prenhe`), mais
`loteWithPaddockByIdProvider` resolvendo lote "Lote A" / piquete "Piquete Norte".
Animal-fixture: `vaca`, `number: 147`, `bodyCondition: 3`, `breed: 'Nelore'`,
`observation` preenchida, membership `veterinarian`. `setUpAll` com
`initializeDateFormatting('pt_BR', null)` como no teste existente.

Casos:
1. **1440x900 — duas colunas.** `find.byKey(const Key('ficha-desktop'))` encontra o
   corpo desktop; "ESTADO CORPORAL" e "Reprodução" estão visíveis; a timeline também
   ("Cadastrado no rebanho"). Provar as duas colunas por geometria: o `dx` do topo-esquerda
   de "ESTADO CORPORAL" é menor que o `dx` de "Cadastrado no rebanho", e a coluna de
   contexto mede 340 de largura (`tester.getSize` do `SizedBox`/coluna com a key própria,
   ou do ancestral do card de estado).
2. **1440x900 — header horizontal.** `#147`, o badge "Prenhe", "1,0" (UA da vaca) e o
   botão "Editar" estão todos na tela, e "Editar" está à direita de `#147`
   (`getTopLeft(...).dx` maior) com sobreposição vertical entre os dois retângulos —
   isto é o que distingue o header horizontal do empilhado.
3. **1440x900 — ações reusadas.** Com role `veterinarian`: "Editar", "Mover" e
   `Icons.arrow_downward` presentes. Com role `reader`: os três ausentes. Com animal
   arquivado (`deletedAt` + `baixaReason: 'sale'`): "Editar" presente, "Mover" e o ícone
   de baixa ausentes, e o banner de baixa visível na coluna de contexto.
4. **800x600 — layout atual intacto.** `find.byKey(const Key('ficha-desktop'))` não
   encontra nada; `find.byType(GlassTile)` encontra os tiles Lote/Piquete do header
   mobile; `tester.takeException()` é nulo.
5. **1024x800 — sem overflow no corte exato.** Com nomes longos de lote e piquete e
   observação longa, `tester.takeException()` é nulo e `#147` continua na tela. Mesmo
   check a 1440x900.

Sem mock de Supabase, sem dependência nova, sem `pumpAndSettle` infinito (os providers
são overrides que resolvem imediatamente — seguir o `pump()` + `pump(Duration(...))` do
teste desktop existente quando `pumpAndSettle` travar).
  </action>
  <verify>
    <automated>flutter test test/widget/animal_detail_desktop_test.dart</automated>
    <automated>flutter test</automated>
    <automated>git diff --exit-code test/widget/animal_detail_screen_test.dart</automated>
  </verify>
  <done>O arquivo novo passa com os 5 casos; a suíte inteira segue verde (>=360 testes, nenhum regredido); `animal_detail_screen_test.dart` continua sem uma linha alterada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente Flutter -> PostgREST | leituras de `animal_atf_memberships` / `dg_records` (status repro) e do histórico reprodutivo atravessam RLS |
| UI -> RPCs de escrita | Editar / Mover / dar baixa continuam sendo as únicas escritas alcançáveis desta tela |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q69-01 | Information Disclosure | `_WideHeader` -> `animalReproStatusByPropertyProvider` | low | mitigate | Nenhuma query nova: é o mesmo provider já usado por `AnimaisTableView`/`AnimalDetailPanel`, filtrado por `property_id` no cliente e sob as políticas SELECT existentes (D-21, FORCE RLS). Sem RPC nova, sem SECURITY DEFINER. |
| T-q69-02 | Elevation of Privilege | ações do header desktop | medium | mitigate | As três ações ficam atrás do MESMO `_canEdit` (role `veterinarian`) do layout mobile e chamam as mesmas callbacks/diálogos — nenhum caminho de escrita novo. Teste 3 do Task 2 fixa o gate para `veterinarian`, `reader` e animal arquivado. |
| T-q69-03 | Tampering | escrita direta em widget de leitura | low | accept | A coluna de contexto e a timeline são read-only; a imutabilidade do snapshot sanitário continua sendo garantida no banco (trigger + RLS), não na UI. |
| T-q69-SC | Tampering | npm/pub installs | low | mitigate | Zero dependências novas — o plano usa só Flutter SDK + pacotes já em `pubspec.yaml`; nada a auditar. |
</threat_model>

<verification>
1. `flutter analyze` — sem erros e sem warnings novos.
2. `flutter test` — suíte inteira verde.
3. `git diff --exit-code test/widget/animal_detail_screen_test.dart` — o contrato "<1024 intacto" provado por arquivo de teste inalterado, não por inspeção.
4. `grep -v '^\s*//' lib/features/animais/presentation/animal_detail_screen.dart | grep -c 'Color(0x'` retorna 0.
5. Nenhum arquivo novo em `lib/` e nenhuma linha alterada em `animal_timeline.dart` — `git status --porcelain` mostra apenas os dois arquivos de `files_modified`.
</verification>

<success_criteria>
- A partir de 1024px: header verde horizontal (hero `#N` mono ~52px, categoria · raça, EC + "na fazenda desde", badge repro, stats UA/Lote/Piquete, ações à direita) e corpo em duas colunas com contexto de 340px e timeline limitada a 760px.
- Abaixo de 1024px: layout de hoje, testes de hoje, sem edição.
- Zero duplicação: timeline, cards de EC/observação, diálogos de ação e fonte de status repro são os que já existem.
- Nenhum dado inventado: sem card de custo, sem previsão de parto.
</success_criteria>

<output>
Create `.planning/quick/260813-q69-ficha-do-animal-expandida-desktop-layout/260813-q69-SUMMARY.md` when done
</output>
