---
phase: quick-260813-x4f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/gastos/data/expense_repository.dart
  - lib/features/gastos/data/expense_calculations.dart
  - lib/features/dashboard/data/dashboard_providers.dart
  - lib/features/gastos/presentation/gastos_property_screen.dart
  - lib/core/router/routes.dart
  - lib/core/router/router.dart
  - lib/core/widgets/app_shell.dart
  - test/features/gastos/expense_calculations_test.dart
  - test/widget/gastos_property_screen_test.dart
  - test/core/router_test.dart
  - test/widget/app_shell_test.dart
autonomous: true
requirements: [GAST-01, GAST-02]

must_haves:
  truths:
    - "Um item 'Gastos' (ícone payments) aparece após Sanitário no rail 600–1439 e no drawer >=1440; o bottom nav <600 continua com exatamente 5 destinos."
    - "Abrir /gastos com largura >=1024 mostra a tabela de gastos da propriedade inteira mais um painel de resumo de 330px."
    - "Abrir /gastos com largura <1024 mostra a lista simples dos mesmos lançamentos (ExpenseListItemCard)."
    - "O total do rodapé da tabela é o mesmo número do subtítulo mono do header (mesma lista filtrada)."
    - "'Novo gasto' no header desktop escolhe o piquete e então abre o ExpenseFormDialog existente sem alterá-lo."
    - "O card de gastos do mês do Início continua mostrando o mesmo valor de antes, agora derivado do provider compartilhado."
  artifacts:
    - lib/features/gastos/presentation/gastos_property_screen.dart
    - test/widget/gastos_property_screen_test.dart
    - "unifiedExpenseListByPropertyProvider em lib/features/gastos/data/expense_repository.dart"
    - "lastMonthsTotals + MonthTotal em lib/features/gastos/data/expense_calculations.dart"
    - "AppRoutes.gastos + 6a StatefulShellBranch em lib/core/router/router.dart"
  key_links:
    - "AppShell._navItems indice 5 <-> StatefulShellBranch indice 5: navigationShell.goBranch(i) usa o indice posicional, um desalinhamento manda o usuario para a tela errada sem erro visivel."
    - "NavigationBar mobile recebe 5 destinos mas navigationShell.currentIndex pode valer 5 (deep link em /gastos): sem clamp o framework lanca assertion."
    - "/gastos (branch do shell) convive com /gastos/:paddockId (root-level): a rota de dois segmentos e declarada antes do StatefulShellRoute e continua vencendo o match."
    - "monthExpensesProvider <-> unifiedExpenseListByPropertyProvider: o Inicio e a tela nova precisam somar exatamente a mesma lista."
  prohibitions:
    - "Nenhum metodo novo em ExpenseRepository: fetchExpensesByProperty ja existe e e a unica fonte de gastos manuais da propriedade."
    - "ExpenseFormDialog nao muda de assinatura: a escolha de piquete acontece antes, encadeada, no chamador."
    - "Nenhuma cor literal fora de AppColors; toda figura numerica usa monoStyle."
    - "O bottom nav <600px continua com 5 destinos: Gastos nao entra no mobile nav."
---

<objective>
Criar a rota global `/gastos` (6a branch do shell) com tela consolidada da propriedade
inteira: tabela densa + painel de resumo de 330px em >=1024px, lista simples em <1024px.
Adicionar o item "Gastos" no rail e no drawer do shell.

Isto **reverte explicitamente** a decisao da Fase 7 registrada em `routes.dart` de nao
existir uma 6a branch do shell. A reversao foi aprovada pelo usuario em 2026-08-13; o
custo conhecido (atualizar `AppRoutes.all`, `router_test.dart` e `app_shell_test.dart`)
esta orcado neste plano.

Purpose: hoje gastos so existem por piquete — nao ha nenhuma visao da propriedade inteira
fora do card agregado do Inicio.
Output: 1 tela nova, 1 provider novo, 1 funcao pura nova, 6a branch do shell, 2 testes
novos e 3 testes existentes atualizados.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Fontes que o executor precisa ler antes de escrever codigo:
@lib/features/gastos/data/expense_repository.dart
@lib/features/gastos/data/expense_model.dart
@lib/features/gastos/data/expense_calculations.dart
@lib/features/gastos/presentation/gastos_screen.dart
@lib/features/gastos/presentation/_expense_list_item_card.dart
@lib/features/dashboard/data/dashboard_providers.dart
@lib/features/sanitario/presentation/sanitario_table_views.dart
@lib/features/sanitario/presentation/sanitario_screen.dart
@lib/core/router/routes.dart
@lib/core/router/router.dart
@lib/core/widgets/app_shell.dart
@lib/core/theme/breakpoints.dart
@lib/core/widgets/ui.dart
@test/widget/lotes_desktop_test.dart
</context>

<planner_notes>
Fatos verificados na leitura do codigo, para o executor nao redescobrir:

- `ExpenseRepository.fetchExpensesByProperty(propertyId)` ja existe (linha ~48) e ja
  exclui soft-deleted. Nenhum metodo novo de repositorio e necessario.
- `monthExpensesProvider` (dashboard_providers.dart:152) ja monta a lista unificada da
  propriedade inteira inline (expenses manuais + `visibleApplications(..., showReversed:
  false)`) e depois filtra pelo mes. A Task 1 move esse "montar a lista" para um provider
  compartilhado e deixa o dashboard so com o filtro de mes.
- `buildUnifiedExpenseItems` NAO serve aqui: ele filtra por `paddockId`. A versao
  property-wide e a mesma logica sem esse filtro.
- `ExpenseListItem` nao carrega nome de piquete. Gastos manuais tem `paddockId`;
  aplicacoes sanitarias tem `paddockName` congelado. A coluna PIQUETE resolve o nome via
  `paddockListProvider` (mapa id -> name) no lado do cliente.
- Nenhum campo do modelo permite derivar R$/UA por linha. R$/UA e uma metrica da
  propriedade: `total do mes / dashboardKpisProvider.totalUa`. Fica so no painel.
- `ExpenseFormDialog` exige `paddockId` (nao tem seletor de piquete, por decisao D-10 da
  Fase 7). Por isso o botao "Novo gasto" encadeia um seletor de piquete antes.
- `dashboard_screen_test.dart:104` faz override de `monthExpensesProvider` inteiro, entao
  a refatoracao da Task 1 nao quebra esse teste.
- `StackedBar` (ui.dart:224) aceita fracoes que somam <1 e preenche o resto com
  `AppColors.track` — serve tanto para a quebra por categoria quanto para as barras de mes.
</planner_notes>

<tasks>

<task type="auto">
  <name>Task 1: Provider property-wide + agregacao mensal pura, com o dashboard reusando</name>
  <files>lib/features/gastos/data/expense_repository.dart, lib/features/gastos/data/expense_calculations.dart, lib/features/dashboard/data/dashboard_providers.dart, test/features/gastos/expense_calculations_test.dart</files>
  <action>
Em `expense_repository.dart`, adicionar `unifiedExpenseListByPropertyProvider`, um
`FutureProvider<List<ExpenseListItem>>` (nao family — resolve `currentPropertyProvider`
internamente, regra ja estabelecida no projeto). Comportamento: se a propriedade for null,
retorna lista vazia; senao chama `fetchExpensesByProperty(property.id)`, chama
`sanitaryApplicationListByPropertyProvider`, envolve os manuais com `ExpenseListItem.manual`
e as aplicacoes de `visibleApplications(applications, showReversed: false)` com
`ExpenseListItem.sanitary`, e devolve `sortExpenseItemsDesc` do conjunto. Nao adicionar
nenhum metodo novo em `ExpenseRepository`. Documentar no dartdoc que este e o gemeo
property-wide de `unifiedExpenseListByPaddockProvider` e que herda o mesmo teto D-18
(soma client-side sobre a lista ja carregada).

Em `expense_calculations.dart` (arquivo Flutter-free — manter assim), adicionar uma classe
simples `MonthTotal` com `year`, `month` e `total`, e a funcao pura
`lastMonthsTotals(List<ExpenseListItem> items, DateTime now, {int months = 6})` que devolve
uma lista de `months` posicoes ordenada do mes mais antigo para o mais recente, terminando
no mes de `now`, somando `amountOf` dos itens cujo `dateOf` cai naquele ano+mes. Meses sem
lancamento aparecem com total zero (a barra fica vazia, o mes nao some do eixo). Usar
aritmetica de `DateTime(now.year, now.month - k)` para atravessar virada de ano.

Em `dashboard_providers.dart`, reescrever o corpo de `monthExpensesProvider` para ler
`await ref.watch(unifiedExpenseListByPropertyProvider.future)` no lugar de remontar a lista
inline, mantendo identicos o filtro `currentMonthRange(DateTime.now())`, o `totalAmount` e
o calculo de `perAnimal`. Remover os imports que ficarem sem uso. O numero exibido no
Inicio nao pode mudar.

Em `test/features/gastos/expense_calculations_test.dart`, adicionar um grupo para
`lastMonthsTotals` cobrindo: comprimento fixo de 6 posicoes, ordem cronologica crescente,
mes vazio somando zero, soma correta de dois lancamentos no mesmo mes, e uma virada de ano
(ex.: `now` em fevereiro devolvendo meses do ano anterior).
  </action>
  <verify>
    <automated>flutter test test/features/gastos/expense_calculations_test.dart</automated>
    <automated>[ "$(grep -c 'unifiedExpenseListByPropertyProvider' lib/features/gastos/data/expense_repository.dart)" -ge 1 ]</automated>
    <automated>[ "$(grep -c 'unifiedExpenseListByPropertyProvider' lib/features/dashboard/data/dashboard_providers.dart)" -ge 1 ]</automated>
    <automated>[ "$(grep -c 'lastMonthsTotals' lib/features/gastos/data/expense_calculations.dart)" -ge 1 ]</automated>
    <automated>flutter analyze --no-fatal-infos lib/features/gastos lib/features/dashboard</automated>
  </verify>
  <done>
`unifiedExpenseListByPropertyProvider` existe e e a unica montagem da lista unificada
property-wide; `monthExpensesProvider` consome esse provider; `lastMonthsTotals` e pura,
Flutter-free e coberta por testes incluindo virada de ano; `ExpenseRepository` nao ganhou
metodo novo; a suite de calculos passa.
  </done>
</task>

<task type="auto">
  <name>Task 2: GastosPropertyScreen — tabela + painel 330px em >=1024, lista em <1024</name>
  <files>lib/features/gastos/presentation/gastos_property_screen.dart, test/widget/gastos_property_screen_test.dart</files>
  <action>
Criar `GastosPropertyScreen` (ConsumerStatefulWidget, sem parametros) em um unico arquivo
novo, com os sub-widgets privados dentro dele — molde de composicao:
`sanitario_screen.dart` (switch por `Breakpoints.rail` dentro de um `LayoutBuilder`,
`CampoAppBar` em ambas as faixas) e `sanitario_table_views.dart` (larguras de coluna
declaradas uma vez como constantes de arquivo e reusadas por cabecalho e linha,
`_HeaderText` mono 10.5 w700 letterSpacing 0.8 em `AppColors.primaryDarkText`).

Estado local: um `ExpensePeriodPreset` (default `mesAtual`). Expor os quatro presets
calculados num `DropdownButton<ExpensePeriodPreset>` usando `preset.label`. NAO incluir
`personalizado` — ele exige `showDateRangePicker` e a maquina de `_customRange`, que nao se
paga nesta tela; o fluxo por piquete continua tendo o intervalo livre.

Dados: `unifiedExpenseListByPropertyProvider` (lista completa), `paddockListProvider` (mapa
`id -> name` para a coluna PIQUETE), `dashboardKpisProvider` (apenas `totalUa`),
`currentPropertyProvider` + `memberPropertiesProvider` alimentando `canManageExpenses`.
A lista do periodo e `filterByDateRange(items, rangeForPreset(preset, DateTime.now()))`.

Header (so em >=`Breakpoints.rail`): titulo "Gastos" 24/w600, subtitulo mono 13 em
`AppColors.textSecondary` no formato "N lancamentos · TOTAL" com `formatCurrencyBrl`, e a
direita um `FilledButton.icon` "Novo gasto" visivel apenas quando `canManageExpenses` e a
propriedade resolveram.

Tabela (>=`Breakpoints.rail`): colunas DATA (mono 12, `dd/MM/yyyy`), CATEGORIA (rotulo via
`kExpenseCategoryLabels`, e `kSanitaryPseudoCategoryLabel` quando `categoryKeyOf` for o
pseudo-key sanitario), DESCRICAO (descricao do gasto manual, `doseName` da aplicacao,
travessao quando nulo), PIQUETE (nome resolvido pelo mapa para gasto manual, `paddockName`
congelado para aplicacao), VALOR alinhado a direita em mono, e uma coluna estreita de acao.
Linha em `ListView.builder` dentro de `Expanded`, altura minima 44, borda inferior
`AppColors.divider`, `InkWell` com `hoverColor: AppColors.rowHover`. Clique: gasto manual
abre o `ExpenseFormDialog` em modo edicao (usando o `paddockId` da propria linha); linha
sanitaria da `context.push(AppRoutes.aplicacaoDetail(id))`. A coluna de acao mostra
`Icons.delete_outline` apenas para gasto manual e apenas com `canManageExpenses`, chamando
`confirmDeleteExpense` e, se confirmado, `ExpenseRepository.archiveExpense`. Estorno de
aplicacao sanitaria nao vive aqui — ele ja mora na tela de detalhe da aplicacao, para onde
a linha navega. Rodapé fixo da tabela em `AppColors.surfaceVariant` com o rotulo TOTAL a
esquerda e o mesmo `totalAmount` do subtitulo a direita em mono w700 — os dois numeros saem
da mesma lista filtrada, nao os recalcule por caminhos diferentes.

Painel direito, `SizedBox(width: 330)` com scroll proprio, quatro blocos em `SectionCard`
com `OverlineLabel`:
1. TOTAL DO MES — `totalAmount` sobre `filterByDateRange(items, currentMonthRange(now))`,
   mono 28/w700. Independe do preset selecionado: e sempre o mes corrente.
2. R$/UA — total do mes dividido por `dashboardKpisProvider.totalUa`; renderiza travessao
   quando `totalUa` for zero ou o provider ainda nao resolveu.
3. POR CATEGORIA — agrupa os itens do periodo por `categoryKeyOf` somando `amountOf`,
   ordena decrescente, mostra as tres maiores num `StackedBar` (cores `AppColors.primary`,
   `AppColors.greenMid`, `AppColors.accent`) seguido de legenda com valor mono.
4. ULTIMOS 6 MESES — `lastMonthsTotals` sobre a lista COMPLETA (nao a filtrada pelo
   periodo), uma linha por mes com rotulo `DateFormat('MMM', 'pt_BR')`, um `StackedBar` de
   segmento unico com fracao `total / maiorTotalDosSeisMeses` e o valor mono a direita.
   Zero query nova — a agregacao roda sobre os dados ja carregados.

"Novo gasto": como `ExpenseFormDialog` exige `paddockId` e nao tem seletor de piquete,
encadear — primeiro um `showAdaptiveForm<String>` com titulo "Em qual piquete?" listando os
piquetes de `paddockListProvider` (cada um um `ListTile` que faz `Navigator.pop` com o id),
depois, se um id voltou, `showAdaptiveForm<bool>` com o `ExpenseFormDialog(propertyId:,
paddockId:)` existente, sem tocar na assinatura dele. Se o dialogo salvou, invalidar
`unifiedExpenseListByPropertyProvider` (o proprio dialogo ja invalida os providers por
piquete). Invalidar o mesmo provider tambem depois de editar e de excluir.

Faixa <`Breakpoints.rail`: `CampoAppBar(title: 'Gastos')`, o mesmo dropdown de periodo e um
`ListView` de `ExpenseListItemCard` (reusar o widget existente de
`_expense_list_item_card.dart`, sem card novo) com os mesmos callbacks de tap e exclusao.
Sem FAB nesta faixa: o lancamento no celular continua sendo o fluxo por piquete e esta rota
so e alcancavel pelo rail/drawer do desktop. `EmptyState` com `Icons.receipt_long_outlined`
nas duas faixas quando o periodo nao tiver lancamentos.

Criar `test/widget/gastos_property_screen_test.dart` com o harness de
`test/widget/lotes_desktop_test.dart` (`setSurfaceSize` + `addTearDown`, `ProviderScope`
com overrides, `MaterialApp.router` com um `GoRouter` minimo servindo a tela em `/` e um
stub em `AppRoutes.aplicacaoById`). Overrides: `unifiedExpenseListByPropertyProvider`,
`paddockListProvider`, `dashboardKpisProvider`, `memberPropertiesProvider` com uma unica
associacao de veterinario. Fixtures: dois gastos manuais em piquetes diferentes e uma
aplicacao sanitaria com custo, todos no mes corrente. Casos: (1) em 1440x900 a tabela e o
painel de 330px renderizam e o total do rodape bate com o subtitulo do header; (2) em
1440x900 os cabecalhos DATA/CATEGORIA/PIQUETE/VALOR aparecem e os nomes dos dois piquetes
aparecem nas linhas; (3) em 800x600 nao ha tabela e ha `ExpenseListItemCard`; (4) em
1440x900 com lista vazia aparece o `EmptyState`.
  </action>
  <verify>
    <automated>flutter test test/widget/gastos_property_screen_test.dart</automated>
    <automated>[ "$(grep -c 'class GastosPropertyScreen' lib/features/gastos/presentation/gastos_property_screen.dart)" -eq 1 ]</automated>
    <automated>[ "$(grep -c 'width: 330' lib/features/gastos/presentation/gastos_property_screen.dart)" -ge 1 ]</automated>
    <automated>[ "$(grep -c 'Breakpoints.rail' lib/features/gastos/presentation/gastos_property_screen.dart)" -ge 1 ]</automated>
    <automated>flutter analyze --no-fatal-infos lib/features/gastos test/widget/gastos_property_screen_test.dart</automated>
  </verify>
  <done>
A tela existe num unico arquivo, alterna em `Breakpoints.rail`, mostra tabela mais painel de
330px no desktop e `ExpenseListItemCard` no mobile, o total do rodape e o do header saem da
mesma lista filtrada, "Novo gasto" encadeia seletor de piquete e dialogo existente sem
mudar a assinatura dele, nenhuma cor literal foi introduzida, e os 4 casos do teste widget
passam.
  </done>
</task>

<task type="auto">
  <name>Task 3: 6a branch do shell, item "Gastos" no rail/drawer, testes de rota e shell atualizados</name>
  <files>lib/core/router/routes.dart, lib/core/router/router.dart, lib/core/widgets/app_shell.dart, test/core/router_test.dart, test/widget/app_shell_test.dart</files>
  <action>
Em `routes.dart`: adicionar `static const gastos = '/gastos';` ao bloco das branches do
shell, depois de `sanitario`, e incluir `gastos` como ultimo elemento de `AppRoutes.all`
(que passa a ter 6 entradas). Reescrever o comentario que hoje precede `gastosById`: ele
afirma que uma 6a branch foi rejeitada — substituir por uma nota registrando que a decisao
foi revertida por aprovacao do usuario em 2026-08-13, que `/gastos` agora e a 6a branch com
a visao consolidada da propriedade, que `/gastos/:paddockId` continua root-level como alvo
de deep link a partir do piquete, e que o bottom nav mobile segue com 5 destinos porque
Gastos e um destino so de desktop. Nao alterar `gastosById` nem `gastosPorPiquete`.

Em `router.dart`: criar `_shellGastosKey` no bloco de GlobalKeys e adicionar uma 6a
`StatefulShellBranch` como ULTIMA das branches, com uma `GoRoute` em `AppRoutes.gastos`
construindo `const GastosPropertyScreen()`. A posicao importa: o indice da branch tem que
casar com o indice do item no `_navItems` do shell. Manter a `GoRoute` root-level de
`AppRoutes.gastos.../:paddockId` onde ela ja esta, declarada antes do `StatefulShellRoute` —
e ela quem continua atendendo o caminho de dois segmentos.

Em `app_shell.dart`: acrescentar um sexto `_NavItem` ao final de `_navItems`, com
`Icons.payments_outlined` / `Icons.payments` e label "Gastos". Como o bottom nav de <600px
nao ganha esse destino, a ramificacao mobile passa a construir os `NavigationDestination`
a partir dos cinco primeiros itens e a passar um `selectedIndex` limitado ao ultimo indice
valido do bottom nav — sem esse limite, entrar em /gastos por deep link e depois estreitar
a janela entrega ao `NavigationBar` um indice fora do alcance dos destinos e o framework
lanca. Deixar um comentario curto explicando os dois pontos. O rail de icones e o drawer de
232px iteram `items` inteiro e ja acomodam o sexto item sem outra mudanca.

Em `test/core/router_test.dart`: atualizar as duas asercoes de `AppRoutes.all.length` de 5
para 6, incluir `expect(AppRoutes.all, contains('/gastos'))` no primeiro teste, ajustar o
nome do teste para 6 caminhos, e adicionar uma asercao de que `AppRoutes.gastosById`
continua valendo `/gastos/:paddockId` (as duas rotas coexistem).

Em `test/widget/app_shell_test.dart`: acrescentar uma sexta `StatefulShellBranch` para
`/gastos` no `_buildTestRouter`, adicionar ao teste de 1440x900 a asercao de que o rotulo
"Gastos" aparece no drawer, e adicionar ao teste de 360x800 a asercao de que o
`NavigationBar` tem exatamente 5 destinos (lendo `tester.widget<NavigationBar>` e contando
`destinations`). Estes dois arquivos de teste sao editaveis neste plano por excecao
explicita: sao o custo orcado da reversao da decisao da Fase 7, e as mudancas sao
estritamente aditivas fora das contagens 5 -> 6.

Rodar a suite inteira ao final: qualquer outro teste que conte destinos de navegacao ou
entradas de `AppRoutes.all` tem que ser corrigido pelo mesmo criterio.
  </action>
  <verify>
    <automated>[ "$(grep -c "static const gastos = '/gastos'" lib/core/router/routes.dart)" -eq 1 ]</automated>
    <automated>[ "$(grep -c 'GastosPropertyScreen' lib/core/router/router.dart)" -ge 1 ]</automated>
    <automated>[ "$(grep -c "label: 'Gastos'" lib/core/widgets/app_shell.dart)" -eq 1 ]</automated>
    <automated>flutter test test/core/router_test.dart test/widget/app_shell_test.dart</automated>
    <automated>flutter analyze --no-fatal-infos</automated>
    <automated>flutter test</automated>
  </verify>
  <done>
`AppRoutes.all` tem 6 entradas incluindo `/gastos`; o comentario da Fase 7 sobre a 6a branch
foi substituido pela nota de reversao datada; a 6a `StatefulShellBranch` e a ultima e serve
`GastosPropertyScreen`; o rail e o drawer mostram "Gastos" depois de "Sanitário"; o bottom
nav mobile continua com 5 destinos e nao lanca com o indice da 6a branch; `flutter analyze
--no-fatal-infos` limpo e `flutter test` verde na suite inteira.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente -> PostgREST (`expenses`) | A tela nova le a propriedade inteira; o escopo real e imposto por RLS, nao pelo filtro do cliente |
| cliente -> ExpenseFormDialog | Escrita de gasto com `paddock_id` escolhido pelo usuario no seletor encadeado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x4f-01 | Information disclosure | `unifiedExpenseListByPropertyProvider` | medium | mitigate | Reusa `fetchExpensesByProperty`, que ja filtra `property_id` e depende das policies SELECT de `expenses` da migracao `20260813_07`; nenhuma consulta nova, nenhum caminho de leitura novo |
| T-x4f-02 | Elevation of privilege | Botao "Novo gasto" e icone de exclusao | medium | mitigate | Ambos ficam atras de `canManageExpenses` na UI, e a escrita continua passando pelas policies INSERT/UPDATE de `expenses` (proprietario/veterinario) — a UI e a segunda barreira, nao a unica |
| T-x4f-03 | Tampering | Seletor de piquete encadeado | low | accept | O seletor so oferece piquetes de `paddockListProvider` (ja escopado por propriedade) e `trg`/RLS rejeitam um `paddock_id` de outra propriedade |
</threat_model>

<verification>
1. `flutter analyze --no-fatal-infos` sem erros.
2. `flutter test` verde na suite inteira (baseline: 360 testes passando; esperado subir com
   os 4 casos widget novos, o grupo de `lastMonthsTotals` e as asercoes adicionadas em
   `app_shell_test.dart`).
3. `AppRoutes.all.length == 6` e `/gastos` presente.
4. `NavigationBar` do mobile com exatamente 5 destinos.
5. Card de gastos do mes do Inicio inalterado apos a refatoracao de `monthExpensesProvider`
   (coberto por `test/widget/dashboard_screen_test.dart`, que faz override do provider).
</verification>

<success_criteria>
- Item "Gastos" navegavel no rail (600–1439) e no drawer (>=1440), apos "Sanitário".
- `/gastos` em >=1024px: tabela da propriedade com total no rodape + painel de 330px com
  total do mes, R$/UA, quebra por categoria e ultimos 6 meses.
- `/gastos` em <1024px: lista de `ExpenseListItemCard` funcional.
- `/gastos/:paddockId` continua funcionando exatamente como antes.
- Bottom nav mobile com 5 destinos, sem assertion ao entrar em /gastos e estreitar.
- Zero metodo novo em `ExpenseRepository`; `ExpenseFormDialog` com assinatura intacta.
</success_criteria>

<output>
Create `.planning/quick/260813-x4f-gastos-consolidados-da-propriedade-tela-/260813-x4f-SUMMARY.md` when done
</output>
