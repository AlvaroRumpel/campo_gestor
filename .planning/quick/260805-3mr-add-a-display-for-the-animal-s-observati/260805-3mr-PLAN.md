---
phase: quick
plan: 260805-3mr
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/animais/presentation/animal_detail_screen.dart
  - test/widget/animal_detail_screen_test.dart
autonomous: true
requirements: [Q-3MR-01]

must_haves:
  truths:
    - "A vet or owner opening an animal's ficha can read the observação text that was captured at creation, edit, or baixa time — it is no longer write-only"
    - "When observation is null or blank, the ficha shows nothing for it — no label, no empty row, no dash placeholder"
    - "Multi-line observation text (e.g. baixa-appended history) wraps and displays in full, not truncated to one line"
  artifacts:
    - lib/features/animais/presentation/animal_detail_screen.dart
    - test/widget/animal_detail_screen_test.dart
  key_links:
    - "AnimalInfoCard.build -> animal.observation -> conditional _KvRow('Observação', Text(...)) inserted into the existing Column, mirroring AtfHeaderCard's atf.observation handling in atf_detail_screen.dart"
---

<objective>
Add a read path for `Animal.observation` to `AnimalDetailScreen`'s `AnimalInfoCard` — the field
has been write-only since Phase 3 (captured in `animal_form_dialog.dart`, editable in
`animal_edit_dialog.dart`, appended-to by `baixa_dialog.dart`'s CR-01 fix in Phase 5) with zero
UI ever reading it back.

Purpose: observação carries operational notes (temperament, treatment history, baixa detail)
that vets and owners need visible in the field — right now that data is captured and then
invisible forever.

Output: one conditional labeled row on the animal ficha showing `animal.observation` when set,
rendering nothing at all when null/blank, plus widget tests proving both states.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@lib/features/animais/presentation/animal_detail_screen.dart
@lib/features/animais/data/animal_model.dart
@lib/features/reproducao/presentation/atf_detail_screen.dart
@test/widget/animal_detail_screen_test.dart

Reference pattern — `AtfHeaderCard` in `atf_detail_screen.dart` (lines ~263-269) already solves
this exact problem for `AtfBatch.observation`:

    if (atf.observation != null) ...[
      const SizedBox(height: 8),
      _KvRow(label: 'Observação', value: Text(atf.observation!)),
    ],

Copy this shape into `AnimalInfoCard` for `animal.observation`. `_KvRow`'s value slot is
`Expanded(child: value)` with a plain `Text` and no `maxLines` — it already wraps multi-line
content with no extra widget needed. Label text 'Observação' is already used identically in
`animal_form_dialog.dart` L221 and `atf_detail_screen.dart` — keep it consistent.

Scope: read-only display. Do NOT modify `animal_form_dialog.dart`, `animal_edit_dialog.dart`,
or `baixa_dialog.dart` — their write paths already work and are out of scope.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Display Observação row on animal ficha + widget tests</name>
  <files>lib/features/animais/presentation/animal_detail_screen.dart, test/widget/animal_detail_screen_test.dart</files>
  <behavior>
    - animal_detail_screen_test: an animal with a non-empty `observation` renders the label
      'Observação' and the observation text somewhere on the ficha.
    - animal_detail_screen_test: an animal with `observation: null` renders no 'Observação'
      label anywhere on the ficha (findsNothing).
    - animal_detail_screen_test: an animal with `observation: ''` (blank, not null — the
      baixa/edit dialogs can produce this if a caller ever passes empty string directly)
      also renders no 'Observação' label.
    - animal_detail_screen_test: a multi-line observation (embedded '\n') renders its full text
      content, not just the first line.
  </behavior>
  <action>
In `lib/features/animais/presentation/animal_detail_screen.dart`, inside `AnimalInfoCard.build`,
insert a new conditional block into the `Column`'s `children` list, placed after the
`'Cadastrado em'` `_KvRow` and before the `'Status'` `_KvRow` (mirrors where `AtfHeaderCard`
places its own observation row: after the last plain descriptive field, before the
derived/status content):

    if (animal.observation != null && animal.observation!.trim().isNotEmpty) ...[
      const SizedBox(height: 8),
      _KvRow(label: 'Observação', value: Text(animal.observation!)),
    ],

This is the exact pattern `AtfHeaderCard` uses for `atf.observation` in
`lib/features/reproducao/presentation/atf_detail_screen.dart`, extended with a blank-string
guard (the ATF version only checks `!= null`; the constraint here also requires hiding on
empty/whitespace-only text, so add the `.trim().isNotEmpty` check). No new imports needed —
`_KvRow` is already defined in this file. Do not add a `maxLines` or `overflow` property to the
`Text` — the unbounded default is what makes multi-line text wrap and display in full inside
`_KvRow`'s `Expanded` value slot.

Then extend `test/widget/animal_detail_screen_test.dart` with a new top-level `group` named
"AnimalDetailScreen — Observação display" containing the four cases in `<behavior>`. Reuse the
existing `_buildScreen` helper and the `_activeAnimal` fixture pattern already in this file —
for each case, build a new `Animal` via `_activeAnimal.copyWith(observation: ...)` (freezed
provides `copyWith`) rather than declaring new top-level fixtures. For the null case, use
`_activeAnimal` as-is (it already has no `observation` set, i.e. null). Assert with
`find.text('Observação')` for the label and `find.textContaining(...)` or `find.text(...)` for
the value text; for the multi-line case, assert on a substring that only exists on the second
line to prove the full string rendered, not just line one.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor && flutter test test/widget/animal_detail_screen_test.dart</automated>
  </verify>
  <done>AnimalInfoCard renders a labeled 'Observação' row with the full (multi-line-capable) text when animal.observation is a non-blank string, and renders nothing for it when observation is null or blank; all cases in test/widget/animal_detail_screen_test.dart pass, including the pre-existing Mover-animal and Histórico Reprodutivo groups in the same file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase row -> AnimalDetailScreen UI | `animal.observation` is operator-authored free text already stored via the existing write paths; this plan only renders it back, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q-3MR-01 | Information disclosure | Observação row visibility | low | accept | The row renders on the same screen already gated by existing property-membership RLS/query scoping (`animalByIdProvider`); no new role check is introduced or removed, and Flutter's `Text` widget does not interpret its content as markup/HTML, so stored free text cannot inject UI. |

No package-manager installs in this plan — no legitimacy gate required.
</threat_model>

<verification>
1. `flutter test test/widget/animal_detail_screen_test.dart` — all cases pass, including pre-existing groups.
2. Manual: open an animal ficha whose observation was set via `animal_form_dialog.dart` or appended to via a baixa — confirm the full text (including any baixa-appended lines) displays under an 'Observação' label. Open an animal with no observation and confirm no trace of the label/row appears.
</verification>

<success_criteria>
- `animal.observation` is readable from the ficha for the first time.
- Null/blank observation renders zero additional UI (no label, no row, no placeholder dash).
- Multi-line text (baixa-appended history) displays in full.
- No changes to `animal_form_dialog.dart`, `animal_edit_dialog.dart`, or `baixa_dialog.dart`.
- `flutter test test/widget/animal_detail_screen_test.dart` passes.
</success_criteria>

<output>
Create `.planning/quick/260805-3mr-add-a-display-for-the-animal-s-observati/260805-3mr-SUMMARY.md` when done
</output>
