---
phase: quick-260814-g9j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260814_10_medium_hardening.sql
  - lib/core/widgets/ui.dart
  - lib/core/widgets/app_shell.dart
  - lib/core/router/router.dart
  - lib/core/auth/role_gates.dart
  - lib/features/dashboard/presentation/dashboard_screen.dart
  - lib/features/reproducao/presentation/atf_detail_screen.dart
  - lib/features/piquetes/presentation/piquetes_screen.dart
  - lib/features/animais/presentation/animais_screen.dart
  - lib/features/sanitario/presentation/registrar_aplicacao_screen.dart
  - test/widget/app_shell_test.dart
  - test/core/router_test.dart
  - test/widget/atf_detail_screen_test.dart
  - test/widget/piquetes_screen_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Propriedade só nasce pelo RPC create_property_with_membership — INSERT direto em properties é recusado pela RLS"
    - "Arquivar um lote que ainda tem animal ativo é recusado pelo banco, em qualquer caminho de escrita"
    - "Duas chamadas concorrentes de move_lot_to_paddock no mesmo lote não reportam sucesso as duas"
    - "Baixa, aplicação sanitária e gasto não aceitam data no futuro"
    - "Um gasto sempre grava created_by = quem está autenticado; não dá para forjar outro autor no INSERT"
    - "anon não consegue executar nenhuma função SECURITY DEFINER do schema public"
    - "O card Gastos do dashboard leva para /gastos e, abaixo de 600px em /gastos, nenhuma tab do bottom nav aparece selecionada"
    - "Deep link interceptado pelo login volta ao destino original depois de autenticar"
    - "Proprietário/leitor no ATF vê o resultado do DG como texto, sem controle desabilitado na tela"
    - "O banner PRECISA DE VOCÊ HOJE só aparece para veterinário"
    - "Empty state de piquetes/animais oferece CTA para veterinário e texto neutro para os demais papéis"
  artifacts:
    - supabase/migrations/20260814_10_medium_hardening.sql
    - "isVeterinarian() em lib/core/auth/role_gates.dart"
    - "SectionCard.onTap em lib/core/widgets/ui.dart"
  key_links:
    - "enforce_lot_archive_no_active_animals() ligado por CREATE TRIGGER BEFORE UPDATE OF deleted_at ON lots"
    - "redirect de router.dart → query param `from` → validação interna → destino pós-login"
    - "isVeterinarian(current, members) → gate de renderização (presente/ausente) em dashboard, ATF, piquetes e animais"
  prohibitions:
    - "NÃO rodar supabase db push / MCP apply_migration / supabase test db — o orquestrador aplica depois"
    - "NÃO editar migrations já aplicadas — correções são forward-only (precedente 20260812_06 e 20260814_09)"
    - "NÃO renderizar controle desabilitado por papel negado — papel negado vê o controle ausente ou o dado como texto"
    - "NÃO adicionar created_by = auth.uid() na policy de UPDATE de expenses (quebraria vet editando gasto de outro autor)"
---

<objective>
Fechar os findings de severidade média do review geral: 7 correções de banco numa única migration forward-only (não aplicada) e 4 correções de Flutter (navegação do card Gastos + bottom nav, deep link no login, convenção de papel no ATF/dashboard, empty states com CTA por papel).

Purpose: o review anterior (quick 260814-f2v) fechou os furos críticos de isolamento multi-tenant. Este fecha a camada seguinte — integridade de dados (arquivar lote com animais, TOCTOU, datas futuras, autoria de gasto), superfície de execução (`anon` executando RPCs, `search_path` mutável) e as lacunas de UX que o próprio código já documentou como convenção mas não cumpre.
Output: 1 arquivo SQL novo (não aplicado), 4 áreas de Flutter corrigidas, `flutter analyze` limpo e suíte verde com testes novos.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260814-f2v-corrigir-findings-do-review-seguranca-mu/260814-f2v-PLAN.md
@supabase/migrations/20260814_09_multitenant_hardening.sql
</context>

<planner_evidence>
Fatos verificados durante o planning — o executor NÃO precisa reconfirmar nenhum destes:

**Banco**

- A policy de INSERT direto em `properties` chama-se **`any_authenticated_can_create_property`** e está em `20260508_02_property_paddock.sql:36-41` (o enunciado da task diz 43-47; o nome é o que importa).
- Nenhum código Dart faz INSERT em `properties`. Os 3 usos de `.from('properties')` (`lib/features/propriedades/data/propriedade_repository.dart:19,59,73`) são select/update. A criação passa por `.rpc('create_property_with_membership')` (mesmo arquivo, ~linha 39).
- **`properties` tem `ENABLE` + `FORCE ROW LEVEL SECURITY`** (`20260504_01:20-21`), e mesmo assim dropar a policy de INSERT é seguro. Prova já existente no projeto: `dg_records` e `animal_atf_memberships` também têm FORCE RLS e **zero policies de escrita**, e os RPCs SECURITY DEFINER escrevem nelas com sucesso em PROD desde a Fase 5 (UAT aprovada). Ou seja, os SECURITY DEFINER deste projeto contornam RLS. Não gastar tempo re-investigando isso.
- **`softDeleteLot` (`lib/features/lotes/data/lote_repository.dart:87-92`) não tem nenhum caller na UI** — é código morto. O trigger de guarda de arquivamento (item b) é defesa contra PATCH direto; **não existe fluxo de tela para ajustar nem mensagem de erro amigável para escrever.**
- `move_lot_to_paddock` está em `20260519_04_movements.sql:19-81`. O UPDATE final (linhas 74-76) filtra só por `id = p_lot_id`, sem repetir `deleted_at IS NULL` e sem `IF NOT FOUND`. O padrão-alvo é o de `move_animal_to_lot` (`20260715_04_gap_move_animal_to_lot.sql`).
- **Versão vigente de `register_baixa` = `20260809_05_fix_register_baixa_null_guards.sql:22-100`.** Já tem guardas `p_reason IS NULL` e `p_date IS NULL`; falta só o guard de data futura.
- **ARMADILHA CRÍTICA: a versão vigente de `register_sanitary_application` NÃO é a de `20260811_06`.** É a de **`20260813_07_expenses_module.sql:192-306`**, que adicionou o congelamento de piquete (D-30: `v_paddock_id`/`v_paddock_name`, o lookup com `JOIN paddocks`, e as duas colunas no INSERT). Copiar de `20260811_06` reverteria a Fase 7 inteira e quebraria `paddock_id NOT NULL`. O guard novo entra logo depois do `IF p_applied_at IS NULL` que já existe em `20260813_07:244-246`.
- Policy de INSERT de gastos: `owner_vet_can_insert_expense` (`20260813_07:52-56`). `expenses.created_by` é `uuid NOT NULL DEFAULT auth.uid()` (`20260813_07:34`). O INSERT do Dart (`lib/features/gastos/data/expense_repository.dart:78-84`) envia `property_id/paddock_id/category/amount/expense_date/description` e **não envia `created_by`** — o DEFAULT resolve, então o predicado `created_by = auth.uid()` passa nos dois casos.
- A policy de UPDATE (`owner_vet_can_update_expense`, `20260813_07:64-72`) **não pode** ganhar o mesmo predicado: um vet editando gasto lançado por outro membro tem `created_by <> auth.uid()` e seria bloqueado. Deixar como está.
- **`generate_animal_number(uuid, text)` NÃO EXISTE mais em PROD** — foi dropada explicitamente em `20260514_03_lots_animals.sql:94`. Só sobrevive a overload `generate_animal_number(uuid)` (`20260514_03:96`). Revogar a assinatura de 2 args aborta a migration com `42883`.
- Assinaturas exatas e completas das 15 funções a revogar (conferidas uma a uma nos arquivos de migration):
  `is_member_of(uuid)` · `get_role(uuid)` · `create_property_with_membership(text, text)` · `generate_animal_number(uuid)` · `create_lot_with_animals(uuid, uuid, text, jsonb, jsonb, integer)` · `move_animal_to_lot(uuid, uuid)` · `move_lot_to_paddock(uuid, uuid)` · `add_animals_to_atf(uuid, jsonb)` · `remove_animal_from_atf(uuid, uuid)` · `close_atf(uuid)` · `save_dg_records(uuid, jsonb)` · `register_baixa(uuid, text, date, text)` · `register_sanitary_application(uuid, uuid, date, jsonb, text)` · `reverse_sanitary_application(uuid, text)` · `deactivate_atf_membership_on_baixa()`
- `prevent_snapshot_mutation()` (`20260508_02:198`) é `LANGUAGE plpgsql` sem cláusula `SET`. `animal_ua_weight(text)` (`20260810_06:61`) é `LANGUAGE sql IMMUTABLE` sem cláusula `SET`. Ambas alvo do item (g).
- CHECK de tabela com `current_date` é aceito pelo Postgres (a exigência de IMMUTABLE vale para CHECK de **domínio**, não de tabela). Trade-off já aprovado pelo usuário.

**Flutter**

- `app_shell.dart` está em **`lib/core/widgets/app_shell.dart`**, não em `lib/core/router/`. O clamp problemático são as linhas 63-64: `navigationShell.currentIndex.clamp(0, _mobileNavCount - 1)` — em `/gastos` (branch 5) destaca "Sanitário" (índice 4).
- `_navItems` tem 6 entradas; `_mobileNavCount = 5`. `NavigationDestination` aceita `selectedIcon` — passar `Icon(item.icon)` nele mata a variante preenchida sem tocar em mais nada.
- `SectionCard` (`lib/core/widgets/ui.dart:83-124`) é um `Card` + `Padding` + `Column`, já com slot `trailing`. **Não tem `onTap`.** `_LotacaoCard` e `_PrenhezCard` também não têm — o card de Gastos não é o único, mas é o único que tem destino próprio (`/gastos`, 6ª branch do shell).
- `_GastosCard` (`dashboard_screen.dart:613-665`) é instanciado em dois pontos: `dashboard_screen.dart:60` (dentro de um `const Row`) e `:706` (dentro de um `const Column`). Adicionar navegação **dentro do `build`** não quebra nenhum desses `const`.
- `EmptyState` (`ui.dart:132-144`) já tem `Widget? action` — parâmetro existente, nunca usado em `lib/`.
- Redirect do router: `lib/core/router/router.dart:55-114`. O ponto de captura é a linha 77 (`return onAuthRoute ? null : AppRoutes.login;`); o ponto de consumo é a linha 95-97 (`if (loc == AppRoutes.login || loc == AppRoutes.signup) return AppRoutes.dashboard;`).
- **`LoginScreen` não navega após o sign-in** (`lib/features/auth/presentation/login_screen.dart:51` chama só `signIn`; os únicos `context.go` são para signup/reset). Quem move o usuário é o redirect — logo a lógica de `from` funciona sem tocar na tela de login.
- `AppRoutes.authRoutes` = `[login, signup, resetPassword]` e **exclui `noAccess` de propósito** (`routes.dart:78-85`).
- `atf_detail_screen.dart`: `_canEdit` em 184-194 (vet only), `canEdit` passado a `_AtfDgBody` (532/547), `editable = widget.canEdit && !_saving` na linha 746. **`editable` mistura papel com estado transitório de salvamento** — o gate de presença/ausência deve usar `widget.canEdit`, nunca `editable` (durante o save o vet deve ver controle desabilitado, isso é correto).
- `DgSegmentButton` (definido em `atf_detail_screen.dart:463`) tem **um único uso**, na linha 923. Não há gêmeo desktop desse controle para corrigir junto.
- `dashboardAlertsProvider` (`lib/features/dashboard/data/dashboard_providers.dart:122-144`) gera dois tipos de alerta — DGs pendentes e UA acima da capacidade. Ambos são ações de veterinário (salvar DG, mover lote), então gatear o banner inteiro é correto; não precisa filtrar por tipo. Consumido em `dashboard_screen.dart:38` e `:675`.
- Empty state de piquetes: `piquetes_screen.dart:127-133`. O `canEdit` já está em escopo (linha 63) e o criador é `_openForm(context)` (linha 338), o mesmo callback do FAB (linha 165).
- Empty state de animais: `animais_screen.dart:318-323` (ramo `animals.isEmpty`). O ramo vizinho (`:325-330`, "Nenhum animal encontrado") é sobre filtro, **não leva CTA**. `canEdit` já está em escopo (linha 143).
- `RegistrarAplicacaoScreen` só é alcançável pelo FAB de `sanitario_screen.dart:278`, que é gateado por `canEdit` (vet). **Não adicionar checagem de papel lá — seria gate morto.** O ponto do "sem dose" é `_pickDose` (`registrar_aplicacao_screen.dart:117-131`), cujo `_PickerSheet` mostra `emptyMessage: 'Nenhuma dose cadastrada — cadastre uma dose primeiro'`.
- `DoseFormDialog` (`lib/features/sanitario/presentation/dose_form_dialog.dart:23`) é público e aceita `existing` opcional; `sanitario_screen.dart:119` o abre via `showDialog`. Reusável direto de `registrar_aplicacao_screen` — o usuário não perde o formulário em andamento.
- `role_gates.dart` tem hoje só `canManageExpenses` (owner+vet). O predicado vet-only está **duplicado privadamente** em `animais_screen:75-85`, `animal_detail_screen:48-57`, `lote_detail_screen:194-203`, `piquetes_screen:331-335`, `atf_detail_screen:184-194`. **Não refatorar esses cinco** — só evitar criar um sexto.
- `test/widget/app_shell_test.dart` já monta um router de teste com **as 6 branches**, incluindo `/gastos` — dá para navegar até lá e assertar sem infraestrutura nova.
- `test/core/router_test.dart` hoje só testa constantes de `AppRoutes` (sem GoRouter, sem Supabase). Um helper puro é o único jeito barato de testar a lógica de `from` nesse arquivo.
</planner_evidence>

<tasks>

<task type="auto">
  <name>Task 1: Migration de hardening médio (7 fixes, forward-only, não aplicada)</name>
  <files>supabase/migrations/20260814_10_medium_hardening.sql</files>
  <action>
Criar UM arquivo novo com este nome exato. Não editar nenhuma migration existente. Cabeçalho comentado no mesmo formato de `20260814_09_multitenant_hardening.sql`: origem (findings médios do review), aviso de que **não foi aplicado** e de que os dois blocos `DO` de pré-checagem existem porque `ADD CONSTRAINT` aborta a transação inteira se PROD tiver linha violando.

Conteúdo, nesta ordem:

1. **Criação de propriedade só pelo RPC**: `DROP POLICY IF EXISTS "any_authenticated_can_create_property" ON properties;`. Comentar que a policy virou obsoleta quando `create_property_with_membership` passou a SECURITY DEFINER, que nenhum caminho Dart faz INSERT direto, e que o definer contorna o FORCE RLS da tabela (evidência: `dg_records` e `animal_atf_memberships` são escritas por RPC apesar de FORCE RLS e zero policies de escrita).

2. **Guarda de arquivamento de lote**: `enforce_lot_archive_no_active_animals()` (`LANGUAGE plpgsql`, `SET search_path = public`) que faz `RAISE EXCEPTION` com `ERRCODE = '23514'` quando existe animal com `lot_id = NEW.id AND deleted_at IS NULL`. Mensagem citando o id do lote e a contagem de animais ativos. Ligar com `DROP TRIGGER IF EXISTS trg_lots_archive_guard ON lots;` seguido de `CREATE TRIGGER trg_lots_archive_guard BEFORE UPDATE OF deleted_at ON lots FOR EACH ROW WHEN (OLD.deleted_at IS NULL AND NEW.deleted_at IS NOT NULL) EXECUTE FUNCTION enforce_lot_archive_no_active_animals();`. O `WHEN` cobre a direção arquivar; restaurar (`NOT NULL` → `NULL`) continua livre.

3. **TOCTOU em `move_lot_to_paddock`**: `CREATE OR REPLACE FUNCTION move_lot_to_paddock(p_lot_id uuid, p_paddock_id uuid)` com o corpo de `20260519_04_movements.sql:19-78` copiado verbatim, alterando **só** o passo 6: o `UPDATE lots SET paddock_id = p_paddock_id WHERE id = p_lot_id` ganha `AND deleted_at IS NULL`, e logo abaixo entra `IF NOT FOUND THEN RAISE EXCEPTION 'lot % was archived concurrently', p_lot_id USING ERRCODE = '23503'; END IF;`. Comentar que é o mesmo padrão WR-01 de `20260715_04_gap_move_animal_to_lot.sql`. Não recriar grants (CREATE OR REPLACE preserva a ACL).

4. **Data futura em `register_baixa`**: `CREATE OR REPLACE FUNCTION register_baixa(p_animal_id uuid, p_reason text, p_date date, p_observation text DEFAULT NULL)` com o corpo de `20260809_05_fix_register_baixa_null_guards.sql:22-100` copiado verbatim, adicionando **um** bloco logo depois do guard `p_date IS NULL`: `IF p_date > current_date THEN RAISE EXCEPTION 'baixa date cannot be in the future: %', p_date USING ERRCODE = '22023'; END IF;`. Nada mais muda — o CASE de append de observação (CR-01) e o `IF NOT FOUND` ficam idênticos.

5. **Data futura em `register_sanitary_application`**: `CREATE OR REPLACE FUNCTION register_sanitary_application(p_lot_id uuid, p_dose_id uuid, p_applied_at date, p_animal_ids jsonb, p_notes text DEFAULT NULL)`. **Copiar o corpo de `supabase/migrations/20260813_07_expenses_module.sql:192-306`, NÃO o de `20260811_06_sanitary_rpcs.sql`** — a versão de 20260813_07 é a que está viva em PROD e é a única que congela `paddock_id`/`paddock_name` (D-30). Ler esse trecho antes de escrever. Adicionar **um** bloco logo depois do guard `p_applied_at IS NULL` já existente: `IF p_applied_at > current_date THEN RAISE EXCEPTION 'applied_at cannot be in the future: %', p_applied_at USING ERRCODE = '22023'; END IF;`. Comentar no cabeçalho da seção que a versão-base é a de 20260813_07 e por quê, para o próximo leitor não repetir o erro.

6. **Data futura em `expenses`**: bloco `DO` que conta `expenses` com `expense_date > current_date` e, se > 0, `RAISE EXCEPTION` reportando a contagem (o operador aplica isso à mão em PROD e precisa saber quantas linhas corrigir). Depois `ALTER TABLE expenses ADD CONSTRAINT expenses_date_not_future CHECK (expense_date <= current_date);`. Comentar que `current_date` não é IMMUTABLE, que Postgres exige IMMUTABLE apenas em CHECK de **domínio** (não de tabela), e que o trade-off foi aceito deliberadamente.

7. **Autoria de gasto**: `DROP POLICY IF EXISTS "owner_vet_can_insert_expense" ON expenses;` seguido do `CREATE POLICY` com o mesmo nome, mesmas condições de `20260813_07:52-56`, mais `AND created_by = auth.uid()` no `WITH CHECK`. Comentar que o INSERT do app não envia `created_by` (usa o DEFAULT `auth.uid()`), então o predicado passa nos dois casos, e que a policy de UPDATE **fica intacta de propósito** — exigir `created_by = auth.uid()` lá bloquearia um vet editando gasto lançado por outro membro.

8. **Superfície de execução**: uma linha `REVOKE EXECUTE ON FUNCTION <assinatura> FROM anon, PUBLIC;` para cada uma das 15 assinaturas listadas em `<planner_evidence>` — copiar de lá, são exatas. `deactivate_atf_membership_on_baixa()` é trigger function e leva `FROM anon, PUBLIC, authenticated` (o trigger roda como definer, o caller nunca precisa de EXECUTE). **Uma função por linha, sem comentário na mesma linha do REVOKE.** Não incluir nenhuma outra assinatura: `generate_animal_number(uuid, text)` foi dropada em `20260514_03:94` e revogá-la aborta a migration com `42883`. Comentar (em linha própria, acima do bloco) que `REVOKE ... FROM public` sozinho não remove o grant que o Supabase dá a `anon` via `ALTER DEFAULT PRIVILEGES` — daí a menção explícita a `anon`.

9. **search_path fixo**: `ALTER FUNCTION prevent_snapshot_mutation() SET search_path = public;` e `ALTER FUNCTION animal_ua_weight(text) SET search_path = public;`. Comentar que `animal_ua_weight` é `LANGUAGE sql IMMUTABLE` e que a cláusula `SET` impede o planner de fazer inlining dela — custo aceito para silenciar o advisor, já que o corpo é um `CASE` puro sem referência a objeto nenhum.

PROIBIDO nesta task: rodar `supabase db push`, `supabase migration up`, `supabase test db` ou qualquer MCP `apply_migration`. O orquestrador aplica depois. Escrever o arquivo e parar.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/campo_gestor" && M=supabase/migrations/20260814_10_medium_hardening.sql && B=$(grep -v '^[[:space:]]*--' "$M") && for t in 'any_authenticated_can_create_property' 'enforce_lot_archive_no_active_animals' 'trg_lots_archive_guard' 'move_lot_to_paddock' 'register_baixa' 'register_sanitary_application' 'expenses_date_not_future' 'owner_vet_can_insert_expense' 'prevent_snapshot_mutation' 'animal_ua_weight'; do echo "$B" | grep -q "$t" || { echo "FALTA: $t"; exit 1; }; done && R=$(echo "$B" | grep -c 'REVOKE EXECUTE ON FUNCTION') && [ "$R" -eq 15 ] || { echo "REVOKE EXECUTE=$R esperado 15"; exit 1; } && A=$(echo "$B" | grep -c 'ALTER FUNCTION') && [ "$A" -eq 2 ] || { echo "ALTER FUNCTION=$A esperado 2"; exit 1; } && N=$(echo "$B" | grep -c 'current_date') && [ "$N" -ge 3 ] && G=$(git status --porcelain supabase/migrations/ | grep -c '^') && [ "$G" -eq 1 ] && echo "OK revokes=$R alters=$A"</automated>
  </verify>
  <done>Arquivo novo existe com os 7 fixes; exatamente 15 linhas `REVOKE EXECUTE ON FUNCTION` e 2 `ALTER FUNCTION`; `current_date` aparece nos 3 guards de data; `git status` mostra 1 único arquivo em `supabase/migrations/` e nenhuma migration existente modificada; nada aplicado ao banco.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card Gastos navegável, bottom nav sem seleção falsa e deep link preservado no login</name>
  <files>lib/core/widgets/ui.dart, lib/core/widgets/app_shell.dart, lib/core/router/router.dart, lib/features/dashboard/presentation/dashboard_screen.dart, test/widget/app_shell_test.dart, test/core/router_test.dart</files>
  <behavior>
    - Em `/gastos` com viewport de 360px, nenhum ícone preenchido de destino aparece no `NavigationBar` (nem o de Início, nem o de Sanitário).
    - Nas 5 branches que existem no bottom nav, o ícone preenchido do destino ativo continua aparecendo normalmente.
    - `safeReturnTo('/piquetes')` devolve `/piquetes`; `safeReturnTo(null)`, `safeReturnTo('/login')`, `safeReturnTo('/sem-acesso')`, `safeReturnTo('https://evil.tld')` e `safeReturnTo('//evil.tld')` devolvem todos `/dashboard`.
  </behavior>
  <action>
**(a) `SectionCard.onTap` (`lib/core/widgets/ui.dart`)** — adicionar um parâmetro opcional `VoidCallback? onTap`. Quando não-nulo, envolver o `Padding` interno num `InkWell` **dentro** do `Card` (fora do `Card` o ripple fica atrás da superfície e não aparece), com `borderRadius` igual ao raio do `Card` do tema. Quando nulo, a árvore fica byte-idêntica à de hoje — nenhum dos outros usos de `SectionCard` muda.

**(b) Card Gastos (`dashboard_screen.dart`, `_GastosCard`)** — passar `onTap: () => context.go(AppRoutes.gastos)` e `trailing: const Icon(Icons.chevron_right, size: 20, color: AppColors.textSecondary)` (o slot `trailing` já existe no `SectionCard`) como affordance. Importar `AppRoutes` se ainda não estiver importado. Não mexer nos construtores `const` dos dois call sites (`:60` e `:706`) — a navegação vive dentro do `build`.

**(c) Bottom nav sem seleção falsa (`lib/core/widgets/app_shell.dart:57-82`)** — trocar o `clamp` por uma flag explícita:

- `final outOfNav = navigationShell.currentIndex >= _mobileNavCount;`
- `selectedIndex: outOfNav ? 0 : navigationShell.currentIndex` (o `NavigationBar` exige um índice válido; 0 é só o valor legal, não uma seleção visível).
- em cada `NavigationDestination`, `selectedIcon: Icon(outOfNav ? item.icon : item.selectedIcon)` — assim o índice 0 nunca mostra a variante preenchida.
- quando `outOfNav`, envolver o `NavigationBar` num `NavigationBarTheme(data: NavigationBarThemeData(indicatorColor: Colors.transparent), child: ...)` para a pílula do indicador sumir.
- substituir o comentário atual (que descreve o clamp) por um que explique o novo comportamento: abaixo de 600px, `/gastos` (branch 6) é alcançável por deep link e pelo card do dashboard, e nesse estado nenhuma tab representa a tela atual.

Aceitar que o **label** do índice 0 mantém a cor de "selecionado" do tema — diferença sutil, não vale um `labelTextStyle` customizado. Não adicionar Gastos como 6º destino do bottom nav (decisão explícita de 260813-x4f).

**(d) Deep link preservado no login (`lib/core/router/router.dart`)** — duas mudanças no `redirect`, mais um helper puro:

- Helper top-level exportado no mesmo arquivo, para ser testável sem Supabase:
  `String safeReturnTo(String? from)` → devolve `from` se ele começar com `/`, **não** começar com `//` (protocol-relative), não estiver em `AppRoutes.authRoutes` e não for `AppRoutes.noAccess`; caso contrário devolve `AppRoutes.dashboard`. Comparar apenas o **path** de `from` contra a lista de rotas (usar `Uri.parse(from).path`), para `/piquetes?x=1` continuar válido.
- **Captura** (linha 77): quando `!isLoggedIn` e não está em rota de auth, redirecionar para `/login` carregando o destino original — `Uri(path: AppRoutes.login, queryParameters: {'from': state.uri.toString()}).toString()`. Não anexar `from` quando `loc == AppRoutes.noAccess`.
- **Consumo** (linhas 95-97): trocar `return AppRoutes.dashboard;` por `return safeReturnTo(state.uri.queryParameters['from']);`. Manter o bounce de `/sem-acesso` (linhas 100-104) apontando para `/dashboard` como está.

`LoginScreen` não precisa de nenhuma alteração — ela não navega após o sign-in, quem move o usuário é este redirect.

**Testes:**
- `test/core/router_test.dart`: um `test` novo cobrindo os 6 casos do `<behavior>` de `safeReturnTo` — função pura, sem `ProviderScope`, sem Supabase.
- `test/widget/app_shell_test.dart`: reusar o `_buildTestRouter` existente (já tem as 6 branches). Um `testWidgets` novo que monta em 360x800, navega para `/gastos` (`tester.element(...)` + `context.go`, ou `initialLocation` num router paralelo — o que o arquivo já facilitar), e assere `find.byIcon(Icons.home)` e `find.byIcon(Icons.medical_services)` `findsNothing` (ícones preenchidos), com `find.byIcon(Icons.home_outlined)` `findsOneWidget`. Manter os testes existentes verdes.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/campo_gestor" && grep -q 'this.onTap' lib/core/widgets/ui.dart && grep -q 'outOfNav' lib/core/widgets/app_shell.dart && grep -q 'indicatorColor' lib/core/widgets/app_shell.dart && grep -q 'safeReturnTo' lib/core/router/router.dart && grep -q "'from'" lib/core/router/router.dart && grep -q 'AppRoutes.gastos' lib/features/dashboard/presentation/dashboard_screen.dart && grep -q 'safeReturnTo' test/core/router_test.dart && flutter analyze --no-pub && flutter test > /tmp/g9j_t2.log 2>&1; RC=$?; tail -6 /tmp/g9j_t2.log; exit $RC</automated>
  </verify>
  <done>`SectionCard` aceita `onTap`; o card Gastos navega para `/gastos` e mostra chevron; em `/gastos` a 360px nenhum ícone preenchido aparece no bottom nav; `safeReturnTo` existe, é puro e cobre os 6 casos em `router_test.dart`; `flutter analyze` sem issues; `flutter test` verde com ≥2 testes novos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convenção de papel — DG somente-leitura, banner de alertas vet-only e empty states com CTA</name>
  <files>lib/core/auth/role_gates.dart, lib/features/dashboard/presentation/dashboard_screen.dart, lib/features/reproducao/presentation/atf_detail_screen.dart, lib/features/piquetes/presentation/piquetes_screen.dart, lib/features/animais/presentation/animais_screen.dart, lib/features/sanitario/presentation/registrar_aplicacao_screen.dart, test/widget/atf_detail_screen_test.dart, test/widget/piquetes_screen_test.dart</files>
  <behavior>
    - Proprietário/leitor abrindo um ATF vê o resultado do DG de cada animal como texto estático e **nenhum** `DgSegmentButton` na árvore; veterinário continua vendo os segmentos.
    - Veterinário salvando (`_saving == true`) continua vendo os segmentos **desabilitados**, não ausentes — papel negado é ausência, ocupação transitória é desabilitação.
    - Com a lista de piquetes vazia, veterinário vê um botão de criar piquete no empty state; leitor vê texto neutro e nenhum botão.
  </behavior>
  <action>
**(a) Helper único (`lib/core/auth/role_gates.dart`)** — adicionar `bool isVeterinarian(SelectedProperty? current, List<PropertyMembership>? members)` ao lado de `canManageExpenses`, com a mesma forma (resolve o `role` da propriedade ativa, `false` se qualquer entrada for nula). Doc comment curto em pt-BR citando a convenção que já está no cabeçalho do arquivo: **papel negado vê o controle ausente, nunca desabilitado**. **Não** refatorar os cinco `_canEdit` privados existentes (`animais_screen`, `animal_detail_screen`, `lote_detail_screen`, `piquetes_screen`, `atf_detail_screen`) — o objetivo é não criar um sexto, não mexer nos cinco.

**(b) Banner de alertas vet-only (`dashboard_screen.dart`)** — em `_MobileDashboard.build` (linha ~37) e `_DesktopDashboard.build` (linha ~674), computar `isVeterinarian(ref.watch(currentPropertyProvider).asData?.value, ref.watch(memberPropertiesProvider).asData?.value)` e trocar a condição `if (alerts.isNotEmpty)` por `if (isVet && alerts.isNotEmpty)`. Os dois tipos de alerta (DG pendente, UA acima da capacidade) resolvem-se com ações vet-only, então o banner inteiro sai — não filtrar por tipo. Comentar a razão numa linha.

**(c) DG somente-leitura para papel negado (`atf_detail_screen.dart`, `_AtfDgBodyState._buildRow`, ~841-958)** — quando `widget.canEdit` for `false`:

- No lugar do `Row` de `DgSegmentButton` (linhas 918-934), renderizar o resultado atual como texto estático: usar `StatusChip` (`lib/core/widgets/ui.dart`) com o rótulo do DG (`Prenhe`/`Vazia`/`Duvidosa`, os mesmos rótulos que `DgSegmentButton` já usa) ou, quando não há DG, o texto secundário `Sem DG`. Reusar o mapeamento de rótulo/cor que já existe no arquivo em vez de re-derivar.
- Omitir por completo os dois `IconButton` de data (linha 897-901) e de observação (902-907), com o mesmo padrão `if (...)` que o `canRemove` já usa na linha 908.
- Deixar `editable` (`= widget.canEdit && !_saving`) governando exatamente como hoje o caminho do veterinário: durante o save os controles continuam presentes e desabilitados. **O gate de presença/ausência é `widget.canEdit`, nunca `editable`.**
- O `TextFormField` de observação (935-955) fica inalcançável sozinho (só abre pelo botão de notas, agora ausente) — não precisa de mudança.
- `_SessionBanner` e `_SaveFooter` já são gateados por `widget.canEdit` (linhas 753 e 828) — não tocar.

**(d) Empty states sensíveis ao papel:**

- `piquetes_screen.dart:127-133`: com `canEdit` (já em escopo, linha 63), manter a mensagem atual e adicionar `action: FilledButton.icon(onPressed: () => _openForm(context), icon: const Icon(Icons.add), label: const Text('Criar piquete'))`. Sem `canEdit`, trocar a mensagem por texto neutro sem instrução de criação (ex.: `'Nenhum piquete cadastrado ainda.'`) e sem `action`. Remover o `const` do `EmptyState` quando ele deixar de ser constante.
- `animais_screen.dart:318-323` (ramo `animals.isEmpty`, `canEdit` em escopo na linha 143): com `canEdit`, manter a mensagem e adicionar `action` que navegue para `/piquetes` (`context.go(AppRoutes.piquetes)`) — é lá que "Novo lote" cria animais; rotular o botão de acordo (ex.: `'Ir para Piquetes'`). Sem `canEdit`, texto neutro sem `action`. **Não** mexer no ramo vizinho `'Nenhum animal encontrado'` (`:325-330`) — é sobre filtro, não sobre ausência de cadastro.
- `registrar_aplicacao_screen.dart`, `_pickDose` (117-131): quando `doses.isEmpty`, **não** abrir o `_PickerSheet` vazio — abrir direto `DoseFormDialog()` (importar de `../../sanitario/presentation/dose_form_dialog.dart`, caminho relativo correto a partir do arquivo) via `showDialog`, exatamente como `sanitario_screen.dart:117-120` já faz. Se o dialog retornar sucesso, `ref.invalidate(doseListByPropertyProvider)` e reabrir o seletor de dose. **Não adicionar checagem de papel aqui** — a tela só é alcançável pelo FAB vet-only de `sanitario_screen.dart:278`, um gate a mais seria código morto.

**Testes** (seguir a convenção de override de providers já usada nos arquivos):
- `test/widget/atf_detail_screen_test.dart`: um teste com papel `reader`/`owner` afirmando `find.byType(DgSegmentButton)` `findsNothing` e que o rótulo do DG aparece como texto; e um com `veterinarian` afirmando que os segmentos continuam presentes.
- `test/widget/piquetes_screen_test.dart`: um teste com lista vazia + papel não-vet afirmando que o botão de criar piquete não está na árvore.
  </action>
  <verify>
    <automated>cd "F:/_geral/Projetos/campo_gestor" && grep -q 'isVeterinarian' lib/core/auth/role_gates.dart && N=$(grep -rl 'isVeterinarian' lib/ | wc -l) && [ "$N" -ge 2 ] && grep -q 'DoseFormDialog' lib/features/sanitario/presentation/registrar_aplicacao_screen.dart && grep -q 'action:' lib/features/piquetes/presentation/piquetes_screen.dart && grep -q 'action:' lib/features/animais/presentation/animais_screen.dart && grep -q 'DgSegmentButton' test/widget/atf_detail_screen_test.dart && flutter analyze --no-pub && flutter test > /tmp/g9j_t3.log 2>&1; RC=$?; tail -6 /tmp/g9j_t3.log; exit $RC</automated>
  </verify>
  <done>`isVeterinarian` existe em `role_gates.dart` e é usado no dashboard; banner de alertas some para não-vet nas duas variantes (mobile e desktop); ATF renderiza DG como texto para papel negado e mantém segmentos desabilitados durante o save do vet; empty states de piquetes e animais têm CTA só para vet; `_pickDose` abre o formulário de dose quando não há nenhuma; `flutter analyze` sem issues; `flutter test` verde com ≥3 testes novos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → PostgREST | Qualquer usuário autenticado emite PATCH/POST arbitrário nas tabelas expostas; os RPCs são caminho voluntário, não obrigatório |
| anon → PostgREST/RPC | Sessão sem `auth.uid()` ainda consegue invocar funções SECURITY DEFINER do schema `public` |
| UI ↔ papel do usuário | A tela decide o que renderizar; o banco decide o que aceita — divergência vira instrução enganosa em campo |
| navegador → rota protegida | Deep link chega antes da sessão; o destino original é estado que pode ser perdido ou forjado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g9j-01 | Elevation of Privilege | policy `any_authenticated_can_create_property` (`WITH CHECK (true)`) em `properties` | medium | mitigate | Task 1.1 — DROP da policy; criação passa só pelo RPC SECURITY DEFINER, que audita nome/dono e cria a membership na mesma transação |
| T-g9j-02 | Tampering | `PATCH /lots?id=eq.X {"deleted_at": "..."}` com animais ativos apontando para o lote | medium | mitigate | Task 1.2 — `trg_lots_archive_guard`, independente do caminho de acesso (a UI sequer expõe arquivamento de lote) |
| T-g9j-03 | Tampering | `move_lot_to_paddock`: janela TOCTOU entre a validação e o UPDATE final | medium | mitigate | Task 1.3 — `AND deleted_at IS NULL` no UPDATE + `IF NOT FOUND` (padrão WR-01 de `move_animal_to_lot`) |
| T-g9j-04 | Repudiation | baixa / aplicação sanitária / gasto gravados com data futura, corrompendo relatório de período | medium | mitigate | Task 1.4–1.6 — guard `> current_date` nos dois RPCs + CHECK `expenses_date_not_future` |
| T-g9j-05 | Spoofing | `POST /expenses {"created_by": "<outro usuário>"}` — o DEFAULT não impede um valor explícito | medium | mitigate | Task 1.7 — `created_by = auth.uid()` no `WITH CHECK` do INSERT; UPDATE deliberadamente inalterado |
| T-g9j-06 | Information Disclosure | `anon` executando RPCs SECURITY DEFINER — oráculo de existência de UUID (`23503` vs `42501`) | medium | mitigate | Task 1.8 — `REVOKE EXECUTE ... FROM anon, PUBLIC` nas 15 funções; fecha o blocker registrado no STATE.md |
| T-g9j-07 | Elevation of Privilege | `search_path` mutável em `prevent_snapshot_mutation` e `animal_ua_weight` (advisor do Supabase) | low | mitigate | Task 1.9 — `ALTER FUNCTION ... SET search_path = public` |
| T-g9j-08 | Spoofing | query param `from` do login apontando para origem externa (open redirect) | medium | mitigate | Task 2 — `safeReturnTo` só aceita path interno (`/`, não `//`), rejeita rotas de auth e `/sem-acesso`, cai em `/dashboard` |
| T-g9j-09 | Information Disclosure | controle desabilitado por papel expõe a existência de uma ação que o usuário nunca poderá executar | low | mitigate | Task 3 — DG vira texto estático, ícones somem, banner de alertas some para não-vet |
| T-g9j-SC | Tampering | instalação de pacotes | low | accept | Nenhuma dependência nova em nenhuma das 3 tasks |
</threat_model>

<verification>
1. `git status` mostra exatamente 1 arquivo novo em `supabase/migrations/` e nenhuma migration existente modificada.
2. `flutter analyze --no-pub` sem issues.
3. `flutter test` verde (≥360 testes) com pelo menos 5 testes novos no total.
4. Nenhum comando de deploy de banco executado (sem `db push`, sem `apply_migration`, sem `test db`) — a SUMMARY registra que a migration está pendente de aplicação pelo orquestrador.
5. `20260814_09_multitenant_hardening.sql` continua intacto (byte-idêntico) — este plano não o revisa nem o reaplica.
</verification>

<success_criteria>
- Os 7 fixes de banco estão num único arquivo SQL novo, forward-only, não aplicado, com as 15 assinaturas de REVOKE exatas e sem a overload dropada de `generate_animal_number`.
- `register_sanitary_application` foi recriada a partir da versão de `20260813_07` (com o congelamento de piquete intacto), não da de `20260811_06`.
- Card Gastos navega para `/gastos`; abaixo de 600px em `/gastos` nenhuma tab aparece selecionada.
- Deep link interceptado pelo login retorna ao destino original; destino externo ou de auth cai em `/dashboard`.
- Nenhum controle aparece desabilitado por papel negado no ATF nem no dashboard.
- Empty states de piquetes e animais têm CTA para vet e texto neutro para os demais; `RegistrarAplicacaoScreen` oferece cadastro de dose quando não há nenhuma.
- `flutter analyze` limpo e suíte de testes verde.
</success_criteria>

<output>
Create `.planning/quick/260814-g9j-corrigir-findings-medios-do-review-banco/260814-g9j-SUMMARY.md` when done.

Na SUMMARY, registrar explicitamente:
- **`20260814_10_medium_hardening.sql` está pendente de aplicação** — o orquestrador aplica via MCP `apply_migration`.
- O `ADD CONSTRAINT expenses_date_not_future` aborta a transação se PROD tiver gasto com data futura; o bloco `DO` de pré-checagem reporta a contagem para o operador corrigir antes de re-rodar.
- Que `register_sanitary_application` foi recriada a partir de `20260813_07` (não de `20260811_06`), preservando `paddock_id`/`paddock_name` (D-30).
- Que os 5 `_canEdit` privados duplicados continuam de pé — `isVeterinarian` foi adicionado só para não criar um sexto; consolidá-los é candidato a quick task própria.
</output>
