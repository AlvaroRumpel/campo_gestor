---
phase: quick
plan: 260813-ok3
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/theme/breakpoints.dart
  - lib/core/theme/app_colors.dart
  - lib/core/widgets/ui.dart
  - lib/core/widgets/app_shell.dart
  - lib/core/widgets/property_selector.dart
  - test/widget/app_shell_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Em 600–1439px o shell mostra um rail de ícones de 76px (não o drawer de 232px)"
    - "Em >=1440px o shell mostra o drawer verde de 232px com header, seletor de fazenda e badges"
    - "Em <600px nada muda: NavigationBar + comportamento atual"
    - "showAdaptiveForm aceita largura configurável e usa 560px por padrão"
    - "flutter analyze limpo e a suíte de testes passa"
  artifacts:
    - lib/core/theme/breakpoints.dart
    - lib/core/widgets/app_shell.dart
  key_links:
    - "AppShell -> Breakpoints (fonte única das faixas de largura)"
    - "Rail/drawer -> PropertySelector (troca de fazenda em ambas as faixas)"
    - "Drawer badges -> atfListByPropertyProvider / animalListByPropertyProvider"
---

<objective>
Implementar o shell adaptativo desktop do redesign "musgo evoluído": três faixas de
largura (bottom nav <600 · rail de ícones 76px 600–1439 · drawer fixo 232px >=1440) e
larguras de diálogo configuráveis em `showAdaptiveForm`.

Purpose: Hoje o shell só tem duas faixas (bottom nav / drawer 232px a partir de 600px).
O drawer de 232px come largura demais em telas 600–1023 e o rail de ícones nunca existiu.
Output: `Breakpoints` como fonte única, `AppShell` com rail + drawer, `showAdaptiveForm`
com parâmetro de largura, testes de widget cobrindo as três faixas.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/core/widgets/app_shell.dart
@lib/core/theme/app_colors.dart
@lib/core/widgets/property_selector.dart
@test/widget/app_shell_test.dart

Estado atual verificado (não re-descobrir):

- `AppShell` (lib/core/widgets/app_shell.dart) é um `ConsumerWidget` com
  `LayoutBuilder`, `_breakpoint = 600`, lista privada `_navItems` de 5 `_NavItem`
  (Início, Piquetes[railLabel "Piquetes e lotes"], Animais, Reprodução, Sanitário)
  e um `_DesktopRail` de 232px + `_RailItem`.
- O router (lib/core/router/router.dart) usa `StatefulShellRoute.indexedStack` com
  exatamente essas 5 branches. **Não existe rota global de Gastos**: só
  `AppRoutes.gastosById = '/gastos/:paddockId'`, que exige um piquete. O comentário
  em `lib/core/router/routes.dart` documenta que uma sexta branch foi rejeitada de
  propósito. Portanto o item "Gastos" do mockup **não entra** nesta implementação —
  a instrução do usuário é explícita: "usar as [rotas] que existem; não inventar
  rotas novas". Não criar rota, branch nem item de nav para Gastos.
- `PropertySelector` (lib/core/widgets/property_selector.dart) é um
  `PopupMenuButton` cujo `child` é `Row(FarmAvatar + nome + expand_more)`.
- `FarmAvatar` (lib/core/widgets/ui.dart:382) já aceita `name`, `size`,
  `background`, `foreground` e deriva iniciais ("São João" -> "SJ").
- `showAdaptiveForm` (lib/core/widgets/ui.dart:482) hoje: `MediaQuery.sizeOf`
  `>= 600` -> `Dialog` com `BoxConstraints(maxWidth: 480)`, senão bottom sheet.
  26 chamadas em 22 arquivos — nenhuma passa largura, todas herdam o default.
- Contagens para badges já têm provider pronto:
  `atfListByPropertyProvider` (lib/features/reproducao/data/atf_repository.dart:389)
  devolve `List<AtfSummary>`; ativos = `.where((s) => s.atf.active)`.
  `animalListByPropertyProvider` (lib/features/animais/data/animal_repository.dart:241).
- `test/widget/app_shell_test.dart` **vai quebrar** com esta mudança: ele testa
  1024x768 esperando o texto "Campo Gestor" e "Sair", que a partir de agora só
  aparecem em >=1440. Atualizar o teste faz parte da Task 2.

Tokens (lib/core/theme/app_colors.dart): `primary` #3D5435, `accent` #E8833A,
`onAccent` #2A1806, `onGreen` #F5F3EB, `glassCard` 0x1FF5F3EB (=.12, bate com a
spec do card de fazenda), `glassStrong` 0x2EF5F3EB (=.18, usar para o estado ativo
no lugar do .16 da spec — diferença imperceptível, evita token duplicado).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Breakpoints nomeados e largura configurável em showAdaptiveForm</name>
  <files>lib/core/theme/breakpoints.dart, lib/core/widgets/ui.dart</files>
  <action>
Criar `lib/core/theme/breakpoints.dart` com uma única `abstract final class Breakpoints`
contendo três constantes `double`: `mobile = 600`, `rail = 1024`, `drawer = 1440`.
Documentar no doc-comment o significado de cada faixa: abaixo de `mobile` é bottom nav;
de `mobile` até abaixo de `drawer` é rail de ícones; a partir de `drawer` é drawer fixo.
Deixar registrado que `rail` existe para as telas usarem em layout mestre-detalhe e que
o shell não muda de forma nesse valor.

Em `lib/core/widgets/ui.dart`, adicionar à mesma seção de `showAdaptiveForm` uma
`abstract final class FormWidth` com três constantes `double`: `confirm = 440`
(confirmação destrutiva curta), `form = 560` (formulário de uma coluna, padrão) e
`wide = 680` (formulários com pares de campos lado a lado, faixa 640–720).

Alterar a assinatura de `showAdaptiveForm` para aceitar um parâmetro nomeado opcional
`double width = FormWidth.form` e usar esse valor no `maxWidth` do `BoxConstraints`
no lugar do 480 fixo. Trocar o `600` literal da comparação por `Breakpoints.mobile`
(importar `../theme/breakpoints.dart`). Atualizar o comentário de seção acima da
função para descrever a nova regra de larguras em vez da largura antiga.

Não tocar em nenhum dos 22 arquivos que chamam `showAdaptiveForm` — todos herdam
`FormWidth.form` por padrão, que é exatamente o comportamento contratado.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos 2>&1 | tail -5 && flutter test test/widget/lote_form_dialog_test.dart test/widget/mover_animal_dialog_test.dart test/features/gastos/expense_form_dialog_test.dart</automated>
  </verify>
  <done>`Breakpoints` existe com mobile/rail/drawer; `showAdaptiveForm(width: ...)` compila e usa 560 por padrão; os testes de dialog existentes continuam passando sem edição.</done>
</task>

<task type="auto">
  <name>Task 2: Rail de ícones 76px e drawer 232px no AppShell</name>
  <files>lib/core/theme/app_colors.dart, lib/core/widgets/property_selector.dart, lib/core/widgets/app_shell.dart, test/widget/app_shell_test.dart</files>
  <action>
**app_colors.dart** — adicionar um único token novo na seção "Texto":
`onGreenMuted = Color(0xCCF5F3EB)` (~0.8), com comentário indicando que é o estado
inativo de itens de navegação sobre verde. Substituir as duas ocorrências literais
de `const Color(0xCCF5F3EB)` que hoje existem em `_RailItem` por esse token durante
a reescrita do shell. Nenhuma outra cor nova.

**property_selector.dart** — adicionar ao `PropertySelector` um parâmetro nomeado
`bool compact = false`. Quando `compact` for true, o `child` do `PopupMenuButton`
passa a ser apenas `FarmAvatar(name: property?.name ?? '?', size: 36)` — sem nome,
sem `expand_more`. Todo o resto (itemBuilder, troca de fazenda, gerenciar, sair)
fica idêntico. O ramo `loading` continua como está.

**app_shell.dart** — reescrever a decisão de layout dentro do `LayoutBuilder` para
três faixas usando `Breakpoints` (remover a constante privada `_breakpoint`):
largura menor que `Breakpoints.mobile` mantém exatamente o comportamento atual
(`navigationShell` no body + `NavigationBar` no `bottomNavigationBar`); largura
menor que `Breakpoints.drawer` renderiza uma `Row` com o novo rail de ícones à
esquerda; caso contrário renderiza a `Row` com o drawer de 232px já existente.
Manter `_navItems` com os 5 itens atuais e a mesma ordem — não adicionar Gastos.

Rail de ícones (novo widget `ConsumerWidget` privado, largura fixa 76,
`color: AppColors.primary`, coluna com padding vertical): logo em quadrado de 36px,
`borderRadius` 10, fundo `AppColors.accent`, `Icons.grass` 20px em
`AppColors.onAccent`; abaixo o `PropertySelector(compact: true)` (avatar 36px que
abre o mesmo menu de fazendas); abaixo os itens de nav em coluna, cada um com 60px
de largura, `minHeight` 54, `borderRadius` 14, ícone 23px e label 10.5px centralizado
em duas linhas no máximo (`textAlign: TextAlign.center`, `maxLines: 2`,
`overflow: TextOverflow.ellipsis`), usando `item.label` (o curto, não `railLabel`);
item ativo usa fundo `AppColors.glassStrong` mais `item.selectedIcon` e cor
`AppColors.onGreen`, inativo usa `AppColors.onGreenMuted`; `Spacer` e no rodapé um
item de logout com `Icons.logout` que chama
`ref.read(currentPropertyProvider.notifier).clear()` seguido de
`ref.read(authRepositoryProvider).signOut()`, igual ao `_DesktopRail` atual.

Badge do rail: contagem de ATFs ativos posicionada no canto superior direito do item
de Reprodução via `Stack`/`Positioned`, círculo/pill em `AppColors.accent` com texto
`monoStyle` pequeno em `AppColors.onAccent`. Ler de
`ref.watch(atfListByPropertyProvider).asData?.value` e contar
`.where((s) => s.atf.active).length`; se for null ou zero, não renderizar badge nenhum.
Importar `atfListByPropertyProvider` de `../../features/reproducao/data/atf_repository.dart`.

Drawer 232px (`_DesktopRail` existente, ajustes pontuais): manter header, card de
fazenda e itens como estão; acrescentar badges à direita do label — em Animais a
contagem de `ref.watch(animalListByPropertyProvider).asData?.value.length` renderizada
com `monoStyle` discreto em `AppColors.onGreenMuted`; em Reprodução a mesma contagem
de ATFs ativos do rail, mas em pill arredondado com fundo `AppColors.accent` e texto
`monoStyle` em `AppColors.onAccent`. Ambos omitidos quando a contagem for null ou zero.
Só o drawer observa `animalListByPropertyProvider`, para que as faixas mobile e rail
não paguem esse fetch. Marcar isso com um comentário `ponytail:` no ponto do watch,
indicando que um provider dedicado de contagem substitui isso se o custo aparecer.

**test/widget/app_shell_test.dart** — o teste atual de 1024x768 espera o drawer e vai
falhar. Ajustar e ampliar para cobrir as três faixas com o mesmo `_pumpAppShell`:
360x800 continua exigindo `NavigationBar` presente e "Campo Gestor" ausente;
800x600 e 1024x768 passam a exigir `NavigationBar` ausente, `PropertySelector`
presente e o título "Campo Gestor" ausente (rail de ícones não tem título);
1440x900 exige "Campo Gestor" presente, "Sair" presente e `NavigationBar` ausente.
Mover a asserção de `PropertySelector` + "Selecionar fazenda" para o caso de 1440,
onde o seletor renderiza com nome.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos 2>&1 | tail -5 && flutter test test/widget/app_shell_test.dart && flutter test</automated>
  </verify>
  <done>`flutter analyze` limpo; `app_shell_test.dart` cobre 360/800/1024/1440 e passa; a suíte completa passa; nenhum literal de cor novo fora de `AppColors`; nenhum item ou rota de Gastos foi criado.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` sem issues.
2. `flutter test` — suíte completa verde (baseline: 360 testes).
3. `grep -n "Color(0x" lib/core/widgets/app_shell.dart` não retorna nada (todas as
   cores vêm de `AppColors`).
4. `grep -rn "gastos" lib/core/widgets/app_shell.dart` não retorna nada.
</verification>

<success_criteria>
- Uma única fonte de breakpoints (`lib/core/theme/breakpoints.dart`) consumível pelas telas.
- Três faixas de shell funcionando, com <600px byte-idêntico em comportamento ao atual.
- `showAdaptiveForm` com largura configurável (440/560/680), default 560, 26 chamadas intactas.
- `flutter analyze` limpo e suíte de testes verde.
</success_criteria>

<output>
Create `.planning/quick/260813-ok3-implementar-shell-adaptativo-desktop-do-/260813-ok3-SUMMARY.md` when done
</output>
