---
phase: quick-260819-mk7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819_14_paddock_archive_guard.sql
  - lib/core/providers/invalidate_property_data.dart
  - lib/features/piquetes/data/piquete_repository.dart
  - lib/features/piquetes/presentation/piquetes_screen.dart
  - lib/features/piquetes/presentation/paddock_form_dialog.dart
  - lib/features/piquetes/presentation/paddock_detail_screen.dart
  - lib/features/lotes/data/lote_repository.dart
  - lib/features/lotes/presentation/_lots_section.dart
  - lib/features/lotes/presentation/lote_form_dialog.dart
  - lib/features/lotes/presentation/mover_lote_dialog.dart
  - lib/features/animais/presentation/animal_form_dialog.dart
  - lib/features/animais/presentation/mover_animal_dialog.dart
  - lib/features/animais/presentation/baixa_dialog.dart
  - lib/features/sanitario/presentation/dose_form_dialog.dart
  - lib/features/sanitario/presentation/sanitario_screen.dart
  - lib/features/sanitario/presentation/registrar_aplicacao_screen.dart
autonomous: true
requirements: [QA-STURZA-1, QA-STURZA-2, QA-STURZA-3]
must_haves:
  truths:
    - "Remover piquete com lote ativo é bloqueado no banco (trigger 23514) e na UI (mensagem clara, sem exceção não tratada)"
    - "Lote sem animais ativos pode ser arquivado pelo menu do card do lote; lote com animais é bloqueado com mensagem"
    - "Após criar/editar/remover piquete, lote (c/ animais), animal, dose, baixa, mover — todas as listas da propriedade refletem a mudança sem reload manual"
    - "registrar_aplicacao_screen não tem mais provider privado duplicado de lotes"
  artifacts:
    - path: lib/core/providers/invalidate_property_data.dart
      provides: "extension InvalidatePropertyData on WidgetRef { void invalidatePropertyData() }"
    - path: supabase/migrations/20260819_14_paddock_archive_guard.sql
      provides: "trigger trg_paddocks_archive_guard + reparo de paddocks arquivados com lotes ativos"
  key_links:
    - from: piquetes_screen.dart _confirmDelete
      to: softDeletePaddock
      via: "catch PostgrestException code 23514 → SnackBar"
---

<objective>
Fechar as 3 falhas do relatório de QA (Jorge Sturza, iPhone 12 / Safari, doc "Teste do sistema campo gestor.pdf"):

1. Remover piquete deixava lotes e animais órfãos (lote sumia do board, animais com label de piquete inexistente, nenhum caminho de limpeza).
2. Listas (piquetes, lotes, animais) não atualizavam após cadastro sem F5.
3. Dose recém-criada não aparecia no seletor de "Registrar aplicação" sem F5.

Root cause (JÁ investigado — NÃO re-investigar):
- (1) `softDeletePaddock` é UPDATE deleted_at sem trigger/guarda; FKs `lots.paddock_id`/`animals.lot_id` são RESTRICT (irrelevante para soft delete). Não existe "Arquivar lote" na UI (`LoteRepository.softDeleteLot` existe sem caller). Trigger `trg_lots_archive_guard` está em `20260814_10_medium_hardening.sql`. NÃO rodar `supabase db push` neste plano — orquestrador aplica.
- (2)(3) Todos os providers de lista são `FutureProvider` keepAlive (riverpod 3). Cada mutação invalida manualmente um subconjunto incompleto (ex.: `lote_form_dialog.dart` cria lote + N animais e invalida só 2 providers; `animalListByPropertyProvider`, `loteListByPropertyProvider` ficam stale). `registrar_aplicacao_screen.dart:23` tem `_lotsWithActiveAnimalsProvider`, duplicata privada de `loteWithPaddockListByPropertyProvider`, que ninguém invalida.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
Exemplos de invalidação completa (referência): lib/features/animais/presentation/baixa_dialog.dart:87-100, lib/features/lotes/presentation/mover_lote_dialog.dart:72-77.
Precedente de tratamento de código de erro: lib/features/membros/data/membro_exception.dart (switch em PostgrestException.code '23514').
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper central de invalidação + aplicar em todas as mutações</name>
  <files>
    lib/core/providers/invalidate_property_data.dart (novo)
    lib/features/piquetes/presentation/piquetes_screen.dart
    lib/features/piquetes/presentation/paddock_form_dialog.dart
    lib/features/piquetes/presentation/paddock_detail_screen.dart
    lib/features/lotes/presentation/lote_form_dialog.dart
    lib/features/lotes/presentation/mover_lote_dialog.dart
    lib/features/animais/presentation/animal_form_dialog.dart
    lib/features/animais/presentation/mover_animal_dialog.dart
    lib/features/animais/presentation/baixa_dialog.dart
    lib/features/sanitario/presentation/dose_form_dialog.dart
    lib/features/sanitario/presentation/sanitario_screen.dart
    lib/features/sanitario/presentation/registrar_aplicacao_screen.dart
  </files>
  <action>
1. Criar `lib/core/providers/invalidate_property_data.dart`:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
// + imports dos arquivos que definem os providers abaixo

/// Invalida todo provider de leitura escopado à propriedade ativa.
/// Chamar após QUALQUER create/update/delete. Custo: refetch das listas —
/// aceitável (ponytail: invalidação seletiva só se virar gargalo medido).
extension InvalidatePropertyData on WidgetRef {
  void invalidatePropertyData() {
    invalidate(paddockListProvider);
    invalidate(paddockByIdProvider);           // family inteira (sem arg)
    invalidate(loteListByPaddockProvider);
    invalidate(loteWithPaddockListByPropertyProvider);
    invalidate(loteListByPropertyProvider);
    invalidate(loteByIdProvider);
    invalidate(loteWithPaddockByIdProvider);
    invalidate(animalListByPropertyProvider);
    invalidate(animalListByLotProvider);
    invalidate(animalByIdProvider);
    invalidate(animalReproStatusByPropertyProvider);
    invalidate(doseListByPropertyProvider);
    invalidate(archivedDoseListByPropertyProvider);
    // + demais FutureProviders de leitura escopados à propriedade em
    //   atf_repository.dart, sanitary_application_repository.dart,
    //   expense_repository.dart, dashboard_providers.dart (grep
    //   "FutureProvider" nesses arquivos). Dashboard* derivam via watch,
    //   mas incluir é barato.
  }
}
```

   Verificar nomes exatos de cada provider nos `*_repository.dart` / `dashboard_providers.dart` antes de escrever. `ref.invalidate(family)` sem argumento invalida todos os membros em riverpod 3 — confirmar que compila.

2. Em cada site de mutação abaixo, substituir o bloco de `ref.invalidate(...)` pós-sucesso por `ref.invalidatePropertyData();` (manter `Navigator.pop` e demais lógica):
   - piquetes_screen.dart: `_openForm` (~:356), `_onCreateLot` (~:334), `_onDropLot` (~:294-296), `_confirmDelete` (~:385)
   - paddock_detail_screen.dart (~:95)
   - lote_form_dialog.dart (~:104 rename, ~:131-132 create)
   - mover_lote_dialog.dart (~:72-77)
   - animal_form_dialog.dart (~:107-120)
   - mover_animal_dialog.dart (~:50-53)
   - baixa_dialog.dart (~:87-100)
   - dose_form_dialog.dart (~:133-134); sanitario_screen.dart `_openDoseForm` (~:122-123) e archive/restore dose (~:133-135)
   - Qualquer outro `ref.invalidate(` após mutação em lib/features (grep; ex.: ATF/DG, aplicação sanitária, gastos, membros não precisa). Deixar intactos os `ref.invalidate` de botões "Tentar novamente"/ErrorRetry.

3. registrar_aplicacao_screen.dart: remover `_lotsWithActiveAnimalsProvider` (:23-29); `_pickLot` passa a `ref.read(loteWithPaddockListByPropertyProvider.future)`. Em `_pickDose`: após `DoseFormDialog` retornar true usar `await ref.refresh(doseListByPropertyProvider.future)` (força refetch) antes de reabrir o seletor. Adicionar no seletor de dose um item final "Cadastrar nova dose" que abre `DoseFormDialog`; ao voltar com true, `ref.refresh(doseListByPropertyProvider.future)` e reabrir o seletor — cobre o fluxo do QA (criar dose no meio do registro) também quando já existem doses. Mínimo possível; `_PickerSheet` é genérico — pode receber um `trailingAction` opcional ou o item extra ser tratado fora dele.
  </action>
  <verify>`flutter analyze` sem erros; `grep -rn "_lotsWithActiveAnimalsProvider" lib/` vazio; `grep -rn "invalidatePropertyData" lib/ | wc -l` ≥ 12.</verify>
  <done>Helper existe, todas as mutações o chamam, duplicata removida, analyze limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Guarda de remoção de piquete (DB + UI) e reparo de órfãos</name>
  <files>
    supabase/migrations/20260819_14_paddock_archive_guard.sql (novo)
    lib/features/piquetes/data/piquete_repository.dart
    lib/features/piquetes/presentation/piquetes_screen.dart
  </files>
  <action>
1. Migration `20260819_14_paddock_archive_guard.sql` (forward-only, seguir estilo/cabeçalho de 20260814_10):
   - a) Reparo: `UPDATE paddocks p SET deleted_at = NULL WHERE p.deleted_at IS NOT NULL AND EXISTS (SELECT 1 FROM lots l WHERE l.paddock_id = p.id AND l.deleted_at IS NULL);` — desfaz os órfãos já existentes (caso QA: piquete "teste" / "lote 1").
   - b) `CREATE OR REPLACE FUNCTION enforce_paddock_archive_no_active_lots() RETURNS trigger LANGUAGE plpgsql SET search_path = public` — conta `lots WHERE paddock_id = NEW.id AND deleted_at IS NULL`; se > 0, `RAISE EXCEPTION 'paddock % cannot be archived: % active lot(s) still assigned to it' USING ERRCODE = '23514'`. Espelhar `enforce_lot_archive_no_active_animals` de 20260814_10.
   - c) `DROP TRIGGER IF EXISTS trg_paddocks_archive_guard ON paddocks; CREATE TRIGGER trg_paddocks_archive_guard BEFORE UPDATE OF deleted_at ON paddocks FOR EACH ROW WHEN (OLD.deleted_at IS NULL AND NEW.deleted_at IS NOT NULL) EXECUTE FUNCTION enforce_paddock_archive_no_active_lots();`
   - NÃO rodar `supabase db push`.

2. `piquete_repository.dart` `softDeletePaddock`: adicionar `.select().single()` (como `DoseRepository.archiveDose`) para que 0 linhas vire erro em vez de sucesso silencioso.

3. `piquetes_screen.dart` `_confirmDelete`: antes de abrir o dialog de confirmação, ler `ref.read(loteWithPaddockListByPropertyProvider.future)` e contar lotes ativos daquele piquete. Se > 0: AlertDialog informativo "Não é possível remover: o piquete tem N lote(s). Mova ou arquive os lotes antes." e retornar sem chamar repo. Senão, fluxo atual. Envolver `softDeletePaddock` em try/catch `PostgrestException` com `code == '23514'` → mesma mensagem via SnackBar (defesa contra corrida). Após sucesso: `ref.invalidatePropertyData()`.
  </action>
  <verify>`flutter analyze` limpo; migration SQL válida (revisão visual; `supabase db lint` se disponível); grep confirma trigger + UPDATE de reparo no arquivo.</verify>
  <done>Piquete com lote não pode ser removido (UI explica, DB bloqueia); órfãos existentes restaurados pela migration.</done>
</task>

<task type="auto">
  <name>Task 3: "Arquivar lote" no menu do card do lote</name>
  <files>
    lib/features/lotes/presentation/_lots_section.dart
    lib/features/lotes/data/lote_repository.dart
  </files>
  <action>
1. `lote_repository.dart` `softDeleteLot`: adicionar `.select().single()` (0 linhas → erro).
2. `_lots_section.dart` `_LotCard` PopupMenu: adicionar item `'archive'` "Arquivar lote", visível só quando `canEdit`. Ao selecionar: ler `ref.read(animalListByLotProvider(lot.id).future)`; se houver animal ativo → SnackBar "Lote tem N animais ativos. Mova ou dê baixa antes de arquivar." e não chamar repo. Senão confirm dialog ("Arquivar lote X?") → `softDeleteLot` em try/catch `PostgrestException` (23514 → mesma mensagem; trigger `trg_lots_archive_guard`) → `ref.invalidatePropertyData()`. Seguir como `onEdit` é passado hoje (callback do pai que tem `ref`) — menor diff.
  </action>
  <verify>`flutter analyze` limpo.</verify>
  <done>Lote vazio arquivável pela UI; lote com animais bloqueado com mensagem.</done>
</task>

</tasks>

<verification>
- `flutter analyze` → 0 issues
- `flutter test` → passa (ajustar testes afetados se houver)
- `grep -rn "_lotsWithActiveAnimalsProvider" lib/` → vazio
- `grep -rn "invalidatePropertyData" lib/ | wc -l` ≥ 12
- Migration nova presente e NÃO aplicada (orquestrador aplica)
</verification>

<success_criteria>
As 4 truths de must_haves verdadeiras; commits atômicos por task; SUMMARY.md com status: complete.
</success_criteria>

<output>
After completion, create `.planning/quick/260819-mk7-qa-sturza-ajustes/260819-mk7-SUMMARY.md` (frontmatter `status: complete`).
</output>
