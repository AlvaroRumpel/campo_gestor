---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - supabase/tests/10_membership_test.sql
autonomous: true
requirements: [MEMB-01, MEMB-02, MEMB-03]

must_haves:
  truths:
    - "A suíte pgTAP cobre as três formas de deixar a fazenda sem veterinário (remover, rebaixar, sair) e as três são rejeitadas com 23514"
    - "A suíte prova que `accept_invite` rejeita o convite de outro e-mail com 42501 e que o aceite cria a linha em `property_members` com o papel do convite"
    - "A suíte prova que INSERT direto em `invites` como `authenticated` não cria linha (FORCE RLS + zero write policies)"
    - "O inteiro passado a `plan()` é igual ao número de asserções do arquivo"
  artifacts:
    - supabase/tests/10_membership_test.sql
  key_links:
    - "Fixtures inserem em `auth.users` diretamente (precedente 07_expenses_test.sql) — é o que torna `current_user_email()` testável"
    - "`SET ROLE authenticated` + `set_config('request.jwt.claim.sub', ...)` é o par que ativa RLS e `auth.uid()` no mesmo bloco"
  prohibitions:
    - "Não usar a forma de 3 argumentos de `has_index()` — a sobrecarga é ambígua nesta combinação Postgres/pgTAP (falha falsa em 05_reproductive_test.sql)"
    - "Nenhuma asserção pode depender de estado global da tabela (`count(*)` sem filtro por fixture) — lição do Group 8 de 06_sanitary_test.sql"
    - "Não fabricar `request.jwt.claims` como GUC JSON — o projeto só tem precedente provado com `request.jwt.claim.sub`"
---

<objective>
Autorar `supabase/tests/10_membership_test.sql`, a suíte pgTAP que prova o contrato da migration `20260814_11_membership_lifecycle.sql`: forma do schema, gates de papel dos 9 RPCs, casamento por e-mail no aceite, e a guarda de último veterinário nos três caminhos.

Purpose: MEMB-03 é a única invariante desta fase que não tem nenhuma reação de UI — se o banco falhar, o tenant fica sem admin e ninguém percebe. Esta suíte é a única prova automatizada disso.
Output: um arquivo `.sql` novo, em estado RED (o schema ainda não está aplicado — mesmo precedente deliberado de 06/07). A execução é do plano 10-10.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-RESEARCH.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-VALIDATION.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-01-SUMMARY.md
</context>

<environment_constraint>
Não existe stack local (`docker info` e `supabase status` falham desde a Fase 3), então `supabase test db` não roda. O caminho estabelecido é replay via MCP `execute_sql` dentro de `BEGIN ... ROLLBACK` — e o executor **não tem** as ferramentas MCP. Este plano portanto **apenas autora o arquivo**. O replay é do plano 10-10, feito pelo orquestrador.

Nome do arquivo: `10_membership_test.sql` (prefixo = número da fase, convenção dos 5 arquivos existentes e do 10-VALIDATION.md). O 10-RESEARCH.md sugeriu `11_...` espelhando o número da migration — está errado, ignorar.
</environment_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Cabeçalho, fixtures e grupos de schema</name>
  <files>supabase/tests/10_membership_test.sql</files>
  <read_first>
    - `supabase/tests/07_expenses_test.sql` (linhas 1–120) — cabeçalho explicativo, `SELECT plan(N)`, idioma de fixture (`INSERT INTO properties`, `INSERT INTO auth.users` com todas as colunas obrigatórias e `ON CONFLICT DO NOTHING`, `INSERT INTO property_members`), e a explicação de por que `SET ROLE authenticated` é necessário
    - `supabase/tests/06_sanitary_test.sql` (linhas 1–60) — nota do Group 8 sobre asserções escopadas a estado global
    - `supabase/migrations/20260814_11_membership_lifecycle.sql` — a forma real do schema criado pelo plano 10-01 (ler, não supor)
  </read_first>
  <action>
Criar `supabase/tests/10_membership_test.sql`. Cabeçalho no formato de `07_expenses_test.sql`: como rodar (com a nota do fallback por replay MCP em `BEGIN`/`ROLLBACK`), aviso de que a suíte é autorada contra schema ainda não aplicado (estado RED deliberado, mesmo precedente de 06/07), a regra de usar apenas a forma de 2 argumentos de `has_index()`, e a regra de escopar toda contagem a ids de fixture.

Abrir com `BEGIN;` e `SELECT plan(N);` — o N final é ajustado na Task 2, quando todas as asserções existirem.

Fixtures (ids fixos, prefixo próprio para não colidir com os das outras suítes):
- Duas propriedades: `MEMB Test Property A` e `MEMB Test Property B`.
- Cinco linhas em `auth.users` com o mesmo conjunto de colunas obrigatórias usado em `07_expenses_test.sql`: `memb-vet-a@test.com`, `memb-vet2-a@test.com`, `memb-owner-a@test.com`, `memb-reader-a@test.com`, `memb-vet-b@test.com`. Uma sexta, `memb-convidado@test.com`, sem membership nenhuma — é o convidado.
- Memberships: vet-a e vet2-a como `veterinarian` em A; owner-a como `owner` em A; reader-a como `reader` em A; vet-b como `veterinarian` em B.

Grupo 1 — forma do schema: `has_table('invites')`; `has_column` para as 8 colunas; `has_index('invites', 'invites_property_email_pending_idx')` e `has_index('invites', 'invites_invited_email_idx')` (forma de 2 argumentos); `is(relrowsecurity, true)` e `is(relforcerowsecurity, true)` lendo `pg_class` para `invites`; `is(count(*), 2)` sobre `pg_policies` filtrado por `tablename = 'invites'`; `is(count(*), 0)` sobre `pg_policies` filtrado por `tablename = 'invites' AND cmd <> 'SELECT'`.

Grupo 2 — existência e blindagem das funções: `has_function` para as 11 funções; para as 10 chamáveis, `ok(has_function_privilege('authenticated', '<assinatura>', 'EXECUTE'))` verdadeiro e `ok(NOT has_function_privilege('anon', '<assinatura>', 'EXECUTE'))`; para `assert_not_last_veterinarian`, `ok(NOT has_function_privilege('authenticated', ...))`.

Grupo 3 — trigger de imutabilidade: `has_trigger('invites', 'trg_invites_property_id_immutable')`.
  </action>
  <verify>
    <automated>bash -c "f=supabase/tests/10_membership_test.sql; test -f \$f && grep -q '^BEGIN;' \$f && grep -q 'SELECT plan(' \$f && grep -q \"has_table('invites')\" \$f && ! grep -qE \"has_index\\('[^']+', *'[^']+', *'\" \$f && echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/10_membership_test.sql` existe, abre com `BEGIN;` e contém uma chamada `SELECT plan(`
    - Nenhuma chamada `has_index(` com três argumentos string
    - Existem asserções lendo `pg_class.relforcerowsecurity` para `invites` e contando `pg_policies` de `invites` (total 2, não-SELECT 0)
    - As fixtures inserem em `auth.users` com `ON CONFLICT (id) DO NOTHING` e usam e-mails minúsculos
    - Existe um usuário de fixture sem nenhuma linha em `property_members` (o convidado)
  </acceptance_criteria>
  <done>Cabeçalho, fixtures e três grupos de schema escritos.</done>
</task>

<task type="auto">
  <name>Task 2: Grupos de comportamento — convites, gestão de membros e guarda MEMB-03</name>
  <files>supabase/tests/10_membership_test.sql</files>
  <read_first>
    - `supabase/tests/10_membership_test.sql` — o que a Task 1 escreveu
    - `supabase/tests/07_expenses_test.sql` (linhas 120–fim) — uso de `throws_ok` com SQLSTATE, `lives_ok`, e a lição registrada de que uma policy RLS que falha no `USING` **filtra a linha silenciosamente** em vez de levantar erro (verificar "0 linhas afetadas", não esperar 42501)
    - `supabase/migrations/20260814_11_membership_lifecycle.sql` — os SQLSTATEs exatos levantados por cada RPC
  </read_first>
  <action>
Anexar os grupos de comportamento. Antes deles, `SET ROLE authenticated;` (a partir daí a RLS de fato se aplica; o efeito é escopado à transação e some no ROLLBACK). A identidade de cada bloco é trocada com `set_config('request.jwt.claim.sub', '<uuid>', true)`.

Grupo 4 — `create_invite`: como vet-a, `lives_ok` criando convite para `memb-convidado@test.com` como `reader`; asserção de que a linha gravada tem `invited_email` minúsculo mesmo quando o parâmetro veio em caixa mista (chamar com `MEMB-Convidado@Test.com` num segundo convite para outra propriedade, ou revogar e re-criar); como owner-a, `lives_ok` (D-10-04 — proprietário também convida); como reader-a, `throws_ok` com `42501`; e-mail sem `@`, `throws_ok` com `22023`; segundo convite pendente para o mesmo par (propriedade, e-mail), `throws_ok` com `23505`; convite para o e-mail de alguém que já é membro, `throws_ok` com `23505`.

Grupo 5 — `accept_invite` / `decline_invite`: como vet-b (e-mail errado), `throws_ok` com `42501`; como o convidado, `lives_ok` e em seguida `is()` provando que existe linha em `property_members` com `(user_id = convidado, property_id = A, role = 'reader')` e que o convite ficou `status = 'accepted'` com `resolved_at` não nulo; aceitar de novo, `throws_ok` com `P0002`. Para `decline_invite`, usar um convite separado numa segunda propriedade e provar `status = 'declined'` sem criar linha em `property_members`.

Grupo 6 — `revoke_invite`: como vet-a, `lives_ok` num convite pendente e `is(status, 'revoked')`; revogar de novo, `throws_ok` com `P0002` (é exatamente a corrida "convite já respondido" que a UI trata como backstop); como reader-a, `throws_ok` com `42501`.

Grupo 7 — leituras: `list_property_members(A)` como reader-a retorna 4+ linhas incluindo a coluna `email` preenchida (leitor visualiza); `list_property_members(A)` como vet-b (não é membro de A), `throws_ok` com `42501`; `list_my_invites()` como o convidado retorna apenas convites `pending` cujo `invited_email` é o dele, com `property_name` preenchido.

Grupo 8 — RLS de escrita direta em `invites`: ainda como `authenticated`, executar um `INSERT INTO invites (...)` cru dentro de `throws_ok`/ou capturar e provar 0 linhas — usar a técnica correta conforme a lição de `07_expenses_test.sql`: uma policy ausente para INSERT faz o comando falhar com `42501`; se o comportamento observado for filtragem silenciosa, asseverar `is(count(*), 0)` sobre a linha que se tentou criar, escopado ao id de fixture. Escrever a asserção na forma que **prova ausência de mutação**, não meramente que um erro foi levantado.

Grupo 9 — guarda MEMB-03 (o núcleo). Preparar o estado: propriedade B tem exatamente um veterinarian (vet-b). Como vet-b: `remove_member(B, vet-b)` → `throws_ok` `23514`; `update_member_role(B, vet-b, 'reader')` → `throws_ok` `23514`; `leave_property(B)` → `throws_ok` `23514`. Em A (dois vets): `remove_member(A, vet2-a)` → `lives_ok`, e depois `remove_member(A, vet-a)` executado por owner-a → `throws_ok` `23514` (prova a sub-regra: o proprietário pode mexer em vet, mas não no último). Também: `update_member_role(A, reader-a, 'veterinarian')` → `lives_ok` (promover nunca aciona a guarda).

Grupo 10 — nota de concorrência: um bloco de comentário registrando que a corrida de duas remoções simultâneas de dois vets distintos **não é asserível** neste harness (o replay roda numa transação única) e que a prova estrutural é a presença do `FOR UPDATE` sobre todo o conjunto de vets, já verificada no Grupo 2/na revisão da migration. Registrar como verificação manual: abrir duas sessões `psql`, `BEGIN` em ambas, `remove_member` em cada uma para vets diferentes, e observar a segunda bloquear até o commit da primeira e então falhar com 23514.

Fechar com `SELECT * FROM finish();` e `ROLLBACK;`. Por fim, contar as asserções e ajustar o inteiro de `SELECT plan(N)` para bater exatamente.
  </action>
  <verify>
    <automated>bash -c "f=supabase/tests/10_membership_test.sql; d=\$(sed -n 's/.*SELECT plan(\\([0-9]*\\)).*/\\1/p' \$f); a=\$(grep -cE '^[[:space:]]*SELECT (ok|is|isnt|has_table|has_column|has_index|has_function|has_trigger|throws_ok|lives_ok|results_eq|is_empty)\\(' \$f); echo \"declared=\$d actual=\$a\"; [ \"\$d\" = \"\$a\" ] && grep -q 'ROLLBACK;' \$f && grep -c '23514' \$f | grep -qv '^0$' && echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - O inteiro em `SELECT plan(N)` é igual ao número de linhas que começam com uma chamada de asserção pgTAP
    - O arquivo termina com `SELECT * FROM finish();` seguido de `ROLLBACK;`
    - Existem no mínimo 4 asserções esperando SQLSTATE `23514` (remover / rebaixar / sair do último vet + a tentativa do proprietário sobre o último vet)
    - Existe asserção provando que `accept_invite` por um e-mail diferente levanta `42501`
    - Existe asserção provando que, após o aceite, existe linha em `property_members` com o papel exato do convite
    - Existe asserção provando `create_invite` por `reader` levantando `42501`
    - O Grupo 8 asserta ausência de mutação (contagem 0 escopada ao id de fixture) e não apenas "erro levantado"
    - Toda asserção de contagem filtra por id/e-mail de fixture — nenhuma lê estado global da tabela
  </acceptance_criteria>
  <done>Suíte completa, em RED, com plan() batendo o número de asserções.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suíte de teste → PROD | O replay roda contra o banco de produção dentro de `BEGIN`/`ROLLBACK` — qualquer fixture não revertida vaza |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-09 | Tampering | Fixtures da suíte deixando linhas em PROD | medium | mitigate | `BEGIN` no topo, `ROLLBACK` no fim, ids de fixture com prefixo próprio; o plano 10-10 confere ausência de resíduo após o replay |
| T-10-10 | Repudiation | Asserção fraca "erro foi levantado" mascarando mutação real | medium | mitigate | Grupo 8 asserta contagem 0 escopada ao id de fixture (lição registrada em `07_expenses_test.sql`) |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `plan(N)` bate com a contagem real de asserções
- Arquivo abre com `BEGIN;` e fecha com `finish()` + `ROLLBACK;`
- `flutter test` continua verde (nada de Dart mudou)
</verification>

<success_criteria>
`supabase/tests/10_membership_test.sql` autorado, cobrindo schema, grants, os 9 RPCs, o casamento por e-mail, a blindagem de escrita direta e os quatro caminhos da guarda MEMB-03.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md` (seção "Artifacts this phase produces"). Contribuição deste plano: `supabase/tests/10_membership_test.sql`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-02-SUMMARY.md` when done
</output>
