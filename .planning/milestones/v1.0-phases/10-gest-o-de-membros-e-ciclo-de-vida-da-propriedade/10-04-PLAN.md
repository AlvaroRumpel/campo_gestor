---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 04
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - lib/core/auth/role_gates.dart
  - lib/features/membros/presentation/invite_form_dialog.dart
  - lib/features/membros/presentation/invite_banner.dart
  - test/features/membros/role_gates_membros_test.dart
  - test/features/membros/invite_form_dialog_test.dart
  - test/features/membros/invite_banner_test.dart
autonomous: true
requirements: [MEMB-01, MEMB-02]

must_haves:
  truths:
    - "`canManageMembers` devolve true para veterinarian e owner e false para reader, para membro de outra propriedade e para estado ainda não carregado"
    - "O diálogo de convite pede e-mail e papel, e o papel oferece exatamente Veterinário / Proprietário / Leitor"
    - "Submeter com e-mail vazio ou sem formato de e-mail não dispara nenhuma chamada ao repositório"
    - "Erro do RPC vira frase pt-BR autorada no SnackBar, nunca texto cru do banco"
    - "Existe um único widget de convite aceitar/recusar, compartilhado por `/sem-acesso` e pelo dashboard — a lógica de aceite não é escrita duas vezes"
    - "Aceitar invalida `memberPropertiesProvider`, o que faz o router reavaliar o redirect sozinho — sem `context.go` manual"
  artifacts:
    - lib/core/auth/role_gates.dart
    - lib/features/membros/presentation/invite_form_dialog.dart
    - lib/features/membros/presentation/invite_banner.dart
    - test/features/membros/role_gates_membros_test.dart
    - test/features/membros/invite_form_dialog_test.dart
    - test/features/membros/invite_banner_test.dart
  key_links:
    - "`canManageMembers` → consumido por `MembrosScreen` (10-05) e pelo item 'Membros' do popup em `PropriedadesScreen` (10-10)"
    - "`InviteFormDialog._submit` → `MembroRepository.createInvite` → RPC `create_invite`"
    - "`InviteBanner` → `acceptInvite`/`declineInvite` → `ref.invalidate(memberPropertiesProvider)` → `_RouterRefreshNotifier` → redirect do GoRouter"
  prohibitions:
    - "Não renderizar controle desabilitado por papel negado — papel sem permissão vê o controle **ausente** (convenção de `role_gates.dart`)"
    - "Não substituir `canManageExpenses` nem `isVeterinarian` por `canManageMembers`; os três coexistem"
    - "Não navegar manualmente com `context.go`/`context.push` após aceitar um convite — o redirect do router é o mecanismo provado do projeto"
    - "Não pedir confirmação para 'Recusar' — é ação de um toque por decisão de UI-SPEC"
    - "Não passar o e-mail do usuário para `acceptInvite`/`declineInvite` — o servidor deriva por `current_user_email()`"
---

<objective>
Adicionar as três peças compartilhadas de UI de membros: o gate de papel `canManageMembers` (vet ou proprietário), o diálogo de convite `InviteFormDialog` e o banner de convite `InviteBanner`.

Purpose: as quatro telas desta fase consomem estas três peças. Escrevê-las aqui, testadas e isoladas, é o que permite `MembrosScreen` (10-05), `/sem-acesso` (10-08), o dashboard (10-09) e o ponto de entrada em Propriedades (10-10) rodarem em paralelo sem duplicar lógica de aceite.
Output: 1 função nova em `role_gates.dart`, 2 widgets novos, 3 arquivos de teste.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-PATTERNS.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-03-SUMMARY.md
@lib/core/auth/role_gates.dart
@lib/features/propriedades/presentation/property_form_dialog.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canManageMembers em role_gates.dart</name>
  <files>lib/core/auth/role_gates.dart, test/features/membros/role_gates_membros_test.dart</files>
  <read_first>
    - `lib/core/auth/role_gates.dart` (arquivo inteiro, 47 linhas) — `canManageExpenses` (linhas 18–28) é o predicado a espelhar, e o comentário de D-23 registra por que ele aceita dois papéis
    - `test/features/gastos/role_gates_test.dart` — forma exata do teste a espelhar (construção de `SelectedProperty` e `PropertyMembership`)
    - `lib/features/auth/data/property_repository.dart` (linhas 1–30) — tipo `PropertyMembership`
  </read_first>
  <behavior>
    - `canManageMembers(prop, [membership(prop, 'veterinarian')])` é `true`
    - `canManageMembers(prop, [membership(prop, 'owner')])` é `true`
    - `canManageMembers(prop, [membership(prop, 'reader')])` é `false`
    - `canManageMembers(null, [...])` é `false` e `canManageMembers(prop, null)` é `false`
    - `canManageMembers(propA, [membership(propB, 'veterinarian')])` é `false` (o papel é resolvido pela propriedade ativa, não pela primeira membership da lista)
  </behavior>
  <action>
Anexar a `lib/core/auth/role_gates.dart` a função `bool canManageMembers(SelectedProperty? current, List<PropertyMembership>? members)`, com o corpo idêntico ao de `canManageExpenses`: retorna `false` se `current` ou `members` for nulo; filtra `members` por `m.property.id == current.id`; pega o primeiro `role` com `firstOrNull`; devolve `role == 'veterinarian' || role == 'owner'`.

Comentário de doc acima da função registrando: (a) é o segundo gate de dois papéis do projeto, depois de `canManageExpenses` (D-23); (b) a decisão travada é que veterinário **e** proprietário convidam, removem e trocam papel, e leitor só visualiza; (c) a aplicação real é a checagem `get_role(...) IN ('veterinarian','owner')` dentro dos RPCs de `20260814_11_membership_lifecycle.sql` — este predicado só decide se o controle é renderizado; (d) papel negado vê o controle ausente, nunca desabilitado.

Não tocar em `canManageExpenses` nem em `isVeterinarian`.

Criar `test/features/membros/role_gates_membros_test.dart` com os cinco casos de `<behavior>`.
  </action>
  <verify>
    <automated>flutter test test/features/membros/role_gates_membros_test.dart test/features/gastos/role_gates_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/membros/role_gates_membros_test.dart` passa com 5 casos
    - `test/features/gastos/role_gates_test.dart` continua passando (nenhuma regressão em `canManageExpenses`)
    - `lib/core/auth/role_gates.dart` contém três funções públicas: `canManageExpenses`, `isVeterinarian`, `canManageMembers`
    - `flutter analyze` sem novas issues
  </acceptance_criteria>
  <done>Gate criado e testado, sem regressão nos gates existentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InviteFormDialog</name>
  <files>lib/features/membros/presentation/invite_form_dialog.dart, test/features/membros/invite_form_dialog_test.dart</files>
  <read_first>
    - `lib/features/propriedades/presentation/property_form_dialog.dart` (arquivo inteiro) — a casca a copiar: `ConsumerStatefulWidget`, `GlobalKey<FormState>`, flag `_saving`, `_submit()` com try/catch/finally, `SafeArea > Padding(EdgeInsets.fromLTRB(20,18,20,16)) > Form > Column`, título 20/700, `SizedBox(height: 20)` antes do rodapé, rodapé `Row` com `Expanded(flex: 10, OutlinedButton 'Cancelar')` + `SizedBox(width: 10)` + `Expanded(flex: 14, FilledButton)`
    - `test/features/gastos/expense_form_dialog_test.dart` — forma do teste de diálogo (montar dentro de `ProviderScope` com override do repositório, bombear, interagir)
    - `lib/features/membros/data/membro_repository.dart` e `membro_exception.dart` e `membro_models.dart` — `createInvite`, `asMembroException`, `roleLabel`
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — "Copywriting Contract" (título "Convidar membro", label "E-mail do convidado *", label "Papel", botão "Enviar convite") e "Layout & Interaction Contract" (largura `FormWidth.form`)
  </read_first>
  <behavior>
    - Montado, mostra o título "Convidar membro", um campo com label "E-mail do convidado *", um dropdown com label "Papel" e os botões "Cancelar" e "Enviar convite"
    - O dropdown oferece exatamente três opções, rotuladas "Veterinário", "Proprietário" e "Leitor"
    - Tocar "Enviar convite" com o campo de e-mail vazio mostra a mensagem de validação e **não** chama `createInvite`
    - Tocar "Enviar convite" com um texto sem `@` também bloqueia e não chama `createInvite`
    - Com e-mail válido, `createInvite` é chamado uma vez com o `propertyId` recebido, o e-mail digitado e o valor cru do papel selecionado
    - Quando `createInvite` lança `PostgrestException(code: '23505')`, o diálogo permanece aberto e a mensagem exibida é a frase pt-BR de "já convidado ou já membro" (nunca o texto cru)
  </behavior>
  <action>
Criar `lib/features/membros/presentation/invite_form_dialog.dart` com `class InviteFormDialog extends ConsumerStatefulWidget`, parâmetro obrigatório `final String propertyId`.

Estado: `_formKey`, `_emailController`, `String _role = 'reader'` (padrão o papel menos privilegiado), `bool _saving = false`. Dispose do controller.

Corpo: mesma casca de `PropertyFormDialog`. Título `Convidar membro` em 20/700. Campo de e-mail: `TextFormField` com `decoration: InputDecoration(labelText: 'E-mail do convidado *')`, `keyboardType: TextInputType.emailAddress`, `autocorrect: false`, `textCapitalization: TextCapitalization.none`, validator que rejeita vazio com `Informe o e-mail do convidado.` e rejeita texto que não case com um padrão simples de e-mail (algo antes de `@`, algo depois, e um ponto no domínio) com `Informe um e-mail válido.`. Depois, `DropdownButtonFormField<String>` com `decoration: InputDecoration(labelText: 'Papel')`, itens de valor cru `'veterinarian'`, `'owner'`, `'reader'` e texto vindo de `roleLabel(...)`.

`_submit()`: valida o form; seta `_saving`; chama `ref.read(membroRepositoryProvider).createInvite(propertyId: widget.propertyId, email: _emailController.text.trim(), role: _role)`; em sucesso, `Navigator.pop(context, true)` com guarda de `mounted`; em erro, converte com `asMembroException(e, fallbackMessage: 'Erro ao enviar o convite. Tente novamente.')` e mostra `ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(ex.message)))` com guarda de `mounted`; `finally` limpa `_saving` com guarda de `mounted`.

O botão primário mostra `Enviar convite` e fica com `onPressed: null` enquanto `_saving` é true (gating de estado de submissão, não de papel).

Não chamar `showAdaptiveForm` de dentro deste arquivo — quem abre o diálogo é a tela (10-05), que passa `width: FormWidth.form`.

Criar `test/features/membros/invite_form_dialog_test.dart` cobrindo os seis casos de `<behavior>`, com um fake de `MembroRepository` registrado via `membroRepositoryProvider.overrideWithValue(...)` que conta chamadas de `createInvite`.
  </action>
  <verify>
    <automated>flutter test test/features/membros/invite_form_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/membros/invite_form_dialog_test.dart` passa com no mínimo 6 casos
    - O widget renderiza os textos exatos `Convidar membro`, `E-mail do convidado *`, `Papel`, `Cancelar`, `Enviar convite`
    - O dropdown contém exatamente 3 `DropdownMenuItem`, com os textos `Veterinário`, `Proprietário`, `Leitor`
    - Nenhum teste observa o texto cru de uma `PostgrestException` na tela
    - `invite_form_dialog.dart` não importa `package:supabase_flutter`
    - `flutter analyze` sem novas issues e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>Diálogo de convite pronto e testado, isolado da tela que o abre.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: InviteBanner compartilhado (aceitar/recusar)</name>
  <files>lib/features/membros/presentation/invite_banner.dart, test/features/membros/invite_banner_test.dart</files>
  <read_first>
    - `lib/core/widgets/ui.dart` (linhas 510–528) — `WarningBanner`, que recebe apenas `child` (o padding é do chamador) e usa `accentContainer`/`accentBorder`
    - `lib/features/dashboard/presentation/dashboard_screen.dart` (a classe `_AlertsBanner`, ~linhas 226–290) — como o projeto compõe conteúdo dentro de `WarningBanner` nos dois modos (mobile e desktop)
    - `lib/core/router/router.dart` (linhas 108–135 e 284–300) — o redirect que devolve `/dashboard` quando `memberPropertiesProvider` deixa de ser vazio, e o `_RouterRefreshNotifier` com `ref.listen(memberPropertiesProvider, ...)`; é por isso que invalidar aquele provider basta e navegação manual é proibida
    - `lib/features/propriedades/presentation/propriedades_screen.dart` (linhas 108–117) — idioma `ref.invalidate(propertyListProvider); ref.invalidate(memberPropertiesProvider);` após mutação de membership
    - `lib/features/membros/data/membro_repository.dart`, `membro_models.dart`, `membro_exception.dart` — `acceptInvite`, `declineInvite`, `MyInvite`, `roleLabel`, `asMembroException`
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — linhas "Convite — aceitar/recusar" e "Dashboard banner — novo convite" do Copywriting Contract
  </read_first>
  <behavior>
    - Renderiza a frase exata "Você recebeu um convite para {fazenda} como {papel}." com o nome da fazenda do `MyInvite` e o rótulo pt-BR do papel
    - Renderiza um botão "Aceitar" (`FilledButton`) e um botão "Recusar" (`OutlinedButton`)
    - Tocar em "Aceitar" chama `acceptInvite` exatamente uma vez, com o id do convite como único argumento
    - Após aceite bem-sucedido, `memberPropertiesProvider` e `myInvitesProvider` são invalidados
    - Nenhuma navegação (`context.go`/`context.push`) acontece no fluxo de aceite
    - Tocar em "Recusar" chama `declineInvite` uma vez, invalida `myInvitesProvider` e **não** invalida `memberPropertiesProvider`
    - "Recusar" não abre diálogo de confirmação
    - Durante uma ação em voo, os dois botões ficam com `onPressed: null` — um segundo toque não dispara segunda chamada
    - Quando `acceptInvite` lança `PostgrestException(code: 'P0002')`, aparece SnackBar com "Não foi possível concluir. Atualize a lista e tente novamente."
    - A 360px, um nome de fazenda longo quebra em várias linhas sem exceção de overflow
  </behavior>
  <action>
Criar `lib/features/membros/presentation/invite_banner.dart` com `class InviteBanner extends ConsumerStatefulWidget`, parâmetro obrigatório `final MyInvite invite`.

Estado local: `bool _busy = false`.

Corpo: `WarningBanner(child: Padding(padding: const EdgeInsets.all(12), child: Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [...])))`. Primeiro filho: `Text('Você recebeu um convite para ${widget.invite.propertyName} como ${roleLabel(widget.invite.role)}.', softWrap: true)` — sem `maxLines`, sem `overflow`, porque o usuário precisa ler o nome inteiro para decidir. Depois `SizedBox(height: 10)` e um `Row` com `Expanded(OutlinedButton 'Recusar')` + `SizedBox(width: 10)` + `Expanded(FilledButton 'Aceitar')`. Ambos com `onPressed: _busy ? null : ...`.

`_accept()`: seta `_busy`; `await ref.read(membroRepositoryProvider).acceptInvite(widget.invite.id)`; em sucesso, `ref.invalidate(memberPropertiesProvider)` e `ref.invalidate(myInvitesProvider)` — e nada mais. Comentário no código registrando que a navegação é consequência do redirect do GoRouter, que reage à invalidação de `memberPropertiesProvider` via `_RouterRefreshNotifier`; chamar `context.go` aqui é proibido. Em erro, `asMembroException(e, fallbackMessage: 'Erro ao aceitar o convite. Tente novamente.')` e `SnackBar(ex.message)`. `finally` limpa `_busy`. Todas as interações com `context`/`setState` sob guarda de `mounted`.

`_decline()`: mesma forma chamando `declineInvite(widget.invite.id)`, invalidando apenas `myInvitesProvider`, com `fallbackMessage: 'Erro ao recusar o convite. Tente novamente.'`. Sem diálogo de confirmação.

Criar `test/features/membros/invite_banner_test.dart` com os dez casos de `<behavior>`, usando um fake de `MembroRepository` que conta chamadas e um `ProviderContainer`/observador para verificar as invalidações.
  </action>
  <verify>
    <automated>flutter test test/features/membros/invite_banner_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/membros/invite_banner_test.dart` passa com no mínimo 10 casos
    - `grep -c "context.go\|context.push" lib/features/membros/presentation/invite_banner.dart` retorna 0
    - `acceptInvite` e `declineInvite` são chamados com um único argumento (o id do convite)
    - Existe um caso provando que "Recusar" não invalida `memberPropertiesProvider`
    - Existe um caso provando que um segundo toque durante a ação em voo não dispara segunda chamada
    - Existe um caso a 360px sem exceção de overflow
    - O widget usa `WarningBanner` de `ui.dart` — nenhum container laranja bespoke
    - `flutter analyze` limpo e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>Widget único de convite pronto — `/sem-acesso` (10-08) e o dashboard (10-09) apenas o renderizam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Formulário → repositório | E-mail e papel digitados/selecionados pelo usuário |
| Gate de papel → renderização | `canManageMembers` decide visibilidade; não é barreira de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Elevation of Privilege | Confundir o gate de UI com autorização real | high | mitigate | Comentário de doc em `canManageMembers` aponta a checagem de papel dentro dos RPCs como a aplicação real (Task 1) |
| T-10-15 | Input Validation | E-mail malformado chegando ao banco | low | mitigate | Validador do formulário (Task 2) + rejeição server-side com `22023` em `create_invite` |
| T-10-16 | Information Disclosure | Texto cru de erro do Postgres num SnackBar | medium | mitigate | `asMembroException` com `fallbackMessage` autorado (Tasks 2 e 3) |
| T-10-26 | Elevation of Privilege | Aceitar convite endereçado a outra pessoa | high | mitigate | `accept_invite` casa `invited_email` com `current_user_email()` no servidor; `InviteBanner` envia só o id (Task 3) |
| T-10-27 | Tampering | Duplo toque em "Aceitar" gerando duas chamadas | low | mitigate | Flag `_busy` desabilita ambos os botões durante a ação (Task 3); `ON CONFLICT DO NOTHING` no RPC é a segunda linha |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `flutter test` verde (suíte completa)
- `flutter analyze` limpo
- `role_gates.dart` expõe três predicados, nenhum removido
</verification>

<success_criteria>
`canManageMembers` disponível e testado; `InviteFormDialog` e `InviteBanner` renderizam o contrato de copy do 10-UI-SPEC.md, validam antes de chamar o RPC e traduzem erro do banco para pt-BR; o aceite de convite depende exclusivamente do redirect do router.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: `canManageMembers` em `lib/core/auth/role_gates.dart`, `InviteFormDialog` em `lib/features/membros/presentation/invite_form_dialog.dart`, `InviteBanner` em `lib/features/membros/presentation/invite_banner.dart`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-04-SUMMARY.md` when done
</output>
