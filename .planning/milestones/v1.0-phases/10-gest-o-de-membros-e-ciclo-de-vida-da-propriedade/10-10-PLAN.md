---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 10
type: execute
wave: 4
depends_on: ["10-05", "10-07"]
files_modified:
  - lib/core/router/routes.dart
  - lib/core/router/router.dart
  - lib/features/propriedades/presentation/propriedades_screen.dart
  - test/core/router_test.dart
  - test/widget/propriedades_screen_test.dart
autonomous: true
requirements: [MEMB-02]

must_haves:
  truths:
    - "`/propriedades/{id}/membros` é uma rota real, deep-linkável, que renderiza `MembrosScreen` com o id da fazenda vindo do path"
    - "O item 'Membros' aparece no menu do cartão de fazenda apenas para veterinário e proprietário — para leitor o item está ausente, não desabilitado"
    - "Cartão de fazenda arquivada não oferece 'Membros'"
    - "As rotas e o shell existentes continuam funcionando — nenhuma branch do `StatefulShellRoute` foi tocada"
  artifacts:
    - lib/core/router/routes.dart
    - lib/core/router/router.dart
    - lib/features/propriedades/presentation/propriedades_screen.dart
  key_links:
    - "`AppRoutes.membros(id)` → `GoRoute(path: AppRoutes.membrosById)` → `MembrosScreen(propertyId: state.pathParameters['propertyId']!)`"
    - "`canManageMembers` (papel na fazenda **ativa**) decide a visibilidade do item de menu"
  prohibitions:
    - "Não colocar a rota dentro do `StatefulShellRoute` — segue o padrão root-level de `loteById`/`atfById`/`aplicacaoById`/`gastosById`"
    - "Não adicionar a rota à lista `AppRoutes.all` (ela conta apenas as branches de navegação do shell)"
    - "Não trocar o gate `_canEditProperties` (vet-only) que governa Editar/Arquivar — 'Membros' usa um gate diferente (vet **ou** proprietário)"
---

<objective>
Registrar a rota `/propriedades/:propertyId/membros` e dar a ela um ponto de entrada: o item "Membros" no menu de contexto do cartão de fazenda.

Purpose: `MembrosScreen` existe desde o plano 10-05 mas é inalcançável. Este é o plano que a liga à navegação.
Output: 1 constante + 1 helper de rota, 1 `GoRoute` registrado, 1 item de menu, testes estendidos.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-05-SUMMARY.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rota /propriedades/:propertyId/membros</name>
  <files>lib/core/router/routes.dart, lib/core/router/router.dart, test/core/router_test.dart</files>
  <read_first>
    - `lib/core/router/routes.dart` (arquivo inteiro) — o padrão de cada rota de detalhe root-level: uma constante `xxxById` com o template de path e um helper `String xxxDetail(String id)`; e o comentário que registra por que rotas root-level de dois segmentos coexistem com branches do shell
    - `lib/core/router/router.dart` (linhas 145–200) — como `loteById`, `atfById`, `aplicacaoById` e `gastosById` são registradas como `GoRoute` root-level com `state.pathParameters[...]`
    - `test/core/router_test.dart` — a forma dos testes de rota já existentes
    - `lib/features/membros/presentation/membros_screen.dart` — assinatura `MembrosScreen({required String propertyId})`
  </read_first>
  <behavior>
    - Navegar para `/propriedades/abc-123/membros` renderiza `MembrosScreen` com `propertyId == 'abc-123'`
    - `AppRoutes.membros('abc-123')` devolve exatamente `'/propriedades/abc-123/membros'`
    - Navegar para `/propriedades` continua renderizando `PropriedadesScreen` (a rota de dois segmentos não engole a de um)
    - `AppRoutes.all` continua com exatamente 6 entradas
    - Todos os testes já existentes em `test/core/router_test.dart` continuam passando
  </behavior>
  <action>
Em `lib/core/router/routes.dart`, acrescentar, na seção de rotas de gestão, `static const membrosById = '/propriedades/:propertyId/membros';` e `static String membros(String id) => '/propriedades/$id/membros';`, com um comentário registrando que é a quinta rota de detalhe root-level do projeto (depois de `loteById`, `atfById`, `aplicacaoById`, `gastosById`) e que fica fora do `StatefulShellRoute` pelo mesmo motivo dos anteriores. Não incluir em `AppRoutes.all`.

Em `lib/core/router/router.dart`, registrar um `GoRoute(path: AppRoutes.membrosById, builder: (ctx, state) => MembrosScreen(propertyId: state.pathParameters['propertyId']!))` logo abaixo do `GoRoute` de `AppRoutes.propriedades`, com o import correspondente. Nenhuma alteração no `redirect` nem nas branches do shell.

Estender `test/core/router_test.dart` com os cinco casos de `<behavior>`.
  </action>
  <verify>
    <automated>flutter test test/core/router_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/router_test.dart` passa, incluindo os casos que já existiam
    - Existe um caso assertando que `/propriedades/abc-123/membros` renderiza `MembrosScreen` com `propertyId == 'abc-123'`
    - Existe um caso assertando que `/propriedades` ainda renderiza `PropriedadesScreen`
    - Existe um caso assertando `AppRoutes.all.length == 6`
    - `membrosById` não aparece dentro da lista `AppRoutes.all`
    - O `GoRoute` novo está fora do `StatefulShellRoute.indexedStack`
    - `flutter analyze` limpo
  </acceptance_criteria>
  <done>Rota registrada e deep-linkável, sem tocar no shell nem no redirect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Item "Membros" no menu do cartão de fazenda</name>
  <files>lib/features/propriedades/presentation/propriedades_screen.dart, test/widget/propriedades_screen_test.dart</files>
  <read_first>
    - `lib/features/propriedades/presentation/propriedades_screen.dart` — estado deixado pelo plano 10-07: `ConsumerStatefulWidget` com `_showArchived`, `_PropertyCard` com `archived`/`onRestore`, `PopupMenuButton` com os itens `Editar` e `Arquivar` gated por `_canEditProperties` (vet-only)
    - `lib/core/auth/role_gates.dart` — `canManageMembers` (vet **ou** proprietário), criado no plano 10-04
    - `lib/core/router/routes.dart` — `AppRoutes.membros(id)` criado na Task 1
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — item "Where the UI lives" do Layout & Interaction Contract
  </read_first>
  <behavior>
    - Com papel veterinarian na fazenda ativa, o menu do cartão contém "Membros", "Editar" e "Arquivar"
    - Com papel owner na fazenda ativa, o menu contém "Membros" e **não** contém "Editar" nem "Arquivar" (aqueles continuam vet-only)
    - Com papel reader, o cartão não tem `PopupMenuButton` nenhum
    - Selecionar "Membros" navega para `/propriedades/{id}/membros`
    - Um cartão arquivado não oferece "Membros" (mostra apenas "Restaurar fazenda")
    - Todos os casos já existentes em `test/widget/propriedades_screen_test.dart` continuam passando
  </behavior>
  <action>
Em `PropriedadesScreen.build`, calcular também `final canManageMembersHere = canManageMembers(currentPropAsync.asData?.value, membersAsync.asData?.value);` ao lado do `canEdit` existente.

`_PropertyCard` passa a receber `final bool canManageMembers` e `final VoidCallback onMembers`. O `PopupMenuButton` é renderizado quando `canEdit || canManageMembers` (antes era só `canEdit`), e seus itens são montados condicionalmente: `Membros` quando `canManageMembers`; `Editar` e `Arquivar` quando `canEdit`. Isso preserva a regra vet-only de Editar/Arquivar e adiciona o proprietário apenas em Membros.

No modo `archived` (plano 10-07), o trailing continua sendo o botão "Restaurar fazenda" — nenhum menu, nenhum item "Membros".

`onMembers` chama `context.push(AppRoutes.membros(property.id))`.

Estender `test/widget/propriedades_screen_test.dart` com os seis casos de `<behavior>`.
  </action>
  <verify>
    <automated>flutter test test/widget/propriedades_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/propriedades_screen_test.dart` passa com no mínimo 21 casos no total (os 15 do plano 10-07 + 6 destes)
    - Existe um caso com papel owner assertando `Membros` presente e `Editar`/`Arquivar` ausentes
    - Existe um caso com papel reader assertando `findsNothing` para `PopupMenuButton`
    - Existe um caso assertando que selecionar "Membros" leva a `/propriedades/{id}/membros`
    - Existe um caso assertando que o cartão arquivado não oferece "Membros"
    - `_canEditProperties` permanece vet-only, sem `'owner'` adicionado
    - `flutter analyze` limpo e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>MEMB-02 alcançável pela UI, com o gate de dois papéis convivendo com o gate vet-only da mesma tela.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → tela | `propertyId` vem do path e é passado direto para os RPCs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-32 | Elevation of Privilege | Deep link para `/propriedades/{id-alheio}/membros` de uma fazenda de que o usuário não é membro | high | mitigate | `list_property_members` levanta `42501` para não-membro (plano 10-01); a tela mostra `ErrorRetry` e nenhum dado |
| T-10-33 | Elevation of Privilege | Proprietário ganhando acesso a Editar/Arquivar junto com Membros | medium | mitigate | Itens do menu montados por gates separados — `canEdit` (vet-only) para Editar/Arquivar, `canManageMembers` para Membros (Task 2) |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `flutter test` verde (suíte completa)
- `flutter analyze` limpo
- `AppRoutes.all` continua com 6 entradas e nenhuma branch do shell foi alterada
</verification>

<success_criteria>
`/propriedades/:propertyId/membros` registrada e alcançável pelo menu do cartão de fazenda, visível a veterinário e proprietário e ausente para leitor.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: `AppRoutes.membrosById` e `AppRoutes.membros(id)` em `routes.dart`, o `GoRoute` correspondente em `router.dart`, e o item de menu "Membros" em `propriedades_screen.dart`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-10-SUMMARY.md` when done
</output>
