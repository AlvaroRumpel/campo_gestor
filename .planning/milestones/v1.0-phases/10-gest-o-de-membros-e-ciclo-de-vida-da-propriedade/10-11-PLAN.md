---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 11
type: execute
wave: 5
depends_on: ["10-02", "10-05", "10-08", "10-09", "10-10"]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [MEMB-01, MEMB-02, MEMB-03, PROPV-01, PROPV-02]

must_haves:
  truths:
    - "`20260814_11_membership_lifecycle.sql` está aplicada em PROD e verificada por leitura de catálogo — não por suposição"
    - "`invites` tem RLS habilitada **e forçada**, com 2 policies SELECT e zero policies de escrita"
    - "As 10 funções chamáveis pelo client estão concedidas a `authenticated` e revogadas de `anon`"
    - "A suíte `10_membership_test.sql` roda contra PROD em `BEGIN…ROLLBACK` sem deixar linha de fixture para trás"
    - "O fluxo de convite ponta a ponta com duas contas reais foi confirmado por um humano — é a única prova possível do casamento por e-mail no cadastro"
  artifacts:
    - .planning/STATE.md
  key_links:
    - "MCP `apply_migration` contra o projeto PROD `wrdwzychjhlpwpivfhhq` — o caminho estabelecido desde a Fase 3 (CLI unlinked, sem TTY, Docker morto)"
  prohibitions:
    - "Não editar `20260814_11_membership_lifecycle.sql` no lugar — se algo estiver errado, autorar uma migration corretiva forward-only (precedente `20260812_06_fix_dose_update_policy.sql`)"
    - "Não re-aplicar `20260814_09` nem `20260814_10` — ambas já estão em PROD"
    - "Não rodar a suíte pgTAP fora de uma transação revertida"
---

<objective>
Aplicar a migration desta fase em PROD, verificar por leitura de catálogo, replicar a suíte pgTAP e submeter os dois fluxos manuais ao usuário.

Purpose: é o único plano da fase que toca o banco de produção e o único que fecha as verificações que nenhum teste automatizado alcança (convite entregue a uma conta criada **depois** do convite).
Output: banco em PROD com a fase aplicada, evidência de verificação registrada, STATE.md atualizado.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-VALIDATION.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-01-SUMMARY.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-02-SUMMARY.md
</context>

<environment_constraint>
**Este plano é do orquestrador, não do `gsd-executor`.** O tipo de agente `gsd-executor` é registrado com uma tool list restrita (`Read, Write, Edit, Bash, Grep, Glob, Skill, context7`) que **não inclui as ferramentas MCP do Supabase**; o CLI local está unlinked sem TTY e o Docker está morto. Um `gsd-executor` despachado para este plano retorna BLOCKED sem mudar nada — foi exatamente o que aconteceu com o plano 07-08 (registrado nos Blockers de STATE.md). O orquestrador executa as Tasks 1 e 2 diretamente.

**Estado do ledger:** `20260814_09_multitenant_hardening.sql` e `20260814_10_medium_hardening.sql` **já estão aplicadas em PROD**. A `_10` foi aplicada via `supabase db query`, então o objeto existe no banco mas o registro em `supabase_migrations.schema_migrations` ficou pendente — conferir e, se ausente, inserir o registro dela junto com a aplicação da `_11`. O cabeçalho da `_10` diz "PENDENTE DE APLICAÇÃO MANUAL" e está desatualizado; não re-aplicar.
</environment_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Preflight, aplicação e verificação de catálogo</name>
  <files>supabase/migrations/20260814_11_membership_lifecycle.sql</files>
  <read_first>
    - `supabase/migrations/20260814_11_membership_lifecycle.sql` — o arquivo completo, exatamente como será aplicado
    - `.planning/STATE.md` (seção Blockers) — o histórico de aplicação das Fases 5, 6 e 7 e o registro da limitação do `gsd-executor`
  </read_first>
  <action>
**Preflight (antes de qualquer escrita).** Via MCP `execute_sql` contra o projeto PROD `wrdwzychjhlpwpivfhhq`:
1. Confirmar que a tabela `invites` **não** existe ainda (`to_regclass('public.invites')` nulo) e que nenhuma das 11 funções novas já existe em `pg_proc` — evita aplicação dupla.
2. Ler `supabase_migrations.schema_migrations` e registrar a contagem atual e se a versão `20260814_10` está presente.
3. Confirmar que `enforce_property_id_immutable()` existe (a migration a reusa) e que `role_enum`, `is_member_of(uuid)` e `get_role(uuid)` existem.

Se qualquer preflight falhar, **parar** e reportar — não improvisar.

**Aplicação.** Via MCP `apply_migration`, aplicar o conteúdo integral de `20260814_11_membership_lifecycle.sql` como uma única migration nomeada `20260814_11_membership_lifecycle`. Uma migration só, uma transação só — um estado meio-aplicado deixaria `invites` sem os RPCs que são sua única porta de escrita. Se o registro de `20260814_10` estava ausente no preflight, inseri-lo em `supabase_migrations.schema_migrations` na mesma sessão.

**Verificação de catálogo** (leitura, nunca suposição) — no mínimo estes 12 pontos:
1. `invites` existe com as 8 colunas e os tipos esperados.
2. `pg_class.relrowsecurity` e `relforcerowsecurity` são ambos `true` para `invites`.
3. `pg_policies` para `invites`: exatamente 2 linhas, ambas com `cmd = 'SELECT'`; zero linhas com `cmd <> 'SELECT'`.
4. `property_members` continua com zero policies de escrita (a fase não deve ter reintroduzido nenhuma).
5. Os dois índices existem, e `invites_property_email_pending_idx` é `UNIQUE` com predicado parcial `status = 'pending'`.
6. `trg_invites_property_id_immutable` existe e está habilitado (`tgenabled = 'O'`), apontando para `enforce_property_id_immutable`.
7. As 11 funções existem em `pg_proc` com `prosecdef` verdadeiro para as 9 SECURITY DEFINER e falso para `assert_not_last_veterinarian`.
8. `proconfig` de todas as 11 contém `search_path=`.
9. `has_function_privilege('authenticated', ..., 'EXECUTE')` verdadeiro para as 10 chamáveis.
10. `has_function_privilege('anon', ..., 'EXECUTE')` **falso** para todas as 10.
11. `has_function_privilege('authenticated', 'assert_not_last_veterinarian(uuid,uuid)', 'EXECUTE')` falso.
12. O corpo de `assert_not_last_veterinarian` lido de `pg_get_functiondef` contém `FOR UPDATE` antes do `count(*)`.

Registrar o resultado ponto a ponto no SUMMARY. Qualquer divergência vira uma migration corretiva forward-only nova, nunca uma edição do arquivo já aplicado.
  </action>
  <verify>
    <automated>bash -c "grep -v '^--' supabase/migrations/20260814_11_membership_lifecycle.sql | grep -c 'FROM anon, PUBLIC' | grep -qx 10 && echo 'arquivo integro'"</automated>
    <human-check>Os 12 pontos de catálogo lidos via MCP `execute_sql` estão registrados no SUMMARY com o valor observado de cada um.</human-check>
  </verify>
  <acceptance_criteria>
    - O preflight confirmou que nada da fase já existia em PROD antes da aplicação
    - `apply_migration` retornou sucesso e a versão `20260814_11_membership_lifecycle` aparece em `supabase_migrations.schema_migrations`
    - Os 12 pontos de verificação de catálogo estão registrados com o valor lido, não com "ok"
    - Pontos 3, 4, 10 e 11 (as verificações de blindagem) passaram; qualquer falha neles bloqueia a fase
    - Nenhum arquivo em `supabase/migrations/` foi editado
  </acceptance_criteria>
  <done>Migration em PROD e verificada por leitura de catálogo.</done>
</task>

<task type="auto">
  <name>Task 2: Replay da suíte pgTAP e suíte Dart completa</name>
  <files>supabase/tests/10_membership_test.sql</files>
  <read_first>
    - `supabase/tests/10_membership_test.sql` — a suíte a executar
    - `.planning/STATE.md` (Blockers) — o precedente de replay via MCP `execute_sql` em `BEGIN…ROLLBACK` e os dois casos de falha ambiental aceita (`06_sanitary_test.sql` Group 8, `07_expenses_test.sql` primeira rodada)
  </read_first>
  <action>
Executar `supabase/tests/10_membership_test.sql` verbatim via MCP `execute_sql` contra PROD, dentro de `BEGIN … ROLLBACK`. Registrar o total de asserções e quantas passaram.

Interpretação do resultado: uma falha só é aceitável se for comprovadamente **ambiental** (dependência de estado global do banco), com a evidência anexada — precedente das Fases 6 e 7. Falha de esquema ou de comportamento bloqueia a fase e vira migration corretiva forward-only.

Se alguma asserção falhar por uma expectativa errada da própria suíte (por exemplo, esperar `42501` onde a RLS filtra a linha silenciosamente — o erro cometido e corrigido em `07_expenses_test.sql`), corrigir a asserção **fortalecendo-a** (provar ausência de mutação), nunca enfraquecendo, e registrar a correção no SUMMARY.

Depois do replay, conferir que nenhuma linha de fixture sobreviveu: contar linhas em `invites`, `property_members` e `properties` com os prefixos de id de fixture da suíte — devem ser zero.

Por fim, rodar a suíte Dart completa e o analisador.
  </action>
  <verify>
    <automated>bash -c "flutter analyze 2>&1 | tail -3 && flutter test"</automated>
    <human-check>O resultado do replay pgTAP (N/N asserções) e a contagem zero de resíduo de fixture estão registrados no SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - `flutter test` verde na suíte completa e `flutter analyze` sem issues
    - O replay pgTAP foi executado e o placar registrado no SUMMARY
    - Toda falha remanescente está classificada como ambiental com evidência, ou foi fechada por migration corretiva
    - A contagem de resíduo de fixture em PROD é zero para as três tabelas
  </acceptance_criteria>
  <done>Prova automatizada de MEMB-01/02/03 executada contra o schema real.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — UAT dos dois fluxos que nenhum teste alcança</name>
  <files>—</files>
  <action>Pausar a execução e apresentar ao usuário os 11 passos de `<how-to-verify>`. Não prosseguir nem marcar a fase como completa até receber o sinal de retomada. Registrar no SUMMARY o resultado passo a passo, incluindo qualquer passo reprovado.</action>
  <what-built>
Fase 10 completa: convite com aceite (`invites` + 4 RPCs), gestão de membros (listar/trocar papel/remover/sair), guarda de último veterinário no banco, arquivamento de fazenda com confirmação por digitação do nome e restauração pela UI. Migration aplicada em PROD e suíte pgTAP replicada.
  </what-built>
  <how-to-verify>
Rodar o app (`flutter run -d edge` ou o deploy) e executar os dois fluxos que nenhum teste automatizado alcança — ambos listados como "Manual-Only Verifications" no 10-VALIDATION.md:

**Fluxo A — convite para conta que ainda não existe (MEMB-01):**
1. Logado como veterinário de uma fazenda, abrir o menu do cartão da fazenda em `/propriedades` e escolher "Membros".
2. Convidar um e-mail que ainda **não** tem conta no sistema, com papel "Leitor". Confirmar que o convite aparece em "Convites pendentes".
3. Sair, criar uma conta nova com exatamente aquele e-mail (inclusive testando digitá-lo com caixa diferente no cadastro).
4. Após confirmar o cadastro e logar, a tela `/sem-acesso` deve mostrar o convite com o nome correto da fazenda e o papel "Leitor".
5. Tocar em "Aceitar" — a navegação deve ir sozinha para o dashboard da fazenda, com ela selecionada no seletor. Nenhum toque extra.

**Fluxo B — ciclo de vida da fazenda (PROPV-01/02):**
6. Voltar à conta de veterinário. Em `/propriedades`, menu do cartão → "Arquivar". Confirmar que o botão "Arquivar" só habilita ao digitar o nome exato da fazenda (testar prefixo e caixa diferente antes).
7. Confirmar. A fazenda some da aba "Ativas" e do seletor de fazenda.
8. Ir à aba "Arquivadas" — a fazenda aparece esmaecida com "Restaurar fazenda". Restaurar.
9. Confirmar que ela volta à aba "Ativas" e ao seletor.

**Checagens complementares (rápidas):**
10. Logado como o leitor convidado, abrir a fazenda: a tela de Membros não deve ser alcançável pelo menu (o item "Membros" não aparece), e nenhum botão de gestão deve existir em lugar nenhum.
11. Como veterinário único da fazenda, tentar "Sair da fazenda" na tela de Membros: deve aparecer a mensagem informando que a fazenda ficaria sem veterinário responsável, com o nome dela.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que falhou, indicando o número do passo</resume-signal>
  <verify>
    <human-check>Os 11 passos foram executados no app real e o resultado de cada um está registrado no SUMMARY.</human-check>
  </verify>
  <done>Usuário aprovou os 11 passos, ou os passos reprovados estão registrados como gaps para `/gsd-plan-phase 10 --gaps`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessão de aplicação → PROD | Escrita de DDL direto no banco de produção |
| Suíte de teste → PROD | Fixtures dentro de uma transação revertida |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-34 | Tampering | Aplicação parcial deixando `invites` sem os RPCs que são sua única porta de escrita | high | mitigate | Uma migration, uma transação, com preflight de não-já-aplicada (Task 1) |
| T-10-35 | Repudiation | Declarar a fase aplicada sem verificação real de catálogo | high | mitigate | 12 pontos de verificação com valor lido registrado, não "ok" (Task 1) |
| T-10-36 | Elevation of Privilege | `anon` mantendo EXECUTE por causa do `ALTER DEFAULT PRIVILEGES` do Supabase | high | mitigate | Ponto 10 da verificação de catálogo checa `has_function_privilege('anon', ...)` falso nas 10 funções |
| T-10-37 | Tampering | Fixtures da suíte pgTAP sobrevivendo em PROD | medium | mitigate | `BEGIN…ROLLBACK` + contagem explícita de resíduo (Task 2) |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- Migration em `supabase_migrations.schema_migrations` e 12 pontos de catálogo registrados
- Replay pgTAP com placar registrado e zero resíduo de fixture
- `flutter test` verde e `flutter analyze` limpo
- Checkpoint humano aprovado nos 11 passos
</verification>

<success_criteria>
Fase 10 em produção e provada: MEMB-01, MEMB-02, MEMB-03, PROPV-01 e PROPV-02 verificados por catálogo, por pgTAP e por UAT humana com duas contas reais.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: nenhum artefato de código — aplica, verifica e registra. Atualiza `.planning/STATE.md` (ledger de migrations, Blockers, Phase Status).

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-11-SUMMARY.md` when done
</output>
