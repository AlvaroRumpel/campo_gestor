---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 05
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - lib/features/membros/presentation/membros_screen.dart
  - test/widget/membros_screen_test.dart
autonomous: true
requirements: [MEMB-01, MEMB-02, MEMB-03]

must_haves:
  truths:
    - "Um leitor não vê nenhum controle de gestão na tela de Membros — os controles estão ausentes, não desabilitados"
    - "A linha do próprio usuário oferece 'Sair da fazenda'; as demais oferecem 'Trocar papel' e 'Remover membro'"
    - "Convites pendentes vazio (visão do gestor) renderiza a linha inline 'Nenhum convite pendente' dentro do SectionCard, não um EmptyState de página inteira"
    - "Nome/e-mail de membro trunca com Expanded + TextOverflow.ellipsis e maxLines 1, tanto na tabela desktop quanto no ListTile mobile"
    - "StatusChip de papel e de status de convite renderiza sem overflow a 360px de largura"
    - "Contagem de membros usa Intl.plural com locale pt_BR — '1 membro' no singular e '{N} membros' no plural"
    - statement: "Revogar um convite que já foi respondido é rejeitado pelo RPC e a UI mostra a frase 'Não foi possível concluir. Atualize a lista e tente novamente.' num SnackBar"
      verification: backstop
    - "Abaixo de Breakpoints.rail a tela renderiza ListView + FAB; a partir dele renderiza tabela densa + painel fixo de 380px"
  artifacts:
    - lib/features/membros/presentation/membros_screen.dart
    - test/widget/membros_screen_test.dart
  key_links:
    - "Toda mutação bem-sucedida invalida `propertyMembersProvider(propertyId)` / `propertyInvitesProvider(propertyId)` e também `memberPropertiesProvider` (sair da fazenda muda o redirect do router)"
    - "Erro 23514 do RPC → `asMembroException(..., propertyName: ...)` → SnackBar com o nome da fazenda interpolado"
  prohibitions:
    - "Não introduzir um padrão de desktop diferente do mestre-detalhe já usado em Animais/Lotes/Reprodução/Sanitário/Gastos"
    - "Não renderizar controle desabilitado para papel negado"
    - "Não criar constantes de espaçamento novas — reusar os valores já embutidos em `ui.dart` (SectionCard 14, painel 380, chip 9x3)"
---

<objective>
Construir `MembrosScreen`: lista de membros com papel, ações de gestão (convidar, trocar papel, remover, sair), e a seção de convites pendentes com revogação — mobile (ListView + FAB) e desktop (tabela densa + painel 380px).

Purpose: é a tela que torna MEMB-02 real e o único lugar de onde um convite é criado. Sem ela, `owner` e `reader` continuam papéis inalcançáveis.
Output: 1 tela nova + 1 arquivo de teste de widget.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-PATTERNS.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-04-SUMMARY.md
</context>

<planner_assumption>
**Concorrência entre sessões (`⚠ unresolved` do 10-UI-SPEC.md):** duas abas disparando a guarda de último veterinário ao mesmo tempo não têm indicador otimista. A verdade é o banco — `assert_not_last_veterinarian` serializa com `FOR UPDATE` e a segunda chamada recebe 23514, que vira o SnackBar genérico da guarda. Assumido como estado aceito, não desenhado. Nada a implementar além do SnackBar.
</planner_assumption>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MembrosScreen — layout mobile, ações e convites pendentes</name>
  <files>lib/features/membros/presentation/membros_screen.dart, test/widget/membros_screen_test.dart</files>
  <read_first>
    - `lib/features/gastos/presentation/gastos_property_screen.dart` (linhas 100–260) — `LayoutBuilder` com `constraints.maxWidth >= Breakpoints.rail`, `itemsAsync.when(loading/error/data)` com `ErrorRetry`, e `_countLabel` usando `Intl.plural(..., locale: 'pt_BR')`
    - `lib/features/propriedades/presentation/propriedades_screen.dart` (linhas 100–250) — `_openForm` com `showAdaptiveForm` + `ref.invalidate` no retorno true, `_confirmDelete` com o bloco de ícone `dangerContainer`/`danger`, e `_PropertyCard` com `PopupMenuButton` gated por `canEdit`
    - `lib/core/widgets/ui.dart` (linhas 11–200 e 478–568) — `StatusChip`/`StatusKind`, `SectionCard`, `EmptyState`, `ErrorRetry`, `FarmAvatar`, `showAdaptiveForm`/`FormWidth`
    - `lib/core/widgets/campo_app_bar.dart` (linhas 40–80) — `DetailAppBar(parentLabel:, onBack:)`
    - `lib/features/membros/data/membro_repository.dart`, `membro_models.dart`, `membro_exception.dart` — providers, `PropertyMember.isSelf`, `roleLabel`, `asMembroException`
    - `lib/features/membros/presentation/invite_form_dialog.dart` — `InviteFormDialog(propertyId:)`
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — "Copywriting Contract" inteiro e "Layout & Interaction Contract"
  </read_first>
  <behavior>
    - Com `canManageMembers` falso (papel reader), nenhum dos textos "Convidar membro", "Trocar papel", "Remover membro" está na árvore, e não existe `FloatingActionButton`
    - Com papel veterinarian, o FAB "Convidar" existe e a linha de cada membro tem um `PopupMenuButton`
    - A linha cujo `isSelf` é true oferece "Sair da fazenda" e **não** oferece "Remover membro"
    - Uma lista com 1 membro mostra "1 membro"; com 3 membros mostra "3 membros"
    - Sem convites pendentes, o SectionCard "Convites pendentes" contém o texto "Nenhum convite pendente" e a árvore **não** contém um segundo `EmptyState`
    - Com 1 convite pendente, aparecem o e-mail convidado, um `StatusChip` "Pendente" e a ação "Revogar"
    - Quando `revokeInvite` lança `PostgrestException(code: 'P0002')`, aparece um SnackBar com "Não foi possível concluir. Atualize a lista e tente novamente."
    - Quando `removeMember` lança `PostgrestException(code: '23514')`, o SnackBar contém o nome da fazenda e a palavra "responsável"
    - A 360px de largura, nenhuma exceção de overflow é levantada (usar `tester.binding.setSurfaceSize(const Size(360, 800))`)
  </behavior>
  <action>
Criar `lib/features/membros/presentation/membros_screen.dart` com `class MembrosScreen extends ConsumerWidget`, parâmetro obrigatório `final String propertyId`.

Resolver o nome da fazenda a partir de `memberPropertiesProvider` (achar a membership cujo `property.id == propertyId`) — necessário para a copy de "Sair da fazenda" e para interpolar na mensagem de 23514. Se não resolver, usar string vazia e deixar `asMembroException` cair no fallback "esta fazenda".

`appBar: DetailAppBar(parentLabel: 'Fazendas', onBack: ...)` seguindo o mesmo tratamento de `context.canPop()` usado em `PropriedadesScreen`.

Corpo: observar `propertyMembersProvider(propertyId)` com `.when(loading: CircularProgressIndicator, error: ErrorRetry(message: 'Erro ao carregar. Verifique sua conexão e tente novamente.', onRetry: invalidar o provider), data: ...)`. Dentro do `data`, um `LayoutBuilder` que decide `isDesktop = constraints.maxWidth >= Breakpoints.rail` e chama `_buildMobile(...)` (esta task) ou `_buildDesktop(...)` (Task 2 — nesta task, deixar `_buildDesktop` delegando para `_buildMobile` como placeholder explícito, para o arquivo compilar).

`_buildMobile`: `ListView` com, no topo, uma linha de resumo mostrando `_countLabel(members.length)` (helper com `Intl.plural(count, one: '1 membro', other: '$count membros', locale: 'pt_BR')`); depois um `Card` por membro; depois o `SectionCard` de convites pendentes.

Linha de membro: `Row` com `FarmAvatar(name: member.email, size: 40, background: AppColors.surfaceVariant, foreground: AppColors.primaryDarkText)`, `Expanded` contendo o e-mail em 16/w600 com `maxLines: 1, overflow: TextOverflow.ellipsis`, um `StatusChip(roleLabel(member.role), kind: ...)` onde veterinarian usa `StatusKind.positive` e owner/reader usam `StatusKind.neutral`, e — somente se `canManageMembers` — um `PopupMenuButton<String>`. Itens do menu: sempre `Trocar papel`; se `member.isSelf`, o segundo item é `Sair da fazenda`, senão é `Remover membro` (texto em `AppColors.danger`, mesmo tratamento do item "Remover" de `_PropertyCard`).

SectionCard `Convites pendentes`: observar `propertyInvitesProvider(propertyId)`; no estado vazio, renderizar uma única linha de texto `Nenhum convite pendente` seguida de `Convide um veterinário, proprietário ou leitor pelo e-mail.` em `AppColors.textSecondary` — **inline dentro do card**, nunca um `EmptyState` de página inteira (evita empilhar dois vazios quando a lista de membros está cheia). Cada convite: e-mail (`Expanded` + ellipsis), `StatusChip(inviteStatusLabel(invite.status), kind: StatusKind.warning)`, `roleLabel(invite.role)`, e — se `canManageMembers` — um botão de texto `Revogar` em `AppColors.danger`.

`floatingActionButton`: `canManageMembers ? FloatingActionButton.extended(label: Text('Convidar'), icon: Icon(Icons.person_add_alt_1), onPressed: abrir o convite) : null`.

Handlers (todos privados, todos com o mesmo esqueleto: chamar o repositório, em sucesso invalidar, em erro converter e mostrar SnackBar):
- `_openInvite`: `showAdaptiveForm<bool>(context: context, width: FormWidth.form, builder: (_) => InviteFormDialog(propertyId: propertyId))`; se voltar true, `ref.invalidate(propertyInvitesProvider(propertyId))`.
- `_confirmRevoke(invite)`: diálogo de confirmação com título `Revogar convite`, corpo `Tem certeza que deseja revogar o convite enviado para {email}?`, botões `Cancelar` / `Revogar`; ao confirmar chama `revokeInvite` e invalida `propertyInvitesProvider(propertyId)`.
- `_confirmRemove(member)`: título `Remover membro`, corpo `Tem certeza que deseja remover {email} desta fazenda? Ele perderá acesso imediatamente.`, confirmar `Remover`; chama `removeMember` e invalida `propertyMembersProvider(propertyId)`.
- `_confirmLeave(member)`: título `Sair da fazenda`, corpo `Tem certeza que deseja sair de {fazenda}? Você perderá acesso imediatamente.`, confirmar `Sair`; chama `leaveProperty` e invalida `propertyMembersProvider(propertyId)` **e** `memberPropertiesProvider` (é o que faz o router reavaliar o redirect quando o usuário sai da última fazenda).
- `_openRoleChange(member)`: `showAdaptiveForm` com `width: FormWidth.confirm` mostrando os três papéis como `RadioListTile<String>` (valores crus, rótulos de `roleLabel`) com o papel atual pré-selecionado, botões `Cancelar` / `Salvar`; ao salvar chama `updateMemberRole` e invalida `propertyMembersProvider(propertyId)` e `memberPropertiesProvider`.

Todo `catch` usa `asMembroException(e, fallbackMessage: '<frase específica da ação>', propertyName: <nome resolvido>)` e mostra `ex.message` num SnackBar, sempre com guarda de `mounted`.

Os diálogos de confirmação destrutiva reusam o bloco de ícone 38x38 com `AppColors.dangerContainer` / `AppColors.danger` e `FilledButton.styleFrom(backgroundColor: AppColors.danger, foregroundColor: AppColors.onDanger)`, exatamente como `_confirmDelete` em `propriedades_screen.dart`.

Criar `test/widget/membros_screen_test.dart` cobrindo os nove casos de `<behavior>`, com um fake de `MembroRepository` via `membroRepositoryProvider.overrideWithValue(...)` e overrides de `propertyMembersProvider`/`propertyInvitesProvider`/`memberPropertiesProvider`/`currentPropertyProvider` conforme necessário. Espelhar a estrutura de `test/widget/gastos_property_screen_test.dart`. Incluir `ProviderScope(retry: (retryCount, error) => null)` se algum caso induzir falha de provider (precedente registrado na Fase 8).
  </action>
  <verify>
    <automated>flutter test test/widget/membros_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/membros_screen_test.dart` passa com no mínimo 9 casos
    - Existe um caso que monta a tela com papel reader e asserta `findsNothing` para os textos `Convidar`, `Trocar papel` e `Remover membro`, e `findsNothing` para `FloatingActionButton`
    - Existe um caso que asserta que a linha com `isSelf: true` mostra `Sair da fazenda` e não mostra `Remover membro`
    - Existe um caso a 360px (`setSurfaceSize`) que termina sem nenhuma exceção de overflow
    - Existe um caso que asserta `1 membro` no singular e outro que asserta `3 membros`
    - O caso de estado vazio de convites asserta o texto `Nenhum convite pendente` e `findsNothing` para um segundo `EmptyState`
    - Existe um caso que induz `PostgrestException(code: '23514')` em `removeMember` e asserta que o SnackBar contém o nome da fazenda
    - `flutter analyze` sem novas issues
  </acceptance_criteria>
  <done>Layout mobile completo com as cinco ações e a seção de convites; desktop ainda delegando ao mobile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MembrosScreen — layout desktop mestre-detalhe</name>
  <files>lib/features/membros/presentation/membros_screen.dart, test/widget/membros_screen_test.dart</files>
  <read_first>
    - `lib/features/membros/presentation/membros_screen.dart` — o que a Task 1 escreveu
    - `lib/features/gastos/presentation/gastos_property_screen.dart` (linhas 140–210 e o método `_buildPanel`, ~428–448, e `_buildTable`) — cabeçalho desktop, `Row` com `Expanded(tabela)` + painel, e a decoração exata do painel: `width: 380`, `color: AppColors.background`, `border: Border(left: BorderSide(color: AppColors.divider))`, conteúdo em `SingleChildScrollView(padding: EdgeInsets.all(12))` com `SectionCard`s separados por `SizedBox(height: 10)`
    - `test/widget/gastos_property_screen_test.dart` — como os testes de desktop forçam a largura da superfície
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — "Layout & Interaction Contract", item Desktop (colunas Nome/E-mail, Papel, Ações; painel com CTA "Convidar membro" no topo e "Convites pendentes" abaixo)
  </read_first>
  <behavior>
    - A 1280px de largura, existe exatamente um container de 380px de largura com borda à esquerda (o painel) e a lista de membros aparece numa tabela, não num ListView de Cards
    - A tabela tem os cabeçalhos de coluna "Nome/E-mail", "Papel" e "Ações"
    - Com `canManageMembers` verdadeiro, o painel contém o botão "Convidar membro"; com papel reader, esse botão está ausente do painel
    - O SectionCard "Convites pendentes" está dentro do painel no desktop (não no corpo principal)
    - A 1280px, um e-mail muito longo na tabela não gera exceção de overflow
    - Zero `FloatingActionButton` no desktop (o CTA vive no painel)
  </behavior>
  <action>
Substituir o placeholder de `_buildDesktop` por uma implementação real, no molde de `GastosPropertyScreen._buildDesktop`:

Coluna com: um cabeçalho (`Padding(EdgeInsets.fromLTRB(16, 14, 16, 4))`) contendo o título `Membros` em 24/w600 e, abaixo, `_countLabel(members.length)` em `AppColors.textSecondary`; depois um `Expanded` com `DecoratedBox(border: Border(top: BorderSide(color: AppColors.divider)))` envolvendo um `Row(crossAxisAlignment: CrossAxisAlignment.stretch)` de `Expanded(ColoredBox(color: AppColors.surface, child: tabela))` + o painel.

Tabela densa: cabeçalho com altura 36 e rótulos em 11/w700 uppercase com letterSpacing, colunas `Nome/E-mail` (flex maior), `Papel`, `Ações`; linhas com `minHeight: 44`, e-mail em `Expanded` + `maxLines: 1` + `TextOverflow.ellipsis`, papel como `StatusChip`, e a coluna de ações renderizando o mesmo `PopupMenuButton` da Task 1 — presente só quando `canManageMembers`. Reusar os mesmos handlers `_confirmRemove` / `_confirmLeave` / `_openRoleChange` já escritos; nenhuma lógica duplicada entre mobile e desktop.

Painel (clone de `_buildPanel`): `Container(key: const ValueKey('membros-painel'), width: 380, decoration: BoxDecoration(color: AppColors.background, border: Border(left: BorderSide(color: AppColors.divider))))` com `SingleChildScrollView(padding: EdgeInsets.all(12))` contendo, nesta ordem: um `SectionCard` com título `Convidar membro`, uma linha curta de explicação e um `FilledButton.icon` `Convidar membro` — o card inteiro só é renderizado quando `canManageMembers`; `SizedBox(height: 10)`; o mesmo `SectionCard` de convites pendentes da Task 1 (extrair num método compartilhado, não duplicar).

No desktop, `floatingActionButton` é nulo — o CTA vive no painel.

Estender `test/widget/membros_screen_test.dart` com os seis casos de `<behavior>`, forçando a superfície para 1280x900 e restaurando com `addTearDown(tester.binding.setSurfaceSize(null))`.
  </action>
  <verify>
    <automated>flutter test test/widget/membros_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/membros_screen_test.dart` passa com no mínimo 15 casos no total (9 da Task 1 + 6 desta)
    - Existe um caso que localiza `find.byKey(const ValueKey('membros-painel'))` e asserta largura 380
    - Existe um caso que asserta ausência de `FloatingActionButton` a 1280px e presença do botão `Convidar membro` dentro do painel
    - Existe um caso com papel reader a 1280px assertando que `Convidar membro` está ausente
    - Os métodos `_confirmRemove`, `_confirmLeave` e `_openRoleChange` aparecem uma única vez no arquivo (compartilhados entre mobile e desktop)
    - O SectionCard de convites pendentes é construído por um único método, chamado dos dois layouts
    - `flutter analyze` limpo e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>Tela de Membros completa nos dois layouts, com zero duplicação de handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tela → repositório → RPC | Ids de membro e de convite escolhidos na UI |
| Gate de papel → renderização | `canManageMembers` só decide visibilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-17 | Elevation of Privilege | Leitor enxergando controles de gestão | medium | mitigate | `canManageMembers` remove o FAB, o `PopupMenuButton`, o CTA do painel e a ação "Revogar" (Tasks 1–2); a barreira real são os RPCs |
| T-10-18 | Tampering | Sair da última fazenda sem o router reavaliar, deixando a UI num estado inconsistente | medium | mitigate | `_confirmLeave` invalida `memberPropertiesProvider`, que é o `ref.listen` do `_RouterRefreshNotifier` (Task 1) |
| T-10-19 | Information Disclosure | Texto cru de erro do banco no SnackBar | medium | mitigate | Todo `catch` roteia por `asMembroException` com `fallbackMessage` autorado (Task 1) |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `flutter test` verde (suíte completa)
- `flutter analyze` limpo
- Caso de 360px sem overflow e caso de 1280px com painel de 380px, ambos verdes
</verification>

<success_criteria>
`MembrosScreen` entrega MEMB-02 nos dois layouts: listar com papel, convidar, revogar convite, trocar papel, remover e sair — com todos os controles ausentes para o papel leitor e todo erro do banco traduzido para pt-BR.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: `MembrosScreen` em `lib/features/membros/presentation/membros_screen.dart` e `test/widget/membros_screen_test.dart`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-05-SUMMARY.md` when done
</output>
