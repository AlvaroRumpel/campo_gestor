---
phase: 04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/features/animais/animal_repository_test.dart
  - test/features/lotes/lote_repository_test.dart
  - test/widget/mover_animal_dialog_test.dart
  - test/widget/mover_lote_dialog_test.dart
  - test/widget/animal_detail_screen_test.dart
  - test/widget/lote_detail_screen_test.dart
autonomous: true
requirements:
  - MOV-01
  - MOV-02
tags:
  - flutter
  - testing
  - wave0

must_haves:
  truths:
    - "All 6 Wave 0 test files compile and execute (red — expected failure stubs)"
    - "Test stubs reference symbols (moveAnimal, moveLot, MoverAnimalDialog, MoverLoteDialog) that will be implemented in Plans 02 and 03"
    - "Existing test/widget/baixa_dialog_test.dart pattern reproduced for new dialogs"
  artifacts:
    - path: "test/features/animais/animal_repository_test.dart"
      provides: "Contract test for AnimalRepository.moveAnimal"
      contains: "expect(repo.moveAnimal, isA<Function>())"
    - path: "test/features/lotes/lote_repository_test.dart"
      provides: "Extended with contract test for LoteRepository.moveLot"
      contains: "expect(repo.moveLot, isA<Function>())"
    - path: "test/widget/mover_animal_dialog_test.dart"
      provides: "Widget tests for MoverAnimalDialog"
      contains: "MoverAnimalDialog"
    - path: "test/widget/mover_lote_dialog_test.dart"
      provides: "Widget tests for MoverLoteDialog"
      contains: "MoverLoteDialog"
    - path: "test/widget/animal_detail_screen_test.dart"
      provides: "Widget tests for 3rd 'Mover animal' button gate (canEdit, isActive)"
      contains: "Mover animal"
    - path: "test/widget/lote_detail_screen_test.dart"
      provides: "Widget tests for 'Mover para piquete' button gate (canEdit, active, animals > 0)"
      contains: "Mover para piquete"
  key_links:
    - from: "test/widget/mover_animal_dialog_test.dart"
      to: "lib/features/animais/presentation/mover_animal_dialog.dart"
      via: "import"
      pattern: "import.*mover_animal_dialog\\.dart"
    - from: "test/widget/mover_lote_dialog_test.dart"
      to: "lib/features/lotes/presentation/mover_lote_dialog.dart"
      via: "import"
      pattern: "import.*mover_lote_dialog\\.dart"
---

<objective>
Create Wave 0 Nyquist test scaffolds for Phase 4 (Movements). All tests live BEFORE implementation so per-task verification in Plans 02 and 03 has automated commands.

Purpose: Comply with Nyquist sampling — every task in Plans 02/03 references an `<automated>` command that points to a file that already exists. This plan creates those files as failing-but-compiling stubs (mirrors the Phase 3 Plan 03-01 pattern).
Output: 6 test files (5 new + 1 extended) covering MOV-01 and MOV-02 surface contracts and widget UI gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-movements/04-CONTEXT.md
@.planning/phases/04-movements/04-RESEARCH.md
@.planning/phases/04-movements/04-UI-SPEC.md
@.planning/phases/04-movements/04-VALIDATION.md

@test/widget/baixa_dialog_test.dart
@test/features/lotes/lote_repository_test.dart
@lib/features/animais/data/animal_repository.dart
@lib/features/lotes/data/lote_repository.dart
@lib/features/animais/presentation/baixa_dialog.dart
@lib/features/animais/data/animal_model.dart
@lib/features/lotes/data/lote_model.dart
@lib/features/piquetes/data/piquete_model.dart
@lib/features/piquetes/data/piquete_repository.dart

<interfaces>
<!-- Existing types/contracts the test scaffolds must reference. -->
<!-- Executor must use these directly — no codebase exploration needed. -->

From lib/features/animais/data/animal_model.dart:
```dart
@freezed
sealed class Animal with _$Animal {
  const factory Animal({
    required String id,
    required String propertyId,
    required String lotId,
    required String category,
    required int number,
    String? breed,
    int? bodyCondition,
    String? observation,
    String? baixaReason,
    DateTime? baixaDate,
    required DateTime createdAt,
    DateTime? deletedAt,
  }) = _Animal;
}
```

From lib/features/lotes/data/lote_model.dart:
```dart
@freezed
sealed class Lot with _$Lot {
  const factory Lot({
    required String id,
    required String propertyId,
    required String paddockId,
    required String name,
    required DateTime createdAt,
    DateTime? deletedAt,
  }) = _Lot;
}
```

From lib/features/piquetes/data/piquete_model.dart (assumed structure — verify on read):
```dart
@freezed
sealed class Paddock with _$Paddock {
  const factory Paddock({
    required String id,
    required String propertyId,
    required String name,
    required double areaHa,
    required double uaCapacity,
    required DateTime createdAt,
    DateTime? deletedAt,
  }) = _Paddock;
}
```

From lib/features/animais/data/animal_repository.dart (planned new method — does not yet exist):
```dart
// Future signature (Plan 02 implements this):
Future<Animal> moveAnimal({
  required String id,
  required String newLotId,
});
```

From lib/features/lotes/data/lote_repository.dart (planned new method — does not yet exist):
```dart
// Future signature (Plan 03 implements this):
Future<void> moveLot({
  required String lotId,
  required String newPaddockId,
});
```

From lib/features/auth/data/property_repository.dart:
```dart
class PropertyMembership {
  const PropertyMembership({required this.property, required this.role});
  final SelectedProperty property;
  final String role; // 'owner' | 'veterinarian' | 'reader'
}
```

Pattern reference: test/widget/baixa_dialog_test.dart uses `_FakeAnimalRepo extends AnimalRepository { _FakeAnimalRepo() : super(SupabaseService()); ... }` to stub repository methods without a real Supabase client.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create animal repository contract test for moveAnimal</name>
  <files>test/features/animais/animal_repository_test.dart</files>
  <read_first>
    - test/features/lotes/lote_repository_test.dart (canonical pattern: MockSupabaseService + contract test "expect(method, isA<Function>())")
    - lib/features/animais/data/animal_repository.dart (current AnimalRepository surface — must NOT modify, only reference)
    - lib/features/animais/data/animal_constants.dart (to import BaixaReason if needed; the existing repo references it)
  </read_first>
  <action>
    Create NEW file `test/features/animais/animal_repository_test.dart` mirroring the structure of `test/features/lotes/lote_repository_test.dart`. The file must:

    1. Imports (exact):
       ```dart
       import 'package:campo_gestor/core/services/supabase_service.dart';
       import 'package:campo_gestor/features/animais/data/animal_model.dart';
       import 'package:campo_gestor/features/animais/data/animal_repository.dart';
       import 'package:flutter_test/flutter_test.dart';
       import 'package:mocktail/mocktail.dart';
       ```

    2. Mock class:
       ```dart
       class MockSupabaseService extends Mock implements SupabaseService {}
       ```

    3. setUp() that creates `mockService` and `repo = AnimalRepository(mockService);`

    4. Group `'AnimalRepository (MOV-01)'` containing four contract tests:
       - `'moveAnimal exists and is callable'` — `expect(repo.moveAnimal, isA<Function>());`
       - `'moveAnimal accepts named params id and newLotId returning Future<Animal>'` — assert via `repo.moveAnimal is Future<Animal> Function({required String id, required String newLotId})` if static type check is feasible, else fall back to `expect(repo.moveAnimal, isA<Function>())` with a comment noting the planned signature.
       - `'fetchAnimalsByLot still exists (regression guard)'` — `expect(repo.fetchAnimalsByLot, isA<Function>());`
       - `'updateAnimal still exists (regression guard for unchanged surface)'` — `expect(repo.updateAnimal, isA<Function>());`

    5. Group `'Animal model (MOV-01 — model side regression)'` with one test that constructs an `Animal.fromJson({...})` with valid fields (id, propertyId, lotId, category=vaca, number=1, createdAt) and asserts `animal.lotId == 'lot-uuid-A'`.

    All tests can pass right now via the contract pattern — `moveAnimal` does not yet exist; the test that references it as `repo.moveAnimal` will FAIL TO COMPILE until Plan 02 adds the method. This is intentional Nyquist behavior: the test gates compilation success against the method's existence. Add a top-of-file comment block:

    ```dart
    // MOV-01 — AnimalRepository.moveAnimal contract.
    // Wave 0 stubs. Method `moveAnimal` lands in Plan 04-02.
    // Until Plan 04-02 commits, the `expect(repo.moveAnimal, ...)` lines below
    // FAIL TO COMPILE — that is the desired Nyquist red state.
    // Pattern mirrors test/features/lotes/lote_repository_test.dart.
    ```

    Do NOT add a `moveAnimal` implementation here. Do NOT modify `lib/features/animais/data/animal_repository.dart`.
  </action>
  <verify>
    <automated>flutter test test/features/animais/animal_repository_test.dart 2>&1 | grep -E "moveAnimal|undefined|compile|Error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `test/features/animais/animal_repository_test.dart` exists
    - File contains string `expect(repo.moveAnimal, isA<Function>())` (grep)
    - File contains import `package:campo_gestor/features/animais/data/animal_repository.dart` (grep)
    - File contains string `class MockSupabaseService extends Mock implements SupabaseService` (grep)
    - File contains group string `'AnimalRepository (MOV-01)'` (grep)
    - `flutter test test/features/animais/animal_repository_test.dart` fails to compile with reference to `moveAnimal` (RED — expected Wave 0 state)
  </acceptance_criteria>
  <done>Test file created; references `repo.moveAnimal`; compilation fails on that symbol (Wave 0 red).</done>
</task>

<task type="auto">
  <name>Task 2: Extend lote repository test with moveLot contract test</name>
  <files>test/features/lotes/lote_repository_test.dart</files>
  <read_first>
    - test/features/lotes/lote_repository_test.dart (the EXISTING file to extend — preserve all current tests)
    - lib/features/lotes/data/lote_repository.dart (current surface — must NOT modify)
  </read_first>
  <action>
    EDIT existing file `test/features/lotes/lote_repository_test.dart`. Inside the existing `group('LoteRepository (PROP-03)', () { ... })` block, AFTER the `'softDeleteLot sets deleted_at = now()'` test, ADD this new test:

    ```dart
    test('moveLot exists and is callable (MOV-02 contract)', () {
      // Contract: moveLot method exists.
      // Method body lands in Plan 04-03 along with the move_lot_to_paddock RPC.
      expect(repo.moveLot, isA<Function>());
    });
    ```

    Do NOT remove or modify any existing tests. Do NOT change imports or setUp. Do NOT add a `moveLot` implementation to the repository here. The line `expect(repo.moveLot, ...)` will FAIL TO COMPILE until Plan 03 adds the method — that is the desired Wave 0 red state.

    Update the top-of-file comment block by appending one new line at the end of the existing header comment:

    ```dart
    // MOV-02 contract added 2026-05-19 — moveLot lands in Plan 04-03.
    ```
  </action>
  <verify>
    <automated>flutter test test/features/lotes/lote_repository_test.dart 2>&1 | grep -E "moveLot|undefined|compile|Error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `test/features/lotes/lote_repository_test.dart` contains string `expect(repo.moveLot, isA<Function>())` (grep)
    - File still contains string `'fetchLotsByPaddock returns active lots'` (grep — regression guard)
    - File still contains string `'softDeleteLot sets deleted_at'` (grep — regression guard)
    - File contains comment `MOV-02 contract added 2026-05-19` (grep)
    - `flutter test test/features/lotes/lote_repository_test.dart` fails to compile with reference to `moveLot` (RED — expected Wave 0 state)
  </acceptance_criteria>
  <done>Existing tests preserved; new moveLot contract test added; compilation fails on `repo.moveLot` (Wave 0 red).</done>
</task>

<task type="auto">
  <name>Task 3: Create mover_animal_dialog widget test scaffold</name>
  <files>test/widget/mover_animal_dialog_test.dart</files>
  <read_first>
    - test/widget/baixa_dialog_test.dart (canonical dialog widget test pattern: _FakeAnimalRepo subclass, ProviderScope overrides, MaterialApp+Scaffold+Builder host)
    - lib/features/animais/data/animal_repository.dart (to know what providers exist for overrides: animalRepositoryProvider, animalListByLotProvider, animalListByPropertyProvider, animalByIdProvider)
    - lib/features/animais/data/animal_model.dart (Animal constructor signature)
    - lib/features/lotes/data/lote_model.dart (Lot constructor signature)
  </read_first>
  <action>
    Create NEW file `test/widget/mover_animal_dialog_test.dart`. Mirror the structure of `test/widget/baixa_dialog_test.dart` exactly. The file must:

    1. Top-of-file comment:
       ```dart
       // MOV-01 — MoverAnimalDialog widget tests.
       // Wave 0 stubs. Implementation lands in Plan 04-02 (MoverAnimalDialog).
       // Decisions enforced: D-02 (lot picker), D-03 (item format), D-05 (SnackBar copy).
       ```

    2. Imports (exact):
       ```dart
       import 'package:campo_gestor/core/services/supabase_service.dart';
       import 'package:campo_gestor/features/animais/data/animal_model.dart';
       import 'package:campo_gestor/features/animais/data/animal_repository.dart';
       import 'package:campo_gestor/features/animais/presentation/mover_animal_dialog.dart';
       import 'package:campo_gestor/features/lotes/data/lote_model.dart';
       import 'package:campo_gestor/features/lotes/data/lote_repository.dart';
       import 'package:flutter/material.dart';
       import 'package:flutter_riverpod/flutter_riverpod.dart';
       import 'package:flutter_test/flutter_test.dart';
       ```

    3. `_FakeAnimalRepo extends AnimalRepository` with `_FakeAnimalRepo() : super(SupabaseService());` and a `@override Future<Animal> moveAnimal({required String id, required String newLotId}) async => _sampleAnimal.copyWith(lotId: newLotId);` — adapt copyWith if freezed.

    4. `_FakeLoteRepo extends LoteRepository` with `_FakeLoteRepo() : super(SupabaseService());` (no method overrides needed; only used because some providers are wired through it).

    5. Sample data constants at top level:
       ```dart
       final _sampleAnimal = Animal(
         id: 'animal-7',
         propertyId: 'prop-1',
         lotId: 'lot-current',
         category: 'vaca',
         number: 7,
         createdAt: DateTime(2025, 1, 1),
       );
       final _sampleLotCurrent = Lot(
         id: 'lot-current',
         propertyId: 'prop-1',
         paddockId: 'pad-1',
         name: 'Lote Atual',
         createdAt: DateTime(2025, 1, 1),
       );
       final _sampleLotTarget = Lot(
         id: 'lot-target',
         propertyId: 'prop-1',
         paddockId: 'pad-2',
         name: 'Lote Destino',
         createdAt: DateTime(2025, 1, 1),
       );
       ```

    6. `_buildDialog({Animal? animal})` helper that returns a `ProviderScope` with overrides for `animalRepositoryProvider`, `loteRepositoryProvider`, `animalByIdProvider`, `animalListByLotProvider`, `animalListByPropertyProvider`, plus the future `loteListByPropertyProvider` (which does not yet exist — this WILL fail to compile until Plan 02 declares it). Wrap in `MaterialApp` with `Scaffold(body: Builder(builder: (context) => Center(child: MoverAnimalDialog(animal: animal ?? _sampleAnimal))))`.

    7. Test group `'MoverAnimalDialog (MOV-01)'` with FOUR tests (titles + body):
       - `'renders title containing animal number'` — pump, then `expect(find.textContaining('#7'), findsWidgets);`
       - `'renders Cancelar and Confirmar movimentação buttons'` — `expect(find.widgetWithText(TextButton, 'Cancelar'), findsOneWidget); expect(find.widgetWithText(FilledButton, 'Confirmar movimentação'), findsOneWidget);`
       - `'confirm button is disabled until a lot is selected'` — find the `FilledButton`, assert its `onPressed` is null before any selection (`final btn = tester.widget<FilledButton>(find.widgetWithText(FilledButton, 'Confirmar movimentação')); expect(btn.onPressed, isNull);`).
       - `'excludes the current lot from the picker'` — assert `find.text('Lote Atual')` is `findsNothing` and `find.text('Lote Destino')` is `findsOneWidget`. (May need `await tester.pumpAndSettle()` after pumpWidget to let the FutureProvider override resolve.)

    The whole file will FAIL TO COMPILE because `mover_animal_dialog.dart` and `loteListByPropertyProvider` do not yet exist. This is the expected Wave 0 state.
  </action>
  <verify>
    <automated>flutter test test/widget/mover_animal_dialog_test.dart 2>&1 | grep -E "MoverAnimalDialog|loteListByPropertyProvider|undefined|compile|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/widget/mover_animal_dialog_test.dart` exists
    - File contains string `MoverAnimalDialog` (grep)
    - File contains string `class _FakeAnimalRepo extends AnimalRepository` (grep)
    - File contains string `'Confirmar movimentação'` (grep)
    - File contains string `'MoverAnimalDialog (MOV-01)'` (grep)
    - File contains string `loteListByPropertyProvider` (grep)
    - `flutter test test/widget/mover_animal_dialog_test.dart` fails to compile referencing `MoverAnimalDialog` or `loteListByPropertyProvider` (RED — expected Wave 0)
  </acceptance_criteria>
  <done>Test file created with 4 tests; compilation fails on missing symbols (Wave 0 red).</done>
</task>

<task type="auto">
  <name>Task 4: Create mover_lote_dialog widget test scaffold</name>
  <files>test/widget/mover_lote_dialog_test.dart</files>
  <read_first>
    - test/widget/baixa_dialog_test.dart (dialog widget test pattern)
    - test/widget/mover_animal_dialog_test.dart (the file you just created in Task 3 — keep style consistent)
    - lib/features/lotes/data/lote_repository.dart (loteRepositoryProvider, loteByIdProvider, loteListByPaddockProvider)
    - lib/features/piquetes/data/piquete_repository.dart (paddockRepositoryProvider, paddockListProvider, paddockByIdProvider)
    - lib/features/lotes/data/lote_model.dart (Lot constructor)
    - lib/features/piquetes/data/piquete_model.dart (Paddock constructor)
  </read_first>
  <action>
    Create NEW file `test/widget/mover_lote_dialog_test.dart`. Structure mirrors Task 3's `mover_animal_dialog_test.dart`. The file must:

    1. Top-of-file comment:
       ```dart
       // MOV-02 — MoverLoteDialog widget tests.
       // Wave 0 stubs. Implementation lands in Plan 04-03 (MoverLoteDialog).
       // Decisions enforced: D-07 (paddock picker + info text), D-09 (current paddock excluded), D-10 (SnackBar copy).
       ```

    2. Imports (exact):
       ```dart
       import 'package:campo_gestor/core/services/supabase_service.dart';
       import 'package:campo_gestor/features/lotes/data/lote_model.dart';
       import 'package:campo_gestor/features/lotes/data/lote_repository.dart';
       import 'package:campo_gestor/features/lotes/presentation/mover_lote_dialog.dart';
       import 'package:campo_gestor/features/piquetes/data/piquete_model.dart';
       import 'package:campo_gestor/features/piquetes/data/piquete_repository.dart';
       import 'package:flutter/material.dart';
       import 'package:flutter_riverpod/flutter_riverpod.dart';
       import 'package:flutter_test/flutter_test.dart';
       ```

    3. Fake repos:
       ```dart
       class _FakeLoteRepo extends LoteRepository {
         _FakeLoteRepo() : super(SupabaseService());
         @override
         Future<void> moveLot({required String lotId, required String newPaddockId}) async {
           // no-op success
         }
       }
       class _FakePaddockRepo extends PaddockRepository {
         _FakePaddockRepo() : super(SupabaseService());
       }
       ```

    4. Sample data:
       ```dart
       final _sampleLot = Lot(
         id: 'lot-7',
         propertyId: 'prop-1',
         paddockId: 'pad-current',
         name: 'Lote Bravo',
         createdAt: DateTime(2025, 1, 1),
       );
       final _padCurrent = Paddock(
         id: 'pad-current',
         propertyId: 'prop-1',
         name: 'Piquete Atual',
         areaHa: 10.0,
         uaCapacity: 20.0,
         createdAt: DateTime(2025, 1, 1),
       );
       final _padTarget = Paddock(
         id: 'pad-target',
         propertyId: 'prop-1',
         name: 'Piquete Destino',
         areaHa: 15.0,
         uaCapacity: 30.0,
         createdAt: DateTime(2025, 1, 1),
       );
       ```

       NOTE: If the actual `Paddock` constructor differs from the assumed shape above (e.g. additional required field), adjust the literal to match the model exactly — do not invent fields.

    5. `_buildDialog({Lot? lot, int activeAnimalCount = 5})` helper returning a `ProviderScope` with overrides for `loteRepositoryProvider`, `paddockRepositoryProvider`, `paddockListProvider` (returns `[_padCurrent, _padTarget]`), and `loteByIdProvider`. Wrap in `MaterialApp` + `Scaffold(body: Builder(builder: (ctx) => Center(child: MoverLoteDialog(lot: lot ?? _sampleLot, activeAnimalCount: activeAnimalCount))))`.

    6. Test group `'MoverLoteDialog (MOV-02)'` with FOUR tests:
       - `'renders title containing lot name'` — `expect(find.textContaining('Lote Bravo'), findsWidgets);`
       - `'renders info text with active animal count'` — `expect(find.textContaining('5 animais'), findsOneWidget);`
       - `'confirm button is disabled until a paddock is selected'` — `final btn = tester.widget<FilledButton>(find.widgetWithText(FilledButton, 'Confirmar movimentação')); expect(btn.onPressed, isNull);`
       - `'excludes the current paddock from the picker'` — `expect(find.text('Piquete Atual'), findsNothing); expect(find.text('Piquete Destino'), findsOneWidget);`

    The file will FAIL TO COMPILE because `mover_lote_dialog.dart` does not yet exist. Expected Wave 0 red.
  </action>
  <verify>
    <automated>flutter test test/widget/mover_lote_dialog_test.dart 2>&1 | grep -E "MoverLoteDialog|undefined|compile|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/widget/mover_lote_dialog_test.dart` exists
    - File contains string `MoverLoteDialog` (grep)
    - File contains string `class _FakeLoteRepo extends LoteRepository` (grep)
    - File contains string `'Confirmar movimentação'` (grep)
    - File contains string `'MoverLoteDialog (MOV-02)'` (grep)
    - File contains string `paddockListProvider` (grep)
    - `flutter test test/widget/mover_lote_dialog_test.dart` fails to compile referencing `MoverLoteDialog` (RED — expected Wave 0)
  </acceptance_criteria>
  <done>Test file created with 4 tests; compilation fails on missing symbol (Wave 0 red).</done>
</task>

<task type="auto">
  <name>Task 5: Create animal_detail_screen widget test scaffold (Mover button gate)</name>
  <files>test/widget/animal_detail_screen_test.dart</files>
  <read_first>
    - lib/features/animais/presentation/animal_detail_screen.dart (current screen — contains `AnimalInfoCard` with onEdit/onBaixa callbacks; to be extended with `onMover` in Plan 02)
    - test/widget/baixa_dialog_test.dart (ProviderScope + MaterialApp testing pattern)
    - lib/core/providers/current_property_provider.dart (memberPropertiesProvider override mechanics)
  </read_first>
  <action>
    Create NEW file `test/widget/animal_detail_screen_test.dart`. The file must:

    1. Top-of-file comment:
       ```dart
       // MOV-01, SC-3 — AnimalDetailScreen: "Mover animal" button gate.
       // Wave 0 stubs. AnimalInfoCard.onMover parameter lands in Plan 04-02.
       // Asserts: button absent when canEdit=false; present when isActive && canEdit.
       ```

    2. Imports (exact — adjust if file paths differ):
       ```dart
       import 'package:campo_gestor/core/providers/current_property_provider.dart';
       import 'package:campo_gestor/features/animais/data/animal_model.dart';
       import 'package:campo_gestor/features/animais/data/animal_repository.dart';
       import 'package:campo_gestor/features/animais/presentation/animal_detail_screen.dart';
       import 'package:campo_gestor/features/auth/data/property_repository.dart';
       import 'package:flutter/material.dart';
       import 'package:flutter_riverpod/flutter_riverpod.dart';
       import 'package:flutter_test/flutter_test.dart';
       ```

    3. Sample animal (active and archived variants):
       ```dart
       final _activeAnimal = Animal(
         id: 'animal-9', propertyId: 'prop-1', lotId: 'lot-A',
         category: 'vaca', number: 9, createdAt: DateTime(2025, 1, 1),
       );
       final _archivedAnimal = Animal(
         id: 'animal-10', propertyId: 'prop-1', lotId: 'lot-A',
         category: 'vaca', number: 10, createdAt: DateTime(2025, 1, 1),
         deletedAt: DateTime(2025, 6, 1), baixaReason: 'sale',
       );
       final _prop = SelectedProperty(id: 'prop-1', name: 'Fazenda Alpha');
       final _vetMembership = PropertyMembership(property: _prop, role: 'veterinarian');
       final _readerMembership = PropertyMembership(property: _prop, role: 'reader');
       ```

    4. `_buildScreen({required Animal animal, required String role})` helper that builds `ProviderScope` overrides for:
       - `animalByIdProvider(animal.id)` returns `animal`
       - `memberPropertiesProvider` returns `[role == 'veterinarian' ? _vetMembership : _readerMembership]`
       - `currentPropertyProvider` override returning `_prop` (use `currentPropertyProvider.overrideWith((ref) => ... AsyncNotifier returning prop)` OR use `.overrideWithValue(AsyncData(_prop))` depending on Riverpod 3.x API — if uncertain, leave a TODO comment for executor)

       Then `MaterialApp` with `home: AnimalDetailScreen(animalId: animal.id)`.

    5. Test group `'AnimalDetailScreen — Mover animal button (MOV-01, SC-3)'` with three tests:
       - `'shows Mover animal button when active && canEdit'` — pump, pumpAndSettle, `expect(find.widgetWithText(OutlinedButton, 'Mover animal'), findsOneWidget);`
       - `'hides Mover animal button when role is reader'` — animal=active, role=reader → `expect(find.widgetWithText(OutlinedButton, 'Mover animal'), findsNothing);`
       - `'hides Mover animal button when animal is archived'` — animal=archived, role=veterinarian → `expect(find.widgetWithText(OutlinedButton, 'Mover animal'), findsNothing);`

    All three tests will FAIL until Plan 02 adds the button — Wave 0 red. The file should compile if all import paths exist; only the `find.widgetWithText(...)` assertions will fail in red state.

    If `currentPropertyProvider`/`memberPropertiesProvider` overrides prove brittle in Riverpod 3.x, add a comment block above `_buildScreen`:
    ```dart
    // If overrides don't satisfy currentPropertyProvider, executor may need to
    // override the underlying authNotifierProvider or skip these tests until
    // Plan 02 lands. Wave 0 red is acceptable.
    ```
  </action>
  <verify>
    <automated>flutter test test/widget/animal_detail_screen_test.dart 2>&1 | grep -E "Mover animal|failed|passed|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/widget/animal_detail_screen_test.dart` exists
    - File contains string `'Mover animal'` (grep)
    - File contains group string `'AnimalDetailScreen — Mover animal button (MOV-01, SC-3)'` (grep)
    - File contains `_archivedAnimal` and `_readerMembership` sample data (grep)
    - File imports `package:campo_gestor/features/animais/presentation/animal_detail_screen.dart` (grep)
    - `flutter test test/widget/animal_detail_screen_test.dart` runs and at least one test fails referencing missing 'Mover animal' button (RED — expected Wave 0)
  </acceptance_criteria>
  <done>Test file created with 3 gate tests; tests fail with missing 'Mover animal' button (Wave 0 red).</done>
</task>

<task type="auto">
  <name>Task 6: Create lote_detail_screen widget test scaffold (Mover para piquete button gate)</name>
  <files>test/widget/lote_detail_screen_test.dart</files>
  <read_first>
    - lib/features/lotes/presentation/lote_detail_screen.dart (current screen — `_LoteHeaderCard` is private; Plan 03 will add `canEdit` parameter and "Mover para piquete" button)
    - test/widget/animal_detail_screen_test.dart (just-created sibling test — keep style consistent)
    - lib/core/providers/current_property_provider.dart (memberPropertiesProvider override mechanics)
  </read_first>
  <action>
    Create NEW file `test/widget/lote_detail_screen_test.dart`. The file must:

    1. Top-of-file comment:
       ```dart
       // MOV-02, SC-3 — LoteDetailScreen: "Mover para piquete" button gate.
       // Wave 0 stubs. Button + canEdit prop on _LoteHeaderCard lands in Plan 04-03.
       // Asserts: button absent when canEdit=false OR activeAnimals=0 OR lot archived.
       ```

    2. Imports:
       ```dart
       import 'package:campo_gestor/core/providers/current_property_provider.dart';
       import 'package:campo_gestor/features/animais/data/animal_model.dart';
       import 'package:campo_gestor/features/animais/data/animal_repository.dart';
       import 'package:campo_gestor/features/auth/data/property_repository.dart';
       import 'package:campo_gestor/features/lotes/data/lote_model.dart';
       import 'package:campo_gestor/features/lotes/data/lote_repository.dart';
       import 'package:campo_gestor/features/lotes/presentation/lote_detail_screen.dart';
       import 'package:campo_gestor/features/piquetes/data/piquete_model.dart';
       import 'package:campo_gestor/features/piquetes/data/piquete_repository.dart';
       import 'package:flutter/material.dart';
       import 'package:flutter_riverpod/flutter_riverpod.dart';
       import 'package:flutter_test/flutter_test.dart';
       ```

    3. Sample data:
       ```dart
       final _lot = Lot(
         id: 'lot-1', propertyId: 'prop-1', paddockId: 'pad-1',
         name: 'Lote Alpha', createdAt: DateTime(2025, 1, 1),
       );
       final _archivedLot = Lot(
         id: 'lot-2', propertyId: 'prop-1', paddockId: 'pad-1',
         name: 'Lote Arquivado', createdAt: DateTime(2025, 1, 1),
         deletedAt: DateTime(2025, 6, 1),
       );
       final _pad = Paddock(
         id: 'pad-1', propertyId: 'prop-1', name: 'Piquete Norte',
         areaHa: 10, uaCapacity: 20, createdAt: DateTime(2025, 1, 1),
       );
       final _activeAnimal = Animal(
         id: 'a1', propertyId: 'prop-1', lotId: 'lot-1',
         category: 'vaca', number: 1, createdAt: DateTime(2025, 1, 1),
       );
       final _prop = SelectedProperty(id: 'prop-1', name: 'Fazenda Alpha');
       final _vet = PropertyMembership(property: _prop, role: 'veterinarian');
       final _reader = PropertyMembership(property: _prop, role: 'reader');
       ```

    4. `_buildScreen({required Lot lot, required String role, required List<Animal> animals})` helper that builds `ProviderScope` overrides for:
       - `loteByIdProvider(lot.id)` → returns `lot`
       - `animalListByLotProvider(lot.id)` → returns `animals`
       - `paddockByIdProvider('pad-1')` → returns `_pad`
       - `memberPropertiesProvider` → returns `[role == 'veterinarian' ? _vet : _reader]`
       - `currentPropertyProvider` → returns `_prop` (see note in Task 5 about Riverpod 3.x override mechanics)

       Wrap in `MaterialApp(home: LoteDetailScreen(loteId: lot.id))`.

    5. Test group `'LoteDetailScreen — Mover para piquete button (MOV-02, SC-3)'` with FOUR tests:
       - `'shows Mover para piquete button when active && animals > 0 && veterinarian'` — `expect(find.widgetWithText(OutlinedButton, 'Mover para piquete'), findsOneWidget);`
       - `'hides Mover para piquete button when role is reader'` — `expect(find.widgetWithText(OutlinedButton, 'Mover para piquete'), findsNothing);`
       - `'hides Mover para piquete button when lot has 0 active animals'` — animals=[], role=vet → `expect(find.widgetWithText(OutlinedButton, 'Mover para piquete'), findsNothing);`
       - `'hides Mover para piquete button when lot is archived'` — lot=archived, role=vet, animals=[_activeAnimal] → `expect(find.widgetWithText(OutlinedButton, 'Mover para piquete'), findsNothing);`

    All four tests will FAIL until Plan 03 adds the button — Wave 0 red.
  </action>
  <verify>
    <automated>flutter test test/widget/lote_detail_screen_test.dart 2>&1 | grep -E "Mover para piquete|failed|passed|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/widget/lote_detail_screen_test.dart` exists
    - File contains string `'Mover para piquete'` (grep)
    - File contains group string `'LoteDetailScreen — Mover para piquete button (MOV-02, SC-3)'` (grep)
    - File contains `_archivedLot` sample data (grep)
    - File imports `package:campo_gestor/features/lotes/presentation/lote_detail_screen.dart` (grep)
    - `flutter test test/widget/lote_detail_screen_test.dart` runs and at least one test fails referencing missing 'Mover para piquete' button (RED — expected Wave 0)
  </acceptance_criteria>
  <done>Test file created with 4 gate tests; tests fail with missing 'Mover para piquete' button (Wave 0 red).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Wave 0 scaffolds — no runtime trust boundary changes. Tests run locally (no network, no auth). The threat model for Phase 4 is fully expressed in Plans 02 and 03.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-W0-01 | Tampering | Wave 0 stubs accidentally pass without implementation | mitigate | Each test asserts a symbol that does not yet exist (e.g. `repo.moveAnimal`, `MoverAnimalDialog`) → compilation fails until Plans 02/03 land. RED state is enforced by symbol absence, not by test logic. |
| T-4-W0-02 | Information Disclosure | Test fakes leak production Supabase credentials | accept | `SupabaseService()` is constructed but `client` getter is never invoked because all repo methods are overridden. No env vars read. Same pattern as `baixa_dialog_test.dart`. |
</threat_model>

<verification>
Run all six Wave 0 commands. Each MUST exit non-zero (compilation or assertion failure) — that proves the test gates the missing implementation:

```bash
flutter test test/features/animais/animal_repository_test.dart 2>&1 | tail -20
flutter test test/features/lotes/lote_repository_test.dart 2>&1 | tail -20
flutter test test/widget/mover_animal_dialog_test.dart 2>&1 | tail -20
flutter test test/widget/mover_lote_dialog_test.dart 2>&1 | tail -20
flutter test test/widget/animal_detail_screen_test.dart 2>&1 | tail -20
flutter test test/widget/lote_detail_screen_test.dart 2>&1 | tail -20
```

Expected: each command exits non-zero with an error message naming a symbol that lands in Plan 02 or 03 (`moveAnimal`, `moveLot`, `MoverAnimalDialog`, `MoverLoteDialog`, `loteListByPropertyProvider`, or a missing button text).

Also verify regression — existing tests still pass:

```bash
flutter test test/features/animais/animal_model_test.dart test/features/animais/ua_calculation_test.dart test/widget/baixa_dialog_test.dart
```

Expected: exit 0.
</verification>

<success_criteria>
1. All 6 test files exist at the paths listed in `files_modified`.
2. Each new file contains a header comment naming the requirement it covers (MOV-01 or MOV-02) and the implementing plan (04-02 or 04-03).
3. Each scaffold references at least one symbol that does not yet exist (the symbol that Plan 02/03 will create).
4. Existing test files (animal_model_test, ua_calculation_test, baixa_dialog_test, animais_screen_test, lote_form_dialog_test, etc.) still pass — `flutter test test/features/animais/animal_model_test.dart test/features/animais/ua_calculation_test.dart test/widget/baixa_dialog_test.dart` exits 0.
5. The extended `lote_repository_test.dart` preserves all 4 original PROP-03 tests AND the 2 original Lot model tests.
6. No production code in `lib/` is modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-movements/04-01-SUMMARY.md` documenting:
- Files created (5 new) + extended (1)
- Total Wave 0 tests added (~17)
- Symbols referenced that need to land in Plans 02/03: `AnimalRepository.moveAnimal`, `LoteRepository.moveLot`, `MoverAnimalDialog` widget, `MoverLoteDialog` widget, `loteListByPropertyProvider`, `'Mover animal'` button in AnimalInfoCard, `'Mover para piquete'` button in `_LoteHeaderCard`
- Confirmation that all 6 commands produce expected RED state
- Confirmation that existing tests still pass (regression guard)
</output>
