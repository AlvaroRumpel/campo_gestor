---
phase: 03-lots-animals-operational-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/features/lotes/lote_repository_test.dart
  - test/features/animais/animal_model_test.dart
  - test/features/animais/ua_calculation_test.dart
  - test/widget/lote_form_dialog_test.dart
  - test/widget/animal_edit_dialog_test.dart
  - test/widget/baixa_dialog_test.dart
  - test/widget/animais_screen_test.dart
autonomous: true
requirements:
  - PROP-03
  - PROP-04
  - PROP-05
  - ANIM-01
  - ANIM-02
  - ANIM-04
  - ANIM-05
  - ANIM-06
tags:
  - testing
  - scaffolding
  - flutter
  - tdd
must_haves:
  truths:
    - "Each of the 7 Wave-0 test files exists in test/ and runs (even if every test is initially `skip`/`expect later`)."
    - "Running `flutter test test/features/lotes/ test/features/animais/` exits 0."
    - "Running `flutter test test/widget/` exits 0."
    - "Each test file documents which requirement IDs it covers in a top-of-file comment."
  artifacts:
    - path: "test/features/lotes/lote_repository_test.dart"
      provides: "PROP-03 stub — LoteRepository contract tests"
    - path: "test/features/animais/animal_model_test.dart"
      provides: "ANIM-01 stub — Animal freezed model serialization round-trip"
    - path: "test/features/animais/ua_calculation_test.dart"
      provides: "PROP-05 stub — UA total computation"
    - path: "test/widget/lote_form_dialog_test.dart"
      provides: "PROP-04 stub — batch form validation"
    - path: "test/widget/animal_edit_dialog_test.dart"
      provides: "ANIM-02 stub — edit dialog renders breed / EC / observation"
    - path: "test/widget/baixa_dialog_test.dart"
      provides: "ANIM-04 stub — baixa dialog renders 3 reasons + date picker"
    - path: "test/widget/animais_screen_test.dart"
      provides: "ANIM-05 + ANIM-06 stubs — search filter + chip filters"
  key_links:
    - from: "test scaffolds"
      to: "Wave 1 implementations"
      via: "RED state — implementations turn skipped/expected-failing tests into passing tests"
      pattern: "test\\(.*?(PROP-03|PROP-04|PROP-05|ANIM-01|ANIM-02|ANIM-04|ANIM-05|ANIM-06).*?\\)"
---

<objective>
Create the 7 Wave-0 test scaffolds required by 03-VALIDATION.md so every requirement in Phase 3 has an `<automated>` verify command from the very first commit. Tests start in RED/skipped state and are progressively turned green by Waves 1–3.

Purpose: Satisfy the Nyquist sampling rule (every task in later plans references one of these test files in its `<verify>` block).
Output: 7 test files committed to the repo, all of which compile and exit-0 when run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lots-animals-operational-core/03-VALIDATION.md
@.planning/phases/03-lots-animals-operational-core/03-RESEARCH.md
@.planning/phases/03-lots-animals-operational-core/03-CONTEXT.md
@pubspec.yaml
</context>

<interfaces>
<!-- Stack already established by Phase 0/1/2 — no new deps needed -->
- flutter_test (SDK), mocktail ^1.0.5 — already in pubspec.yaml dev_dependencies
- All test files use the standard Flutter `test('<description>', () async { ... })` API
- Mock pattern: `class MockSupabaseService extends Mock implements SupabaseService {}` (mocktail)
- For widget tests: `await tester.pumpWidget(MaterialApp(home: ProviderScope(child: <widget>)))` then `tester.pumpAndSettle()`
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create unit-test stubs for data layer (lote repo, animal model, UA calc)</name>
  <files>test/features/lotes/lote_repository_test.dart, test/features/animais/animal_model_test.dart, test/features/animais/ua_calculation_test.dart</files>
  <read_first>
    - .planning/phases/03-lots-animals-operational-core/03-VALIDATION.md (Per-Task Verification Map § Wave 0 Requirements)
    - .planning/phases/03-lots-animals-operational-core/03-RESEARCH.md § "UA weight constants" (must encode the kUaWeights map shape)
    - test/features/piquetes/ if it exists, else any existing test/ to mirror the file layout convention
  </read_first>
  <action>
Create three Flutter unit-test files. Each file MUST start with a top-of-file comment naming the requirement ID it covers, an `import 'package:flutter_test/flutter_test.dart';` import, and at least one `test(...)` block per behavior. Tests use `expect(true, isFalse, reason: 'TODO Wave 1: implement <X>'); /* skip: 'pending Wave 1' */` to express RED state without breaking the suite. Suite must compile and `flutter test` must exit 0 — use `, skip: 'pending Wave 1 implementation'` parameter on every test that isn't yet executable.

File 1 — `test/features/lotes/lote_repository_test.dart` (covers PROP-03):
```dart
// PROP-03 — Usuário pode criar, editar e listar lotes operacionais de um piquete.
// Wave 0 stubs. Implementations land in Plan 03 (data layer) + Plan 04 (form).
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('LoteRepository (PROP-03)', () {
    test('fetchLotsByPaddock returns active lots filtered by paddock_id and ordered by name',
        () {}, skip: 'pending Wave 1 implementation');
    test('createLot inserts {property_id, paddock_id, name} and returns Lot',
        () {}, skip: 'pending Wave 1 implementation');
    test('updateLotName updates only the name column',
        () {}, skip: 'pending Wave 1 implementation');
    test('softDeleteLot sets deleted_at = now()',
        () {}, skip: 'pending Wave 1 implementation');
  });
}
```

File 2 — `test/features/animais/animal_model_test.dart` (covers ANIM-01):
```dart
// ANIM-01 — Animal model serializes/deserializes snake_case JSON.
// Wave 0 stubs. Implementations land in Plan 03 (Animal freezed model).
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Animal model (ANIM-01)', () {
    test('Animal.fromJson parses snake_case payload from Supabase',
        () {}, skip: 'pending Wave 1 implementation');
    test('Animal.toJson emits snake_case keys (lot_id, body_condition, baixa_reason, baixa_date)',
        () {}, skip: 'pending Wave 1 implementation');
    test('Animal supports nullable breed / bodyCondition / observation / baixaReason / baixaDate',
        () {}, skip: 'pending Wave 1 implementation');
    test('Animal.number is int and required',
        () {}, skip: 'pending Wave 1 implementation');
  });
}
```

File 3 — `test/features/animais/ua_calculation_test.dart` (covers PROP-05):
```dart
// PROP-05 — UA total = sum(kUaWeights[a.category]) for all active animals.
// Wave 0 stubs. Implementations land in Plan 03 (calcTotalUa).
// Reference table from REQUIREMENTS.md Business Rules:
// vaca=1.0, novilha=0.75, terneiro=0.5, terneira=0.5, touro=1.5, boi=1.5, novilho=0.75
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('UA calculation (PROP-05)', () {
    test('calcTotalUa returns 0.0 for empty list',
        () {}, skip: 'pending Wave 1 implementation');
    test('calcTotalUa sums per-category weights — 10 vacas + 8 terneiros + 1 touro = 15.5',
        () {}, skip: 'pending Wave 1 implementation');
    test('calcTotalUa returns 0.0 contribution for unknown category',
        () {}, skip: 'pending Wave 1 implementation');
    test('kUaWeights contains all 7 categories with exact decimal values',
        () {}, skip: 'pending Wave 1 implementation');
  });
}
```
Create the parent directories `test/features/lotes/` and `test/features/animais/` as needed. Do NOT add any production-code imports yet — those types do not exist in Wave 0.
  </action>
  <verify>
    <automated>flutter test test/features/lotes/lote_repository_test.dart test/features/animais/animal_model_test.dart test/features/animais/ua_calculation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `test/features/lotes/lote_repository_test.dart` exists
    - File `test/features/animais/animal_model_test.dart` exists
    - File `test/features/animais/ua_calculation_test.dart` exists
    - `grep -l "PROP-03" test/features/lotes/lote_repository_test.dart` returns the file
    - `grep -l "ANIM-01" test/features/animais/animal_model_test.dart` returns the file
    - `grep -l "PROP-05" test/features/animais/ua_calculation_test.dart` returns the file
    - `grep -c "skip:" test/features/lotes/lote_repository_test.dart` returns >= 4
    - `grep -c "skip:" test/features/animais/animal_model_test.dart` returns >= 4
    - `grep -c "skip:" test/features/animais/ua_calculation_test.dart` returns >= 4
    - Command `flutter test test/features/lotes/lote_repository_test.dart test/features/animais/animal_model_test.dart test/features/animais/ua_calculation_test.dart` exits 0
  </acceptance_criteria>
  <done>3 unit-test stubs exist, compile, and exit 0 with all tests `skip`-ed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create widget-test stubs for dialogs and screen (lote form, animal edit, baixa, animais screen)</name>
  <files>test/widget/lote_form_dialog_test.dart, test/widget/animal_edit_dialog_test.dart, test/widget/baixa_dialog_test.dart, test/widget/animais_screen_test.dart</files>
  <read_first>
    - .planning/phases/03-lots-animals-operational-core/03-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/03-lots-animals-operational-core/03-UI-SPEC.md § "Copywriting Contract" — for the exact strings each widget must render
    - .planning/phases/03-lots-animals-operational-core/03-CONTEXT.md § D-10/D-11/D-13/D-16/D-17/D-18/D-19/D-20 for behaviors under test
  </read_first>
  <action>
Create four Flutter widget-test stub files. Each file documents the requirement ID it covers in a top-of-file comment, includes a `void main()`, and lists `testWidgets(...)` cases for each behavior with `, skip: 'pending Wave 2/3 implementation'`. The suite must compile and exit 0.

File 1 — `test/widget/lote_form_dialog_test.dart` (covers PROP-04):
```dart
// PROP-04 — LoteFormDialog: batch composition validation.
// Wave 0 stubs. Implementation lands in Plan 04 (LoteFormDialog).
// Decisions enforced: D-10 (7 categorias), D-11 (nome required + total>0), D-12 (paddock readonly).
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('LoteFormDialog (PROP-04)', () {
    testWidgets('renders 7 category counter rows: Vacas, Novilhas, Terneiros, Terneiras, Touros, Bois, Novilhos',
        (tester) async {}, skip: 'pending Plan 04 implementation');
    testWidgets('rejects submit when name is empty (shows "Nome do lote é obrigatório")',
        (tester) async {}, skip: 'pending Plan 04 implementation');
    testWidgets('rejects submit when sum of all category quantities == 0 (shows "Informe ao menos 1 animal para criar o lote")',
        (tester) async {}, skip: 'pending Plan 04 implementation');
    testWidgets('shows optional "Iniciar do número" field with hint "Ex: 101 (deixe vazio para auto)"',
        (tester) async {}, skip: 'pending Plan 04 implementation');
    testWidgets('shows raça dropdown per category row (search-select, optional)',
        (tester) async {}, skip: 'pending Plan 04 implementation');
  });
}
```

File 2 — `test/widget/animal_edit_dialog_test.dart` (covers ANIM-02):
```dart
// ANIM-02 — AnimalEditDialog: editable raça / EC 1-5 / observação.
// Wave 0 stubs. Implementation lands in Plan 06 (AnimalEditDialog).
// Decisions enforced: D-14 (raça hardcoded list), D-16 (EC 5 chips).
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AnimalEditDialog (ANIM-02)', () {
    testWidgets('shows Número and Categoria as read-only display fields',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('shows Raça dropdown with hardcoded breed list (Nelore, Angus, Brahman, Gir, ...)',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('shows 5 EC ChoiceChip widgets labeled 1..5 in a Row',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('shows Observação multi-line TextFormField (maxLines: 3)',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('Cancel button labeled "Cancelar"; submit button labeled "Salvar"',
        (tester) async {}, skip: 'pending Plan 06 implementation');
  });
}
```

File 3 — `test/widget/baixa_dialog_test.dart` (covers ANIM-04):
```dart
// ANIM-04 — BaixaDialog: motivo (Venda/Morte/Descarte) + data.
// Wave 0 stubs. Implementation lands in Plan 06 (BaixaDialog).
// Decisions enforced: D-17 (3 reasons mapped to enum 'sale'|'death'|'discard').
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BaixaDialog (ANIM-04)', () {
    testWidgets('renders title "Confirmar baixa do animal #<N>?"',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('renders 3 motivo options labeled "Venda", "Morte", "Descarte"',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('renders Data da baixa field with date picker, defaulting to today (DD/MM/AAAA)',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('confirm button labeled "Confirmar baixa", uses colorScheme.error background',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('rejects submit when motivo not selected',
        (tester) async {}, skip: 'pending Plan 06 implementation');
  });
}
```

File 4 — `test/widget/animais_screen_test.dart` (covers ANIM-05 and ANIM-06):
```dart
// ANIM-05, ANIM-06 — AnimaisScreen: search by number + filter by category/lot/paddock.
// Wave 0 stubs. Implementation lands in Plan 06 (AnimaisScreen).
// Decisions enforced: D-18 (filters), D-19 (tile format), D-20 (debounce 300ms), D-21 (toggle archived).
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AnimaisScreen — Search (ANIM-05)', () {
    testWidgets('SearchBar with hint "Buscar por número..." filters list in-memory after 300ms debounce',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('typing "4" matches animals whose number contains "4" (e.g. #4, #14, #42)',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('clear button (X) appears when search field is non-empty',
        (tester) async {}, skip: 'pending Plan 06 implementation');
  });
  group('AnimaisScreen — Filters (ANIM-06)', () {
    testWidgets('renders FilterChips: Todas | Vaca | Novilha | Terneiro | Terneira | Touro | Boi | Novilho',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('selecting "Vaca" chip filters list to category == "vaca"',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('Lote dropdown with "Todos os lotes" default filters by lot_id when set',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('Piquete dropdown with "Todos os piquetes" default filters by paddock_id when set',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('SummaryBar shows "X animais · Y,Y UA total" updating with filters',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('"Mostrar arquivados" toggle reveals archived animals with reason badge',
        (tester) async {}, skip: 'pending Plan 06 implementation');
    testWidgets('list tile primary line format: "#42 · Vaca"; secondary line: "<Lote> · <Piquete>"',
        (tester) async {}, skip: 'pending Plan 06 implementation');
  });
}
```
Create `test/widget/` directory if it doesn't exist.
  </action>
  <verify>
    <automated>flutter test test/widget/lote_form_dialog_test.dart test/widget/animal_edit_dialog_test.dart test/widget/baixa_dialog_test.dart test/widget/animais_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `test/widget/lote_form_dialog_test.dart` exists
    - File `test/widget/animal_edit_dialog_test.dart` exists
    - File `test/widget/baixa_dialog_test.dart` exists
    - File `test/widget/animais_screen_test.dart` exists
    - `grep -l "PROP-04" test/widget/lote_form_dialog_test.dart` returns the file
    - `grep -l "ANIM-02" test/widget/animal_edit_dialog_test.dart` returns the file
    - `grep -l "ANIM-04" test/widget/baixa_dialog_test.dart` returns the file
    - `grep -l "ANIM-05" test/widget/animais_screen_test.dart` returns the file
    - `grep -l "ANIM-06" test/widget/animais_screen_test.dart` returns the file
    - `grep -c "skip:" test/widget/lote_form_dialog_test.dart` returns >= 5
    - `grep -c "skip:" test/widget/animais_screen_test.dart` returns >= 10
    - Command `flutter test test/widget/lote_form_dialog_test.dart test/widget/animal_edit_dialog_test.dart test/widget/baixa_dialog_test.dart test/widget/animais_screen_test.dart` exits 0
  </acceptance_criteria>
  <done>4 widget-test stubs exist, compile, and exit 0 with all tests `skip`-ed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan creates only test scaffolds — no runtime trust boundaries are crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-W0-01 | Tampering | test scaffolds | accept | Test files cannot affect production behavior; CI prevents shipping `skip:` tests in later phases via the wave verification gate. |
</threat_model>

<verification>
After both tasks:
```bash
flutter test test/features/lotes/ test/features/animais/ test/widget/
```
Must exit 0. Every test is `skip`-ed; suite passes vacuously, providing a baseline for Wave 1+ to flip skips into real assertions.
</verification>

<success_criteria>
- 7 test files exist at the paths declared in `files_modified`
- Each file documents its requirement ID(s) in a top-of-file comment
- `flutter test test/features/lotes/ test/features/animais/ test/widget/` exits 0
- No production-code imports in any Wave 0 test (those types/widgets do not exist yet)
</success_criteria>

<output>
After completion, create `.planning/phases/03-lots-animals-operational-core/03-01-SUMMARY.md` describing which tests were stubbed, which `skip:` strings were used, and a checklist mapping each requirement ID to its test file.
</output>
