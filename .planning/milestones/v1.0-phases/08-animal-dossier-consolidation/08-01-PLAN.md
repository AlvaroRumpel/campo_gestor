---
phase: 08-animal-dossier-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/lotes/data/lote_repository.dart
  - lib/features/reproducao/data/atf_model.dart
  - lib/features/reproducao/data/atf_repository.dart
  - test/features/lotes/lote_repository_test.dart
  - test/features/reproducao/atf_repository_test.dart
  - test/widget/animal_detail_screen_test.dart
autonomous: true
requirements: [ANIM-03]

must_haves:
  truths:
    - "Uma única query PostgREST devolve o lote e o nome do piquete juntos — `LoteRepository.fetchLotWithPaddockName(id)` faz um select embarcado e nenhum segundo request é disparado para resolver o piquete (D-01, SC-1)."
    - "`ReproductiveHistoryEntry` carrega a lista completa de DGs daquele animal naquele ATF, ordenada do mais recente para o mais antigo pela regra única `isLaterDg` (D-10, SC-2, SC-3)."
    - "`ReproductiveHistoryEntry` carrega o touro do ATF e a data de implantação, vindos do mesmo embed de `atf_batches` — sem request adicional (D-09)."
    - "O provider novo do lote+piquete é auto-dispose por padrão: nenhum `keepAlive` é aplicado, de modo que reabrir a ficha refaz a query e nunca serve dado velho (D-03)."
    - "Um ATF sem nenhum DG produz uma lista de DGs vazia (nunca null) e um ATF com N DGs produz exatamente N itens — a camada de dados nunca descarta DG (UI-SPEC zero-one-many: DGs por ATF)."
    - "O resumo do último DG (`lastDgResult`/`lastDgDate`) continua existindo e continua sendo escolhido por `isLaterDg` — a lista completa é aditiva, não substitui o resumo (D-10)."
  artifacts:
    - "lib/features/lotes/data/lote_repository.dart — classe `LotWithPaddockName`, método `fetchLotWithPaddockName`, provider `loteWithPaddockByIdProvider`"
    - "lib/features/reproducao/data/atf_model.dart — `ReproductiveHistoryEntry` com `dgRecords`, `bullName`, `implantationDate`"
    - "lib/features/reproducao/data/atf_repository.dart — `fetchReproductiveHistory` com select ampliado e agrupamento completo de DGs por ATF"
  key_links:
    - "loteWithPaddockByIdProvider → loteRepositoryProvider → fetchLotWithPaddockName → select embarcado em `lots` com `paddocks!inner(name)`"
    - "fetchReproductiveHistory → agrupamento `dgsByAtf` → ReproductiveHistoryEntry.dgRecords (consumido pela expansão do plano 08-05)"
    - "atf_batches embed → bull_name / implantation_date → ReproductiveHistoryEntry.bullName / .implantationDate"
  prohibitions:
    - statement: "MUST NOT servir dado de ficha em cache como se fosse o estado atual do animal — nenhum provider desta ficha pode ser marcado keepAlive, porque um veterinário decide manejo em campo a partir do que a tela mostra e uma ficha silenciosamente desatualizada é pior que uma ficha lenta"
      category: safety
      requirement_id: ANIM-03
      status: unresolved
      verification: null
---

<objective>
Fechar as duas lacunas de camada de dados da ficha consolidada, sem tocar em nenhuma tela.

1. **D-01 (SC-1):** matar o waterfall lote → piquete. Hoje `AnimalInfoCard` resolve `loteByIdProvider`, lê `lot.paddockId` e só então dispara `paddockByIdProvider` — dois requests em série. Este plano cria a leitura única embarcada; a troca do consumo acontece no plano 08-04.
2. **D-09/D-10 (SC-2):** `fetchReproductiveHistory` já busca todos os DGs do animal, mas descarta todos menos o último. Este plano passa a carregar a lista completa por ATF, mais o touro e a data de implantação, na mesma query.

Purpose: sem estes dois, o plano 08-04 não tem provider para consumir e o plano 08-05 não tem DG para expandir. Este é o único plano de dados da fase.
Output: um método + wrapper + provider novos em `lote_repository.dart`; três campos novos em `ReproductiveHistoryEntry` alimentados por `fetchReproductiveHistory`; testes de contrato e fixtures atualizadas.

**Fase 100% Flutter:** nenhuma migration, nenhum objeto de banco novo, nenhuma policy RLS nova, nenhum pacote novo. Não existe tarefa de `supabase db push` nem suíte pgTAP nesta fase (D-23/D-24, RESEARCH §Package Legitimacy Audit "Not applicable").

**Wave 1 — cabeça da cadeia (revisão pós-checker).** A fase executa em 5 waves sequenciais, um plano por wave. Motivo: `test/widget/animal_detail_screen_test.dart` monta `AnimalDetailScreen`, que importa `lote_repository.dart`, `atf_model.dart`, `atf_repository.dart`, `sanitary_history_section.dart` e — depois do 08-02 — `animal_reproductive_history_section.dart`. Quatro dos cinco planos rodam esse arquivo no próprio `<verify>`, então o fecho de compilação de cada plano cobre arquivos que os outros editam: não existe par de planos com fechos disjuntos. O split em 5 planos continua valendo pelo orçamento de contexto (2-3 tarefas por plano), que independe de wave. Este plano é o primeiro porque é o único que não depende de nenhum outro — e porque a Task 2 é dona das fixtures de `animal_detail_screen_test.dart`, que todos os planos seguintes compilam.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animal-dossier-consolidation/08-CONTEXT.md
@.planning/phases/08-animal-dossier-consolidation/08-PATTERNS.md
@.planning/phases/08-animal-dossier-consolidation/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Leitura única lote + piquete (D-01)</name>
  <files>lib/features/lotes/data/lote_repository.dart, test/features/lotes/lote_repository_test.dart</files>

  <read_first>
    - `lib/features/lotes/data/lote_repository.dart` — arquivo alvo. Ver `fetchLot` (estilo de leitura de linha única com `.maybeSingle()`), `fetchLotsWithCountByProperty` (embed `paddocks!inner(name)` + desembrulho do JSON aninhado), `LotWithPaddockCount` (convenção de classe wrapper simples), `loteByIdProvider` (forma do `FutureProvider.family`).
    - `lib/features/animais/data/animal_repository.dart` — `fetchAnimalsByProperty`: o precedente exato de embed aninhado nomeado pelo D-01.
    - `test/features/lotes/lote_repository_test.dart` — a convenção de teste de contrato deste projeto (asserções de existência de método), e o comentário de cabeçalho que explica por que não se faz mock da cadeia de query-builder.
    - `.planning/phases/08-animal-dossier-consolidation/08-PATTERNS.md` §`lib/features/lotes/data/lote_repository.dart` — a forma alvo já extraída linha a linha.
  </read_first>

  <behavior>
    - Uma leitura de lote existente devolve `LotWithPaddockName` com o `Lot` completo e o nome do piquete preenchido.
    - Um id inexistente ou soft-deleted devolve `null`.
    - O provider é uma família por id, auto-dispose (nenhum `keepAlive`).
  </behavior>

  <action>
Adicionar em `lib/features/lotes/data/lote_repository.dart`:

**Classe wrapper.** Ao lado da `LotWithPaddockCount` existente, no fim do arquivo, criar `LotWithPaddockName` — classe simples com construtor `const` e dois campos finais: `lot` do tipo `Lot` e `paddockName` do tipo `String`. Classe simples de propósito, NÃO `@freezed` e NÃO um row do Supabase: é a mesma convenção de `LotWithPaddockCount` e `AtfMembershipView` para resultado de select embarcado.

**Método.** Adicionar `Future<LotWithPaddockName?> fetchLotWithPaddockName(String id)` a `LoteRepository`, espelhando o estilo de linha única de `fetchLot`: `_service.client.from('lots').select('*, paddocks!inner(name)').eq('id', id).isFilter('deleted_at', null).maybeSingle()`. Se a linha vier `null`, devolver `null`. Caso contrário, ler a chave `paddocks` do row como `Map<String, dynamic>`, montar uma cópia do row via `Map<String, dynamic>.from(row)` removendo a chave `paddocks`, passar essa cópia limpa para `Lot.fromJson` e devolver `LotWithPaddockName` com o nome lido de dentro do JSON embarcado.

O modificador `!inner` é deliberado: é exatamente o que `fetchLotsWithCountByProperty` e `AnimalRepository.fetchAnimalsByProperty` já usam para a mesma relação lote → piquete, e é o que faz o PostgREST devolver o piquete como objeto aninhado em vez de array. Não inventar variante left-join (RESEARCH Pitfall 2).

**Provider.** Registrar `loteWithPaddockByIdProvider` como `FutureProvider.family<LotWithPaddockName?, String>` logo abaixo de `loteByIdProvider`, com o mesmo corpo: `ref.watch(loteRepositoryProvider)` e chamada ao método novo. Sem `keepAlive` — o auto-dispose padrão do Riverpod 3.x é o comportamento exigido pelo D-03.

**Não remover `loteByIdProvider` nem `fetchLot`** — `LoteDetailScreen` e o diálogo de mover ainda os consomem. Este plano só adiciona.

**Teste de contrato.** No grupo `LoteRepository (PROP-03)` de `test/features/lotes/lote_repository_test.dart`, acrescentar uma asserção de existência de método para `fetchLotWithPaddockName`, citando D-01, na mesma forma das linhas já presentes (`expect(repo.<metodo>, isA<Function>())`). Não fazer mock da cadeia `.from().select().eq().isFilter().maybeSingle()` — o cabeçalho do próprio arquivo declara essa abordagem frágil e RESEARCH Pitfall 3 a rejeita explicitamente para esta fase.

Respeitar os lints do projeto: `require_trailing_commas`, `prefer_const_constructors`.
  </action>

  <verify>
    <automated>flutter analyze lib/features/lotes &amp;&amp; flutter test test/features/lotes/lote_repository_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/features/lotes/data/lote_repository.dart` contém `class LotWithPaddockName`.
    - O mesmo arquivo contém a assinatura `Future<LotWithPaddockName?> fetchLotWithPaddockName(`.
    - O mesmo arquivo contém a string de select `'*, paddocks!inner(name)'`.
    - O mesmo arquivo contém `final loteWithPaddockByIdProvider`.
    - Regressão: `grep -c "final loteByIdProvider" lib/features/lotes/data/lote_repository.dart` retorna 1 — o provider antigo continua existindo para os outros consumidores.
    - Nenhuma ocorrência de `keepAlive` no arquivo (D-03): `grep -v '^\s*//' lib/features/lotes/data/lote_repository.dart | grep -c keepAlive` retorna 0.
    - `flutter test test/features/lotes/lote_repository_test.dart` sai com código 0.
    - `flutter analyze lib/features/lotes` sai com código 0.
  </acceptance_criteria>

  <done>Existe um caminho de leitura única que devolve lote + nome do piquete, exposto por um provider family auto-dispose, e o caminho antigo continua intacto para seus consumidores atuais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DGs completos, touro e data de implantação no histórico reprodutivo (D-09, D-10)</name>
  <files>lib/features/reproducao/data/atf_model.dart, lib/features/reproducao/data/atf_repository.dart, test/features/reproducao/atf_repository_test.dart, test/widget/animal_detail_screen_test.dart</files>

  <read_first>
    - `lib/features/reproducao/data/atf_model.dart` — `ReproductiveHistoryEntry` (classe simples a estender) e `AtfBatch` (confirma que `bullName` e `implantationDate` já existem como colunas do batch).
    - `lib/features/reproducao/data/atf_repository.dart` §`fetchReproductiveHistory` — o método a estender: select embarcado de `atf_batches`, busca de `dg_records`, laço `lastDgByAtf`, construção das entries, ordenação final.
    - `lib/features/reproducao/data/dg_summary.dart` §`isLaterDg` — a REGRA ÚNICA de ordenação de DG (G-05-4). O comentário do arquivo registra que três cópias independentes já divergiram; reusar a função, nunca reescrever a comparação.
    - `lib/features/reproducao/data/dg_record_model.dart` — campos de `DgRecord` (`examDate`, `createdAt`, `result`, `observation`).
    - `test/widget/animal_detail_screen_test.dart` linhas 46-73 — as três fixtures `ReproductiveHistoryEntry` que precisam dos campos novos para o arquivo voltar a compilar.
    - `test/features/reproducao/atf_repository_test.dart` — convenção de teste de contrato já aplicada a `fetchReproductiveHistory`.
  </read_first>

  <behavior>
    - Um ATF com 3 DGs produz uma entry cujo `dgRecords` tem 3 itens, do mais recente para o mais antigo.
    - Um ATF sem DG produz `dgRecords` vazio e `lastDgResult`/`lastDgDate` nulos — nunca null na lista.
    - `lastDgResult` continua sendo o vencedor de `isLaterDg`, exatamente como antes.
    - `bullName` é nulo quando o ATF não tem touro nomeado; `implantationDate` é sempre preenchida.
  </behavior>

  <action>
**Modelo** (`atf_model.dart`, `ReproductiveHistoryEntry`): acrescentar três campos, mantendo a classe simples com construtor `const` (NÃO converter para `@freezed` — quebraria a convenção de `AtfMembershipView`/`LotWithPaddockCount` para resultado de select embarcado):
- `dgRecords` do tipo `List<DgRecord>`, obrigatório no construtor — todos os DGs daquele animal naquele ATF.
- `bullName` do tipo `String?`, opcional — rótulo legível do touro, já resolvido na Phase 5 (WR-01, plano 05-13): reusar o valor da coluna, não reimplementar rotulagem.
- `implantationDate` do tipo `DateTime`, obrigatório.

Documentar no dartdoc da classe que `dgRecords` vem ordenado decrescente e que `lastDgResult`/`lastDgDate` continuam sendo o resumo da linha colapsada.

**Repositório** (`atf_repository.dart`, `fetchReproductiveHistory`):

1. Ampliar a lista de colunas do embed de `atf_batches` para incluir também `implantation_date` e `bull_name`, além das quatro já pedidas (`id`, `name`, `insemination_date`, `active`). Nenhuma query nova — só mais colunas na mesma.

2. Depois do laço `lastDgByAtf` existente (que NÃO deve ser removido — ele alimenta o resumo da linha colapsada), acrescentar um segundo agrupamento sobre a mesma lista `dgRecords` já em memória: um `Map<String, List<DgRecord>>` chaveado por `atfBatchId`, preenchido com `putIfAbsent` + `add`. É uma passada aditiva sobre dado já carregado — zero request extra.

3. Na construção de cada entry, passar `dgRecords` como uma **lista mutável nova** — copiar com `List<DgRecord>.from(...)` a partir do mapa (ou lista vazia quando a chave não existe) e só então ordenar. Aplicar `..sort` direto sobre a constante de lista vazia lança em runtime; a cópia é obrigatória.

4. Ordenar essa cópia em ordem decrescente **reusando `isLaterDg`** como comparador: retornar `-1` quando o primeiro argumento é o mais recente pela regra, `1` quando é o segundo, `0` no empate. Isso mantém a regra de `examDate` com desempate por `createdAt` num lugar só (G-05-4) e satisfaz o SC-3 dentro da expansão.

5. Preencher `bullName` a partir da chave `bull_name` do JSON embarcado como `String?`, e `implantationDate` fazendo `DateTime.parse` da chave `implantation_date`, no mesmo estilo já usado para `insemination_date`.

A ordenação final das entries por data de inseminação decrescente permanece inalterada.

**Teste de contrato** (`test/features/reproducao/atf_repository_test.dart`): a asserção de existência de `fetchReproductiveHistory` já existe e continua válida — a assinatura não muda. Atualizar apenas o texto do teste para registrar que a entry agora carrega os DGs completos (D-10). Não acrescentar mock de payload completo (PATTERNS.md §Repository contract tests: "do not over-specify test depth here").

**Fixtures** (`test/widget/animal_detail_screen_test.dart`): as três constantes `_atfPrimavera`, `_atfOutono` e `_atfInverno` param de compilar com campos obrigatórios novos. Acrescentar a cada uma `dgRecords` vazio e uma `implantationDate` plausível (anterior à respectiva data de inseminação). Manter as fixtures mínimas — as fixtures com DG de verdade nascem no plano 08-05, no arquivo de teste da seção reprodutiva. Nenhuma asserção existente deste arquivo pode ser alterada ou removida: o teste `'populated: renders one row per entry, in the order supplied (insemination date descending)'` é a guarda de regressão do SC-3 e precisa continuar verde exatamente como está.
  </action>

  <verify>
    <automated>flutter analyze lib/features/reproducao &amp;&amp; flutter test test/features/reproducao/ test/widget/animal_detail_screen_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/features/reproducao/data/atf_model.dart` contém `final List<DgRecord> dgRecords;`, `final String? bullName;` e `final DateTime implantationDate;` dentro de `ReproductiveHistoryEntry`.
    - `lib/features/reproducao/data/atf_repository.dart` contém `implantation_date` e `bull_name` na string de select de `fetchReproductiveHistory`.
    - O mesmo arquivo contém `putIfAbsent` no novo agrupamento por ATF.
    - Regressão: `grep -c "lastDgByAtf" lib/features/reproducao/data/atf_repository.dart` é maior que 0 — o cálculo do último DG não foi removido.
    - Regra única: `grep -c "isLaterDg" lib/features/reproducao/data/atf_repository.dart` é maior ou igual a 2 (seleção do último DG + ordenação da lista completa); nenhuma comparação de `examDate` reescrita à mão foi introduzida.
    - `flutter test test/widget/animal_detail_screen_test.dart` sai com código 0 e o teste `populated: renders one row per entry, in the order supplied (insemination date descending)` passa (guarda de regressão do SC-3).
    - `flutter test test/features/reproducao/` sai com código 0.
    - `flutter analyze lib/features/reproducao` sai com código 0.
  </acceptance_criteria>

  <done>Uma entry do histórico reprodutivo carrega todos os DGs daquele ATF em ordem decrescente, mais touro e data de implantação, sem nenhuma query adicional, e o resumo do último DG continua funcionando como antes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plano 08-01)

Símbolos e arquivos criados por ESTE plano — não existiam antes da Phase 8:

| Símbolo | Tipo | Arquivo |
|---|---|---|
| `LotWithPaddockName` | classe wrapper Dart | `lib/features/lotes/data/lote_repository.dart` |
| `LoteRepository.fetchLotWithPaddockName` | método de repositório | `lib/features/lotes/data/lote_repository.dart` |
| `loteWithPaddockByIdProvider` | `FutureProvider.family` | `lib/features/lotes/data/lote_repository.dart` |
| `ReproductiveHistoryEntry.dgRecords` | campo de modelo | `lib/features/reproducao/data/atf_model.dart` |
| `ReproductiveHistoryEntry.bullName` | campo de modelo | `lib/features/reproducao/data/atf_model.dart` |
| `ReproductiveHistoryEntry.implantationDate` | campo de modelo | `lib/features/reproducao/data/atf_model.dart` |

Nenhum arquivo novo é criado por este plano. Nenhum símbolo é removido.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → PostgREST (Supabase) | Toda leitura desta fase atravessa aqui; a autorização é RLS no servidor, nunca filtro no cliente |
| PostgREST → tabela embarcada (`paddocks`, `dg_records`) | O embed avalia RLS por linha na tabela embarcada, não só na tabela raiz |

## STRIDE Threat Register

ASVS L1; bloqueio configurado em `high`. Nenhuma ameaça `high`/`critical` nesta fase — nenhum checkpoint bloqueante de segurança é necessário.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `fetchLotWithPaddockName` — embed `paddocks!inner(name)` | medium | mitigate | O embed não concede acesso novo: o PostgREST avalia a policy SELECT de `paddocks` por linha, exatamente como o `paddockByIdProvider` que ele substitui. Nenhuma policy nova é criada. Confirmação em execução: o select alcança apenas colunas `id/name` do piquete e é escopado por `.eq('id', <lotId>)`, cuja linha raiz já é filtrada pela RLS de `lots` (Fases 2/3). Precedente idêntico já em produção em `fetchLotsWithCountByProperty`. |
| T-08-02 | Information Disclosure | `fetchReproductiveHistory` — lista completa de `dg_records` | low | accept | Mudança de forma de leitura, não de escopo: a query de `dg_records` já é `.eq('animal_id', animalId)` e já traz todas as linhas: hoje elas são reduzidas em Dart e as descartadas simplesmente somem. Passar a exibi-las não amplia o que o cliente já recebia. A policy SELECT property-scoped de `dg_records` (Fase 5) permanece a única autorização. |
| T-08-03 | Tampering | Colunas novas do embed (`bull_name`, `implantation_date`) | low | accept | Ambas já pertencem a `atf_batches`, já são lidas em outras telas do módulo reprodutivo e não são graváveis a partir da ficha (D-22: blocos read-only). |
| T-08-SC | Tampering | instalação de pacotes (pub/npm) | low | accept | Nenhum pacote é adicionado, atualizado ou removido nesta fase — RESEARCH §Package Legitimacy Audit registra "Not applicable". Sem instalação, não há superfície de supply-chain nova e nenhum checkpoint de legitimidade se aplica. |
</threat_model>

<verification>
- `flutter analyze` sai com código 0 no repositório inteiro.
- `flutter test` (suíte completa, 312+ testes) sai com código 0 — em particular `test/widget/animal_detail_screen_test.dart`, que consome as fixtures alteradas.
- Nenhum arquivo sob `supabase/` foi tocado: `git diff --name-only` do plano não contém `supabase/`.
- Nenhum arquivo de apresentação foi tocado: o diff deste plano fica restrito a `data/` e `test/`.
</verification>

<success_criteria>
- Existe um caminho de leitura única lote + nome do piquete, exposto por provider family auto-dispose, sem remover o caminho antigo.
- `ReproductiveHistoryEntry` carrega todos os DGs por ATF em ordem decrescente pela regra `isLaterDg`, mais touro e data de implantação.
- Zero requests adicionais introduzidos: as duas mudanças são colunas/embeds a mais nas queries que já existiam.
- Suíte completa verde, incluindo a guarda de regressão de ordenação do SC-3.
</success_criteria>

<output>
Create `.planning/phases/08-animal-dossier-consolidation/08-01-SUMMARY.md` when done
</output>
