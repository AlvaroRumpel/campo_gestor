---
phase: 08-animal-dossier-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - lib/features/sanitario/presentation/sanitary_history_section.dart
  - lib/features/animais/presentation/animais_screen.dart
  - test/widget/sanitary_history_section_test.dart
  - test/widget/animais_screen_test.dart
autonomous: true
requirements: [ANIM-03]

must_haves:
  truths:
    - "Quando o histórico sanitário falha, o veterinário vê a mensagem de erro E uma ação 'Tentar novamente' ao lado dela — nunca uma tela morta cuja única saída é sair da ficha e voltar (D-04, UI-SPEC error/sanitary-block)."
    - "Tocar em 'Tentar novamente' no bloco sanitário invalida apenas a instância daquele provider para aquele animal; o bloco reprodutivo irmão não é refeito nem perde o que já mostrava (D-04, UI-SPEC partial/reproductive-block-sanitary-block)."
    - "O bloco sanitário vazio continua mostrando 'Nenhuma aplicação sanitária registrada para este animal.' — cópia inalterada (D-37, UI-SPEC empty/sanitary-history-block)."
    - "Zero aplicações mostra a mensagem de vazio; até 10 mostram todas; acima de 10 mostra as 10 primeiras mais 'Ver todas' — o truncamento assimétrico do D-06 é preservado byte a byte (UI-SPEC zero-one-many: aplicações sanitárias por animal)."
    - "Buscar o número exato de um animal com baixa em /animais encontra o animal mesmo com o toggle 'Mostrar arquivados' desligado, e abrir a ficha dele funciona — é exatamente o caso em que o veterinário quer o histórico (D-17)."
    - "Um animal com baixa que aparece na lista sempre carrega o selo de arquivado com o motivo, inclusive quando apareceu por match exato com o toggle desligado — nenhum animal arquivado é jamais renderizado como se estivesse ativo."
    - "Um match parcial de número continua respeitando o toggle 'Mostrar arquivados' — só o match exato passa por cima dele (D-17)."
  artifacts:
    - "lib/features/sanitario/presentation/sanitary_history_section.dart — ação 'Tentar novamente' dentro dos dois ramos de erro existentes, e nada mais"
    - "lib/features/animais/presentation/animais_screen.dart — regra de bypass do toggle de arquivados para match numérico exato"
    - "test/widget/sanitary_history_section_test.dart — NOVO arquivo de teste do retry por bloco"
    - "test/widget/animais_screen_test.dart — grupo de teste do D-17"
  key_links:
    - "TextButton 'Tentar novamente' → ref.invalidate(sanitaryHistoryByAnimalProvider(widget.animalId)) — instância da família, nunca a família nua"
    - "TextButton 'Tentar novamente' (variante de lote) → ref.invalidate(sanitaryApplicationsByLotProvider(widget.lotId))"
    - "AnimaisScreen filtro em memória → match exato de número → ignora _showArchived para aquele animal → rota /animais/:id"
  prohibitions:
    - statement: "MUST NOT deixar um bloco que falhou ao carregar indistinguível de um bloco genuinamente vazio — um erro de rede que o veterinário lê como 'este animal não tem histórico sanitário' o leva a decidir manejo em cima de uma ausência que não existe"
      category: transparency
      requirement_id: ANIM-03
      status: unresolved
      verification: null
    - statement: "MUST NOT tornar o histórico clínico de um animal com baixa inalcançável por causa de um filtro de listagem — o histórico do animal vendido ou morto é justamente o que precisa continuar consultável, e a busca por número é o caminho de entrada do veterinário em campo"
      category: safety
      requirement_id: ANIM-03
      status: unresolved
      verification: null
---

<objective>
Duas correções independentes de acesso à informação, nenhuma das duas tocando a ficha do animal:

1. **D-04 no bloco sanitário:** hoje o texto de erro é morto — a única saída do veterinário é descobrir sozinho que precisa sair da ficha e voltar. Ganha uma ação de recarregar ao lado da mensagem, escopada ao provider daquele bloco.
2. **D-17 na busca de /animais:** um animal com baixa é filtrado em memória pelo toggle "Mostrar arquivados" antes de a busca por número ser considerada, então buscar o número exato de um animal vendido não abre nada. O match exato passa a passar por cima do toggle.

Purpose: o D-17 é o caminho de entrada do SC-1 (a ficha abre por busca de número) e o D-04 é o que impede a ficha de virar uma tela morta em campo.
Output: a ação de retry nos dois ramos de erro do arquivo sanitário; a regra de bypass no filtro da listagem; testes de widget para os dois.

**Wave 3 — depende do 08-01 e do 08-02 (revisão pós-checker).** O código deste plano é de fato independente dos dois, mas o `<verify>` dele não é, e é o `<verify>` que decide a wave:
- O teste de escopo de invalidação da Task 1 monta `AnimalSanitaryHistorySection` e `AnimalReproductiveHistorySection` lado a lado — a segunda classe só passa a existir no plano 08-02. Dependência de símbolo, dura.
- A Task 1 verifica com `flutter test test/widget/animal_detail_screen_test.dart` (fixtures editadas pelo 08-01, tela editada pelo 08-02) e `test/widget/lote_detail_screen_test.dart`, que compila `lote_repository.dart` — arquivo em edição no 08-01.

A fase inteira roda em 5 waves sequenciais; a justificativa completa está no `<objective>` do 08-01. Ao chegar a hora deste plano, tudo que o `<verify>` dele compila já está estável, e os dois arquivos que ele edita não são tocados por nenhum outro plano da fase.

**FRONTEIRA DURA — contrato D-37 (Phase 6), reafirmado para a Phase 8.** O ÚNICO edit permitido em `lib/features/sanitario/presentation/sanitary_history_section.dart` nesta fase é acrescentar a ação de recarregar dentro dos dois ramos de erro que já existem. Query, repositório, construção de linha, shell de card, corte em 10, botão "Ver todas", toggle "Mostrar estornadas", mensagens de vazio e a cópia dos textos de erro são todos INTOCÁVEIS. Nenhuma passada de "deixar consistente com o bloco reprodutivo novo". Um diff que alcance `sanitary_application_repository.dart` está fora de escopo por definição — esta fase não tem SQL.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animal-dossier-consolidation/08-CONTEXT.md
@.planning/phases/08-animal-dossier-consolidation/08-PATTERNS.md
@.planning/phases/08-animal-dossier-consolidation/08-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ação de recarregar no bloco sanitário (D-04, dentro da fronteira D-37)</name>
  <files>lib/features/sanitario/presentation/sanitary_history_section.dart, test/widget/sanitary_history_section_test.dart</files>

  <read_first>
    - `lib/features/sanitario/presentation/sanitary_history_section.dart` — arquivo alvo, por inteiro. Localizar precisamente os dois ramos de erro: o de `_AnimalSanitaryHistorySectionState.build()` (que hoje devolve `_SectionMessage` com a cópia sanitária) e o de `_LoteSanitaryHistorySectionState.build()` (cópia de conexão). Ler também `_SectionMessage` e `_SectionSpinner` no fim do arquivo.
    - `lib/features/sanitario/data/sanitary_application_repository.dart` — apenas para confirmar os nomes exatos das duas famílias de provider a invalidar. **Não editar.**
    - `.planning/phases/08-animal-dossier-consolidation/08-PATTERNS.md` §Retry-per-block e §Hard Boundaries — a forma alvo e a lista explícita do que é intocável neste arquivo.
    - `.planning/phases/08-animal-dossier-consolidation/08-UI-SPEC.md` §Layout Contract "Retry affordance" e §Copywriting Contract — colocação, alinhamento e o rótulo exato.
    - `lib/features/animais/presentation/animal_detail_screen.dart` — o idioma de invalidação por instância de família já usado no projeto (após sucesso de diálogo). **Não editar.**
    - `test/widget/animal_detail_screen_test.dart` — o harness de `ProviderScope` com overrides e `GoRouter` a copiar para o arquivo de teste novo. **Não editar.**
  </read_first>

  <behavior>
    - Bloco sanitário em erro: renderiza a mensagem de erro E um controle rotulado "Tentar novamente".
    - Tocar no controle refaz a busca daquele animal; se a segunda tentativa tem sucesso, as linhas aparecem no lugar do erro.
    - Tocar no controle não refaz o bloco reprodutivo irmão: o contador de chamadas do provider reprodutivo permanece em 1.
    - Bloco sanitário sem erro (vazio, carregando ou com dados): nenhum controle "Tentar novamente" é renderizado.
  </behavior>

  <action>
Em `lib/features/sanitario/presentation/sanitary_history_section.dart`, alterar EXCLUSIVAMENTE os dois ramos de erro:

**Variante do animal.** Substituir o `_SectionMessage` solto do ramo de erro por um `Column` com `crossAxisAlignment: CrossAxisAlignment.start` contendo (a) o mesmo `_SectionMessage` com a cópia inalterada e (b) um `TextButton` rotulado "Tentar novamente" cujo `onPressed` chama `ref.invalidate` sobre a instância `sanitaryHistoryByAnimalProvider(widget.animalId)`. Passar a instância da família COM o id, nunca a família nua — é isso que mantém o raio de explosão em um bloco só, que é o requisito literal do D-04.

**Variante do lote.** Mesma forma no ramo de erro de `_LoteSanitaryHistorySectionState`, com a cópia de erro dela inalterada e `ref.invalidate` sobre `sanitaryApplicationsByLotProvider(widget.lotId)`.

Ambas as classes já são `ConsumerState`, então `ref` está disponível no `build` sem nenhuma mudança de tipo de widget. Nenhum campo de estado novo, nenhum widget helper novo, nenhum import novo.

Colocação e estilo vêm do UI-SPEC: o `TextButton` fica imediatamente abaixo do texto de erro, dentro do mesmo `Column`, alinhado à esquerda. Rótulo exatamente "Tentar novamente". Nenhum ícone, nenhuma cor customizada — `TextButton` padrão do tema.

**Nada mais neste arquivo muda.** Se o diff tocar `_buildAnimalRow`, `_buildLoteRow`, `visibleApplications`, `reversedApplicationIds`, `_SanitaryHistoryCardShell`, `_HistoryRowShell`, `_VerTodasButton`, `_badgeLabel` ou os ramos `loading`/`data`, o contrato D-37 foi violado e o edit deve ser revertido.

**Teste novo:** criar `test/widget/sanitary_history_section_test.dart`, copiando a estrutura de `test/widget/animal_detail_screen_test.dart` (constantes de dado de amostra no topo, uma função construtora que monta `ProviderScope` com overrides e devolve uma árvore `MaterialApp`). O widget sob teste é `AnimalSanitaryHistorySection` montado direto, não a ficha inteira — mais rápido e isola o comportamento. Cobrir:
- Estado de erro renderiza a mensagem de erro sanitária E um `TextButton` com o rótulo "Tentar novamente".
- Um override de provider que lança na primeira invocação e devolve uma lista na segunda: tocar em "Tentar novamente" faz as linhas aparecerem e a mensagem de erro sumir.
- Escopo da invalidação: montar `AnimalSanitaryHistorySection` e `AnimalReproductiveHistorySection` lado a lado no mesmo `ProviderScope`, com um contador de invocações em cada override; tocar no retry sanitário e afirmar que o contador sanitário foi para 2 enquanto o reprodutivo permaneceu em 1.
- Estado com dados não renderiza nenhum `TextButton` com aquele rótulo.

Respeitar os lints do projeto: `require_trailing_commas`, `prefer_const_constructors`.
  </action>

  <verify>
    <automated>flutter analyze lib/features/sanitario test/widget/sanitary_history_section_test.dart &amp;&amp; flutter test test/widget/sanitary_history_section_test.dart test/widget/animal_detail_screen_test.dart test/widget/lote_detail_screen_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/features/sanitario/presentation/sanitary_history_section.dart` contém exatamente 2 ocorrências de `Tentar novamente`: `grep -c "Tentar novamente" lib/features/sanitario/presentation/sanitary_history_section.dart` retorna 2.
    - O mesmo arquivo contém `ref.invalidate(sanitaryHistoryByAnimalProvider(widget.animalId))` e `ref.invalidate(sanitaryApplicationsByLotProvider(widget.lotId))` — instâncias de família, com id.
    - Fronteira D-37, verificada mecanicamente: `git diff --unified=0 -- lib/features/sanitario/presentation/sanitary_history_section.dart` mostra alterações apenas dentro dos dois ramos `error:`; nenhuma linha adicionada ou removida em `_buildAnimalRow`, `_buildLoteRow`, `visibleApplications`, `reversedApplicationIds`, `_SanitaryHistoryCardShell`, `_HistoryRowShell`, `_VerTodasButton` ou `_badgeLabel`.
    - Fronteira D-37, cópia intacta: `grep -c "Nenhuma aplicação sanitária registrada para este animal." lib/features/sanitario/presentation/sanitary_history_section.dart` retorna 1 e `grep -c "Erro ao carregar histórico sanitário." lib/features/sanitario/presentation/sanitary_history_section.dart` retorna 1.
    - `git diff --name-only` deste plano NÃO contém `lib/features/sanitario/data/sanitary_application_repository.dart` nem nenhum caminho sob `supabase/`.
    - `test/widget/sanitary_history_section_test.dart` existe e `flutter test test/widget/sanitary_history_section_test.dart` sai com código 0.
    - Comportamento provado pelo teste: com um provider que falha na primeira chamada e responde na segunda, tocar no controle de retry substitui a mensagem de erro pelas linhas de aplicação.
    - Comportamento provado pelo teste: após tocar no retry sanitário, o contador de invocações do provider reprodutivo continua em 1 — o bloco irmão não foi refeito.
    - Regressão: `flutter test test/widget/animal_detail_screen_test.dart test/widget/lote_detail_screen_test.dart` sai com código 0.
  </acceptance_criteria>

  <done>O bloco sanitário em erro oferece uma saída ao veterinário, e essa saída refaz só a leitura daquele bloco — com o resto do arquivo provadamente intocado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Busca por número exato encontra animal com baixa (D-17)</name>
  <files>lib/features/animais/presentation/animais_screen.dart, test/widget/animais_screen_test.dart</files>

  <read_first>
    - `lib/features/animais/presentation/animais_screen.dart` — arquivo alvo. Ler o `build` de `_AnimaisScreenState` por inteiro: o campo de estado `_query`, o campo `_showArchived`, o `.where(...)` do filtro em memória (a primeira condição exclui arquivados, a segunda faz o match de número por `contains`), o `Switch` "Mostrar arquivados", e as classes `_AnimalListTile` e `_ArchiveBadge` no fim do arquivo.
    - `lib/features/animais/data/animal_repository.dart` §`animalListByPropertyProvider` — confirma que o provider já busca com arquivados incluídos, ou seja, o dado já está em memória no cliente e nenhuma mudança de repositório ou de assinatura de provider é necessária (RESEARCH Pitfall 5).
    - `test/widget/animais_screen_test.dart` — harness existente e convenções de dado de amostra deste arquivo.
    - `.planning/phases/08-animal-dossier-consolidation/08-RESEARCH.md` §Pitfall 5 — descreve exatamente onde está a linha culpada e por que a correção é de filtro, não de repositório.
  </read_first>

  <behavior>
    - Toggle desligado + busca pelo número exato de um animal arquivado: o animal aparece na lista.
    - Toggle desligado + busca por um prefixo/substring que casa com um animal arquivado sem ser o número inteiro: o animal continua oculto.
    - Toggle desligado + nenhuma busca: nenhum animal arquivado aparece (comportamento de hoje, preservado).
    - Qualquer animal arquivado renderizado na lista carrega o selo com o motivo da baixa, independentemente do estado do toggle.
  </behavior>

  <action>
Em `lib/features/animais/presentation/animais_screen.dart`, dentro do `.where(...)` do filtro em memória:

1. Antes das condições existentes, calcular um booleano local de match exato: a busca não está vazia E a representação em texto do número do animal é IGUAL ao termo buscado (comparação de igualdade, não `contains`). O termo já vem com `trim()` aplicado no debounce.

2. Acrescentar esse booleano como negação na condição que exclui arquivados: um animal arquivado só é excluído quando o toggle está desligado **e** ele não é o match exato. Match parcial continua sujeito ao toggle — o D-17 fala em "busca por número **exato**", e afrouxar isso para `contains` faria a listagem inteira vazar arquivados a cada dígito digitado.

3. Manter a condição de match de número por `contains` como está: ela continua sendo o filtro de busca; o novo booleano só governa a exceção ao toggle.

**Correção acoplada, obrigatória.** `_AnimalListTile` hoje só renderiza o selo de arquivado quando `isArchived && showArchived`. Com o bypass do D-17, um animal com baixa pode aparecer com o toggle desligado — e nessa combinação o selo não renderizaria, deixando o animal vendido visualmente idêntico a um ativo. Corrigir tornando o selo condicionado apenas a o animal estar arquivado. O parâmetro `showArchived` de `_AnimalListTile` fica sem uso: remover o parâmetro, o campo e o argumento no local de construção. Isso é o menor diff que fecha o buraco.

Nada mais nesta tela muda: filtros de categoria, lote e piquete, ordenação por número, estados vazios e o `Switch` ficam como estão.

**Testes novos** em `test/widget/animais_screen_test.dart`, num grupo próprio citando D-17, seguindo o harness já usado no arquivo:
- Toggle desligado, busca pelo número exato de um animal arquivado: o item do animal é encontrado na lista.
- Toggle desligado, busca por uma substring que casa com o número do arquivado sem ser igual a ele: o item NÃO é encontrado.
- Toggle desligado, busca vazia: o item arquivado NÃO é encontrado.
- Um animal arquivado renderizado com o toggle desligado exibe o selo com o rótulo do motivo da baixa.

Atenção ao debounce de 300 ms do campo de busca: o teste precisa avançar o relógio (por exemplo com `tester.pump` de duração maior que o debounce) depois de digitar, antes de afirmar sobre a lista. Alternativamente, dirigir o estado pelo `TextEditingController` e aguardar o mesmo intervalo — o harness existente do arquivo indica qual das duas o projeto já usa.

Respeitar os lints do projeto: `require_trailing_commas`, `prefer_const_constructors`.
  </action>

  <verify>
    <automated>flutter analyze lib/features/animais/presentation/animais_screen.dart &amp;&amp; flutter test test/widget/animais_screen_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/features/animais/presentation/animais_screen.dart` contém uma comparação de igualdade entre a representação em texto do número do animal e o termo de busca, usada na condição de exclusão de arquivados.
    - O parâmetro `showArchived` não é mais passado para o tile de animal: `grep -v '^\s*//' lib/features/animais/presentation/animais_screen.dart | grep -c "showArchived: _showArchived"` retorna 0.
    - O campo de estado `_showArchived` e o `Switch` continuam existindo: `grep -c "_showArchived" lib/features/animais/presentation/animais_screen.dart` é maior que 0.
    - `flutter analyze lib/features/animais/presentation/animais_screen.dart` sai com código 0, sem aviso de parâmetro ou campo não utilizado.
    - `flutter test test/widget/animais_screen_test.dart` sai com código 0, com os quatro testes novos do grupo D-17 verdes.
    - Comportamento provado pelo teste: com o toggle desligado, o número exato de um animal arquivado o traz para a lista, e uma substring do mesmo número não traz.
    - Comportamento provado pelo teste: um animal arquivado visível com o toggle desligado renderiza o selo com o rótulo do motivo — nunca aparece indistinguível de um ativo.
    - `git diff --name-only` deste plano não contém `lib/features/animais/data/animal_repository.dart` — a correção é de filtro em memória, não de repositório (RESEARCH Pitfall 5).
  </acceptance_criteria>

  <done>Buscar o número de um animal vendido em campo abre a ficha dele, e a lista nunca mostra um animal com baixa como se estivesse ativo.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plano 08-03)

| Símbolo | Tipo | Arquivo |
|---|---|---|
| ação "Tentar novamente" (variante animal) | `TextButton` inline no ramo `error:` | `lib/features/sanitario/presentation/sanitary_history_section.dart` |
| ação "Tentar novamente" (variante lote) | `TextButton` inline no ramo `error:` | `lib/features/sanitario/presentation/sanitary_history_section.dart` |

Arquivo novo: `test/widget/sanitary_history_section_test.dart`.

Símbolo REMOVIDO: o parâmetro e o campo `showArchived` de `_AnimalListTile` em `lib/features/animais/presentation/animais_screen.dart` (o selo de arquivado passa a depender só do estado do animal).

Nenhuma classe pública nova é criada por este plano.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → PostgREST (Supabase) | O retry reemite exatamente a mesma leitura já autorizada; o filtro de arquivados é 100% client-side sobre dado já entregue |
| Filtro de listagem em memória | Não é fronteira de autorização — a RLS já decidiu o que o cliente pode ver; o filtro é conveniência de UI |

## STRIDE Threat Register

ASVS L1; bloqueio configurado em `high`. Nenhuma ameaça `high`/`critical` — nenhum checkpoint bloqueante de segurança é necessário.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Information Disclosure | Bypass do toggle de arquivados por match exato (D-17) | low | mitigate | O bypass não amplia acesso: `animalListByPropertyProvider` já busca com arquivados incluídos, então a linha já está no cliente, já filtrada pela RLS property-scoped de `animals`. A mudança apenas para de escondê-la na UI. Mitigação concreta contra confusão: o critério de aceite exige que todo animal arquivado renderizado exiba o selo com o motivo, provado por teste de widget — a informação fica visível, não ambígua. |
| T-08-07 | Denial of Service | Retry manual do bloco sanitário | low | accept | Um `TextButton` de invalidação só pode ser acionado por toque humano e refaz uma única leitura escopada a um animal. A política de auto-retry app-wide (`providerRetryPolicy`, G-06-9) já governa a repetição automática com backoff; esta ação é a camada manual depois que aquela se esgotou, e não introduz laço automático. |
| T-08-08 | Information Disclosure | Retry reemitindo a leitura sanitária | low | accept | O `ref.invalidate` refaz a mesma query já autorizada, com o mesmo id de animal e o mesmo JWT; a policy SELECT de `sanitary_applications` (Fase 6) é reavaliada do zero no servidor a cada tentativa. Não há cache client-side a envenenar (D-03: auto-dispose, sem `keepAlive`). |
| T-08-SC | Tampering | instalação de pacotes (pub/npm) | low | accept | Nenhum pacote é adicionado, atualizado ou removido nesta fase — RESEARCH §Package Legitimacy Audit registra "Not applicable". |
</threat_model>

<verification>
- `flutter analyze` sai com código 0 no repositório inteiro.
- `flutter test` (suíte completa) sai com código 0.
- Fronteira D-37 respeitada: o diff em `sanitary_history_section.dart` fica contido nos dois ramos de erro; `sanitary_application_repository.dart` não aparece no diff.
- Nenhum arquivo sob `supabase/` foi tocado.
- `lib/features/animais/presentation/animal_detail_screen.dart` NÃO aparece no diff deste plano (é território dos planos 08-02 e 08-04).
</verification>

<success_criteria>
- Um bloco sanitário que falhou oferece uma ação de recarregar que refaz só aquele bloco, provado por contagem de invocações no teste.
- O restante do arquivo sanitário está provadamente intocado (contrato D-37).
- A busca por número exato encontra animal com baixa com o toggle desligado, e todo arquivado exibido carrega o selo do motivo.
</success_criteria>

<output>
Create `.planning/phases/08-animal-dossier-consolidation/08-03-SUMMARY.md` when done
</output>
