---
phase: 06-sanitary-module-snapshot
plan: 12
type: execute
wave: 6
depends_on: ["06-10", "06-11"]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: [SANI-01, SANI-02, SANI-03, SANI-04, SANI-05]

flagged_assumptions:
  - id: FA-04
    source: "06-UI-SPEC.md § UI Considerations header arithmetic vs its own tables"
    assumption: >-
      The section header reports 59 applicable considerations (57 covered, 2 backstop), but the ten
      element tables E1 through E10 contain 69 rows (67 covered, 2 backstop). The planner authored
      all 69 visible rows into plan must_haves rather than 59, so no row is silently dropped; the
      header count is treated as stale arithmetic, not as evidence that 10 rows are inapplicable. If
      the header is right and 10 rows should have been excluded, the extra truths are harmless but
      the discrepancy should be corrected in the UI-SPEC before it is reused as a template.

must_haves:
  truths:
    - "Both Phase 6 migrations are applied to the live project and every declared object is confirmed present by a catalog query, not by the absence of an error."
    - "`sanitary_applications` carries exactly one policy after the push and its command is SELECT — the zero-write-policy invariant holds in the live database, not only in the migration file."
    - "The Phase 2 immutability trigger is still present and enabled on `sanitary_applications` after the column extension."
    - "`supabase/tests/06_sanitary_test.sql` runs to completion against the live database inside a rolled-back transaction, and every assertion passes."
    - "`supabase/tests/04_movements_test.sql`, unrun since Phase 4, runs in the same session; any failure is triaged as a real migration defect rather than weakened away (D-42)."
    - "The transaction wrapping each replay is rolled back, leaving zero rows behind in the live project."
    - "A human confirms the five success criteria of the phase against the running application before the phase is considered done."
    - "STATE.md records the migration ledger count, the two suite results, and the resolution of the outstanding `04_movements_test.sql` blocker."
  artifacts:
    - .planning/STATE.md
  key_links:
    - "This plan is the only place Phase 6 touches the live database; every prior plan authored files that a green `flutter analyze` and `flutter test` would pass without the schema existing — which is exactly the false-positive verification state this split exists to prevent."
    - "The pgTAP suite written in 06-01 is executable only after Task 1; its red state until now is intentional."
  prohibitions:
    - "Never weaken or delete a failing pgTAP assertion to make a suite green — a failure here is a defect in the migration or a defect in the test, and both are findings, not obstacles."
    - "Never leave rows behind in the live project; every replay runs inside a transaction that is rolled back."
---

<objective>
Apply both Phase 6 migrations to the live Supabase project, replay both pgTAP suites against it, and
hold a human verification checkpoint against the phase's five success criteria.

Purpose: D-41 puts the push, the SQL proof and the UAT in a dedicated blocking plan of their own,
because in Phases 4 and 5 a push buried as the third task of a passing plan left the phase silently
BLOCKED. The Dart layer does not read the live schema, so `flutter analyze` and `flutter test` go
green whether or not this plan ran — that is the false positive this plan exists to close.
Output: a live schema, two green suites, a human sign-off, and an updated STATE.md.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-sanitary-module-snapshot/06-CONTEXT.md
@.planning/phases/06-sanitary-module-snapshot/06-VALIDATION.md
@.planning/phases/06-sanitary-module-snapshot/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply both migrations to the live project and verify the catalog</name>
  <files>supabase/migrations/20260810_06_sanitary_module.sql, supabase/migrations/20260811_06_sanitary_rpcs.sql</files>
  <read_first>
    - .planning/STATE.md § Blockers (the established MCP path, the project ref, and the ledger count from Phase 5's fourteen migrations)
    - .planning/phases/06-sanitary-module-snapshot/06-RESEARCH.md § Environment Availability (why the CLI path is unavailable and what the sanctioned fallback is)
    - supabase/migrations/20260810_06_sanitary_module.sql and supabase/migrations/20260811_06_sanitary_rpcs.sql (the exact contents to apply)
    - .planning/phases/05-reproductive-module-loteatf/05-10-SUMMARY.md if present (how the equivalent Phase 5 push and verification were carried out and recorded)
  </read_first>
  <action>
    The nominal command for this project is `supabase db push`. It is unavailable here: the CLI is
    authenticated but unlinked, with no TTY for a database password. Every migration since Phase 3
    has been applied through the Supabase MCP `apply_migration` tool against project
    `wrdwzychjhlpwpivfhhq`, and that is the path to use. Do not attempt an interactive CLI link.

    Apply the schema migration first, then the RPC migration — the RPCs reference the `doses` table
    and the UA helper. Use the migration file's own name as the migration name so the ledger stays
    readable.

    Then verify by catalog query, not by the absence of an error. Confirm, in one or more
    `execute_sql` reads: that `doses` exists with its full column list; that `properties.kg_per_ua`
    exists with its default; that every column added to `sanitary_applications` is present with the
    declared nullability; that `animal_ua_weight`, `register_sanitary_application`,
    `reverse_sanitary_application` and `enforce_sanitary_application_same_property` all exist; that
    both new triggers on `sanitary_applications` are present and enabled, including the Phase 2
    immutability trigger which must have survived the column extension; that all five new indexes
    exist, with the reversal index unique and partial and the composition index using the
    `jsonb_path_ops` operator class; that `sanitary_applications` has exactly one policy and its
    command is SELECT; that `doses` has exactly three policies and none of them is a DELETE policy;
    and that both RPCs are executable by the authenticated role.

    Record the resulting migration ledger count.
  </action>
  <verify>
    <automated>Run an MCP `execute_sql` catalog query returning one row per checked object with a boolean present flag, and assert every flag is true; then run a second query asserting the policy count on the applications table is 1 with command SELECT and the policy count on doses is 3 with no DELETE entry.</automated>
  </verify>
  <acceptance_criteria>
    - Both migrations appear in the project's migration ledger, taking the count from 14 to 16.
    - The catalog query returns a present flag of true for every one of the objects listed above, with no null or false row.
    - `pg_trigger` shows both `trg_snapshot_immutable` and `trg_sanitary_applications_same_property` on `sanitary_applications`, both enabled.
    - `pg_indexes.indexdef` for the reversal index contains both UNIQUE and a WHERE clause on the reversal column; the composition index definition names the `jsonb_path_ops` operator class.
    - `pg_policies` returns exactly one row for `sanitary_applications`, with command SELECT.
    - `pg_policies` returns exactly three rows for `doses`, with commands SELECT, INSERT and UPDATE and no DELETE.
    - No migration file is edited to make the push succeed; a failure is fixed forward with a new corrective migration, matching the Phase 5 precedent.
  </acceptance_criteria>
  <done>The live schema matches the two authored migrations, proven by catalog reads rather than by a silent success.</done>
</task>

<task type="auto">
  <name>Task 2: Replay both pgTAP suites against the live project inside rolled-back transactions</name>
  <files>supabase/tests/06_sanitary_test.sql, supabase/tests/04_movements_test.sql</files>
  <read_first>
    - supabase/tests/06_sanitary_test.sql (this phase's suite, written in 06-01)
    - supabase/tests/04_movements_test.sql (the Phase 4 suite that has never been run — D-42)
    - .planning/STATE.md § Blockers (the Docker unavailability, the MCP replay pattern, and the known three-argument `has_index` overload bug found in the Phase 5 suite)
    - .planning/phases/06-sanitary-module-snapshot/06-VALIDATION.md § Sampling Rate and § Locked Convention
  </read_first>
  <action>
    Docker is unavailable, so `supabase test db` cannot start a local stack. Use the proven fallback:
    replay each suite verbatim through MCP `execute_sql`, wrapped so the whole run is rolled back —
    the same path that ran the 371-line Phase 5 suite successfully.

    Run `06_sanitary_test.sql` first. Report the pass and fail counts. For any failure, determine
    whether the defect is in the migration or in the assertion before touching anything: the Phase 5
    precedent is that one failure was a genuine bug in the test file and the rest of the schema was
    correct, and the response was to prove the real state with a direct catalog read, not to relax
    the assertion. A migration defect is fixed with a new forward-only corrective migration, never by
    editing an applied file.

    Then run `04_movements_test.sql`. It has never been executed since Phase 4, so a failure here is
    new information about a migration that has been live for months. Report its counts separately and
    triage the same way. If it fails, record the finding in STATE.md and surface it in the checkpoint
    below rather than silently absorbing it into this phase's scope.

    Confirm after both runs that the applications table row count is unchanged from before, proving
    the transactions rolled back.
  </action>
  <verify>
    <automated>Both replays report zero failing assertions, and a post-run count query on `sanitary_applications`, `doses` and `animals` returns the same values as a pre-run count query.</automated>
  </verify>
  <acceptance_criteria>
    - `06_sanitary_test.sql` reports every assertion passing, including the immutability, role rejection, concurrency abort, double reversal, reversal-of-reversal, zero-sum and GIN containment groups.
    - `04_movements_test.sql` has been executed and its result recorded — a pass closes the long-standing blocker, a failure is written up as a defect with its own follow-up, and either way the "never run" state ends here.
    - Row counts on `sanitary_applications`, `doses` and `animals` are identical before and after both replays.
    - No assertion in either suite was deleted, skipped or weakened; if a suite was edited, the edit is a fix to a genuinely wrong assertion with the real state independently proven by a catalog read, and the reasoning is recorded in the summary.
  </acceptance_criteria>
  <done>Both suites have been run against real Postgres, their results are recorded, and the Phase 4 test blocker no longer exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT against the five phase success criteria</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 6 (the five success criteria being verified)
    - .planning/phases/06-sanitary-module-snapshot/06-VALIDATION.md § Manual-Only Verifications (the three behaviours that cannot be automated and why)
    - .planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md § Copywriting Contract (the exact strings the reviewer is checking against)
  </read_first>
  <action>
    Pause and hand control to the human. Present the what-built summary and the numbered verification
    steps below verbatim, then wait for the resume signal. Do not proceed, do not auto-approve, and
    do not mark the phase complete without an explicit response. On issues, record each one against
    its numbered step in the plan summary and stop rather than attempting a fix inside this plan.
    After approval, update `.planning/STATE.md` with the migration ledger count from Task 1, both
    pgTAP suite results from Task 2, the resolution of the outstanding `04_movements_test.sql`
    blocker, and the reviewer's disposition of flagged assumptions FA-01 through FA-04.
  </action>
  <verify>
    <automated>Assert `.planning/STATE.md` records both suite results and the ledger count: the file must reference `06_sanitary_test.sql`, `04_movements_test.sql`, and a migration count of 16.</automated>
  </verify>
  <done>A human has responded to the resume signal, every issue found is recorded against its numbered step, and STATE.md reflects the live database state and the suite results.</done>
  <what-built>
    The complete sanitary module: dose cadastro with calculated per-UA values, application
    registration with a frozen composition snapshot, individual deselection before confirming,
    per-lote and per-animal sanitary history, and estorno by reversal row. Both migrations are live
    and both pgTAP suites are green.
  </what-built>
  <how-to-verify>
    Run the app (`flutter run -d edge`) signed in as a veterinarian of a property that has at least
    one lote with three or more active animals.

    1. SANI-01 — Open Sanitário, Doses tab, tap Nova dose. Enter a commercial name and a dosage of
       1 mL/kg. Confirm the "Dosagem por UA (calculado)" field shows 400 and is not editable. Leave
       the cost blank and confirm no "Custo por UA" field appears and no "R$ 0,00" is rendered
       anywhere. Save; confirm the dose appears in the list with its calculated value in green.
    2. SANI-02 and SANI-03 — Open the lote, tap "Registrar aplicação". Confirm the lote field is
       plain text and cannot be changed. Pick the dose, keep today's date, continue. Confirm every
       active animal is already checked and the counter reads "N de N selecionados · X,X UA".
       Uncheck one animal; confirm both the count and the UA figure drop. Continue, read the summary,
       confirm the permanence warning is shown, then register. Confirm the snackbar reads
       "Aplicação registrada — N animais".
    3. SANI-02 immutability — Open the new application from the lote's Histórico Sanitário section.
       Confirm the composition lists exactly the animals you kept, with the numbers, categories and
       UA figures frozen, and that the skipped-animal note reports the one you unchecked.
    4. SANI-04 — Confirm the application appears in the lote's history section and in the global
       Aplicações list. Filter the global list by that lote and by that dose; confirm it still shows.
       Set a period that excludes today; confirm the filtered-empty state appears with "Nenhuma
       aplicação encontrada".
    5. SANI-05 — Open one of the applied animals' fichas. Confirm the Histórico Sanitário block now
       shows the application with the dose name, the date and the lote name. Then move that animal to
       a different lote and reopen its ficha: the application must still be listed, and the lote name
       must still be the ORIGINAL lote's name, not the new one.
    6. Estorno — Reopen the application, tap "Estornar aplicação", try to confirm with an empty
       motivo (must be blocked), then enter a motivo and estornar. Confirm the snackbar, then confirm
       the application disappears from all three lists until "Mostrar estornadas" is switched on,
       where it appears struck through with the "Estornada" badge.
    7. Role gate — Sign in as an owner or reader of the same property. Confirm the FAB on Sanitário,
       the dose edit and archive icons, the "Registrar aplicação" button on the lote, and the
       "Estornar aplicação" button are all ABSENT, not greyed out. Confirm both history sections are
       still readable.

    Also review the two items surfaced by this plan: the `04_movements_test.sql` result from Task 2,
    and flagged assumptions FA-01, FA-02, FA-03 and FA-04 recorded across this phase's plans.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found, one per numbered step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local migration files → live production project | This is the only plan that writes to the live database |
| pgTAP replay → live production data | Test fixtures execute against real rows and must leave nothing behind |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Tampering | test fixtures persisting into the live project | high | mitigate | Every replay is wrapped in a transaction that is rolled back, and the acceptance criteria require identical row counts before and after |
| T-06-01 | Tampering | the immutability trigger being lost by the column extension | high | mitigate | The catalog verification explicitly asserts that the Phase 2 trigger is still present and enabled after the ALTER, rather than assuming an ALTER preserves it |
| T-06-18 | Tampering | a write policy reaching the live applications table | high | mitigate | The catalog verification asserts a policy count of exactly one with command SELECT on the live table, closing the gap between what the file declares and what is deployed |
| T-06-07 | Information Disclosure | `anon` execute on the two new RPCs | low | accept | Inherited from the project's default privilege grants and pre-existing since Phase 1; fails closed. Recorded, not fixed here — routed to `/gsd-secure-phase` |

ASVS level 1; blocking severity is high. No package-manager installs occur in this phase, so no
supply-chain row or legitimacy checkpoint applies.
</threat_model>

## Artifacts this phase produces

### Produced by this plan

- Live database objects: the two migrations from 06-02 applied to project `wrdwzychjhlpwpivfhhq`,
  taking the ledger from 14 to 16
- Recorded results for `06_sanitary_test.sql` and `04_movements_test.sql`
- Updated `.planning/STATE.md`: ledger count, both suite results, resolution of the outstanding
  `04_movements_test.sql` blocker, and any new finding it produced

### Full phase inventory

See `06-01-PLAN.md` § Artifacts this phase produces.

<verification>
- Catalog reads confirm every declared object, both triggers, all five indexes, and the exact policy
  counts on both tables.
- Both pgTAP suites report zero failures.
- Row counts are unchanged after both replays.
- A human has approved all seven UAT steps.
</verification>

<success_criteria>
The phase's five ROADMAP success criteria are demonstrably true against the running application and
the live database, and no criterion is satisfied only by code that has never met a schema.
</success_criteria>

<output>
Create `.planning/phases/06-sanitary-module-snapshot/06-12-SUMMARY.md` when done
</output>
