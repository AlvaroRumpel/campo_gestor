---
phase: 07-expenses-by-paddock
plan: 06
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - lib/features/gastos/presentation/_expense_list_item_card.dart
  - lib/features/gastos/presentation/gastos_screen.dart
  - lib/core/router/router.dart
  - test/features/gastos/role_gates_test.dart
autonomous: true
requirements: [GAST-01, GAST-02]

must_haves:
  truths:
    - "`/gastos/:paddockId` is a root-level `GoRoute` outside the `AppShell`, built with `state.pathParameters['paddockId']` (D-08)."
    - "The AppBar title reads `Gastos — {nome do piquete}` and the route is reachable by deep link with a working back affordance (D-14)."
    - "The screen opens with the current-month range selected and no category filter, every time — no filter state survives leaving the screen (D-15, D-21)."
    - "A sanitary row is read-only: it renders no edit and no delete affordance, and tapping it navigates to `/aplicacoes/{id}` (D-32)."
    - "A manual row tapped opens `ExpenseFormDialog` in edit mode; its delete affordance runs `confirmDeleteExpense` then `archiveExpense` and invalidates both list providers (D-12, D-22, D-28)."
    - "The \"Novo gasto\" FAB and every edit/delete affordance are absent — not disabled — when `canManageExpenses` is false (D-23, established project rule)."
    - "The header total sums only non-deleted rows. With \"Mostrar excluídos\" on, a deleted row renders with a visible \"Excluído\" marker and contributes 0, so the on-screen total and the visible rows still reconcile (planner resolution; D-18's property preserved by the marker, the same shape as the sanitary \"—\" rule)."
    - "Reversed sanitary applications and the rows that reverse them never appear in this list and never contribute to the total (D-33)."
    - "E1 empty: two distinct empty states per the UI-SPEC Copywriting Contract — \"Nenhum gasto lançado neste piquete\" when the paddock never had one, and \"Nenhum gasto no período selecionado\" with a \"Limpar filtro\" action when a filter hid them (D-13)."
    - "E1 loading: a centered `CircularProgressIndicator`, identical to `SanitarioScreen`/`PiquetesScreen`."
    - "E1 error: the exact reused copy \"Erro ao carregar. Verifique sua conexão e tente novamente.\", not reworded."
    - "E1 populated: manual expenses and read-only sanitary rows in one date-descending list; sanitary rows carry the sanitary icon, a currency figure or \"—\", and the \"Sanitário\" badge, and tap through to `/aplicacoes/:id` (D-32)."
    - statement: "E1 partial — a sanitary row whose cost is NULL renders \"—\" instead of a value, contributes 0 to the total, and still counts toward the item count. No visual truth proves the total math; an explicit Dart test must assert both the 0-contribution and the count-inclusion."
      verification: backstop
    - "E1 overflow: a `ListView.builder` that scrolls, with the D-18 client-side-sum ceiling recorded as a code comment; pagination is explicitly out of scope this phase."
    - "E1 zero-one-many: the total header pluralises via `Intl.plural` pt-BR — \"1 lançamento\" / \"N lançamentos\" — the same idiom as `_AplicacaoCard`'s animal-count string."
    - "E1 long-text: `maxLines: 1` plus `TextOverflow.ellipsis` on every card title (category label, paddock name, dose name), the established pattern on every card title in this codebase."
    - "E3 populated: five preset chips — \"Mês atual\" / \"Mês passado\" / \"Últimos 3 meses\" / \"Ano\" / \"Personalizado\" — plus the \"Mostrar excluídos\" toggle; exactly one preset selected at a time, filled in `colorScheme.primary` (D-16)."
    - "E3 partial: tapping \"Personalizado\" and cancelling `showDateRangePicker` without choosing a range is a no-op — the previously selected preset stays selected and the list does not re-filter; the screen never sits on \"Personalizado\" with no range."
    - "E3 overflow: a horizontal `SingleChildScrollView` filter row, identical to `SanitarioScreen._buildFilterRow`."
    - "E3 long-text: the \"Personalizado\" chip renders its selected date label at `maxLines: 1` with `TextOverflow.ellipsis`; the row scrolls horizontally rather than wrapping."
    - "E4 empty: the header stays visible at zero items showing \"R$ 0,00\" and \"0 lançamentos\", so a filter dropping N items to 0 causes no layout jump (D-13)."
    - "E4 loading: the header lives inside E1's single `AsyncValue` and never renders independently — one centered `CircularProgressIndicator` replaces header and list together."
    - "E4 error: the same shared `AsyncValue` — the full-screen error copy replaces header and list together, and the header never shows a stale or partial total."
    - "E4 populated: the R$ total in `colorScheme.primary` plus the computed \"{N} lançamento(s)\" count, per the UI-SPEC Copywriting Contract \"Total header (list)\" row (D-17)."
    - statement: "E4 partial — the displayed total is the same client-side sum as E1's partial case: sanitary rows with NULL cost contribute 0 while still counting. Covered by the same explicit Dart test; a rendered figure alone does not prove the arithmetic."
      verification: backstop
    - "E4 overflow: `maxLines: 1` plus `TextOverflow.ellipsis` on the total and the count text — no `FittedBox` (it would break the declared two-size type scale) and no forced line wrap."
    - "E4 zero-one-many: `Intl.plural` pt-BR gives \"0 lançamentos\" / \"1 lançamento\" / \"N lançamentos\"."
    - "E4 long-text: the same `maxLines: 1` plus ellipsis treatment as the overflow case; the exact figure stays readable in the list below."
  artifacts:
    - lib/features/gastos/presentation/gastos_screen.dart
    - lib/features/gastos/presentation/_expense_list_item_card.dart
    - lib/core/router/router.dart
    - test/features/gastos/role_gates_test.dart
  key_links:
    - "`GoRoute(path: AppRoutes.gastosById)` → `GastosScreen(paddockId:)` — the only registration of the route constant added in 07-03."
    - "`unifiedExpenseListByPaddockProvider` / `unifiedExpenseListWithDeletedByPaddockProvider` (07-04) are the screen's only data sources; the toggle swaps between them."
    - "`canManageExpenses` (07-03) gates the FAB and every row affordance; `ExpenseFormDialog` / `confirmDeleteExpense` (07-05) are the write surfaces it opens."
    - "A sanitary row's tap target is `AppRoutes.aplicacaoDetail(application.id)` — the Phase 6 detail screen, unchanged."
  prohibitions:
    - requirement_id: GAST-02
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT let a sanitary row with an unknown cost silently shrink the displayed total — the row must always be visible in the list, carry a \"—\" in place of a currency figure, and still be counted, so the user can see why the sum is what it is."
---

<objective>
Build the `/gastos/:paddockId` screen (D-08): AppBar with the paddock name, the preset+custom
period filter row and category filter (D-07, D-16), the total header (D-17), the unified
date-descending list of manual and read-only sanitary rows (D-19, D-32, D-33), the two contextual
empty states (D-13), the single "Novo gasto" FAB (D-10), and the row card that renders both
variants. Register the route (D-08) and extend the role-gate test with the FAB-visibility case.

Purpose: this is the screen GAST-02 is graded on, and the place where the phase's answer to
"quanto este piquete me custou" actually appears.

Output: two presentation files, one `GoRoute` registration, and the widget half of the role-gate
test.

Carries the flagged, unresolved GAST-02 edge-probe row recorded in 07-02's
`<planner_assumptions>`: no probe-derived edge criterion exists for this requirement, so its
boundary behaviour is exactly what this plan's acceptance criteria state and nothing more.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-expenses-by-paddock/07-CONTEXT.md
@.planning/phases/07-expenses-by-paddock/07-UI-SPEC.md
@.planning/phases/07-expenses-by-paddock/07-PATTERNS.md
@.planning/phases/07-expenses-by-paddock/07-04-SUMMARY.md
@.planning/phases/07-expenses-by-paddock/07-05-SUMMARY.md
</context>

<!-- planner-discipline-allow: FittedBox -->
<!-- planner-discipline-allow: NumberFormat.currency -->

<tasks>

<task type="auto">
  <name>Task 1: ExpenseListItemCard rendering both union variants</name>
  <files>lib/features/gastos/presentation/_expense_list_item_card.dart</files>
  <read_first>
    - lib/features/sanitario/presentation/sanitario_screen.dart — `_AplicacaoCard` and `_DoseCard`: the `Card` shell, the `bodyLarge.copyWith(fontWeight: FontWeight.w600)` title idiom, the dimmed `onSurface.withValues(alpha: 0.6)` subtitle, the badge shape, and the `Intl.plural` animal-count string
    - lib/features/piquetes/presentation/piquetes_screen.dart — `_PaddockCard`, for the tappable-card shape and the `maxLines: 1` + `TextOverflow.ellipsis` title treatment
    - lib/features/gastos/data/expense_model.dart — `ExpenseListItem`, `ManualExpenseItem`, `SanitaryExpenseItem`, `amountOf`, `dateOf` (07-02)
    - lib/features/gastos/data/expense_constants.dart — `kExpenseCategoryLabels`, `kExpenseCategoryIcons`, `kSanitaryCategoryIcon`, `kSanitaryPseudoCategoryLabel` (07-02)
    - lib/features/sanitario/data/sanitary_calculations.dart — `formatCurrencyBrl`
    - .planning/phases/07-expenses-by-paddock/07-UI-SPEC.md § Typography, § Color, § Copywriting Contract
  </read_first>
  <action>
Create `lib/features/gastos/presentation/_expense_list_item_card.dart` with a public
`ExpenseListItemCard extends StatelessWidget` taking `required ExpenseListItem item`,
`required bool canManage`, `VoidCallback? onTap`, `VoidCallback? onDelete`.

Render with a `switch` on the sealed union (two branches, no `if (item is …)` chains):

**`ManualExpenseItem`** — leading `Icon(kExpenseCategoryIcons[expense.category] ??
Icons.more_horiz)` in the default icon colour (no per-category colour, D-05). Title:
`kExpenseCategoryLabels[expense.category] ?? expense.category` in
`theme.textTheme.bodyLarge?.copyWith(fontWeight: FontWeight.w600)` with `maxLines: 1` and
`TextOverflow.ellipsis`. Subtitle: the `dd/MM/yyyy` date and, when non-empty, the description, in
`bodyMedium` tinted `onSurface.withValues(alpha: 0.6)`, also `maxLines: 1` + ellipsis. Trailing:
`formatCurrencyBrl(expense.amount)`. When `canManage` is true, add a delete `IconButton` wired to
`onDelete`; when false render **no** delete control at all — absent, never disabled. The whole
card is tappable via `onTap` only when `canManage` is true.

When `expense.deletedAt != null` (only reachable with the "Mostrar excluídos" toggle on), render
an `Excluído` badge next to the title and strike through the value
(`TextDecoration.lineThrough`). Document in a comment that a deleted row contributes 0 to the
header total, and that the visible marker is what keeps D-18's "the total is the sum of what is
on screen" property true rather than the row's omission.

**`SanitaryExpenseItem`** — leading `Icon(kSanitaryCategoryIcon)`. Title: the application's
`doseName`, same typography, `maxLines: 1` + ellipsis. Subtitle: the `dd/MM/yyyy` `appliedAt`
date and the `lotName`, dimmed. A badge reading `Sanitário` in `surfaceContainerHigh` (D-32).
Trailing: `formatCurrencyBrl(application.totalCost!.abs())` when `totalCost` is non-null, and the
literal `—` when it is null — never `R$ 0,00`, which would read as a real zero (Claude's
Discretion resolution to D-11 Phase 6). Render **no** edit and **no** delete control on this
branch under any value of `canManage` — a sanitary row is read-only here (D-32). Its `onTap`
always fires, regardless of `canManage`, and the caller routes it to
`AppRoutes.aplicacaoDetail(application.id)`.

Use `formatCurrencyBrl` from `sanitary_calculations.dart` for every currency figure; do not
introduce a second formatter. Use `intl`'s `DateFormat('dd/MM/yyyy')` for dates, locale `pt_BR`.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter analyze lib/features/gastos/presentation/_expense_list_item_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - `_expense_list_item_card.dart` declares `class ExpenseListItemCard` with `item`, `canManage`, `onTap`, `onDelete` parameters
    - The build method branches with a Dart `switch` over `ManualExpenseItem` / `SanitaryExpenseItem`, not with `is` checks
    - Every title `Text` in the file carries `maxLines: 1` and `TextOverflow.ellipsis`
    - The sanitary branch contains no `IconButton` and no edit/delete callback wiring
    - The sanitary branch renders the literal `—` when `totalCost` is null and never falls back to a formatted zero
    - The string `Sanitário` appears in the sanitary branch as a badge label
    - The manual branch renders a delete `IconButton` only under `if (canManage)` — there is no `onPressed: null` disabled variant
    - `grep -c "Colors\." lib/features/gastos/presentation/_expense_list_item_card.dart` returns 0 — colours come from `theme.colorScheme` roles only
    - `flutter analyze lib/features/gastos/presentation/_expense_list_item_card.dart` exits 0
  </acceptance_criteria>
  <done>One card widget renders both list variants with the correct affordances, the read-only sanitary treatment, and the "—" for an unknown cost.</done>
</task>

<task type="auto">
  <name>Task 2: GastosScreen — filters, total header, unified list, FAB</name>
  <files>lib/features/gastos/presentation/gastos_screen.dart</files>
  <read_first>
    - lib/features/sanitario/presentation/sanitario_screen.dart lines 230-400 — `_pickDateRange` (`showDateRangePicker` with `locale: const Locale('pt','BR')`), `_buildFilterRow`'s horizontal `SingleChildScrollView`, the "Mostrar estornadas" toggle label placement, and the `visibleApplications` + sort composition at lines 250-265
    - lib/features/piquetes/presentation/piquetes_screen.dart lines 110-160 — `_EmptyState`: `Center` → `Padding(32)` → `Column` → `Icon(size: 64, colour onSurface at alpha 0.3)` → `SizedBox(16)` → `Text(titleMedium)`
    - lib/features/piquetes/data/piquete_repository.dart — `paddockByIdProvider`, the source of the paddock name for the AppBar title
    - lib/features/gastos/data/expense_repository.dart — the four providers from 07-04
    - lib/features/gastos/data/expense_calculations.dart — `totalAmount`, `itemCount`, `filterByDateRange`, `filterByCategory`, `rangeForPreset`, `currentMonthRange`, `ExpensePeriodPreset` (07-02)
    - lib/features/gastos/presentation/expense_form_dialog.dart — `ExpenseFormDialog`, `confirmDeleteExpense` (07-05)
    - lib/core/auth/role_gates.dart — `canManageExpenses` (07-03)
    - .planning/phases/07-expenses-by-paddock/07-UI-SPEC.md § Copywriting Contract and § UI Considerations rows for E1, E3 and E4
  </read_first>
  <action>
Create `lib/features/gastos/presentation/gastos_screen.dart` with
`class GastosScreen extends ConsumerStatefulWidget` taking `required String paddockId`.

**Local state (never persisted, D-21):** `ExpensePeriodPreset _preset = ExpensePeriodPreset.mesAtual`,
`DateTimeRange? _customRange`, `String? _categoryFilter` (null = todas), `bool _showDeleted = false`.
Every entry to the screen therefore starts on the current month with no category filter (D-15).

**AppBar:** `Text('Gastos — ${paddock.name}')` resolved from `paddockByIdProvider(paddockId)`,
with the default GoRouter back affordance (root-level route, D-14). While the paddock is loading
show `Text('Gastos')` rather than an empty title.

**Body:** watch `_showDeleted ? unifiedExpenseListWithDeletedByPaddockProvider(paddockId) :
unifiedExpenseListByPaddockProvider(paddockId)` and render its single `AsyncValue` with `.when`:
- `loading` → centered `CircularProgressIndicator` replacing header and list together.
- `error` → the exact existing copy `Erro ao carregar. Verifique sua conexão e tente novamente.`
  reused verbatim from `sanitario_screen.dart`/`piquetes_screen.dart`; do not reword it. The
  header must not render a stale or partial total in this state.
- `data` → a `Column` of the filter row, the total header, then `Expanded(ListView.builder(...))`.

**Filter row (E3):** a horizontal `SingleChildScrollView(scrollDirection: Axis.horizontal)`
wrapping a `Row`, mirroring `SanitarioScreen._buildFilterRow`, containing:
- five `FilterChip`s labelled `Mês atual`, `Mês passado`, `Últimos 3 meses`, `Ano`,
  `Personalizado`, exactly one selected at a time, selected fill `colorScheme.primary` (D-16).
  Tapping the first four sets `_preset` and clears `_customRange`. Tapping `Personalizado` opens
  `showDateRangePicker(context: context, firstDate: DateTime(2000), lastDate: DateTime(2100),
  initialDateRange: <the effective range>, locale: const Locale('pt','BR'))`. If the picker
  returns null (cancelled), change nothing — the previously selected preset stays selected and no
  re-filter occurs. Only on a non-null result set `_preset = personalizado` and `_customRange`.
  When `Personalizado` is selected with a range, its chip label shows the range dates at
  `maxLines: 1` with `TextOverflow.ellipsis`; the row scrolls horizontally rather than wrapping.
- a category `DropdownButton<String?>` offering `Todas as categorias` (null) plus
  `kExpenseFilterCategories` — the 8 real categories plus the `Sanitário` pseudo-category (D-07,
  D-32).
- a `Mostrar excluídos` toggle bound to `_showDeleted` (D-22), labelled exactly that.

Compute the effective range as `_customRange ?? rangeForPreset(_preset, DateTime.now())`, then
`final filtered = filterByCategory(filterByDateRange(items, range), _categoryFilter);`.

**Total header (E4):** always rendered above the list, including at zero items. Compute the total
over the non-deleted subset only — `filtered.where((i) => switch (i) { ManualExpenseItem(:final
expense) => expense.deletedAt == null, SanitaryExpenseItem() => true })` — and pass that to
`totalAmount` and `itemCount`. Leave a comment recording this planner resolution: a soft-deleted
row is not a cost, so it contributes 0, and the row's visible `Excluído` marker (task 1) is what
keeps the on-screen total reconcilable with the visible rows instead of the row being hidden.
Render `formatCurrencyBrl(total)` in `colorScheme.primary` and, beside it, the count via
`Intl.plural(n, zero: '$n lançamentos', one: '1 lançamento', other: '$n lançamentos', locale:
'pt_BR')`. Both `Text`s carry `maxLines: 1` and `TextOverflow.ellipsis`; do not use a `FittedBox`
and do not force a wrap.

**List (E1):** `ListView.builder` over `filtered`, each item an `ExpenseListItemCard` with
`canManage: canManageExpenses(currentPropAsync.asData?.value, membersAsync.asData?.value)`.
- Manual row `onTap` → `showDialog` of `ExpenseFormDialog(propertyId:, paddockId:, expense:)`
  in edit mode; on a `true` result invalidate both list providers **and**
  `paddockMonthExpenseTotalProvider(paddockId)` (D-12).
- Manual row `onDelete` → `confirmDeleteExpense(context, expense)`; on `true` call
  `ref.read(expenseRepositoryProvider).archiveExpense(expense.id)` then the same three
  invalidations (D-22, D-28).
- Sanitary row `onTap` → `context.push(AppRoutes.aplicacaoDetail(application.id))`, always
  available regardless of `canManage` (D-32).
Add the D-18 ceiling comment above the builder: the total is summed client-side over the loaded
list, so introducing pagination here silently turns it into "total of this page" — upgrade to a
server-side `SUM()` RPC at that point, not before.

**Empty states (E1, D-13):** when `filtered` is empty, render an `_EmptyState` copied in shape
from `PiquetesScreen`. Distinguish the two cases by whether the **unfiltered** `items` list is
also empty:
- `items.isEmpty` → heading `Nenhum gasto lançado neste piquete`, body `Lance o primeiro gasto
  deste piquete.`
- otherwise → heading `Nenhum gasto no período selecionado`, body `Tente ajustar os filtros.`
  plus a `Limpar filtro` `TextButton` resetting `_preset` to `mesAtual`, `_customRange` to null
  and `_categoryFilter` to null.
The total header stays visible in both cases, reading `R$ 0,00` and `0 lançamentos`.

**FAB (D-10):** a single `FloatingActionButton` with tooltip `Novo gasto` and an add icon,
rendered only when `canManageExpenses` is true — otherwise `null`, never a disabled button. It
opens `ExpenseFormDialog(propertyId:, paddockId:, expense: null)` and invalidates the same three
providers on a `true` result. `propertyId` comes from `currentPropertyProvider`.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter analyze lib/features/gastos/ &amp;&amp; flutter test test/features/gastos/</automated>
  </verify>
  <acceptance_criteria>
    - `gastos_screen.dart` declares `class GastosScreen extends ConsumerStatefulWidget` with a `paddockId` field
    - The AppBar title interpolates the paddock name after the literal `Gastos — `
    - Exactly five `FilterChip` labels appear: `Mês atual`, `Mês passado`, `Últimos 3 meses`, `Ano`, `Personalizado`
    - The `showDateRangePicker` call passes `locale: const Locale('pt', 'BR')` and its null result path performs no `setState`
    - The screen watches `unifiedExpenseListWithDeletedByPaddockProvider` under the toggle and `unifiedExpenseListByPaddockProvider` otherwise, and the header and list read one shared `AsyncValue`
    - The error branch contains the string `Erro ao carregar. Verifique sua conexão e tente novamente.` verbatim
    - Both empty-state headings appear verbatim: `Nenhum gasto lançado neste piquete` and `Nenhum gasto no período selecionado`
    - The filtered-empty branch renders a `Limpar filtro` action; the never-had branch does not
    - The total header renders outside the empty/non-empty branch so it is present at zero items
    - The total is computed over a subset that filters out `expense.deletedAt != null`
    - The count string is produced by `Intl.plural` with `locale: 'pt_BR'`
    - `grep -c "FittedBox" lib/features/gastos/presentation/gastos_screen.dart` returns 0
    - The FAB expression is a conditional yielding `null` when the gate is false — there is no `onPressed: null` FAB
    - The tooltip string `Novo gasto` appears exactly once
    - Both `ref.invalidate` call sites (form result and delete result) invalidate all three providers
    - `flutter analyze lib/features/gastos/` exits 0 and `flutter test test/features/gastos/` exits 0
  </acceptance_criteria>
  <done>The expense screen renders the filter row, the always-present total header, the unified list with both row variants, both contextual empty states, and a single role-gated FAB.</done>
</task>

<task type="auto">
  <name>Task 3: Register the route and prove the FAB role gate</name>
  <files>lib/core/router/router.dart, test/features/gastos/role_gates_test.dart</files>
  <read_first>
    - lib/core/router/router.dart lines 130-155 — the `loteById`, `atfById` and `aplicacaoById` `GoRoute` blocks, all root-level and outside the `StatefulShellRoute`; the exact placement and comment style to mirror
    - lib/core/router/routes.dart — `AppRoutes.gastosById` (07-03)
    - test/features/gastos/role_gates_test.dart — the pure-function cases written in 07-03, which this task extends rather than replaces
    - test/widget/piquetes_screen_test.dart — the `ProviderScope` override of `memberPropertiesProvider` used by existing gate tests to simulate a role
  </read_first>
  <behavior>
    - With `memberPropertiesProvider` overridden to an `owner` membership, `GastosScreen` renders a `FloatingActionButton`
    - With a `veterinarian` membership, `GastosScreen` renders a `FloatingActionButton`
    - With a `reader` membership, `GastosScreen` renders no `FloatingActionButton` at all (absent, not disabled)
  </behavior>
  <action>
Add a `GoRoute` to `lib/core/router/router.dart`, placed immediately after the `aplicacaoById`
block and before the `StatefulShellRoute.indexedStack`, so it stays root-level and outside every
shell branch (D-08):

path `AppRoutes.gastosById`, builder returning
`GastosScreen(paddockId: state.pathParameters['paddockId']!)`. The segment name must match the
`:paddockId` in the constant exactly. Add a comment matching the Phase 3/5/6 blocks' style:
Phase 7 detail route, root-level, the expense list for one paddock, reached from
`PaddockDetailScreen`'s summary card and by deep link. Import `GastosScreen`.

Then extend `test/features/gastos/role_gates_test.dart` — do not create a second file — with a
new `group` covering the three `<behavior>` cases. Pump `GastosScreen` inside a `ProviderScope`
overriding `memberPropertiesProvider` and `currentPropertyProvider` (mirroring the override style
in `test/widget/piquetes_screen_test.dart`) and the gastos list provider with a fixture list, then
assert `find.byType(FloatingActionButton)` finds one widget for owner and for veterinarian, and
`findsNothing` for reader. This is the widget half of D-36's "gate de dois papéis" coverage and it
closes RESEARCH Pitfall 2's stated warning sign: an owner who cannot see the "Novo gasto" FAB.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter test test/features/gastos/role_gates_test.dart &amp;&amp; flutter test test/widget/app_shell_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `router.dart` contains a `GoRoute` whose `path` is `AppRoutes.gastosById` and whose builder reads `state.pathParameters['paddockId']`
    - That `GoRoute` sits outside the `StatefulShellRoute.indexedStack` branch list
    - `flutter test test/widget/app_shell_test.dart` exits 0 — the shell navigation-count assertions still pass
    - `test/features/gastos/role_gates_test.dart` contains all three `<behavior>` cases plus the seven pure-function cases from 07-03
    - The reader case asserts `findsNothing` for `FloatingActionButton`, not a disabled-button assertion
    - `flutter test test/features/gastos/` exits 0
  </acceptance_criteria>
  <done>`/gastos/:paddockId` resolves to `GastosScreen` as a root-level route, and a widget test proves the FAB is present for owner and veterinarian and absent for reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deep link → screen | `/gastos/:paddockId` can be typed directly with any uuid; the screen must not assume the caller had access |
| rendered financial figure → user decision | The total on this screen is what a farm owner acts on; a silently wrong figure has no error path |
| role predicate → visible write affordance | The FAB and per-row controls are the only client-side expression of who may write |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Information Disclosure | deep-linked foreign `paddockId` | high | mitigate | The screen issues no privileged query of its own; `members_can_read_expenses` (07-01) scopes every row, so a foreign paddock resolves to an empty list. The AppBar falls back to `Gastos` when `paddockByIdProvider` yields nothing rather than rendering an unresolved name |
| T-07-24 | Elevation of Privilege | write affordances visible to a reader | high | mitigate | FAB and per-row delete render only under `canManageExpenses`; task 3 asserts `findsNothing` for a reader. RLS remains the enforcement — hiding the control is convenience (D-23) |
| T-07-25 | Tampering (data integrity) | total diverging from the visible rows | medium | mitigate | The total is `totalAmount` over the same filtered list the `ListView` renders, minus soft-deleted rows which carry a visible `Excluído` marker; the arithmetic itself is covered by `expense_calculations_test.dart` (D-18) |
| T-07-26 | Repudiation | a voided sanitary application inflating the piquete cost | medium | mitigate | Reversal rows and their originals are removed by `visibleApplications(showReversed: false)` inside `buildUnifiedExpenseItems` (07-04), before this screen ever sees them (D-33) |
| T-07-27 | Tampering | edit/delete affordance on an immutable sanitary row | medium | mitigate | The sanitary branch of `ExpenseListItemCard` wires no edit and no delete callback under any `canManage` value; `sanitary_applications` has zero write RLS policies and `trg_snapshot_immutable` blocks mutation server-side regardless (D-32) |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this plan — `showDateRangePicker` and `Intl.plural` ship with the SDK and the already-installed `intl` (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `flutter test test/features/gastos/` exits 0
- `flutter test test/widget/app_shell_test.dart` exits 0
- `flutter analyze lib/features/gastos/ lib/core/router/` exits 0
- `git diff --quiet -- lib/features/sanitario/` exits 0 — the Phase 6 detail screen is linked to, never modified
</verification>

<success_criteria>
Opening `/gastos/{paddock}` shows the paddock's expenses and its sanitary costs in one
date-descending list under a live total that respects both filters, with contextual empty states
and a role-gated create affordance.
</success_criteria>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 7 and does not exist in the codebase before this phase.

**SQL — `supabase/migrations/20260813_07_expenses_module.sql`:** table `expenses` (`id`, `property_id`, `paddock_id`, `category`, `amount`, `expense_date`, `description`, `created_by`, `updated_by`, `created_at`, `updated_at`, `deleted_at`); indexes `expenses_property_idx`, `expenses_paddock_date_idx`; policies `members_can_read_expenses`, `owner_vet_can_insert_expense`, `owner_vet_can_update_expense`; functions `enforce_expenses_paddock_same_property()`, `set_expenses_updated_by()`; triggers `trg_expenses_paddock_same_property`, `trg_expenses_set_updated_by`; new columns `sanitary_applications.paddock_id`, `sanitary_applications.paddock_name`; replaced bodies (signatures unchanged) `register_sanitary_application(uuid,uuid,date,jsonb,text)`, `reverse_sanitary_application(uuid,text)`

**pgTAP:** `supabase/tests/07_expenses_test.sql`

**Dart — `lib/features/gastos/data/`:** `expense_model.dart` (`Expense`, `ExpenseListItem`, `ManualExpenseItem`, `SanitaryExpenseItem`, `dateOf`, `amountOf`, `categoryKeyOf`, `sortExpenseItemsDesc`); `expense_constants.dart` (`kExpenseCategories`, `kExpenseCategoryLabels`, `kExpenseCategoryIcons`, `kExpenseFilterCategories`, `kSanitaryPseudoCategory`, `kSanitaryPseudoCategoryLabel`, `kSanitaryCategoryIcon`); `expense_calculations.dart` (`totalAmount`, `itemCount`, `filterByDateRange`, `filterByCategory`, `currentMonthRange`, `rangeForPreset`, `ExpensePeriodPreset`); `expense_repository.dart` (`ExpenseRepository`, `buildUnifiedExpenseItems`, `expenseRepositoryProvider`, `unifiedExpenseListByPaddockProvider`, `unifiedExpenseListWithDeletedByPaddockProvider`, `paddockMonthExpenseTotalProvider`)

**Dart — `lib/core/auth/role_gates.dart`:** `canManageExpenses`

**Dart — `lib/features/gastos/presentation/`:** `gastos_screen.dart` (`GastosScreen`); `expense_form_dialog.dart` (`ExpenseFormDialog`, `confirmDeleteExpense`); `_expense_list_item_card.dart` (`ExpenseListItemCard`); `paddock_expense_summary_card.dart` (`PaddockExpenseSummaryCard`)

**Dart — modified files, new symbols only:** `AppRoutes.gastosById`, `AppRoutes.gastosPorPiquete` (`lib/core/router/routes.dart`); the `GoRoute(path: AppRoutes.gastosById, …)` registration (`lib/core/router/router.dart`); the `PaddockExpenseSummaryCard` insertion in `lib/features/piquetes/presentation/paddock_detail_screen.dart` (no new symbol declared there)

**Tests:** `test/features/gastos/expense_calculations_test.dart`, `test/features/gastos/role_gates_test.dart`, `test/features/gastos/expense_unified_list_test.dart`, `test/features/gastos/expense_form_dialog_test.dart`

<output>
Create `.planning/phases/07-expenses-by-paddock/07-06-SUMMARY.md` when done
</output>
