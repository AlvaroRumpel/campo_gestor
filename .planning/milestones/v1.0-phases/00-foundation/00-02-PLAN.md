---
phase: 00-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .vscode/launch.json.example
  - README.md
autonomous: false
requirements: []
nyquist_compliant: true
tags: [foundation, environment, prerequisites]

user_setup:
  - service: supabase-cli
    why: "SC-2 requires `supabase db reset` to run locally; CLI not installed on host (verified: `where supabase` fails)"
    install_command: "scoop install supabase (Windows) or follow https://supabase.com/docs/guides/cli/getting-started"
    env_vars: []
    dashboard_config: []
  - service: docker-desktop
    why: "Supabase CLI requires Docker daemon running for `supabase start`"
    install_command: "Already installed (Docker 29.4.0); user must START Docker Desktop before Plan 06 executes"
    env_vars: []
    dashboard_config:
      - task: "Open Docker Desktop application"
        location: "Windows Start Menu"

must_haves:
  truths:
    - "Supabase CLI installed and reachable via `supabase --version`"
    - "Developer has a template (`launch.json.example`) showing how to wire SUPABASE_URL/ANON_KEY via dart-define"
    - ".gitignore prevents secrets and build artifacts from leaking"
    - "README documents the bootstrap commands for a new clone"
  artifacts:
    - path: ".gitignore"
      provides: "Coverage for .vscode/launch.json, *.g.dart, *.freezed.dart, supabase/.env, /coverage/"
      contains: ".vscode/launch.json"
    - path: ".vscode/launch.json.example"
      provides: "Commitable template for VSCode debug config with placeholder secrets"
      contains: "SUPABASE_URL"
    - path: "README.md"
      provides: "Bootstrap instructions: install Supabase CLI, copy launch.json, run flutter pub get"
      contains: "scoop install supabase"
  key_links:
    - from: ".gitignore"
      to: ".vscode/launch.json"
      via: "exclusion rule"
      pattern: "\\.vscode/launch\\.json"
    - from: "README.md"
      to: ".vscode/launch.json.example"
      via: "Setup instructions reference template"
      pattern: "launch\\.json\\.example"
---

<objective>
Prepare the host environment and the repository scaffolding that supports D-07 (CLI local + Docker) and D-08 (dart-define + launch.json secrets). This plan does NOT install dependencies via flutter pub get (Plan 03) and does NOT run supabase init (Plan 06). It only delivers prerequisites: an installed CLI, gitignore coverage for secrets/build artifacts, a launch.json template, and a README onboarding section.

Purpose: Without this plan, Plan 06 cannot run `supabase init`, and Plan 04's secret loader has no path to receive values. The `.vscode/launch.json` file MUST be gitignored before any developer adds real keys to it.

Output: Updated `.gitignore`, new `.vscode/launch.json.example`, README bootstrap section. Supabase CLI installed on host (autonomous=false because install requires user shell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@CLAUDE.md
@.gitignore
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install Supabase CLI on host</name>
  <files>(host environment, no repo files)</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Environment Availability section, lines 645-661)
  </read_first>
  <what-built>
    Supabase CLI must be installed on this Windows host. RESEARCH.md confirmed `where supabase` fails — CLI is not present. Recommended install path is Scoop. Claude attempted install via Bash but Scoop and PowerShell elevation may not be available from within the agent shell, hence this is a checkpoint.
  </what-built>
  <how-to-verify>
    Run in PowerShell (NOT bash):
    1. Check if Scoop is installed: `scoop --version`
       - If not installed: `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser; irm get.scoop.sh | iex`
    2. Add Supabase bucket: `scoop bucket add supabase https://github.com/supabase/scoop-bucket.git`
    3. Install CLI: `scoop install supabase`
    4. Verify: `supabase --version` → should print a version string (e.g., `1.x.x`).
    5. Verify Docker is running: `docker info` → exits 0 (no errors).

    Expected outcomes:
    - `supabase --version` exits 0 and prints a version.
    - `where supabase` (cmd) or `Get-Command supabase` (PowerShell) returns a path.
    - Docker Desktop is running (Docker icon green/active in tray).
  </how-to-verify>
  <action>
    See `<how-to-verify>` block above — Claude does not execute this task. The user runs the listed PowerShell commands (Scoop bootstrap if needed, `scoop bucket add supabase ...`, `scoop install supabase`) on the host, then confirms via `supabase --version` and `docker info`. This is a checkpoint:human-action because Scoop install requires user shell elevation and Docker daemon control is not available from the agent shell.
  </action>
  <verify>
    <automated>command -v supabase >/dev/null 2>&1 && supabase --version && docker info >/dev/null 2>&1 && echo "OK"</automated>
  </verify>
  <resume-signal>Type "installed" when `supabase --version` and `docker info` both succeed, OR describe blocker (e.g., Scoop unavailable, install failure).</resume-signal>
  <acceptance_criteria>
    - User has confirmed `supabase --version` exits 0 on host.
    - User has confirmed `docker info` exits 0 on host (or commits to starting Docker before Plan 06).
    - Reply text contains "installed" or equivalent confirmation.
  </acceptance_criteria>
  <done>Supabase CLI version printed; Docker reachable; user confirmed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update .gitignore + create launch.json.example + README bootstrap section</name>
  <files>.gitignore, .vscode/launch.json.example, README.md</files>
  <read_first>
    - .gitignore (current state — Flutter template defaults)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-08: dart-define + launch.json)
    - .planning/phases/00-foundation/00-RESEARCH.md (Security Domain T-V14, lines 727-736)
  </read_first>
  <action>
    1. **Update `.gitignore`** — APPEND (do not replace existing) the following block at the end:

```gitignore

# Phase 0: Foundation additions
# VSCode debug config contains SUPABASE_URL/ANON_KEY via dart-define — never commit
.vscode/launch.json

# Codegen outputs (regenerated locally; do not commit)
*.g.dart
*.freezed.dart

# Supabase local CLI artifacts
supabase/.env
supabase/.branches/
supabase/.temp/

# Coverage reports
/coverage/
lcov.info
```

    Note: keep existing lines (Miscellaneous, IntelliJ, Flutter/Dart/Pub) — only APPEND the block above.

    2. **Create `.vscode/launch.json.example`** — commitable template developers copy to `.vscode/launch.json` (which is gitignored) and fill with real values. EXACT content:

```jsonc
{
  // Copy this file to .vscode/launch.json and replace placeholder values.
  // .vscode/launch.json is gitignored — never commit real keys.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Flutter (Edge, dev)",
      "request": "launch",
      "type": "dart",
      "deviceId": "edge",
      "args": [
        "--dart-define=SUPABASE_URL=http://127.0.0.1:54321",
        "--dart-define=SUPABASE_ANON_KEY=PASTE_LOCAL_ANON_KEY_HERE"
      ]
    },
    {
      "name": "Flutter (Edge, profile)",
      "request": "launch",
      "type": "dart",
      "deviceId": "edge",
      "flutterMode": "profile",
      "args": [
        "--dart-define=SUPABASE_URL=http://127.0.0.1:54321",
        "--dart-define=SUPABASE_ANON_KEY=PASTE_LOCAL_ANON_KEY_HERE"
      ]
    }
  ]
}
```

    Note: SUPABASE_ANON_KEY for local dev is printed by `supabase start` after Plan 06 runs. The placeholder value `PASTE_LOCAL_ANON_KEY_HERE` is intentional — never put a real key in this file.

    3. **Create or update `README.md`** — if README.md does not exist (or is the Flutter default), REPLACE with this content:

```markdown
# Campo Gestor

App de gestão de propriedades rurais voltado para pecuária. Stack: Flutter web-first + Supabase.

See `.planning/PROJECT.md` for vision and `.planning/ROADMAP.md` for phase plan.

## Bootstrap (new clone)

Prerequisites:
- Flutter SDK >= 3.24 (`flutter --version`)
- Docker Desktop (running)
- Supabase CLI: `scoop install supabase` (Windows) or see https://supabase.com/docs/guides/cli/getting-started
- Microsoft Edge (or Chrome) for web target

Setup:
1. `flutter pub get` — install Dart dependencies (after Plan 03 lands)
2. `supabase start` — boot local Postgres + Studio + Auth (after Plan 06 lands)
3. Copy local anon key from `supabase start` output into `.vscode/launch.json` (copy from `.vscode/launch.json.example` first)
4. F5 in VSCode (or `flutter run -d edge --dart-define=...`) to launch the app

## Development

- Tests: `rtk flutter test --no-pub`
- Static analysis: `rtk flutter analyze`
- Custom lint (no Supabase imports in features): `bash scripts/verify_no_supabase_in_features.sh`
- Migrations check: `bash scripts/verify_supabase.sh`

## Architecture

Feature-first hybrid (`lib/core/` shared infra + `lib/features/{name}/{data,domain,presentation}`). Repository abstraction means features NEVER import `package:supabase_flutter` directly — only `lib/core/services/` does (per D-06).

Phase 0 status: scaffolding only. Domain features land in Phase 1+.
```

    If README.md already exists with project content, INSTEAD just append a `## Bootstrap (new clone)` section if absent.
  </action>
  <verify>
    <automated>grep -q "^\.vscode/launch\.json$" .gitignore && grep -q "^\*\.g\.dart$" .gitignore && grep -q "^\*\.freezed\.dart$" .gitignore && test -f .vscode/launch.json.example && grep -q "PASTE_LOCAL_ANON_KEY_HERE" .vscode/launch.json.example && grep -q "scoop install supabase" README.md && test ! -f .vscode/launch.json</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains exact line `.vscode/launch.json` (gitignored).
    - `.gitignore` contains exact lines `*.g.dart` and `*.freezed.dart`.
    - `.gitignore` contains line `supabase/.env`.
    - `.gitignore` contains line `/coverage/`.
    - File `.vscode/launch.json.example` exists at exact path.
    - `.vscode/launch.json.example` contains literal string `PASTE_LOCAL_ANON_KEY_HERE` (no real keys).
    - `.vscode/launch.json.example` contains exactly 2 configurations (`Flutter (Edge, dev)` and `Flutter (Edge, profile)`).
    - `.vscode/launch.json.example` references `--dart-define=SUPABASE_URL=http://127.0.0.1:54321`.
    - File `.vscode/launch.json` does NOT exist (would leak — must be gitignored AND absent).
    - `README.md` contains string `scoop install supabase`.
    - `README.md` contains string `Bootstrap`.
    - `git check-ignore .vscode/launch.json` exits 0 (would be ignored if created).
  </acceptance_criteria>
  <done>.gitignore covers secrets + codegen artifacts; launch.json.example committable template exists; README bootstrap section explains onboarding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → git | Secrets entering launch.json must NEVER cross into commits |
| host shell → external scoop | Supabase CLI install pulls signed binary from supabase/scoop-bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04 | I (Information Disclosure) | .vscode/launch.json | mitigate | .gitignore line `.vscode/launch.json`; commitable template uses placeholder `PASTE_LOCAL_ANON_KEY_HERE` only |
| T-00-05 | I (Information Disclosure) | supabase/.env | mitigate | .gitignore line `supabase/.env` (Supabase CLI may emit env files in Plan 06) |
| T-00-06 | T (Tampering) | scoop install of supabase CLI | accept | Scoop bucket maintained by Supabase org; checksum verification by scoop |
| T-00-07 | I (Information Disclosure) | *.g.dart codegen leak via commit | mitigate | .gitignore lines `*.g.dart` and `*.freezed.dart` so generated files never enter history |
</threat_model>

<verification>
- `git check-ignore .vscode/launch.json` exits 0.
- `test -f .vscode/launch.json.example` exits 0.
- `grep -q "PASTE_LOCAL_ANON_KEY_HERE" .vscode/launch.json.example` exits 0.
- `supabase --version` exits 0 on host (verified by user in Task 1 checkpoint).
- `docker info` exits 0 on host (verified by user in Task 1 checkpoint).
</verification>

<success_criteria>
1. Supabase CLI installed (user confirmed at checkpoint).
2. .gitignore covers all secrets, codegen, and build artifacts called out by RESEARCH.md.
3. .vscode/launch.json.example provides committable starting point with placeholder secrets only.
4. README.md documents bootstrap path for a new clone.
5. No real key checked in (verified by absence of .vscode/launch.json file).
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-02-SUMMARY.md` documenting Supabase CLI version installed, the exact .gitignore lines added, and a confirmation that .vscode/launch.json was NOT committed.
</output>
