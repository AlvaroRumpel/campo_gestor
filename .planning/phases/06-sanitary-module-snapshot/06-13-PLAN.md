---
phase: 06-sanitary-module-snapshot
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260812_06_fix_dose_update_policy.sql
  - .planning/STATE.md
autonomous: true
gap_closure: true
gap_ids: [G-06-2]
requirements: [SANI-01]

must_haves:
  truths:
    - "Restoring an archived dose actually clears deleted_at — the UPDATE affects one row instead of silently affecting zero."
    - "Editing an archived dose (name, dosage, cost) persists, since it fails today for the same reason."
    - "The live doses UPDATE policy constrains membership and role only; an already-archived row is no longer excluded by the pre-update USING check."
    - "supabase/tests/06_sanitary_test.sql runs to completion against the live project at its current plan of 81 and every assertion passes, Group 12 included."
    - "The replay leaves zero rows behind in the live project."
    - "No already-applied migration file is edited; the correction ships forward-only as its own migration."
    - "STATE.md records the new ledger count and the 81-assertion result, replacing the stale 74 figure that caused this gap to be missed."
  artifacts:
    - supabase/migrations/20260812_06_fix_dose_update_policy.sql
  key_links:
    - "The defect class is applied-vs-on-disk drift: 20260810_06_sanitary_module.sql already reads correctly on disk, which is why every file-level check passed while the live project stayed broken. Only a catalog read of pg_policies can prove this fix; reading the migration file proves nothing."
    - "pgTAP Group 12 (added by ae08dba, plan 74 to 81) has never executed against the live project. It is the regression gate for exactly this policy."
  prohibitions:
    - "Never edit an already-applied migration file to correct live state — that is the mistake that produced this gap."
    - "Never weaken or delete a pgTAP assertion to make the suite green."
    - "Never leave fixture rows behind; every replay runs inside a transaction that is rolled back."
---

<objective>
Ship a forward-only corrective migration that replaces the live doses UPDATE policy, apply it to the
live project, and replay the 81-assertion pgTAP suite against it.

Purpose: G-06-2. The live project still runs the policy as first applied on 2026-08-07 at ~01:27.
Commit ae08dba corrected the clause 13 hours later by editing the already-applied migration file,
which does not re-run, so the live policy never changed. Row-level security evaluates the USING
expression against the pre-update row, so an UPDATE that clears deleted_at on an archived dose
matches zero rows: PostgREST answers 2xx, the client treats it as success, and the dose stays
archived. Editing an archived dose is silently broken by the same mechanism.
Output: one new migration file, a live policy proven correct by catalog read, a green 81-assertion
suite, and a STATE.md ledger entry that is no longer stale.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-sanitary-module-snapshot/06-UAT.md
@.planning/debug/dose-restore-noop.md
@supabase/migrations/20260810_06_sanitary_module.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the corrective migration and apply it to the live project</name>
  <files>supabase/migrations/20260812_06_fix_dose_update_policy.sql</files>
  <read_first>
    - .planning/debug/dose-restore-noop.md (the full diagnosis, including why the client layer was cleared)
    - supabase/migrations/20260810_06_sanitary_module.sql lines 41-51 (the three doses policies; the UPDATE policy body to reproduce verbatim)
    - .planning/STATE.md § Blockers (the established MCP apply path, project ref `wrdwzychjhlpwpivfhhq`, ledger at 16)
    - .planning/phases/06-sanitary-module-snapshot/06-12-PLAN.md Task 1 (how the Phase 6 push was carried out and verified)
  </read_first>
  <action>
    Create `supabase/migrations/20260812_06_fix_dose_update_policy.sql`. It drops the existing doses
    UPDATE policy if present and recreates it with the body that already sits in
    20260810_06_sanitary_module.sql: membership and veterinarian role in both the USING and the
    WITH CHECK expression, with no soft-delete predicate in either.

    Keep the policy name `veterinarian_can_update_active_dose` unchanged. The word "active" is now a
    misnomer, but renaming would churn the pgTAP policy assertions and every catalog check written in
    06-12 for zero behavioural gain. Say so in the file header, together with a short account of why
    this migration exists at all: the source file was corrected after it had already been applied, and
    applied migrations do not re-run. A fresh environment therefore creates the correct policy twice,
    which is harmless; an existing environment gets it once.

    Then apply it. `supabase db push` is the nominal command and is unavailable on this project: the
    CLI is authenticated but unlinked with no TTY for a database password. Every migration since Phase
    3 has gone through the Supabase MCP `apply_migration` tool against project `wrdwzychjhlpwpivfhhq`,
    and that is the path to use. Use the migration file's own name as the migration name so the ledger
    stays readable. Do not attempt an interactive CLI link.

    If the Supabase MCP tools are not present in your tool set, stop and report
    `BLOCKED — MCP apply_migration unavailable, orchestrator must apply 20260812_06_fix_dose_update_policy.sql`.
    Do not simulate the apply, do not mark the task done from the file alone, and do not fall back to
    editing 20260810_06_sanitary_module.sql — that file is already correct and re-editing it is the
    exact mistake that produced this gap.

    Verify by catalog read rather than by the absence of an error: query `pg_policies` for the `doses`
    table and confirm the row set and each policy's expressions, not merely that the apply returned
    success.
  </action>
  <verify>
    <automated>MCP `execute_sql`: `SELECT policyname, cmd, qual, with_check FROM pg_policies WHERE tablename = 'doses' ORDER BY policyname;` — assert exactly three rows (SELECT, INSERT, UPDATE, no DELETE), and that the UPDATE row's `qual` references membership and role only, with no soft-delete predicate.</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260812_06_fix_dose_update_policy.sql` exists and contains a DROP followed by a CREATE of the doses UPDATE policy.
    - `supabase/migrations/20260810_06_sanitary_module.sql` is byte-identical to its committed state — `git diff --stat -- supabase/migrations/20260810_06_sanitary_module.sql` is empty.
    - The migration ledger contains the new entry, taking the count from 16 to 17.
    - `pg_policies` returns exactly three rows for `doses`, with commands SELECT, INSERT and UPDATE and no DELETE row.
    - The UPDATE policy's `qual` expression no longer constrains the row's soft-delete column; `with_check` is unchanged from the applied version.
    - A round-trip against the live project inside a rolled-back transaction shows an UPDATE clearing the soft-delete column on an archived fixture dose reporting one affected row, where it previously reported zero.
  </acceptance_criteria>
  <done>The live doses UPDATE policy matches the on-disk source, proven by a pg_policies read, and archived doses are reachable by UPDATE again.</done>
</task>

<task type="auto">
  <name>Task 2: Replay the 81-assertion pgTAP suite against the live project and refresh STATE.md</name>
  <files>supabase/tests/06_sanitary_test.sql, .planning/STATE.md</files>
  <read_first>
    - supabase/tests/06_sanitary_test.sql (note `SELECT plan(81)` at line 28 and Group 12 at lines 546-583 — the restore and edit-while-archived regression added by ae08dba)
    - .planning/STATE.md § Blockers (Docker is down, `supabase test db` cannot start, the MCP rolled-back replay is the standing workaround; also the two known pgTAP overload traps on this project)
    - .planning/phases/06-sanitary-module-snapshot/06-12-SUMMARY.md (the previous replay, recorded at 74 assertions)
  </read_first>
  <action>
    Replay the suite verbatim through MCP `execute_sql`, wrapped so the entire run is rolled back —
    the same path that ran it at 74 assertions in 06-12 and the Phase 5 suite before that. Do not
    edit the suite; it already carries the plan count and the Group 12 assertions that this fix exists
    to satisfy.

    Report pass and fail counts. Group 12 failing after Task 1 means the policy did not actually
    change on the live project — treat that as the apply having failed, not as a bad assertion, and
    re-verify with a direct pg_policies read before touching anything. A failure anywhere else is
    triaged the Phase 5 way: prove the real state with a catalog read, decide whether the defect is in
    the schema or in the assertion, and fix a schema defect with another forward-only migration.

    Confirm the doses and sanitary_applications row counts are unchanged from before the replay.

    Then update `.planning/STATE.md`: add the new migration to the Blockers narrative with the ledger
    count now at 17, and replace the stale "74/74" record with the 81-assertion result. The old entry
    reads as though the suite that exists today had passed, which is part of why this gap survived to
    UAT — say plainly that the 74 run predates Group 12.
  </action>
  <verify>
    <automated>The MCP replay of `supabase/tests/06_sanitary_test.sql` reports 81 passing assertions and zero failures, and a post-run `SELECT count(*) FROM doses` / `SELECT count(*) FROM sanitary_applications` returns the same values as the same query run before the replay.</automated>
  </verify>
  <acceptance_criteria>
    - The suite runs to completion (reaches `finish()`) with 81 assertions passing and zero failing.
    - Group 12's four `lives_ok` calls and its two state assertions all pass — the restore and the edit-while-archived cases specifically.
    - Row counts on `doses` and `sanitary_applications` are identical before and after the replay.
    - No assertion was deleted, skipped or weakened, and `supabase/tests/06_sanitary_test.sql` is unmodified by this plan.
    - `.planning/STATE.md` states the ledger count as 17 and records the suite result at 81 assertions, noting that the earlier 74 figure predates the Group 12 regression tests.
  </acceptance_criteria>
  <done>The regression gate for this policy has executed against the live project and passes, and the project's written record of live database state is accurate again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local migration file → live production project | This plan writes to the live database |
| pgTAP fixtures → live production data | Test rows are created against real tables and must not survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-19 | Elevation of Privilege | the widened doses UPDATE policy | medium | mitigate | The predicate that is removed is a soft-delete check, not an authorization check. Membership and veterinarian role remain in both USING and WITH CHECK, and the acceptance criteria assert both expressions by catalog read rather than assuming the recreate preserved them |
| T-06-20 | Tampering | the recreate dropping more than intended | high | mitigate | The verification asserts the full policy set on `doses` is exactly three rows with the expected commands, so a dropped SELECT or INSERT policy is caught immediately rather than surfacing as a silent read outage |
| T-06-17 | Tampering | pgTAP fixtures persisting into the live project | high | mitigate | The replay is wrapped in a transaction that is rolled back, and row counts are compared before and after |

ASVS level 1; blocking severity is high. No package-manager installs occur in this plan, so no
supply-chain row or legitimacy checkpoint applies.
</threat_model>

<verification>
- `pg_policies` proves the live doses policy set and the corrected UPDATE expression.
- A rolled-back round trip proves an archived dose is now reachable by UPDATE.
- The pgTAP suite passes at 81 assertions, Group 12 included.
- Row counts are unchanged after the replay.
- `git diff` shows no change to any previously applied migration file.
</verification>

<success_criteria>
G-06-2 is closed: clicking "Reativar dose" on an archived dose restores it, and the fix is proven in
the live database rather than in a file on disk.
</success_criteria>

<output>
Create `.planning/phases/06-sanitary-module-snapshot/06-13-SUMMARY.md` when done
</output>
