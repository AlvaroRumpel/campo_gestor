---
phase: 06-sanitary-module-snapshot
plan: 11
type: execute
wave: 5
depends_on: ["06-08", "06-09"]
files_modified:
  - lib/features/lotes/presentation/lote_detail_screen.dart
autonomous: true
requirements: [SANI-02, SANI-04]

must_haves:
  truths:
    - "The lote screen gains a 'Registrar aplicação' footer button alongside the existing move button, gated on the viewer being a veterinarian, the lote being active, and the lote having at least one active animal — the exact mirror of the move button's gate (D-18)."
    - "When the gate fails the control is absent from the widget tree, not rendered disabled."
    - "The button opens the application form dialog with this lote pre-filled and locked, so the lot cannot be silently changed downstream (D-17)."
    - "On a successful registration the screen invalidates its animal list and this lote's application list, then shows the locked success snackbar with the animal count."
    - "A sanitary history section is added below the animal list, using the same card shell as the animal ficha's history block (D-20)."
    - "Each history row reads the application date, the dose name and the animal count in the locked format, appending the cost segment only when the frozen total is non-null."
    - "Rows are ordered by application date descending and tap through to the application detail route."
    - "A lote with no applications renders the locked no-applications-in-this-lote sentence with no icon."
    - "The section renders its own spinner while loading and its own error copy on failure — neither ever blanks the whole lote screen."
    - "Reversed originals and reversal records are hidden until the section's 'Mostrar estornadas' switch is on, and render struck through with their badge when shown."
    - "The section renders at most ten rows and, past that, a 'Ver todas' action navigating to the sanitary module pre-filtered by this lote."
    - "Rows are single-line with ellipsis overflow, the dose name being the variable-length part."
    - "No count label is rendered on the section — the rows are the count — and the 'Ver todas' action appears only past ten applications."
    - "The existing header card, the animal list and the move-lote flow are structurally untouched; this is an additive change."
  artifacts:
    - lib/features/lotes/presentation/lote_detail_screen.dart
  key_links:
    - "The section widget is imported from the sanitary feature built in 06-09; the lote screen contributes placement, not presentation."
    - "The registration button reuses the header card's existing active-animal count derivation rather than introducing a second count."
  prohibitions:
    - "Never display an application's live lote, dose or animal attributes in a history row; the frozen values are the only truth these surfaces may show."
---

<objective>
Wire the sanitary module into the lote screen: the second registration entry point (D-17/D-18) and
the per-lote sanitary history section (D-20, SANI-04).

Purpose: the vet's real workflow starts at the lote, not at the module. This is the entry point that
matters in the field, and the history section is where SANI-04 is actually consumed.
Output: two additive changes to one existing screen.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sanitary-module-snapshot/06-CONTEXT.md
@.planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md
@.planning/phases/06-sanitary-module-snapshot/06-PATTERNS.md
@lib/features/sanitario/presentation/sanitary_history_section.dart
@lib/features/sanitario/presentation/aplicacao_form_dialog.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar aplicação footer button on the lote header card</name>
  <files>lib/features/lotes/presentation/lote_detail_screen.dart</files>
  <read_first>
    - lib/features/lotes/presentation/lote_detail_screen.dart (the file being edited, read in full — the `_canEdit` derivation at lines 127-137, the `_LoteHeaderCard` constructor and its `onMover` callback wiring at lines 64-88, the move-button gate `Builder` at lines 227-249 which is the exact gate to mirror, and the active-animal count derivation it already performs)
    - lib/features/sanitario/presentation/aplicacao_form_dialog.dart (the constructor taking an optional lot id, and what its flow returns)
    - lib/features/sanitario/data/sanitary_application_repository.dart (`sanitaryApplicationsByLotProvider`)
    - .planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md § Screen Inventory item 8 (the registration button block) and § Copywriting Contract
  </read_first>
  <action>
    Extend `_LoteHeaderCard` with a second callback alongside the existing move callback, and render
    a second `OutlinedButton.icon` beside the move button inside the same footer `Builder` that
    already computes the active-animal count. Use the medical-services outline icon and the locked
    register label. Reuse that Builder's existing count rather than deriving a second one, and apply
    the identical gate the move button uses: the viewer can edit, the lote's deletion timestamp is
    null, and the active count is above zero. When the gate fails, both buttons are already omitted by
    the existing early return — keep that structure so the two controls cannot drift apart.

    Lay the two buttons out so they do not overflow at a 360-pixel width — wrap them in a `Wrap` with
    eight-pixel spacing right-aligned, rather than a `Row` that would clip.

    In `LoteDetailScreen`'s build, pass the new callback. Its body opens the application form dialog
    with this lote's id, which locks the lote field. When the flow returns an animal count,
    invalidate the lot's animal list provider and this lote's application list provider, then show
    the locked success snackbar with that count. Guard every context use after the await with a
    mounted check, following the existing move handler's shape.
  </action>
  <verify>
    <automated>flutter analyze lib/features/lotes/ &amp;&amp; flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/lotes/` reports no issues and `flutter test` is green repo-wide, including the existing lote widget tests.
    - The file contains the locked register label and the locked application-registered success sentence.
    - Both footer buttons live inside the same gate condition; `grep -c "activeCount > 0" lib/features/lotes/presentation/lote_detail_screen.dart` is 1.
    - The registration dialog is constructed with this lote's id, not with a null lot argument.
    - The post-await context use is guarded by a mounted check.
    - The existing move button, its callback and the header card's composition chips are unchanged.
  </acceptance_criteria>
  <done>A veterinarian on an active lote with animals sees a second footer button that starts the registration flow with the lote already resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Sanitary history section below the animal list</name>
  <files>lib/features/lotes/presentation/lote_detail_screen.dart</files>
  <read_first>
    - lib/features/lotes/presentation/lote_detail_screen.dart (Task 1 output — the body `ListView` children at lines 61-100 where the section is appended, and the existing 'Animais' section header spacing to match)
    - lib/features/sanitario/presentation/sanitary_history_section.dart (the lote variant widget and its constructor)
    - .planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md § Screen Inventory item 8 (the section block) and § UI Considerations E9
  </read_first>
  <action>
    Append to the body `ListView`'s children, after the animal list: a 16-pixel gap and the lote
    variant of the sanitary history section from the sanitary feature, passing this lote's id. Add
    the import.

    Add nothing else. The section owns its own card shell, header, reversed toggle, empty state,
    loading spinner, error copy, ten-row cap and "Ver todas" action — every one of those is already
    implemented once in 06-09, and reimplementing any of it here would let the lote and animal
    surfaces drift.

    Do not restructure the existing body, do not move the animal list, and do not convert the
    `ListView` to a builder.
  </action>
  <verify>
    <automated>flutter analyze lib/features/lotes/ &amp;&amp; flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/lotes/` reports no issues and `flutter test` is green repo-wide.
    - The lote history section widget is instantiated exactly once, after the animal list, with this lote's id.
    - No card shell, toggle, empty-state string, badge or row-format string for sanitary history is declared in `lote_detail_screen.dart` — all of it comes from the imported widget.
    - The existing `_AnimalList` invocation and the 'Animais' section header remain in place and in order.
  </acceptance_criteria>
  <done>The lote screen shows its sanitary history below the animal list, visually identical to the animal ficha's block and sharing its reversal filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter role gate → SECURITY DEFINER RPC | The hidden registration button is convenience; the RPC is the guard |
| Frozen snapshot → rendered history row | Rendering must not consult live records |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | registration started by a reader or owner | high | mitigate | The button is omitted from the tree by the same gate the move button uses, and `register_sanitary_application` raises 42501 independently |
| T-06-16 | Tampering | registering into an archived or empty lote | medium | mitigate | The gate excludes archived lotes and lotes with zero active animals; the RPC additionally resolves only non-archived lots and rejects an empty selection |
| T-06-11 | Tampering | history row showing a live rather than frozen value | medium | mitigate | The section widget reads only the frozen application row, and this plan's acceptance criteria forbid declaring any row-format logic in the lote screen |

ASVS level 1; blocking severity is high. No new packages, so no supply-chain row.
</threat_model>

## Artifacts this phase produces

### Produced by this plan

- Modified: `LoteDetailScreen` and `_LoteHeaderCard` — a second footer button and its callback, plus
  the lote sanitary history section placement

### Full phase inventory

See `06-01-PLAN.md` § Artifacts this phase produces.

<verification>
- `flutter analyze lib/features/lotes/` clean.
- `flutter test` green repo-wide, including the existing lote widget tests and their fake repository.
- No sanitary presentation logic is duplicated into the lotes feature.
</verification>

<success_criteria>
The field workflow closes: from a lote a veterinarian registers an application in two taps and reads
that lote's whole sanitary history on the same screen.
</success_criteria>

<output>
Create `.planning/phases/06-sanitary-module-snapshot/06-11-SUMMARY.md` when done
</output>
