---
phase: 06-sanitary-module-snapshot
plan: 05
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - lib/core/router/routes.dart
  - lib/core/router/router.dart
  - lib/features/sanitario/presentation/aplicacao_detail_screen.dart
  - lib/features/sanitario/presentation/estornar_aplicacao_dialog.dart
autonomous: true
requirements: [SANI-02, SANI-04]

must_haves:
  truths:
    - "`AppRoutes.aplicacaoById` is the root-level template `/aplicacoes/:id` and `AppRoutes.aplicacaoDetail(id)` builds the concrete path, mirroring the existing lote and ATF pairs (D-19)."
    - "`/aplicacoes/:id` is registered as a root-level GoRoute outside every shell branch, so the three list origins all deep-link to it and a browser reload resolves it."
    - "`AplicacaoDetailScreen`'s back button pops when a stack exists and otherwise navigates to the sanitary module, because the screen has three possible origins and cannot assume a push stack."
    - "The detail screen renders header, then the skipped-animals note when the frozen skipped count is above zero, then the composition section, in that order."
    - "A detail screen for a missing id renders the not-found copy rather than an empty scaffold, matching the animal ficha's precedent."
    - "Loading renders a spinner and the header is never built against partial data; a load failure renders the generic load-failure copy."
    - "Optional header rows — observação, custo, and the two reversal links — are omitted entirely when absent rather than rendered blank, matching the existing key-value omission convention."
    - "The cost row is omitted entirely when the dose had no cost at capture time; a zero currency value is never rendered (D-11)."
    - "The composition count header reads at least one animal for every valid application, because a zero-animal registration cannot be created."
    - "Observação renders unconstrained and wraps on the detail view, unlike the capped entry field."
    - "The lote row shows the frozen lote name and links to the live lote only when that lote still exists and is not archived; the frozen name is displayed either way (D-04)."
    - "The estorno action is rendered only for a veterinarian, only when the row is not itself a reversal, and only when it has not already been reversed — absence, not a disabled control."
    - "`EstornarAplicacaoDialog` requires a non-blank motivo before its confirm button submits, and the confirm button is the one destructive-toned button in the phase."
    - "During the reversal call the confirm button is disabled and shows an inline spinner."
    - "A reversal failure renders inline inside the dialog with the motivo text preserved and the dialog open; the already-reversed case renders its own copy plus a link to the existing reversal row."
    - "Dose and lote names in key-value rows wrap rather than truncate on the detail surface; only the dialog title's dose name is single-line ellipsised."
    - statement: "A 200-row composition renders inside the page scroll as one continuous scroll with no nested-scroll trap, and the list builds without exception."
      verification: backstop
  artifacts:
    - lib/features/sanitario/presentation/aplicacao_detail_screen.dart
    - lib/features/sanitario/presentation/estornar_aplicacao_dialog.dart
  key_links:
    - "The route helper added here is what every sanitary history row in 06-09, 06-10 and 06-11 navigates through; adding it before those plans is why this is wave 3."
    - "The detail screen invalidates the by-id, by-lot, by-property and per-animal providers after a successful estorno, so all four surfaces reflect it without a manual reload."
  prohibitions:
    - "Never display an animal's live number, category, UA or current lot on this screen — every row renders the frozen snapshot value, because the point of the snapshot is that it stays true after the animal moves."
    - "Never cap or truncate the composition list in a way that makes an application appear to cover fewer animals than it froze."
---

<objective>
Add the root-level application detail route and build the read-only detail screen plus the estorno
confirmation dialog.

Purpose: this is the leaf of the navigation graph — three different lists link into it, and it owns
the only reversal entry point. Building it first lets every later list plan navigate to a real route.
Output: two route constants, one GoRoute registration, two new widgets.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sanitary-module-snapshot/06-CONTEXT.md
@.planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md
@.planning/phases/06-sanitary-module-snapshot/06-PATTERNS.md
@lib/features/sanitario/data/sanitary_application_model.dart
@lib/features/sanitario/data/sanitary_application_repository.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route constants for the application detail route</name>
  <files>lib/core/router/routes.dart</files>
  <read_first>
    - lib/core/router/routes.dart (the file being edited — the `loteById`/`loteDetail` and `atfById`/`atfDetail` pairs at lines 24-35 are the exact shape to mirror, including the trailing comment convention)
    - lib/core/router/router.dart (read-only here — the root-level `GoRoute` registrations for the lote and ATF templates at lines 134-149 show where Task 3 will register this route)
    - .planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md § Interaction Contracts, Navigation
  </read_first>
  <action>
    In `lib/core/router/routes.dart`, add a Phase 6 block after the Phase 5 ATF pair: a constant
    `aplicacaoById` holding the template `/aplicacoes/:id`, and a static helper `aplicacaoDetail`
    taking an id and returning the concrete path. Follow the existing comment style, naming D-19 and
    stating that the route is root-level and outside the shell because it is reachable from three
    list origins. Do not add it to the `all` list — that list is the shell branch inventory and tests
    count it.

    Do not touch `router.dart` in this task. Registering the route requires importing a screen that
    does not exist yet, which would leave the repository non-compiling between task commits; Task 3
    creates the screen and registers the route in the same commit.
  </action>
  <verify>
    <automated>flutter analyze lib/core/router/routes.dart &amp;&amp; flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "aplicacaoById" lib/core/router/routes.dart` matches and the constant value is the template string with the `:id` segment.
    - `grep -n "aplicacaoDetail" lib/core/router/routes.dart` matches a static String helper.
    - `git diff --stat lib/core/router/router.dart` is empty — the registration belongs to Task 3.
    - Any existing test that counts `AppRoutes.all` entries still passes — the new route is not added to that list.
    - `flutter analyze lib/core/router/routes.dart` reports no issues.
  </acceptance_criteria>
  <done>The route template and its path helper exist and the helper is available to every list surface built later.</done>
</task>

<task type="auto">
  <name>Task 2: EstornarAplicacaoDialog — required motivo, destructive confirm, inline error</name>
  <files>lib/features/sanitario/presentation/estornar_aplicacao_dialog.dart</files>
  <read_first>
    - lib/features/animais/presentation/baixa_dialog.dart (the required-reason confirmation template this mirrors: form key, validator, progress indicator in the title slot, action pair, saving flag)
    - lib/features/reproducao/presentation/encerrar_atf_dialog.dart (the most recent confirm-dialog in the codebase, for current spacing and button idiom)
    - lib/features/sanitario/data/sanitary_application_repository.dart and sanitary_application_exception.dart
    - .planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md § Screen Inventory item 7, § Color (the destructive reserved-for list), and § Copywriting Contract
  </read_first>
  <action>
    Create `lib/features/sanitario/presentation/estornar_aplicacao_dialog.dart` with a
    `ConsumerStatefulWidget` named `EstornarAplicacaoDialog` taking the application id and the frozen
    dose name.

    Render an `AlertDialog` constrained to 400 pixels wide. The title slot shows a
    `LinearProgressIndicator` while saving and otherwise the locked title text with the dose name
    interpolated, single-line with ellipsis overflow.

    Content column: the locked body paragraph in the medium body role; a gap; a `TextFormField` for
    the motivo with three lines, the locked label, and a validator returning the locked validation
    message when the trimmed value is empty; then the inline error slot, rendered only when an error
    is held in state, in the error colour, followed by a link button to the existing reversal when the
    error reason is the already-reversed one.

    Actions: a cancel `TextButton`, and a confirm `FilledButton` styled with the error colour as its
    background — this is the single destructive-red button in the phase — disabled while saving and
    showing a small inline spinner in that state.

    Submit handler: validate the form; set the saving flag; call the repository's reverse method;
    on success pop with true; on error map the caught object through the shared exception helper
    passing the locked estorno-specific generic sentence as the fallback, store the resulting
    exception in state so the inline slot renders its message, and leave the dialog open with the
    motivo text intact. Reset the saving flag in a finally block guarded on the widget still being
    mounted. Do not show a snackbar from this dialog — success snackbars are the caller's job and
    errors render inline here (D-36).

    When the mapped reason is already-reversed, resolve the existing reversal id by reading the
    by-lot provider and finding the sibling whose reversal pointer targets this application, then
    render the locked link label navigating to that application; if it cannot be resolved, render the
    message without the link rather than a dead control.
  </action>
  <verify>
    <automated>flutter analyze lib/features/sanitario/ &amp;&amp; flutter test test/features/sanitario/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/sanitario/` reports no issues.
    - The file contains the exact locked strings for the dialog title pattern, the body paragraph, the motivo label, the motivo validation message, the already-estornada error, the link label, and the estorno-specific generic failure.
    - `grep -c "ScaffoldMessenger" lib/features/sanitario/presentation/estornar_aplicacao_dialog.dart` is 0 — errors render inline, success is the caller's snackbar.
    - The confirm button's style sets its background from the theme error colour.
    - The submit handler's catch block routes through the shared exception helper with a required fallback argument, and no `PostgrestException` type appears in this file.
    - The saving flag reset is inside a `finally` guarded by a mounted check.
  </acceptance_criteria>
  <done>Estorno requires a typed reason, reads as destructive, and every failure path leaves the user inside the dialog with their text and a next action.</done>
</task>

<task type="auto">
  <name>Task 3: AplicacaoDetailScreen — header card, skipped note, composition list</name>
  <files>lib/features/sanitario/presentation/aplicacao_detail_screen.dart, lib/core/router/router.dart</files>
  <read_first>
    - lib/features/lotes/presentation/lote_detail_screen.dart (the resolved-fallback back button at lines 34-47, the scaffold-plus-ListView body shape, the AsyncValue loading/error/not-found treatment, and the `_canEdit` role helper at lines 127-137 — copy that helper's exact logic)
    - lib/features/reproducao/presentation/atf_detail_screen.dart (the header card plus key-value row pattern and the badge treatment)
    - lib/features/animais/presentation/animal_detail_screen.dart (the `_KvRow` implementation and the conditional-row omission convention used by `AnimalInfoCard`)
    - lib/features/sanitario/data/sanitary_application_model.dart and sanitary_application_repository.dart
    - lib/features/sanitario/data/sanitary_calculations.dart (the formatting helpers — do not re-implement UA, volume or currency formatting here)
    - lib/features/animais/data/animal_constants.dart (`kCategoryLabels` for the composition rows)
    - lib/core/router/router.dart (the file also edited here — the root-level lote and ATF `GoRoute` registrations at lines 134-149 and the import block ordering)
    - lib/features/sanitario/presentation/estornar_aplicacao_dialog.dart (Task 2 output — the dialog this screen shows and the result it returns)
    - .planning/phases/06-sanitary-module-snapshot/06-UI-SPEC.md § Screen Inventory item 6 and § Copywriting Contract
  </read_first>
  <action>
    Create `lib/features/sanitario/presentation/aplicacao_detail_screen.dart` with a
    `ConsumerWidget` named `AplicacaoDetailScreen` taking a required application id.

    Watch `sanitaryApplicationByIdProvider` for the row and
    `sanitaryApplicationsByLotProvider` for its lot, so the screen can determine whether this row has
    itself been reversed by looking for a sibling whose reversal pointer targets this id — use
    `reversedApplicationIds` from the model file rather than adding a new query.

    AppBar: title is the frozen dose name; leading is a `BackButton` whose handler pops when the
    router can pop and otherwise navigates to the sanitary module route.

    Body is a `ListView` with 16-pixel padding containing, in order: the header card; a 16-pixel gap;
    the skipped-animals note rendered only when the frozen skipped count is above zero, using
    `Intl.plural` with the exact singular and plural copy from the Copywriting Contract; another gap;
    and the composition section.

    Header card: a `Card` on the high-emphasis container colour with 16-pixel padding. First row is
    the medical-services outline icon, the dose name in the title role, a spacer, and at most one
    status chip — the reversed-original chip on the error container tokens when a sibling reverses
    this row, or the reversal-record chip on the neutral high container tokens when this row is
    itself a reversal, never both.

    Then key-value rows with a 120-pixel label column: the lote row showing the frozen lote name,
    wrapped in an `InkWell` to the lote detail route only when the live lote still exists and is not
    archived and otherwise rendered as muted plain text — the frozen name is what is displayed in
    both cases; the application date formatted day/month/year; the applied dosage row combining the
    per-kg and per-UA frozen values in the exact bracketed format from the Copywriting Contract; the
    cost row in the analogous format, omitted entirely when the frozen cost per kg is null; and the
    observação row, omitted when null or blank and rendered with unconstrained wrapping when present.

    Then, when this row reverses another, an additional row labelled for the original with a tappable
    link to that application; and when a sibling reverses this row, an additional row labelled for
    the reversal carrying its date and a tappable link to it.

    Then the totals line in the large body role at weight 600 in the primary colour: the animal count
    through `Intl.plural`, the UA total, the volume total, and the cost total, the last two through
    the shared formatting helpers and the cost segment omitted when the frozen total cost is null.

    Finally the estorno action: an `OutlinedButton.icon` with the undo icon, the locked label, and
    the error colour as its foreground — rendered only when the viewer is a veterinarian by the same
    role derivation `LoteDetailScreen` uses, and only when this row neither reverses another nor has
    been reversed. Absence, not disabled. Its handler shows `EstornarAplicacaoDialog`; on a true
    result it invalidates the by-id provider for this row, the by-lot provider for its lot, the
    property list provider, and the per-animal history provider for every animal id in the frozen
    composition, then shows the success snackbar with the locked copy.

    Composition section: a section header with the locked label in the title role plus a muted count
    in parentheses through `Intl.plural`, then a `ListView.builder` with shrink-wrap and
    never-scrollable physics rendering one row per frozen composition entry — the number at weight
    600, the pt-BR category label, and the frozen UA figure, tapping through to the live animal ficha.
    Render every entry; do not cap this list. The count in the header is the array length, so a cap
    would make the header and the body disagree about a permanent record.

    Finally, in `lib/core/router/router.dart`, import this screen and register a `GoRoute` on
    `AppRoutes.aplicacaoById` immediately after the ATF registration, inside the root `routes` array
    and outside the `StatefulShellRoute`, reading the path parameter named `id`. Match the
    surrounding registration formatting exactly. Doing it here keeps the import and its target in one
    commit.

    Handle the three async states: a centred spinner while loading with no header built against
    partial data; the generic load-failure copy on error; and the not-found copy when the provider
    resolves to null.
  </action>
  <verify>
    <automated>flutter analyze lib/features/sanitario/ lib/core/router/ &amp;&amp; flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/features/sanitario/ lib/core/router/` reports no issues and `flutter test` is green repo-wide.
    - `grep -n "AppRoutes.aplicacaoById" lib/core/router/router.dart` matches inside the root routes array, at a nesting depth above the `StatefulShellRoute` call.
    - The file contains the exact locked strings for the section label, the estorno action label, the two status badges, the not-found copy and the generic load-failure copy.
    - `grep -n "Intl.plural" lib/features/sanitario/presentation/aplicacao_detail_screen.dart` matches at least 3 times — skipped note, totals line, composition count.
    - `grep -n "shrinkWrap: true" lib/features/sanitario/presentation/aplicacao_detail_screen.dart` matches exactly once, inside the composition section, paired with never-scrollable physics.
    - The composition builder's item count is the frozen array's length with no minimum, maximum or `take` applied.
    - `grep -n "formatUa\|formatVolumeMl\|formatCurrencyBrl" lib/features/sanitario/presentation/aplicacao_detail_screen.dart` matches — formatting is delegated, not re-implemented.
    - The cost key-value row and the cost segment of the totals line are each inside a null check on the frozen cost field.
  </acceptance_criteria>
  <done>A frozen application renders in full from its own row with no live re-lookup of animal or dose data, and the estorno affordance appears only where it is legal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter UI role gate → SECURITY DEFINER RPC | The hidden estorno button is convenience; the RPC is the guard |
| Deep link → detail screen | An arbitrary application uuid can be typed into the URL bar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | estorno triggered by a non-veterinarian | high | mitigate | Button omitted from the tree for non-veterinarians, and `reverse_sanitary_application` independently raises 42501 which maps to the locked refusal copy |
| T-06-05 | Tampering | double reversal from a stale detail screen | medium | mitigate | The dialog's already-reversed branch renders the locked copy plus a link to the existing reversal; the database unique index is what actually prevents the second row |
| T-06-06 | Information Disclosure | deep-linking another property's application id | high | mitigate | The by-id read passes through the SELECT policy scoped by `is_member_of`, so a foreign id resolves to null and renders the not-found copy rather than data |
| T-06-11 | Tampering | history surface silently showing current instead of frozen attributes | medium | mitigate | Every rendered value on this screen reads the frozen row or its composition array; the only live read is the existence check that decides whether the lote name is tappable |

ASVS level 1; blocking severity is high. No new packages, so no supply-chain row.
</threat_model>

## Artifacts this phase produces

### Produced by this plan

- Routes: `AppRoutes.aplicacaoById`, `AppRoutes.aplicacaoDetail(id)`, root-level
  `GoRoute('/aplicacoes/:id')`
- Widgets: `AplicacaoDetailScreen`, `EstornarAplicacaoDialog`

### Full phase inventory

See `06-01-PLAN.md` § Artifacts this phase produces.

<verification>
- `flutter analyze` clean for the router and sanitary feature directories.
- `flutter test` green repo-wide, including any existing route-count test.
- The composition list renders every frozen entry with no cap.
</verification>

<success_criteria>
A frozen application is fully readable at a deep-linkable URL, and reversing it is a role-gated,
reason-required, single-shot action whose every failure mode has authored pt-BR copy.
</success_criteria>

<output>
Create `.planning/phases/06-sanitary-module-snapshot/06-05-SUMMARY.md` when done
</output>
