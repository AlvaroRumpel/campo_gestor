---
phase: 08-animal-dossier-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/reproducao/presentation/animal_reproductive_history_section.dart
  - lib/features/animais/presentation/animal_detail_screen.dart
autonomous: true
requirements: [ANIM-03]

must_haves:
  truths:
    - "O bloco reprodutivo da ficha é um widget público em `lib/features/reproducao/presentation/`, que recebe apenas um id de animal e resolve o próprio provider — simétrico ao bloco sanitário (D-11)."
    - "A ficha do animal passa a ser composição de dois blocos simétricos, cada um pertencendo ao seu módulo; `animal_detail_screen.dart` não contém mais lógica de renderização de histórico reprodutivo (D-11)."
    - "O bloco reprodutivo continua renderizando uma linha por ATF, na ordem em que o repositório entrega — data de inseminação decrescente — e essa ordem não muda por causa da extração (SC-3, guarda de regressão)."
    - "O bloco reprodutivo vazio continua mostrando a mensagem 'Nenhum ATF registrado para este animal.' — a ausência é informação, prova que a ficha consultou e não há nada (D-20, UI-SPEC empty/reproductive-history-block)."
    - "Zero ATFs mostra a mensagem de vazio; 1..N ATFs mostram uma linha cada, sem truncamento nem botão 'Ver todos' — o truncamento assimétrico do D-06 é preservado (UI-SPEC zero-one-many: ATFs por animal)."
    - "O bloco reprodutivo continua estritamente read-only para todo papel: nenhum controle de mutação, nenhum botão de registrar DG, nenhum atalho para o fluxo de DG (D-22, D-13 da Phase 5)."
    - "O shell do card do bloco reprodutivo permanece byte-idêntico ao de hoje — card com borda arredondada de 12, contorno a 38% de opacidade, cor de superfície e padding de 16 — porque a extração é um movimento, não um restyle (UI-SPEC Existing Widget Inventory)."
  artifacts:
    - "lib/features/reproducao/presentation/animal_reproductive_history_section.dart — NOVO arquivo com `AnimalReproductiveHistorySection` público"
    - "lib/features/animais/presentation/animal_detail_screen.dart — compõe o widget público importado e não declara mais as classes privadas do bloco reprodutivo"
  key_links:
    - "AnimalDetailScreen (ListView) → AnimalReproductiveHistorySection(animalId) → reproductiveHistoryByAnimalProvider(animalId)"
    - "animal_detail_screen.dart → import de ../../reproducao/presentation/animal_reproductive_history_section.dart"
---

<objective>
Extrair o bloco de histórico reprodutivo de dentro de `animal_detail_screen.dart` para um widget público em `lib/features/reproducao/presentation/`, espelhando `AnimalSanitaryHistorySection` (D-11).

Purpose: depois disso a ficha vira composição de dois blocos simétricos, cada um pertencendo ao seu módulo, e o bloco reprodutivo fica testável isolado — que é o que os planos 08-05 (expansão de DGs, retry) precisam para trabalhar sem tocar na tela.
Output: um arquivo novo com o widget público; a tela reduzida à composição.

**Este plano é um MOVIMENTO, não um redesenho.** Nenhum pixel muda, nenhuma cópia de texto muda, nenhuma query muda. A expansão de DGs (D-08), os campos novos do D-09 e o retry do D-04 chegam no plano 08-05, depois desta extração.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animal-dossier-consolidation/08-CONTEXT.md
@.planning/phases/08-animal-dossier-consolidation/08-PATTERNS.md
@.planning/phases/08-animal-dossier-consolidation/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar o widget público AnimalReproductiveHistorySection</name>
  <files>lib/features/reproducao/presentation/animal_reproductive_history_section.dart</files>

  <read_first>
    - `lib/features/animais/presentation/animal_detail_screen.dart` — o código de origem: `_ReproductiveHistorySection` (a partir do dartdoc "Read-only reproductive history list on the animal ficha") e `_ReproductiveHistoryRow` logo abaixo. Ler ambos por inteiro, incluindo o dartdoc, antes de mover.
    - `lib/features/sanitario/presentation/sanitary_history_section.dart` — o contrato a espelhar: widget público, construtor só com `animalId`, dartdoc explicando o contrato D-37, e o shell de card. **Não editar este arquivo neste plano.**
    - `.planning/phases/08-animal-dossier-consolidation/08-PATTERNS.md` §`animal_reproductive_history_section.dart` — padrão de imports, contrato do widget e a instrução explícita "Move, do not rewrite".
    - `lib/features/reproducao/data/atf_model.dart` e `lib/features/reproducao/data/dg_record_model.dart` — tipos consumidos pelas duas classes movidas.
  </read_first>

  <action>
Criar `lib/features/reproducao/presentation/animal_reproductive_history_section.dart`.

**Imports:** `package:flutter/material.dart`, `package:flutter_riverpod/flutter_riverpod.dart`, `package:go_router/go_router.dart`, `package:intl/intl.dart`, mais os relativos `../../../core/router/routes.dart`, `../data/atf_model.dart`, `../data/atf_repository.dart`, `../data/dg_record_model.dart`. Mesma ordem de blocos usada por `sanitary_history_section.dart`.

**Classe pública:** `AnimalReproductiveHistorySection extends ConsumerWidget`, construtor `const AnimalReproductiveHistorySection({super.key, required this.animalId})` com o campo final `animalId` do tipo `String`. `ConsumerWidget` (stateless) basta — o `ExpansionTile` do plano 08-05 gerencia o próprio estado de expansão internamente, então não há necessidade de `ConsumerStatefulWidget`. Note a diferença deliberada em relação ao `AnimalSanitaryHistorySection`, que é stateful só por causa do toggle "Mostrar estornadas".

**Corpo:** copiar o `build` de `_ReproductiveHistorySection` verbatim — o card com `RoundedRectangleBorder` de raio 12 e `BorderSide` no contorno a 38% de opacidade, cor de superfície, padding de 16, o título "Histórico Reprodutivo" em `titleMedium`, o espaçamento de 8, e o `when` do provider com spinner centralizado, o texto de erro "Erro ao carregar histórico reprodutivo." em `bodyMedium` esmaecido, a mensagem de vazio "Nenhum ATF registrado para este animal." e o `Column` com uma linha por entry usando o formato de data curto de dia/mês. A única troca no corpo é `widget.animalId` não se aplica (é `ConsumerWidget`, então segue `animalId` direto).

**Classe de linha:** mover `_ReproductiveHistoryRow` para este arquivo, privado, sem nenhuma alteração de comportamento — chip de resultado com as três cores por resultado de DG, texto "aguardando DG" quando não há DG, badge de status Ativo/Encerrado, e o `InkWell` que navega para a rota de detalhe do ATF.

**Dartdoc:** adaptar o dartdoc existente das duas classes e acrescentar uma linha registrando o contrato novo, no espírito do D-37 do bloco sanitário: a seção recebe apenas um id de animal e resolve o próprio provider, para que a ficha a componha sem passar dado de fora.

Neste momento o app compila com o bloco reprodutivo declarado em dois lugares (o privado antigo na tela, o público novo aqui). A remoção do antigo é a Task 2. Não trocar o local de composição ainda.

Respeitar os lints do projeto: `require_trailing_commas`, `prefer_const_constructors`, `prefer_const_literals_to_create_immutables`.
  </action>

  <verify>
    <automated>flutter analyze lib/features/reproducao/presentation/animal_reproductive_history_section.dart</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo `lib/features/reproducao/presentation/animal_reproductive_history_section.dart` existe.
    - Contém `class AnimalReproductiveHistorySection extends ConsumerWidget`.
    - Contém `const AnimalReproductiveHistorySection({super.key, required this.animalId})`.
    - Contém `ref.watch(` aplicado a `reproductiveHistoryByAnimalProvider`.
    - Contém as três strings de cópia inalteradas: `Histórico Reprodutivo`, `Nenhum ATF registrado para este animal.` e `Erro ao carregar histórico reprodutivo.`.
    - Contém `BorderRadius.circular(12)` e `withValues(alpha: 0.38)` — shell de card idêntico ao de origem.
    - `flutter analyze` sobre o arquivo novo sai com código 0.
  </acceptance_criteria>

  <done>Existe um widget público de histórico reprodutivo, no módulo reprodução, com o mesmo visual e a mesma cópia do bloco que hoje vive dentro da tela do animal.</done>
</task>

<task type="auto">
  <name>Task 2: Compor a ficha com o widget público e remover o bloco privado da tela</name>
  <files>lib/features/animais/presentation/animal_detail_screen.dart</files>

  <read_first>
    - `lib/features/animais/presentation/animal_detail_screen.dart` — arquivo alvo, por inteiro: o bloco de imports no topo, o `ListView` do `build` de `AnimalDetailScreen` (onde `_ReproductiveHistorySection` é instanciado entre `AnimalInfoCard` e `AnimalSanitaryHistorySection`), e as duas classes privadas a remover no fim do arquivo.
    - `lib/features/reproducao/presentation/animal_reproductive_history_section.dart` — criado na Task 1; confirmar o nome exato da classe e do parâmetro nomeado.
    - `test/widget/animal_detail_screen_test.dart` — os seis testes do grupo `AnimalDetailScreen — Histórico Reprodutivo` que precisam continuar verdes sem alteração. **Não editar este arquivo neste plano** — ele pertence ao plano 08-01 (fixtures) e ao 08-04 (testes novos).
  </read_first>

  <action>
Em `lib/features/animais/presentation/animal_detail_screen.dart`:

1. Acrescentar o import relativo `../../reproducao/presentation/animal_reproductive_history_section.dart`, no bloco de imports relativos, mantendo a ordem alfabética já usada pelo arquivo (fica próximo ao import de `../../reproducao/data/...`).

2. No `ListView` do `build` de `AnimalDetailScreen`, trocar a instância do bloco privado pela pública: `AnimalReproductiveHistorySection(animalId: animal.id)`, mantendo a mesma posição entre `AnimalInfoCard` e `AnimalSanitaryHistorySection` e os mesmos espaçamentos de 16 antes e depois. A ordem card → reprodutivo → sanitário é contrato (D-18) e não muda.

3. Remover do arquivo as duas classes privadas do bloco reprodutivo — a seção e a linha — junto com seus dartdocs. Todo o conteúdo delas já vive no arquivo novo.

4. Remover os imports que ficaram sem uso após a remoção. Verificar caso a caso com o analisador: os imports de `atf_model.dart`, `atf_repository.dart` e `dg_record_model.dart` provavelmente ficam órfãos; `intl` e `go_router` continuam em uso por `AnimalInfoCard`. Não remover nada por suposição — remover apenas o que `flutter analyze` apontar como não utilizado.

Nada mais neste arquivo muda neste plano. O banner de baixa, a remoção da linha de Status, o `_KvRow` adaptativo e a troca do provider de lote são todos do plano 08-04.
  </action>

  <verify>
    <automated>flutter analyze &amp;&amp; flutter test test/widget/animal_detail_screen_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/features/animais/presentation/animal_detail_screen.dart` contém `AnimalReproductiveHistorySection(animalId: animal.id)`.
    - O mesmo arquivo contém o import `animal_reproductive_history_section.dart`.
    - `flutter analyze` sobre o repositório inteiro sai com código 0 — nenhum aviso `unused_element` (o que prova que as classes privadas antigas foram de fato removidas, não apenas deixadas órfãs) e nenhum `unused_import`.
    - `flutter test test/widget/animal_detail_screen_test.dart` sai com código 0, com os seis testes do grupo `AnimalDetailScreen — Histórico Reprodutivo` verdes **sem nenhuma alteração no arquivo de teste**, incluindo:
      - `empty: renders "Nenhum ATF registrado para este animal."`
      - `error: renders "Erro ao carregar histórico reprodutivo." and the info card stays present`
      - `populated: renders one row per entry, in the order supplied (insemination date descending)` — guarda de regressão do SC-3
      - `read-only: the section renders no ChoiceChip and no button (D-13)` — guarda do D-22
      - `navigation: tapping a row navigates to /atf/:atfId`
    - Comportamento: o override de `reproductiveHistoryByAnimalProvider` feito pelo teste continua alcançando o bloco extraído — nenhum override novo foi necessário, provando que o alvo do provider não mudou.
  </acceptance_criteria>

  <done>A ficha compõe o bloco reprodutivo público e a tela não declara mais nenhuma classe de renderização de histórico reprodutivo; todos os testes existentes passam sem edição.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plano 08-02)

| Símbolo | Tipo | Arquivo |
|---|---|---|
| `AnimalReproductiveHistorySection` | `ConsumerWidget` público | `lib/features/reproducao/presentation/animal_reproductive_history_section.dart` (NOVO) |
| `_ReproductiveHistoryRow` | widget privado (movido de `animal_detail_screen.dart`) | `lib/features/reproducao/presentation/animal_reproductive_history_section.dart` (NOVO) |

Arquivo novo: `lib/features/reproducao/presentation/animal_reproductive_history_section.dart`.

Símbolos REMOVIDOS de `lib/features/animais/presentation/animal_detail_screen.dart`: as classes privadas `_ReproductiveHistorySection` e `_ReproductiveHistoryRow` (movidas, não deletadas — o conteúdo passa a viver no arquivo novo).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → PostgREST (Supabase) | Inalterado por este plano: a query do bloco reprodutivo é a mesma, movida de arquivo |
| Widget → provider | O bloco extraído resolve seu próprio provider por id de animal, sem receber dado de fora |

## STRIDE Threat Register

ASVS L1; bloqueio configurado em `high`. Nenhuma ameaça `high`/`critical` — este plano é um movimento de código sem mudança de query, de escopo de dados ou de superfície de escrita.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Elevation of Privilege | `AnimalReproductiveHistorySection` — widget público novo | low | mitigate | O widget é read-only por construção: nenhum callback de mutação no construtor, nenhum controle interativo além do `InkWell` de navegação. O critério de aceite exige que o teste `read-only: the section renders no ChoiceChip and no button (D-13)` continue verde após a extração, o que prova mecanicamente que nenhum afordance de escrita entrou junto com a mudança de visibilidade da classe. |
| T-08-05 | Information Disclosure | Tornar público um widget antes privado | low | accept | Visibilidade Dart não é fronteira de autorização: a autorização real é a RLS de `animal_atf_memberships`/`atf_batches` (Fase 5), inalterada. O widget continua exigindo um id de animal que o chamador só obtém navegando por uma rota já protegida. |
| T-08-SC | Tampering | instalação de pacotes (pub/npm) | low | accept | Nenhum pacote é adicionado, atualizado ou removido nesta fase — RESEARCH §Package Legitimacy Audit registra "Not applicable". |
</threat_model>

<verification>
- `flutter analyze` sai com código 0 no repositório inteiro, sem `unused_element` e sem `unused_import`.
- `flutter test` (suíte completa) sai com código 0.
- `git diff --stat` deste plano toca exatamente dois arquivos de `lib/` e nenhum arquivo de `test/`.
- Nenhum arquivo sob `supabase/` foi tocado.
- `lib/features/sanitario/presentation/sanitary_history_section.dart` NÃO aparece no diff deste plano (fronteira D-37).
</verification>

<success_criteria>
- O bloco reprodutivo é um widget público no módulo reprodução, com contrato de id-apenas, simétrico ao bloco sanitário.
- A ficha compõe dois blocos simétricos e não declara mais renderização de histórico reprodutivo.
- Nenhum teste existente precisou ser editado para continuar verde — a prova de que a extração preservou comportamento, ordem e cópia.
</success_criteria>

<output>
Create `.planning/phases/08-animal-dossier-consolidation/08-02-SUMMARY.md` when done
</output>
