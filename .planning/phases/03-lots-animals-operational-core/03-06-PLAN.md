---
phase: 03-lots-animals-operational-core
plan: 06
type: execute
wave: 3
depends_on:
  - "03-04"
files_modified:
  - lib/features/animais/presentation/animais_screen.dart
  - lib/features/animais/presentation/animal_detail_screen.dart
  - lib/features/animais/presentation/animal_edit_dialog.dart
  - lib/features/animais/presentation/baixa_dialog.dart
  - test/widget/animais_screen_test.dart
  - test/widget/animal_edit_dialog_test.dart
  - test/widget/baixa_dialog_test.dart
autonomous: true
requirements:
  - ANIM-02
  - ANIM-04
  - ANIM-05
  - ANIM-06
  - PROP-05
tags:
  - flutter
  - ui
  - search
  - filters
  - dialog
  - role-gate
  - tdd
must_haves:
  truths:
    - "AnimaisScreen replaces the placeholder. Top: SearchBar (hint 'Buscar por número...') + clear button when non-empty."
    - "AnimaisScreen filter row: 8 FilterChips ('Todas' + 7 categorias) + Lote dropdown ('Todos os lotes') + Piquete dropdown ('Todos os piquetes')."
    - "AnimaisScreen SummaryBar: '<N> animais · <UA>,<UA> UA total' that updates as filters change (computed in-memory)."
    - "AnimaisScreen archived toggle: Switch labeled 'Mostrar arquivados' (default off). When on, archived animals appear with a Chip badge ('Vendido' / 'Morto' / 'Descartado')."
    - "Search uses 300ms debounce (Timer); list filters in-memory."
    - "List tile primary line '#42 · Vaca'; secondary line '<lot> · <paddock>'; tap → /animais/:id."
    - "Two empty states: 'Nenhum animal encontrado' (filters return zero) vs 'Nenhum animal cadastrado' (property has zero animals)."
    - "AnimalDetailScreen replaces the Plan 04 stub. AppBar '#<N> — <Categoria>'. Body: AnimalInfoCard + 2 placeholder sections ('Histórico Reprodutivo' = 'Disponível na Fase 5.', 'Histórico Sanitário' = 'Disponível na Fase 6.'). Veterinarian-only buttons: 'Editar animal' (opens AnimalEditDialog) + 'Dar baixa' (opens BaixaDialog, only if animal active)."
    - "AnimalEditDialog: read-only Número + Categoria; editable Raça (search-select kBreeds), EC (5 ChoiceChips), Observação. Submit calls AnimalRepository.updateAnimal."
    - "BaixaDialog: title 'Confirmar baixa do animal #<N>?'; SegmentedButton or RadioListTile group with 3 options ('Venda'/'Morte'/'Descarte'); Data field with date picker (default today, format dd/MM/aaaa); optional observação; confirm button uses colorScheme.error."
    - "BaixaDialog submit calls AnimalRepository.registerBaixa; on success invalidates animalByIdProvider(id) and animalListByPropertyProvider; animal stays visible with archived badge until next reload OR until 'Mostrar arquivados' toggle is turned off."
    - "Wave 0 widget tests (animais_screen_test, animal_edit_dialog_test, baixa_dialog_test) — `skip:` markers REMOVED — pass GREEN."
  artifacts:
    - path: "lib/features/animais/presentation/animais_screen.dart"
      provides: "AnimaisScreen — search + filters + list + archived toggle"
      min_lines: 250
    - path: "lib/features/animais/presentation/animal_detail_screen.dart"
      provides: "AnimalDetailScreen — full view + edit/baixa actions"
      min_lines: 180
    - path: "lib/features/animais/presentation/animal_edit_dialog.dart"
      provides: "AnimalEditDialog (raça, EC, observação)"
      min_lines: 120
    - path: "lib/features/animais/presentation/baixa_dialog.dart"
      provides: "BaixaDialog (motivo + data + observação)"
      min_lines: 130
  key_links:
    - from: "AnimaisScreen list tile"
      to: "/animais/:id"
      via: "context.go(AppRoutes.animalDetail(id))"
      pattern: "context\\.go\\(.*animalDetail"
    - from: "AnimalDetailScreen 'Editar animal' button"
      to: "AnimalEditDialog"
      via: "showDialog"
      pattern: "AnimalEditDialog"
    - from: "AnimalDetailScreen 'Dar baixa' button"
      to: "BaixaDialog"
      via: "showDialog"
      pattern: "BaixaDialog"
    - from: "BaixaDialog submit"
      to: "AnimalRepository.registerBaixa"
      via: "ref.read"
      pattern: "registerBaixa"
    - from: "AnimalEditDialog submit"
      to: "AnimalRepository.updateAnimal"
      via: "ref.read"
      pattern: "updateAnimal"
---

<objective>
Replace the AnimaisScreen placeholder and the AnimalDetailScreen stub with the real implementations: a global search/filter list and an individual animal record screen with edit + baixa flows. Then turn the three remaining Wave 0 widget-test files GREEN.

Purpose: Delivers the four animal-side requirements (ANIM-02 edit, ANIM-04 baixa, ANIM-05 search, ANIM-06 filter) plus PROP-05 (UA total visibility on AnimaisScreen).
Output: Search/filter screen + individual animal screen + edit & baixa dialogs + 3 widget-test files GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lots-animals-operational-core/03-CONTEXT.md
@.planning/phases/03-lots-animals-operational-core/03-RESEARCH.md
@.planning/phases/03-lots-animals-operational-core/03-UI-SPEC.md
@.planning/phases/03-lots-animals-operational-core/03-VALIDATION.md
@lib/features/animais/data/animal_model.dart
@lib/features/animais/data/animal_repository.dart
@lib/features/animais/data/animal_constants.dart
@lib/features/animais/presentation/animais_screen.dart
@lib/features/animais/presentation/animal_detail_screen.dart
@lib/features/animais/presentation/animal_form_dialog.dart
@lib/features/lotes/data/lote_model.dart
@lib/features/lotes/data/lote_repository.dart
@lib/features/piquetes/data/piquete_model.dart
@lib/features/piquetes/data/piquete_repository.dart
@lib/core/router/routes.dart
@lib/core/providers/current_property_provider.dart
@lib/features/auth/data/property_repository.dart
@lib/features/piquetes/presentation/piquetes_screen.dart
</context>

<interfaces>
<!-- AnimaisScreen public API (constructor unchanged) -->
```dart
class AnimaisScreen extends ConsumerStatefulWidget { const AnimaisScreen({super.key}); }
```

<!-- AnimalDetailScreen public API (constructor unchanged from Plan 04 stub) -->
```dart
class AnimalDetailScreen extends ConsumerWidget {
  const AnimalDetailScreen({super.key, required this.animalId});
  final String animalId;
}
```

<!-- AnimalEditDialog public API -->
```dart
class AnimalEditDialog extends ConsumerStatefulWidget {
  const AnimalEditDialog({super.key, required this.animal});
  final Animal animal;
}
```

<!-- BaixaDialog public API -->
```dart
class BaixaDialog extends ConsumerStatefulWidget {
  const BaixaDialog({super.key, required this.animal});
  final Animal animal;
}
```

<!-- Reuse from Plan 03 -->
- `Animal`, `AnimalWithContext` — `animal_model.dart`
- `AnimalRepository.updateAnimal`, `.registerBaixa` — `animal_repository.dart`
- `kCategories`, `kCategoryLabels`, `kCategoryLabelsPlural`, `kBreeds`, `BaixaReason`, `calcTotalUa` — `animal_constants.dart`
- Providers: `animalListByPropertyProvider` (returns `List<AnimalWithContext>`), `animalByIdProvider(id)`, `animalRepositoryProvider`
- `loteListByPaddockProvider` is per-paddock — for the global Lote dropdown we need ALL lots of the active property; helper added inline (or via a new provider in this plan).
- `paddockListProvider` (existing, Phase 2) — for the Piquete dropdown.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AnimaisScreen (search + filters + list + archived toggle) and turn animais_screen_test.dart GREEN</name>
  <files>lib/features/animais/presentation/animais_screen.dart, test/widget/animais_screen_test.dart</files>
  <read_first>
    - lib/features/animais/presentation/animais_screen.dart (current placeholder — replace entirely)
    - lib/features/piquetes/presentation/piquetes_screen.dart (`_canEdit` helper + empty-state pattern + provider invalidation pattern)
    - lib/features/animais/data/animal_repository.dart (animalListByPropertyProvider returns AnimalWithContext)
    - lib/features/piquetes/data/piquete_repository.dart (paddockListProvider for Piquete dropdown)
    - lib/features/lotes/data/lote_repository.dart (loteListByPaddockProvider — note this is per-paddock)
    - .planning/phases/03-lots-animals-operational-core/03-UI-SPEC.md § Screen 5 (full layout, copywriting, behavior)
    - .planning/phases/03-lots-animals-operational-core/03-CONTEXT.md § D-18/D-19/D-20/D-21
    - .planning/phases/03-lots-animals-operational-core/03-RESEARCH.md § "Pattern 7: In-memory Filter with Debounce" + § "Pitfall 5/6"
    - test/widget/animais_screen_test.dart (Wave 0 stubs to flip from skip)
  </read_first>
  <behavior>
    - Renders AppBar('Animais') + body Column.
    - Body order: SearchBar (full-width, hint 'Buscar por número...') → SizedBox(8) → filter row (FilterChips + 2 dropdowns, horizontal scroll) → SummaryBar → toggle row → Expanded(list).
    - SearchBar: leading icon = search; clear (X) icon visible when query non-empty; debounce 300ms via Timer.
    - 8 FilterChips: 'Todas' + 7 from kCategoriesLabels. Single-select. Selecting 'Todas' clears category filter.
    - Lote dropdown: items = 'Todos os lotes' + (all active lots in active property derived from animalListByPropertyProvider's `lotName` per AnimalWithContext OR from a fresh `lotListByPropertyProvider`).
    - Piquete dropdown: items = 'Todos os piquetes' + paddocks from `paddockListProvider`.
    - SummaryBar: Container with surfaceContainerHighest, padding (16h × 8v), Row(left: '<N> animais', right: '<UA>,<UA> UA total').
    - Archived toggle: 'Mostrar arquivados' Switch right-aligned. Default off — list filters out animals with deletedAt != null.
    - Empty state (no animals at all): icon Icons.pets_outlined size 64 onSurface@30%, heading 'Nenhum animal cadastrado', body 'Crie um lote no piquete para registrar os animais da fazenda.'.
    - Empty state (no filter matches): icon Icons.search_off size 48 onSurface@30%, heading 'Nenhum animal encontrado', body 'Tente ajustar os filtros ou a busca por número.'.
    - List tile (StatelessWidget): primary line as Text.rich `'#42'` weight 600 + `' · Vaca'` regular; secondary line `'Lote A · Piquete Norte'` bodyMedium onSurface@60%; trailing = archived badge if a.deletedAt != null AND showArchived; tap → context.go(AppRoutes.animalDetail(animal.id)).
    - Archived badge Chip text uses BaixaReason mapping: 'sale' → 'Vendido', 'death' → 'Morto', 'discard' → 'Descartado'.
  </behavior>
  <action>
1. Replace `lib/features/animais/presentation/animais_screen.dart` entirely. Use `ConsumerStatefulWidget`. Internal state:
   ```dart
   String _query = '';
   String? _category;       // null = 'Todas'
   String? _lotId;          // null = 'Todos os lotes'
   String? _paddockId;      // null = 'Todos os piquetes'
   bool _showArchived = false;
   Timer? _debounce;
   final _searchCtrl = TextEditingController();
   ```
   Define `_onSearchChanged(String v) { _debounce?.cancel(); _debounce = Timer(const Duration(milliseconds: 300), () { setState(() => _query = v.trim()); }); }`.
   In `dispose()`: `_debounce?.cancel(); _searchCtrl.dispose();`.

2. Build the body inside `build`:
   - `final animalsAsync = ref.watch(animalListByPropertyProvider);`
   - `final paddocksAsync = ref.watch(paddockListProvider);`
   - In data callback, derive: `final allLots = <String, String>{}; for (final a in animals) { allLots[a.animal.lotId] = a.lotName; }` (deduped lotId → lotName map).
   - Compute `final filtered = animals.where((aw) {`
       - `final a = aw.animal;`
       - `if (!_showArchived && a.deletedAt != null) return false;`
       - `if (_query.isNotEmpty && !a.number.toString().contains(_query)) return false;`
       - `if (_category != null && a.category != _category) return false;`
       - `if (_lotId != null && a.lotId != _lotId) return false;`
       - `if (_paddockId != null && aw.paddockId != _paddockId) return false;`
       - `return true;`
     `}).toList()..sort((a, b) => a.animal.number.compareTo(b.animal.number));`
   - SummaryBar text: `'${filtered.length} animais · ${calcTotalUa(filtered.map((aw)=>aw.animal)).toStringAsFixed(1).replaceAll('.', ',')} UA total'`.
   - Filter row: a horizontal `SingleChildScrollView` containing:
     ```dart
     Row(children: [
       FilterChip(label: const Text('Todas'), selected: _category == null, onSelected: (_) => setState(() => _category = null), showCheckmark: false),
       const SizedBox(width: 8),
       for (final c in kCategories) ...[
         FilterChip(label: Text(kCategoryLabels[c]!), selected: _category == c, onSelected: (sel) => setState(() => _category = sel ? c : null), showCheckmark: false),
         const SizedBox(width: 8),
       ],
       const SizedBox(width: 8),
       _LotDropdown(allLots: allLots, value: _lotId, onChanged: (v) => setState(() => _lotId = v)),
       const SizedBox(width: 8),
       _PaddockDropdown(paddocks: paddocksAsync.asData?.value ?? const [], value: _paddockId, onChanged: (v) => setState(() => _paddockId = v)),
     ])
     ```
   - Toggle row: `Padding(padding: EdgeInsets.symmetric(horizontal: 16, vertical: 4), child: Row(mainAxisAlignment: MainAxisAlignment.end, children: [Text('Mostrar arquivados'), Switch(value: _showArchived, onChanged: (v) => setState(() => _showArchived = v))]))`
   - List: `Expanded(child: filtered.isEmpty ? (animals.isEmpty ? _EmptyAllState() : _EmptyFilterState()) : ListView.builder(itemCount: filtered.length, itemBuilder: (ctx, i) => _AnimalListTile(item: filtered[i], showArchived: _showArchived)))`
   - SearchBar: `Padding(padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8), child: SearchBar(controller: _searchCtrl, hintText: 'Buscar por número...', leading: const Icon(Icons.search), trailing: _query.isEmpty ? null : [IconButton(onPressed: () { _searchCtrl.clear(); setState(() => _query = ''); }, icon: const Icon(Icons.close))], onChanged: _onSearchChanged))`

3. Define private widgets (same file):
   - `_LotDropdown extends StatelessWidget` rendering `DropdownButton<String?>` with `'Todos os lotes'` for the null option, then one item per `allLots.entries`.
   - `_PaddockDropdown extends StatelessWidget` for paddocks (uses `paddock.name` and `paddock.id`).
   - `_AnimalListTile extends StatelessWidget` taking `AnimalWithContext` + `showArchived` flag. Renders ListTile with the formatted primary/secondary text + trailing badge if archived. tap → `context.go(AppRoutes.animalDetail(item.animal.id))`.
   - `_ArchiveBadge extends StatelessWidget` taking the baixaReason db value and rendering `Chip(label: Text(_label(baixaReason)))` where `_label('sale')='Vendido', 'death'='Morto', 'discard'='Descartado'`.
   - `_EmptyAllState` and `_EmptyFilterState` per UI-SPEC copywriting.

4. Flip Wave 0 `test/widget/animais_screen_test.dart` from skip to GREEN. Use `ProviderScope(overrides: [animalListByPropertyProvider.overrideWith((ref) async => _fakeData), paddockListProvider.overrideWith((ref) async => _fakePaddocks)])` to inject test data. Each test removes `, skip:` and asserts the documented UI fragment.

   Recommended test bodies (assertion-only — no end-to-end flows needed):
   - search bar with hint: `expect(find.text('Buscar por número...'), findsOneWidget)`
   - typing '4' filters: enter '4' in SearchBar, advance time by 350ms via `await tester.pump(Duration(milliseconds: 350))`, then `expect(find.textContaining('#4'), findsWidgets)`.
   - clear button visible: enter '4', pump, `expect(find.byIcon(Icons.close), findsOneWidget)`.
   - filter chips: `for (final lbl in const ['Todas','Vaca','Novilha','Terneiro','Terneira','Touro','Boi','Novilho']) expect(find.widgetWithText(FilterChip, lbl), findsOneWidget);`
   - selecting 'Vaca' filters: tap 'Vaca' chip, pump, count list tiles match the fake-data subset of category=='vaca'.
   - Lote dropdown default 'Todos os lotes': `expect(find.text('Todos os lotes'), findsOneWidget)`.
   - Piquete dropdown default 'Todos os piquetes': `expect(find.text('Todos os piquetes'), findsOneWidget)`.
   - SummaryBar formatting: `expect(find.textContaining('animais ·'), findsOneWidget); expect(find.textContaining('UA total'), findsOneWidget);`
   - "Mostrar arquivados" toggle: tap Switch, archived animals appear with badge text 'Vendido' / 'Morto' / 'Descartado'.
   - Tile format: `expect(find.textContaining('#'), findsWidgets); expect(find.textContaining(' · '), findsWidgets);`
  </action>
  <verify>
    <automated>flutter analyze lib/features/animais && flutter test test/widget/animais_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/animais/presentation/animais_screen.dart` exists with size > 6000 bytes
    - `grep -c "ConsumerStatefulWidget" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Buscar por número" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Todos os lotes" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Todos os piquetes" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Mostrar arquivados" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Nenhum animal encontrado" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Nenhum animal cadastrado" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Tente ajustar os filtros ou a busca por número" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Crie um lote no piquete para registrar os animais da fazenda" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Duration(milliseconds: 300)" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "calcTotalUa" lib/features/animais/presentation/animais_screen.dart` returns >= 1
    - `grep -c "context.go(AppRoutes.animalDetail" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Vendido" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Morto" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -c "Descartado" lib/features/animais/presentation/animais_screen.dart` returns 1
    - `grep -L "skip:" test/widget/animais_screen_test.dart` returns the file
    - `flutter analyze lib/features/animais` reports `No issues found!`
    - `flutter test test/widget/animais_screen_test.dart` exits 0
  </acceptance_criteria>
  <done>AnimaisScreen ships search + filters + summary + archived toggle, GREEN widget tests, lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AnimalDetailScreen + AnimalEditDialog + BaixaDialog and turn the remaining 2 widget-test stubs GREEN</name>
  <files>lib/features/animais/presentation/animal_detail_screen.dart, lib/features/animais/presentation/animal_edit_dialog.dart, lib/features/animais/presentation/baixa_dialog.dart, test/widget/animal_edit_dialog_test.dart, test/widget/baixa_dialog_test.dart</files>
  <read_first>
    - lib/features/animais/presentation/animal_detail_screen.dart (Plan 04 stub — replace, keep constructor)
    - lib/features/animais/presentation/animal_form_dialog.dart (Plan 05 — pattern for ChoiceChip row, dialog scaffolding)
    - lib/features/animais/data/animal_repository.dart (`updateAnimal`, `registerBaixa` signatures)
    - lib/features/lotes/data/lote_repository.dart (`loteByIdProvider` for the lote name link)
    - lib/features/piquetes/data/piquete_repository.dart (`paddockByIdProvider`)
    - lib/features/piquetes/presentation/piquetes_screen.dart (`_canEdit` pattern)
    - .planning/phases/03-lots-animals-operational-core/03-UI-SPEC.md § Screen 6 (AnimalDetailScreen layout) + § Screen 7 (AnimalEditDialog) + § Screen 8 (BaixaDialog)
    - .planning/phases/03-lots-animals-operational-core/03-CONTEXT.md § D-13/D-14/D-16/D-17/D-22
    - test/widget/animal_edit_dialog_test.dart and test/widget/baixa_dialog_test.dart (Wave 0 stubs)
  </read_first>
  <behavior>
    - AnimalDetailScreen: AppBar title `'#${animal.number} — ${kCategoryLabels[animal.category]}'`. Body ListView padding 16: AnimalInfoCard, then SizedBox(16), then `_PlaceholderSection(title: 'Histórico Reprodutivo', body: 'Disponível na Fase 5.')`, SizedBox(16), `_PlaceholderSection(title: 'Histórico Sanitário', body: 'Disponível na Fase 6.')`. Loading/error/null guards on animalByIdProvider.
    - AnimalInfoCard fields: Número, Categoria, Raça (or '—'), Estado Corporal ('3 / 5' or '—'), Lote atual (tappable → /lotes/:id), Piquete atual (tappable → /piquetes/:id), Cadastrado em (DateFormat dd/MM/yyyy pt_BR), Status ('Ativo' green badge OR 'Arquivado — Vendido em DD/MM/AAAA' error color).
    - Veterinarian-only buttons inside the card footer: OutlinedButton 'Editar animal' (always visible if can edit) + TextButton 'Dar baixa' colorScheme.error (visible only if animal is active, i.e. deletedAt == null).
    - AnimalEditDialog: read-only display of Número (e.g. '#42') and Categoria; editable Raça (DropdownButtonFormField<String?> with 'Sem raça' + kBreeds), EC chip row (5 ChoiceChips, deselectable to null), Observação multi-line. Submit calls `updateAnimal(id, breed: _breed, bodyCondition: _ec, observation: _obsNullIfEmpty)`. On success invalidates `animalByIdProvider(animal.id)` and `animalListByPropertyProvider`, pops with true. SnackBar 'Erro ao salvar animal. Tente novamente.' on failure.
    - BaixaDialog: title `'Confirmar baixa do animal #${animal.number}?'`. Body: 'Esta ação registra o animal como arquivado. O histórico é preservado.' (italic / bodyMedium); SegmentedButton<BaixaReason> labeled 'Venda'/'Morte'/'Descarte' (showSelectedIcon: false); Data field (TextFormField with date picker IconButton, default DateTime.now(), formatter dd/MM/aaaa); Observação multi-line (optional). Submit calls `registerBaixa(id, reason, date, observation)`. Confirm button text 'Confirmar baixa' with FilledButton.styleFrom backgroundColor: colorScheme.error, foregroundColor: colorScheme.onError. On success invalidate providers, pop with true.
    - Test files: every `, skip:` removed; assertions per the documented behavior, mounted inside ProviderScope with appropriate overrides.
  </behavior>
  <action>
1. Replace `lib/features/animais/presentation/animal_detail_screen.dart` with the full implementation.

   Use the same `_canEdit` helper (private function at file scope) from PiquetesScreen. Watch `animalByIdProvider(animalId)`, `currentPropertyProvider`, `memberPropertiesProvider`. The current lot/paddock for tile links comes from watching `loteByIdProvider(animal.lotId)` then `paddockByIdProvider(lot.paddockId)`.

   Date formatting: `DateFormat('dd/MM/yyyy', 'pt_BR').format(date)` from `package:intl/intl.dart`. Initialize date formatting in `initializeDateFormatting('pt_BR', null)` once — likely already done at app bootstrap; if not, call `initializeDateFormatting` lazily in main.dart (out of scope here — assume done).

   `AnimalInfoCard`:
   - Card with `colorScheme.surfaceContainerHighest`, padding 16.
   - Internal `Column` of rows. Each row uses a small `_KvRow(label: '...', value: Widget)` private builder (label width 120, then Expanded value).
   - Status row uses `Container(padding: EdgeInsets.symmetric(h: 8, v:2), decoration: BoxDecoration(color: status==active ? Colors.green.shade100 : colorScheme.errorContainer, borderRadius: BorderRadius.circular(12)), child: Text(...))`.
   - Footer Row aligned end: `[OutlinedButton(onPressed: canEdit ? () => _openEdit(...) : null, child: Text('Editar animal')), const SizedBox(width: 8), if (animal.deletedAt == null && canEdit) TextButton(style: TextButton.styleFrom(foregroundColor: colorScheme.error), onPressed: () => _openBaixa(...), child: const Text('Dar baixa'))]`.

   `_PlaceholderSection`: Card with surface color + 1px outline at 38% alpha; padding 16; Column with title (titleMedium) + 4px gap + body (bodyMedium onSurface@60%).

2. Create `lib/features/animais/presentation/animal_edit_dialog.dart`. Follow the AnimalFormDialog skeleton from Plan 05 but:
   - Constructor takes the `Animal animal` and pre-fills `_breed = animal.breed`, `_ec = animal.bodyCondition`, `_obsCtrl.text = animal.observation ?? ''`.
   - Read-only display rows for Número and Categoria at the top (use `_KvRow` style or simple Text widgets with subdued color).
   - Submit calls:
     ```dart
     await ref.read(animalRepositoryProvider).updateAnimal(
       id: widget.animal.id,
       breed: _breed,
       bodyCondition: _ec,
       observation: _obsCtrl.text.trim().isEmpty ? null : _obsCtrl.text.trim(),
     );
     ref.invalidate(animalByIdProvider(widget.animal.id));
     ref.invalidate(animalListByPropertyProvider);
     ```
   - Title 'Editar animal'; submit button text 'Salvar'; cancel 'Cancelar'.

3. Create `lib/features/animais/presentation/baixa_dialog.dart`. State: `BaixaReason? _reason;`, `DateTime _date = DateTime.now();`, `_obsCtrl`. Date field renders the formatted date in a TextField (read-only) with a calendar IconButton suffix that calls `showDatePicker(context: context, initialDate: _date, firstDate: DateTime(2000), lastDate: DateTime.now(), locale: const Locale('pt','BR'))` and updates _date.

   Body: `Column(children: [Text('Esta ação registra o animal como arquivado. O histórico é preservado.', style: theme.textTheme.bodyMedium), const SizedBox(height:16), SegmentedButton<BaixaReason>(segments: [for (final r in BaixaReason.values) ButtonSegment(value: r, label: Text(r.label))], selected: _reason == null ? <BaixaReason>{} : {_reason!}, onSelectionChanged: (s) => setState(() => _reason = s.firstOrNull), emptySelectionAllowed: true), const SizedBox(height:16), TextFormField(read-only date field), const SizedBox(height:16), TextFormField(observation multi-line)])`.

   Submit:
   - Validate _reason != null else SnackBar 'Selecione o motivo da baixa'.
   - `await ref.read(animalRepositoryProvider).registerBaixa(id: widget.animal.id, reason: _reason!, date: _date, observation: ...);`
   - `ref.invalidate(animalByIdProvider(widget.animal.id)); ref.invalidate(animalListByPropertyProvider);`
   - `Navigator.pop(context, true);`
   On error: SnackBar 'Erro ao registrar baixa. Tente novamente.'

4. Flip `test/widget/animal_edit_dialog_test.dart`:
   - Build a sample `Animal` (use minimal required fields).
   - Mount the dialog inside `ProviderScope(overrides: [animalRepositoryProvider.overrideWithValue(_FakeRepo())])`.
   - Each test removes its skip and asserts via find.
     - 'Número' read-only: `expect(find.text('#42'), findsOneWidget)`.
     - 'Categoria' read-only: `expect(find.text('Vaca'), findsOneWidget)`.
     - Raça dropdown shows kBreeds: open dropdown, expect `find.text('Nelore')`.
     - 5 EC chips: `expect(find.byType(ChoiceChip), findsNWidgets(5))`.
     - 3-line observation field present: `expect(find.byType(TextFormField), findsWidgets);` plus check `maxLines: 3` via `tester.widget<TextFormField>(...)`.
     - Buttons 'Cancelar' and 'Salvar' present.

5. Flip `test/widget/baixa_dialog_test.dart`:
   - Mount with `animalRepositoryProvider` overridden.
   - Title 'Confirmar baixa do animal #42?': `expect(find.text('Confirmar baixa do animal #42?'), findsOneWidget)`.
   - 3 reason segments: `for (final lbl in const ['Venda', 'Morte', 'Descarte']) expect(find.text(lbl), findsOneWidget);`
   - Date field default today (formatted dd/MM/aaaa): assert the formatted-today string appears.
   - Confirm button label 'Confirmar baixa' with error background: tap submit without selecting motivo → expect SnackBar 'Selecione o motivo da baixa'.
   - Confirm button color check: `final btn = tester.widget<FilledButton>(find.widgetWithText(FilledButton, 'Confirmar baixa')); expect(btn.style?.backgroundColor?.resolve(<MaterialState>{}), isNotNull);` (or simply confirm the button exists — color resolution is finicky in unit tests).
  </action>
  <verify>
    <automated>flutter analyze lib/features/animais && flutter test test/widget/animal_edit_dialog_test.dart test/widget/baixa_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/features/animais/presentation/animal_detail_screen.dart` size > 4500 bytes (replaced stub)
    - `grep -c "AnimalEditDialog" lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "BaixaDialog" lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "Disponível na Fase 5." lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "Disponível na Fase 6." lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "Editar animal" lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "Dar baixa" lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "loteByIdProvider" lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - `grep -c "paddockByIdProvider" lib/features/animais/presentation/animal_detail_screen.dart` returns 1
    - File `lib/features/animais/presentation/animal_edit_dialog.dart` exists
    - `grep -c "class AnimalEditDialog" lib/features/animais/presentation/animal_edit_dialog.dart` returns 1
    - `grep -c "updateAnimal" lib/features/animais/presentation/animal_edit_dialog.dart` returns 1
    - `grep -c "kBreeds" lib/features/animais/presentation/animal_edit_dialog.dart` returns 1
    - `grep -c "ChoiceChip" lib/features/animais/presentation/animal_edit_dialog.dart` returns >= 1
    - `grep -c "Salvar" lib/features/animais/presentation/animal_edit_dialog.dart` returns 1
    - File `lib/features/animais/presentation/baixa_dialog.dart` exists
    - `grep -c "class BaixaDialog" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -c "registerBaixa" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -c "BaixaReason" lib/features/animais/presentation/baixa_dialog.dart` returns >= 1
    - `grep -c "Venda" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -c "Morte" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -c "Descarte" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -c "Confirmar baixa" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -c "showDatePicker" lib/features/animais/presentation/baixa_dialog.dart` returns 1
    - `grep -L "skip:" test/widget/animal_edit_dialog_test.dart` returns the file
    - `grep -L "skip:" test/widget/baixa_dialog_test.dart` returns the file
    - `flutter analyze lib/features/animais` reports `No issues found!`
    - `flutter test test/widget/animal_edit_dialog_test.dart test/widget/baixa_dialog_test.dart` exits 0
  </acceptance_criteria>
  <done>AnimalDetailScreen, AnimalEditDialog, BaixaDialog all ship; 2 widget tests GREEN with no skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → AnimaisScreen filter / search | Pure client-side filtering of data already fetched under RLS — no new trust boundary; the rows came from authorized SELECT. |
| User → AnimalDetailScreen actions (Editar / Dar baixa) | Action buttons absent for non-veterinarians (`_canEdit`). UPDATE policies at DB enforce role independent of UI. |
| Edit/baixa dialog → Repository | Repository only sends the explicit fields documented in the API; UI cannot inject extra columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-21 | Tampering | Reader/owner uses devtools to call AnimalEditDialog or BaixaDialog | mitigate | RLS UPDATE policies (Plan 02) require veterinarian role and active deleted_at IS NULL — UPDATE rejected by Postgres regardless of UI. |
| T-3-22 | Tampering | BaixaDialog reused on already-archived animal (double-baixa) | mitigate | The 'Dar baixa' button is rendered ONLY when `animal.deletedAt == null`. RLS UPDATE policy `veterinarian_can_update_active_animal` also enforces `deleted_at IS NULL` — Postgres rejects the second baixa. |
| T-3-23 | Information Disclosure | Search query leaks via URL or analytics | accept | Search is client-side state only; never serialized to URL or sent to analytics. No PII in numbers. |
| T-3-24 | Tampering | AnimalDetailScreen reached via deep link to other tenant's animal id | mitigate | `animalByIdProvider` calls PostgREST under RLS — non-member receives null; screen shows 'Animal não encontrado'. |
| T-3-25 | DoS | Filter computation on a property with 10k+ animals causes UI jank | accept | MVP property sizes are hundreds to low thousands (RESEARCH §A2). If we hit the cap, server-side `.ilike` search is the documented fallback. |
</threat_model>

<verification>
After both tasks:
```bash
flutter analyze lib/features/animais
flutter test
```
Manual smoke (web): `flutter run -d chrome` → /animais → search '4', filter by 'Vaca', open detail of #4 → 'Editar animal' → change EC, save → list reflects EC subtitle. From animal detail, 'Dar baixa' → choose 'Venda' + today → confirm. Animal stays visible (toggle off → animal hides; toggle on → archived badge 'Vendido' appears).

Phase-3 success criteria SC-1..SC-5 are now end-to-end verifiable across Plans 02–06.
</verification>

<success_criteria>
- AnimaisScreen replaces the placeholder; search + 8 chips + 2 dropdowns + summary + archived toggle all present per UI-SPEC.
- AnimalDetailScreen replaces the Plan 04 stub; renders the documented info card + 2 placeholder sections.
- AnimalEditDialog updates breed / EC / observation only.
- BaixaDialog records the soft-delete with reason + date.
- All three Wave 0 widget-test stubs (animais_screen_test, animal_edit_dialog_test, baixa_dialog_test) GREEN with no `skip:` markers.
- `flutter analyze` reports zero new issues; `flutter test` (full suite) is green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lots-animals-operational-core/03-06-SUMMARY.md` covering:
- AnimaisScreen layout + provider set
- AnimalDetailScreen sections + role-gated actions
- AnimalEditDialog and BaixaDialog flows
- Wave 0 test transitions (animais_screen / animal_edit / baixa — skip → green)
- Decision references: D-13, D-14, D-16, D-17, D-18, D-19, D-20, D-21, D-22
- Confirmation that Phase-3 SC-1..SC-5 all have an end-to-end path through Plans 02–06.
</output>
