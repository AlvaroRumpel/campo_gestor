---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 09
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - lib/features/dashboard/presentation/dashboard_screen.dart
  - test/widget/dashboard_screen_test.dart
autonomous: true
requirements: [MEMB-01]

must_haves:
  truths:
    - "Um usuário que já é membro de alguma fazenda vê seus convites pendentes no dashboard — o único outro caminho, `/sem-acesso`, é inalcançável para ele porque o router só o manda para lá quando não há nenhuma membership"
    - "Sem convites pendentes, o dashboard renderiza exatamente como antes — nenhum espaço reservado, nenhum banner vazio"
    - "O banner de convite aparece nos dois layouts (mobile e desktop) e independe do papel do usuário na fazenda ativa"
    - "O banner de convite reusa `InviteBanner` — nenhuma lógica de aceitar/recusar é reescrita aqui"
  artifacts:
    - lib/features/dashboard/presentation/dashboard_screen.dart
    - test/widget/dashboard_screen_test.dart
  key_links:
    - "`myInvitesProvider` → banner no topo da coluna principal, acima de `_AlertsBanner`"
    - "Aceitar pelo dashboard invalida `memberPropertiesProvider`, que atualiza o seletor de fazenda do header"
  prohibitions:
    - "Não esconder o banner de convite por papel — `_AlertsBanner` é vet-only por causa da ação que ele exige; um convite é pessoal e independe do papel na fazenda atualmente ativa"
    - "Não duplicar os handlers de aceitar/recusar — eles vivem em `InviteBanner` (plano 10-04)"
    - "Não alterar o comportamento nem a ordem de `_AlertsBanner`, `_LotacaoCard`, `_PrenhezCard` e `_GastosCard`"
---

<objective>
Surfacear convites pendentes no dashboard, para quem já é membro de pelo menos uma fazenda.

Purpose: o router só manda para `/sem-acesso` quem não tem **nenhuma** membership. Um veterinário que já tem a própria fazenda e é convidado para uma segunda nunca passaria por aquela tela — sem este banner, o convite dele seria invisível e MEMB-01 ficaria pela metade.
Output: 1 tela modificada + testes de widget estendidos.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-04-SUMMARY.md
@lib/features/dashboard/presentation/dashboard_screen.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Banner de convites pendentes no dashboard (mobile e desktop)</name>
  <files>lib/features/dashboard/presentation/dashboard_screen.dart, test/widget/dashboard_screen_test.dart</files>
  <read_first>
    - `lib/features/dashboard/presentation/dashboard_screen.dart` (linhas 34–80 e 681–735) — os dois pontos de inserção: em `_MobileDashboard`, o bloco `if (isVet && alerts.isNotEmpty) ...[ _AlertsBanner(alerts: alerts), SizedBox(height: 12) ]` dentro do `Padding(EdgeInsets.all(14))`; em `_DesktopDashboard`, o bloco equivalente dentro do `Expanded(flex: 125)` com `SizedBox(height: 16)`
    - `lib/features/membros/presentation/invite_banner.dart` — `InviteBanner(invite:)` criado no plano 10-04
    - `lib/features/membros/data/membro_repository.dart` — `myInvitesProvider`
    - `test/widget/dashboard_screen_test.dart` — a estrutura de overrides já usada para montar o dashboard nos testes
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — linha "Dashboard banner — novo convite" do Copywriting Contract
  </read_first>
  <behavior>
    - Com `myInvitesProvider` vazio, a árvore do dashboard mobile não contém nenhum `InviteBanner` e a ordem dos cartões existentes é idêntica à de antes
    - Com um convite pendente, o dashboard mobile contém um `InviteBanner` posicionado acima de `_AlertsBanner`
    - Com dois convites pendentes, aparecem dois `InviteBanner`
    - No desktop (largura ≥ `Breakpoints.rail`), o mesmo banner aparece na coluna principal, acima de `_AlertsBanner`
    - O banner aparece mesmo quando `isVeterinarian` é false para a fazenda ativa (convite é pessoal, não depende do papel atual)
    - Com `myInvitesProvider` em erro ou carregando, nenhum banner é renderizado e o dashboard não quebra
    - Todos os testes já existentes em `test/widget/dashboard_screen_test.dart` continuam passando
  </behavior>
  <action>
Em `_MobileDashboard.build`, ler `final invites = ref.watch(myInvitesProvider).asData?.value ?? const <MyInvite>[];` — o padrão `.asData?.value ?? const []` é o mesmo já usado para `alerts` na linha 39, e cobre de graça os estados de carregando e erro (lista vazia → nada renderizado).

Dentro do `Column` do `Padding(EdgeInsets.all(14))`, **antes** do bloco de `_AlertsBanner`, inserir: `for (final invite in invites) ...[ InviteBanner(invite: invite), const SizedBox(height: 12) ]`. Sem gate de papel — comentário no código registrando o contraste com `_AlertsBanner` (que é vet-only porque exige uma ação vet-only para se resolver, enquanto um convite é endereçado à pessoa e independe do papel na fazenda atualmente ativa).

Em `_DesktopDashboard.build`, a mesma leitura e o mesmo laço dentro do `Column` do `Expanded(flex: 125)`, antes do bloco de `_AlertsBanner`, usando `const SizedBox(height: 16)` como separador (o espaçamento desktop já em uso ali).

Nada mais muda: `_AlertsBanner`, `_LotacaoCard`, `_PrenhezCard`, `_GastosCard`, headers e KPIs ficam intocados, na mesma ordem.

Estender `test/widget/dashboard_screen_test.dart` com os sete casos de `<behavior>`, adicionando override de `myInvitesProvider` (e de `membroRepositoryProvider` quando o caso exercitar o aceite) ao conjunto de overrides já usado no arquivo. Os casos existentes precisam continuar montando — se o `myInvitesProvider` real tentar bater no Supabase durante os testes antigos, adicionar o override com lista vazia no helper de montagem compartilhado do arquivo.
  </action>
  <verify>
    <automated>flutter test test/widget/dashboard_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/dashboard_screen_test.dart` passa, incluindo todos os casos que já existiam antes deste plano
    - Existe um caso assertando `find.byType(InviteBanner)` com `findsNothing` quando não há convites
    - Existe um caso assertando `findsNWidgets(2)` com dois convites
    - Existe um caso desktop (superfície ≥ 1024px) assertando a presença do banner
    - Existe um caso com papel não-veterinário assertando que o banner de convite aparece mesmo assim
    - O laço de convites aparece exatamente duas vezes em `dashboard_screen.dart` (uma no mobile, uma no desktop) e nenhum handler de aceitar/recusar é definido neste arquivo
    - `flutter analyze` limpo e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>Convite chega a quem já tem fazenda — MEMB-01 coberto nos dois caminhos de entrega.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tela → `list_my_invites` | Retorno do servidor é a única fonte dos convites exibidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-30 | Information Disclosure | Convite de outra pessoa aparecendo no dashboard | high | mitigate | `list_my_invites` filtra por `current_user_email()` no servidor; a tela não passa nenhum filtro (plano 10-01) |
| T-10-31 | Denial of Service | Falha na carga de convites derrubando o dashboard inteiro | medium | mitigate | Padrão `.asData?.value ?? const []` — estado de erro/carregando degrada para "nenhum banner", sem propagar exceção |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `flutter test` verde (suíte completa, incluindo os casos pré-existentes de dashboard)
- `flutter analyze` limpo
</verification>

<success_criteria>
O dashboard surfaceia convites pendentes nos dois layouts, sem gate de papel, sem alterar nada do que já existia quando não há convites.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: laço de `InviteBanner` em `_MobileDashboard` e `_DesktopDashboard`, em `lib/features/dashboard/presentation/dashboard_screen.dart`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-09-SUMMARY.md` when done
</output>
