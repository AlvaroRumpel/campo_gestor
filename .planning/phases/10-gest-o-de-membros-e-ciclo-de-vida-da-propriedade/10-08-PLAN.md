---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 08
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - lib/features/auth/presentation/no_access_screen.dart
  - test/widget/no_access_screen_test.dart
autonomous: true
requirements: [MEMB-01]

must_haves:
  truths:
    - "`/sem-acesso` deixa de mandar o usuário 'entrar em contato com o proprietário' e passa a listar os convites pendentes endereçados ao e-mail da conta logada"
    - "Convites do usuário em `/sem-acesso` vazio renderiza EmptyState com Icons.mail_outline e a copy 'Nenhum convite no momento'"
    - "As duas ações existentes da tela ('Criar minha fazenda' e 'Sair') continuam disponíveis nos dois estados, com e sem convites"
    - "A tela reusa `InviteBanner` — nenhuma lógica de aceitar/recusar é reescrita aqui"
  artifacts:
    - lib/features/auth/presentation/no_access_screen.dart
    - test/widget/no_access_screen_test.dart
  key_links:
    - "`myInvitesProvider` (RPC `list_my_invites`) → é o único caminho pelo qual o convidado enxerga o **nome** da fazenda, já que ainda não é membro e a RLS de `properties` o esconderia"
    - "`InviteBanner` → `ref.invalidate(memberPropertiesProvider)` → `_RouterRefreshNotifier` → o redirect leva o usuário a `/dashboard` sozinho"
  prohibitions:
    - "Não duplicar os handlers de aceitar/recusar aqui — eles vivem em `InviteBanner` (plano 10-04)"
    - "Não remover os botões 'Criar minha fazenda' e 'Sair' — sem eles a tela vira um beco sem saída"
---

<objective>
Transformar `/sem-acesso` de um beco sem saída informativo na caixa de entrada de convites do usuário: listar os convites pendentes endereçados ao e-mail da conta, renderizando um `InviteBanner` por convite.

Purpose: sem e-mail transacional no MVP (decisão travada), esta tela é o principal ponto de entrega de um convite — é para cá que o router manda todo usuário sem nenhuma membership.
Output: 1 tela modificada + 1 arquivo de teste de widget novo.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-CONTEXT.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-04-SUMMARY.md
@lib/features/auth/presentation/no_access_screen.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lista de convites, estado vazio e estado de erro em /sem-acesso</name>
  <files>lib/features/auth/presentation/no_access_screen.dart, test/widget/no_access_screen_test.dart</files>
  <read_first>
    - `lib/features/auth/presentation/no_access_screen.dart` (arquivo inteiro, 43 linhas) — a estrutura atual: `ConsumerWidget` > `Scaffold` > `SafeArea` > `EmptyState` com `action` contendo os botões "Criar minha fazenda" (que faz `context.push(AppRoutes.propriedades)`) e "Sair" (que faz `clear()` + `signOut()`)
    - `lib/core/widgets/ui.dart` (linhas 147–200 e 478–508) — `EmptyState` e `ErrorRetry`
    - `lib/features/membros/presentation/invite_banner.dart` — `InviteBanner(invite:)` criado no plano 10-04
    - `lib/features/membros/data/membro_repository.dart` — `myInvitesProvider`
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — "Empty state — convites do usuário (`/sem-acesso`)"
  </read_first>
  <behavior>
    - Com `myInvitesProvider` resolvendo lista vazia, a tela mostra `EmptyState` com o título "Nenhum convite no momento" e o corpo "Peça para um veterinário da fazenda te convidar pelo seu e-mail de cadastro." — e a copy antiga "Entre em contato com o proprietário da fazenda para receber acesso." não está mais na árvore
    - No estado vazio, os botões "Criar minha fazenda" e "Sair" continuam presentes e funcionais
    - Com dois convites, a árvore contém dois `InviteBanner`
    - No estado com convites, "Criar minha fazenda" e "Sair" continuam presentes
    - Com `myInvitesProvider` em erro, aparece `ErrorRetry` com "Erro ao carregar. Verifique sua conexão e tente novamente." e tocar em "Tentar novamente" invalida o provider
    - Enquanto `myInvitesProvider` carrega, aparece um `CircularProgressIndicator`
    - A 360px de largura, dois convites com nome de fazenda longo não geram exceção de overflow
  </behavior>
  <action>
Reescrever o corpo de `NoAccessScreen`, mantendo-a `ConsumerWidget`. `Scaffold` > `SafeArea` permanecem; dentro, observar `myInvitesProvider` com `.when(...)`.

Extrair os dois botões existentes num método/getter privado `_exitActions(BuildContext, WidgetRef)` devolvendo a `Column` atual (FilledButton "Criar minha fazenda" com `context.push(AppRoutes.propriedades)`, `SizedBox(height: 8)`, TextButton "Sair" com `clear()` + `signOut()`) — assim os dois estados a reusam sem duplicação.

`loading`: `Center(child: CircularProgressIndicator())`.
`error`: `Center(child: ErrorRetry(message: 'Erro ao carregar. Verifique sua conexão e tente novamente.', onRetry: () => ref.invalidate(myInvitesProvider)))`.
`data` vazio: o `EmptyState` atual com `icon: Icons.mail_outline`, `title: 'Nenhum convite no momento'`, `message: 'Peça para um veterinário da fazenda te convidar pelo seu e-mail de cadastro.'` e `action: _exitActions(...)`.
`data` não vazio: `ListView(padding: EdgeInsets.all(16))` com, no topo, `Text('Convites pendentes', style: 16/w700)`, `SizedBox(height: 12)`, depois um `InviteBanner(invite: i)` por convite separado por `SizedBox(height: 10)`, depois `SizedBox(height: 24)` e `_exitActions(...)`.

Nenhum handler de aceitar/recusar neste arquivo — `InviteBanner` já os traz.

Criar `test/widget/no_access_screen_test.dart` cobrindo os sete casos de `<behavior>`, com override de `myInvitesProvider` e do `membroRepositoryProvider`.
  </action>
  <verify>
    <automated>flutter test test/widget/no_access_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/no_access_screen_test.dart` passa com no mínimo 7 casos
    - Existe um caso assertando `findsNothing` para o trecho "Entre em contato com o proprietário"
    - Existe um caso assertando "Nenhum convite no momento" e "Criar minha fazenda" juntos no estado vazio
    - Existe um caso assertando `find.byType(InviteBanner)` com `findsNWidgets(2)`
    - Existe um caso a 360px (`setSurfaceSize`) sem exceção de overflow
    - `no_access_screen.dart` não contém nenhuma chamada a `acceptInvite` nem a `declineInvite`
    - `grep -rn "Entre em contato com o proprietário" lib/` não retorna nada
    - `flutter analyze` limpo e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>MEMB-01 fechado no lado do convidado sem acesso: convite entregue in-app, com estado vazio e erro autorados, e a tela nunca vira beco sem saída.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tela → `list_my_invites` | Retorno do servidor é a única fonte dos ids de convite exibidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-28 | Information Disclosure | Convite de fazenda arquivada oferecido ao usuário | low | mitigate | `list_my_invites` filtra `p.deleted_at IS NULL` (plano 10-01) |
| T-10-29 | Denial of Service | Usuário preso em `/sem-acesso` sem saída quando a carga de convites falha | medium | mitigate | `_exitActions` renderizado nos três estados, incluindo o de erro via `ErrorRetry` + botões |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `flutter test` verde (suíte completa)
- `flutter analyze` limpo
- A copy antiga "Entre em contato com o proprietário" não existe mais em `lib/`
</verification>

<success_criteria>
`/sem-acesso` entrega os convites pendentes do e-mail logado usando `InviteBanner`, com estado vazio, carregando e erro autorados, e mantendo as duas saídas existentes da tela.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: `NoAccessScreen` reescrita (com `_exitActions`) em `lib/features/auth/presentation/no_access_screen.dart` e `test/widget/no_access_screen_test.dart`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-08-SUMMARY.md` when done
</output>
