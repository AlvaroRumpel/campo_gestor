---
phase: 10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade
plan: 07
type: execute
wave: 2
depends_on: ["10-06"]
files_modified:
  - lib/features/propriedades/presentation/propriedades_screen.dart
  - test/widget/propriedades_screen_test.dart
autonomous: true
requirements: [PROPV-01, PROPV-02]

must_haves:
  truths:
    - "A tela de Propriedades tem um alternador Ativas / Arquivadas; a aba Arquivadas lista as fazendas com `deleted_at` preenchido"
    - "Aba Arquivadas sem fazendas renderiza EmptyState com Icons.archive_outlined e a copy 'Nenhuma fazenda arquivada'"
    - "Arquivar exige digitar o nome exato da fazenda antes de o botão habilitar"
    - "Cartão arquivado renderiza esmaecido e troca o menu de contexto por um único botão 'Restaurar fazenda'"
    - "Restaurar devolve a fazenda à aba Ativas e ao seletor de propriedade (invalida `propertyListProvider`, `archivedPropertyListProvider` e `memberPropertiesProvider`)"
  artifacts:
    - lib/features/propriedades/presentation/propriedades_screen.dart
    - test/widget/propriedades_screen_test.dart
  key_links:
    - "`ArchiveConfirmDialog` → `softDeleteProperty` → invalidação tripla de providers"
    - "`restoreProperty` → invalidação tripla → a fazenda reaparece no seletor e o router deixa de mandar para `/sem-acesso`"
  prohibitions:
    - "Não remover o gate `_canEditProperties` (vet-only) desta tela — arquivar/restaurar continua sendo ação de veterinário"
    - "Não substituir o `AlertDialog` de confirmação por um controle desabilitado por papel — papel negado vê o controle ausente"
    - "Não adicionar o item 'Membros' ao popup neste plano — é do plano 10-10 (evita conflito de arquivo na mesma onda)"
---

<objective>
Evoluir `PropriedadesScreen` para o ciclo de vida completo da fazenda: alternador Ativas/Arquivadas, arquivamento com confirmação forte por digitação do nome, e restauração pela UI.

Purpose: PROPV-01 e PROPV-02. Hoje "Remover" dispara um `AlertDialog` genérico de "Tem certeza?" e não existe volta nenhuma pela interface.
Output: 1 tela modificada + testes de widget estendidos.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md
@.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-06-SUMMARY.md
@lib/features/propriedades/presentation/propriedades_screen.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alternador Ativas/Arquivadas, cartão esmaecido e restauração</name>
  <files>lib/features/propriedades/presentation/propriedades_screen.dart, test/widget/propriedades_screen_test.dart</files>
  <read_first>
    - `lib/features/propriedades/presentation/propriedades_screen.dart` (arquivo inteiro, 253 linhas) — `PropriedadesScreen` é hoje um `ConsumerWidget` sem estado local; precisará virar `ConsumerStatefulWidget` para guardar a aba selecionada. `_PropertyCard` e `_canEditProperties` (vet-only) permanecem
    - `lib/features/gastos/presentation/gastos_property_screen.dart` (a classe privada `_PeriodChip` e seu uso no `Row` de chips) — o visual do alternador a espelhar; como é privada de outro arquivo, escrever um equivalente local, não tentar importar
    - `lib/features/propriedades/data/propriedade_repository.dart` — `archivedPropertyListProvider` e `restoreProperty` criados no plano 10-06
    - `lib/core/widgets/ui.dart` (linhas 147–200) — `EmptyState`
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — "Empty state — fazendas arquivadas" e o item "Arquivar/Restaurar" do Layout Contract (alternador acima da lista, cartão arquivado esmaecido, botão "Restaurar fazenda" em vez do menu)
  </read_first>
  <behavior>
    - Ao montar, a aba "Ativas" está selecionada e a lista vem de `propertyListProvider`
    - Tocar em "Arquivadas" troca a lista para `archivedPropertyListProvider`
    - Com `archivedPropertyListProvider` vazio, a aba Arquivadas mostra "Nenhuma fazenda arquivada" e o corpo "Fazendas arquivadas por você aparecem aqui para restaurar quando precisar."
    - Com uma fazenda arquivada, o cartão mostra o botão "Restaurar fazenda" e **não** mostra `PopupMenuButton`
    - Um cartão da aba Ativas continua mostrando o `PopupMenuButton` quando o usuário é veterinário
    - Tocar em "Restaurar fazenda" chama `restoreProperty` uma vez com o id daquela fazenda
    - O `FloatingActionButton` "Fazenda" só aparece na aba Ativas
    - A aba Arquivadas com papel não-veterinário não mostra o botão "Restaurar fazenda"
  </behavior>
  <action>
Converter `PropriedadesScreen` para `ConsumerStatefulWidget` guardando `bool _showArchived = false`.

Acima da lista, um `Padding(EdgeInsets.fromLTRB(14, 10, 14, 6))` com um `Row` de dois chips (`Ativas`, `Arquivadas`), no visual de `_PeriodChip`: `InkWell` + `Container` com `borderRadius: BorderRadius.circular(999)`, `padding: EdgeInsets.symmetric(horizontal: 12)`, altura 34, fundo `AppColors.primary`/texto `AppColors.onGreen` quando selecionado e fundo `AppColors.surfaceVariant`/texto `AppColors.ink` quando não. Escrever como uma classe privada local `_ScopeChip` neste arquivo — `_PeriodChip` é privada de `gastos_property_screen.dart` e não é importável.

O corpo observa `_showArchived ? archivedPropertyListProvider : propertyListProvider`, mantendo o mesmo `.when(loading/error/data)` e o mesmo `ErrorRetry` (o `onRetry` invalida o provider correspondente ao modo atual).

Estado vazio: na aba Ativas, manter exatamente a copy atual ("Nenhuma fazenda cadastrada" / "Crie sua primeira fazenda..."). Na aba Arquivadas, `EmptyState(icon: Icons.archive_outlined, title: 'Nenhuma fazenda arquivada', message: 'Fazendas arquivadas por você aparecem aqui para restaurar quando precisar.')`.

`_PropertyCard` ganha `final bool archived` (padrão false) e `final VoidCallback? onRestore`. Quando `archived` é true: envolver o conteúdo do cartão num `Opacity(opacity: 0.6)` (o mesmo tratamento "arquivada esmaecida" já usado nas aplicações sanitárias) e, no trailing, renderizar `FilledButton.icon(icon: Icon(Icons.unarchive_outlined), label: Text('Restaurar fazenda'))` no lugar do `PopupMenuButton` — e só quando `canEdit` for true. Quando `archived` é false, o comportamento atual permanece byte a byte.

`floatingActionButton`: manter a lógica atual, mas só renderizar quando `!_showArchived`.

`_restore(property)`: chama `ref.read(propertyRepositoryProvider).restoreProperty(property.id)` e depois invalida `propertyListProvider`, `archivedPropertyListProvider` e `memberPropertiesProvider` (a última é o que devolve a fazenda ao seletor e faz o router reavaliar o redirect). Em erro, `SnackBar` com `Erro ao restaurar a fazenda. Tente novamente.`, com guarda de `mounted`.

Estender `test/widget/propriedades_screen_test.dart` (hoje 1 caso) com os oito casos de `<behavior>`, mantendo o caso existente de estado vazio funcionando sem alteração.
  </action>
  <verify>
    <automated>flutter test test/widget/propriedades_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/propriedades_screen_test.dart` passa com no mínimo 9 casos (o existente + 8 novos)
    - O caso original "shows empty state copy when list is empty" continua verde sem edição na sua asserção
    - Existe um caso que asserta `Nenhuma fazenda arquivada` na aba Arquivadas vazia
    - Existe um caso que asserta `findsNothing` para `PopupMenuButton` num cartão arquivado e `findsOneWidget` para `Restaurar fazenda`
    - Existe um caso que asserta `findsNothing` para `FloatingActionButton` na aba Arquivadas
    - Existe um caso que asserta `restoreProperty` chamado exatamente uma vez, com o id correto
    - `_canEditProperties` permanece vet-only (nenhuma adição de `'owner'`)
    - `flutter analyze` limpo
  </acceptance_criteria>
  <done>Alternador, estado vazio, cartão esmaecido e restauração funcionando (PROPV-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Arquivamento com confirmação forte substituindo o AlertDialog genérico</name>
  <files>lib/features/propriedades/presentation/propriedades_screen.dart, test/widget/propriedades_screen_test.dart</files>
  <read_first>
    - `lib/features/propriedades/presentation/propriedades_screen.dart` — `_confirmDelete` (o método a substituir) e o estado deixado pela Task 1
    - `lib/features/propriedades/presentation/archive_confirm_dialog.dart` — `ArchiveConfirmDialog(propertyName:)` criado no plano 10-06
    - `lib/core/widgets/ui.dart` (linhas 530–568) — `showAdaptiveForm` e `FormWidth.confirm`
    - `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-UI-SPEC.md` — a linha "Destrutivo — arquivar fazenda" do Copywriting Contract
  </read_first>
  <behavior>
    - O item de menu do cartão ativo passa a se chamar "Arquivar" (não mais "Remover")
    - Selecionar "Arquivar" abre `ArchiveConfirmDialog` com o nome daquela fazenda
    - Cancelar o diálogo **não** chama `softDeleteProperty`
    - Confirmar com o nome exato chama `softDeleteProperty` uma vez com o id daquela fazenda
    - Após confirmar, `propertyListProvider`, `archivedPropertyListProvider` e `memberPropertiesProvider` são invalidados
    - Erro na chamada mostra um SnackBar com frase autorada, sem texto cru do banco
  </behavior>
  <action>
Renomear `_confirmDelete` para `_confirmArchive` e substituir o corpo: em vez do `AlertDialog` inline, chamar `showAdaptiveForm<bool>(context: context, width: FormWidth.confirm, builder: (_) => ArchiveConfirmDialog(propertyName: property.name))`. Se o retorno for `true`, chamar `softDeleteProperty(property.id)` e invalidar `propertyListProvider`, `archivedPropertyListProvider` e `memberPropertiesProvider`. Em erro, `SnackBar` com `Erro ao arquivar a fazenda. Tente novamente.` sob guarda de `mounted`.

No `PopupMenuButton` de `_PropertyCard`, trocar o valor `'delete'` por `'archive'` e o rótulo de `Remover` para `Arquivar` (mantendo o estilo em `AppColors.danger`), e renomear o callback `onDelete` para `onArchive`. O item `Editar` permanece intocado.

Remover o `AlertDialog` antigo e seus imports órfãos, se algum ficar sem uso.

Estender `test/widget/propriedades_screen_test.dart` com os seis casos de `<behavior>`.
  </action>
  <verify>
    <automated>flutter test test/widget/propriedades_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/propriedades_screen_test.dart` passa com no mínimo 15 casos no total
    - O arquivo não contém mais o rótulo de menu `Remover` para propriedades (foi substituído por `Arquivar`)
    - Existe um caso provando que cancelar o diálogo não chama `softDeleteProperty`
    - Existe um caso provando que digitar o nome exato e confirmar chama `softDeleteProperty` uma vez
    - `_confirmArchive` invoca `showAdaptiveForm` com `width: FormWidth.confirm`
    - Nenhum `AlertDialog` restante em `propriedades_screen.dart`
    - `flutter analyze` limpo e `flutter test` (suíte completa) verde
  </acceptance_criteria>
  <done>PROPV-01 fechado: arquivar exige digitar o nome exato, e a operação é reversível pela aba Arquivadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tela → `PATCH /properties` | Escrita direta gated pela policy de UPDATE (vet-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-23 | Tampering | Arquivamento acidental por toque errado no menu | high | mitigate | `ArchiveConfirmDialog` exige digitação exata do nome (Task 2) |
| T-10-24 | Denial of Service | Fazenda arquivada por engano sem caminho de volta | high | mitigate | Aba Arquivadas + `Restaurar fazenda` (Task 1); `/propriedades` continua alcançável a partir de `/sem-acesso` pela exceção já existente no redirect do router |
| T-10-25 | Elevation of Privilege | Leitor/proprietário arquivando ou restaurando | high | mitigate | Gate `_canEditProperties` (vet-only) preservado; barreira real é a policy `veterinarian_can_update_property` |
| T-10-SC | Tampering | Instalação de pacotes | low | accept | Nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `flutter test` verde (suíte completa)
- `flutter analyze` limpo
- Nenhuma alteração em `supabase/`
</verification>

<success_criteria>
PROPV-01 e PROPV-02 entregues na tela de Propriedades: alternador de escopo, arquivamento com confirmação forte por digitação do nome, cartão arquivado esmaecido e restauração em um toque.
</success_criteria>

## Artifacts this phase produces

Ver a lista completa em `10-01-PLAN.md`. Contribuição deste plano: alternador Ativas/Arquivadas (`_ScopeChip`), `_confirmArchive`, `_restore` e o `_PropertyCard` com modo arquivado, em `lib/features/propriedades/presentation/propriedades_screen.dart`.

<output>
Create `.planning/phases/10-gest-o-de-membros-e-ciclo-de-vida-da-propriedade/10-07-SUMMARY.md` when done
</output>
