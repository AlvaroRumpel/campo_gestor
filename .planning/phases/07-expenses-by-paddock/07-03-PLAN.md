---
phase: 07-expenses-by-paddock
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/auth/role_gates.dart
  - test/features/gastos/role_gates_test.dart
  - lib/core/router/routes.dart
autonomous: true
requirements: [GAST-01]

must_haves:
  truths:
    - "`canManageExpenses` returns true for a member whose role in the currently selected property is `owner`, true for `veterinarian`, and false for `reader` (D-23)."
    - "`canManageExpenses` returns false when the selected property is null, when the membership list is null, and when the user holds no membership in the selected property."
    - "`PaddockDetailScreen._canEdit` is left byte-identical — the vet-only gate that guards the \"Novo lote\" FAB is untouched, and the two gates coexist deliberately (D-23)."
    - "`AppRoutes.gastosById` is the literal `/gastos/:paddockId` and `AppRoutes.gastosPorPiquete(id)` returns `/gastos/$id` — the path template's segment name and the helper agree (D-08)."
    - "`AppRoutes.all` still holds exactly the 5 shell branches — the expense route is root-level and adds no navigation branch (D-08)."
  artifacts:
    - lib/core/auth/role_gates.dart
    - test/features/gastos/role_gates_test.dart
    - lib/core/router/routes.dart
  key_links:
    - "`canManageExpenses` is consumed by two features — `GastosScreen`'s FAB (07-06) and `PaddockDetailScreen`'s summary card (07-07) — which is why it lives in `core/` rather than private to either."
    - "`AppRoutes.gastosPorPiquete` is called from `PaddockDetailScreen` (07-07); the matching `GoRoute` registration lands in 07-06 with the screen it builds."
  prohibitions:
    - requirement_id: GAST-01
      category: safety
      status: unresolved
      verification: null
      resolution: null
      reason: null
      statement: "MUST NOT gate expense writes on the vet-only `_canEdit` predicate — an owner is the person most likely to be logging their own farm's spending, and copying `_canEdit` silently locks out exactly the role D-23 was written to unlock."
---

<objective>
Add the project's first two-role permission gate (`canManageExpenses`, D-23) with its unit test,
and register the `/gastos/:paddockId` path constant and helper (D-08).

Purpose: every existing role gate in this codebase (`PiquetesScreen`, `PaddockDetailScreen`,
`SanitarioScreen`, `AnimaisScreen`) is the same vet-only 10-line private method. RESEARCH names
copy-pasting one of them and forgetting `'owner'` as the single most likely mistake in this
phase. Extracting the two-role predicate to `core/` with a test that fails on a missing role is
the mechanical fix.

Output: one new `core/auth` file, one test file, and two constants in `routes.dart`. Both are
dependency-free of the rest of the phase, so this plan runs fully parallel with 07-01 and 07-02.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-expenses-by-paddock/07-CONTEXT.md
@.planning/phases/07-expenses-by-paddock/07-RESEARCH.md
@.planning/phases/07-expenses-by-paddock/07-PATTERNS.md
</context>

<!-- planner-discipline-allow: role == 'veterinarian' -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canManageExpenses two-role gate and its unit test</name>
  <files>lib/core/auth/role_gates.dart, test/features/gastos/role_gates_test.dart</files>
  <read_first>
    - lib/features/piquetes/presentation/paddock_detail_screen.dart lines 84-94 — the existing `_canEdit`, whose membership-lookup shape this mirrors and whose `role == 'veterinarian'` return this deliberately diverges from
    - lib/core/providers/current_property_provider.dart — `SelectedProperty`, `PropertyMembership` (`.property.id`, `.role`) and `memberPropertiesProvider`
    - test/features/sanitario/sanitary_calculations_test.dart — the pure-function test file shape used in this project
    - .planning/phases/07-expenses-by-paddock/07-RESEARCH.md § Pattern 5 and § Common Pitfalls #2
  </read_first>
  <behavior>
    - `canManageExpenses(selected, members)` returns true when the membership for `selected.id` has role `'owner'`
    - returns true when that membership's role is `'veterinarian'`
    - returns false when that membership's role is `'reader'`
    - returns false when `selected` is null
    - returns false when `members` is null
    - returns false when `members` is non-empty but holds no membership for `selected.id`
    - returns true for `'owner'` even when the list also contains a `'reader'` membership in a different property (the lookup is scoped to the selected property, not to any membership)
  </behavior>
  <action>
Create `test/features/gastos/role_gates_test.dart` FIRST, covering every case in the
`<behavior>` block with `flutter_test`. Construct `SelectedProperty` and `PropertyMembership`
fixtures directly — no `ProviderContainer`, no widget pump, no mocks.

Then create `lib/core/auth/role_gates.dart` declaring a single public top-level function:

`bool canManageExpenses(SelectedProperty? current, List<PropertyMembership>? members)`

Body: return false when `current` or `members` is null; otherwise resolve the role by
`members.where((m) => m.property.id == current.id).map((m) => m.role).firstOrNull` and return
`role == 'veterinarian' || role == 'owner'`. Import `SelectedProperty` and `PropertyMembership`
from `../providers/current_property_provider.dart`.

Write a doc comment recording D-23 in full: this is the first capability in the project where
two roles may write, gasto is the farm owner's own financial data, the database enum is
`role_enum ('owner','veterinarian','reader')`, and the RLS policies on `expenses` are the actual
enforcement — this predicate only decides whether a control renders. Follow the project rule
that a denied role sees the control **absent**, never disabled.

Add a second doc line stating explicitly that `PaddockDetailScreen._canEdit` must NOT be replaced
by this function and must NOT gain `'owner'`: it guards the "Novo lote" FAB, which stays
vet-only, and the two gates are meant to coexist on the same screen (D-23).

Place the file at `lib/core/auth/role_gates.dart`, creating the `lib/core/auth/` directory. It
lives in `core/` rather than private to the gastos feature because two features consume it —
`GastosScreen`'s FAB (07-06) and `PaddockDetailScreen`'s summary card (07-07).

Iterate until the test passes.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter test test/features/gastos/role_gates_test.dart &amp;&amp; git diff --quiet -- lib/features/piquetes/presentation/paddock_detail_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/gastos/role_gates_test.dart` exits 0 with all 7 `<behavior>` cases present as named tests
    - `lib/core/auth/role_gates.dart` declares exactly one public function, `canManageExpenses`
    - The function body contains both `'owner'` and `'veterinarian'` string comparisons
    - `git diff --quiet -- lib/features/piquetes/presentation/paddock_detail_screen.dart` exits 0 — `_canEdit` is untouched by this plan
    - `grep -c "role == 'veterinarian'" lib/core/auth/role_gates.dart` returns 1 and that occurrence is disjuncted with the owner check on the same expression
    - `flutter analyze lib/core/auth/role_gates.dart` exits 0
  </acceptance_criteria>
  <done>The two-role gate exists in `core/auth` with a test that goes red if `'owner'` is dropped, and the vet-only `_canEdit` on `PaddockDetailScreen` is provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: /gastos/:paddockId route constant and helper</name>
  <files>lib/core/router/routes.dart</files>
  <read_first>
    - lib/core/router/routes.dart — the whole file; specifically the `aplicacaoById` block (lines 37-41), the `atfById` block (lines 32-35) and the `AppRoutes.all` list (lines 50-57)
    - .planning/phases/07-expenses-by-paddock/07-PATTERNS.md § routes.dart / router.dart
  </read_first>
  <action>
Edit `lib/core/router/routes.dart` only — do not touch `router.dart` in this plan (the `GoRoute`
registration lands in 07-06 with the screen it builds).

Add, immediately after the existing `aplicacaoById` / `aplicacaoDetail` block and following its
exact two-line shape:

- `static const gastosById = '/gastos/:paddockId';` with the trailing
  `// template — used by GoRoute path` comment the sibling constants carry.
- `static String gastosPorPiquete(String id) => '/gastos/$id';`

Above them add a comment block matching the style of the Phase 3/5/6 blocks: Phase 7 detail
route, root-level outside the shell (D-08), fourth use of the pattern after `loteById`,
`atfById` and `aplicacaoById`; the expense list needs deep-link and back-button context of its
own paddock, and a sixth shell branch was rejected because Material 3 recommends 3–5 destinations
and it would change `AppShell._navItems`, `AppRoutes.all` and the tests that count navigation
items.

The path parameter is named `paddockId` (not `id`) so the route reads self-documentingly; the
`GoRoute` builder in 07-06 will read `state.pathParameters['paddockId']`.

Do **not** add the route to `AppRoutes.all` — that list is the shell-branch inventory only, and
the navigation-count tests assert its length.

Update the file's top-of-file doc comment with a Phase 7 line, matching the existing Phase 0/1/2
lines.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; grep -q "gastosById = '/gastos/:paddockId'" lib/core/router/routes.dart &amp;&amp; grep -q "gastosPorPiquete" lib/core/router/routes.dart &amp;&amp; flutter test test/widget/app_shell_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/router/routes.dart` contains `static const gastosById = '/gastos/:paddockId';`
    - It contains `static String gastosPorPiquete(String id) => '/gastos/$id';`
    - `AppRoutes.all` still contains exactly 5 entries and does not mention gastos
    - `flutter test test/widget/app_shell_test.dart` exits 0 — the navigation-item count assertions still pass
    - `flutter analyze lib/core/router/routes.dart` exits 0
  </acceptance_criteria>
  <done>The expense route path template and its type-safe helper exist as constants, with the shell branch inventory unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side role predicate → server-side authorization | `canManageExpenses` runs in the browser and can be bypassed by anyone who opens devtools; it is a rendering decision, never an authorization decision |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Elevation of Privilege | client-side-only role gating | high | mitigate | The gate is convenience only; enforcement is the `owner_vet_can_insert_expense` / `owner_vet_can_update_expense` RLS policies from 07-01, asserted by `07_expenses_test.sql` group 4. Documented in the function's own doc comment so no future reader mistakes it for a boundary |
| T-07-12 | Elevation of Privilege | wrong-predicate regression (`_canEdit` copy) | medium | mitigate | `role_gates_test.dart` fails if `'owner'` is removed; the gate is a single shared function rather than a per-screen private copy, so there is one place to get wrong instead of four |
| T-07-13 | Information Disclosure | deep-linked `/gastos/:paddockId` for a foreign paddock | medium | mitigate | The route carries no authorization of its own; the data behind it is scoped by the `members_can_read_expenses` RLS policy (07-01), so a foreign paddock id resolves to an empty list, not another tenant's data |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this plan — Phase 7 introduces no `pubspec.yaml` change (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `flutter test test/features/gastos/role_gates_test.dart` exits 0
- `flutter test test/widget/app_shell_test.dart` exits 0
- `git diff --quiet -- lib/features/piquetes/presentation/paddock_detail_screen.dart` exits 0
- `flutter analyze lib/core/` exits 0
</verification>

<success_criteria>
The two-role expense gate exists once, in `core/auth`, provably distinct from the vet-only
`_canEdit`; the `/gastos/:paddockId` path constant and helper are available to downstream plans.
</success_criteria>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 7 and does not exist in the codebase before this phase.

**SQL — `supabase/migrations/20260813_07_expenses_module.sql`:** table `expenses` (`id`, `property_id`, `paddock_id`, `category`, `amount`, `expense_date`, `description`, `created_by`, `updated_by`, `created_at`, `updated_at`, `deleted_at`); indexes `expenses_property_idx`, `expenses_paddock_date_idx`; policies `members_can_read_expenses`, `owner_vet_can_insert_expense`, `owner_vet_can_update_expense`; functions `enforce_expenses_paddock_same_property()`, `set_expenses_updated_by()`; triggers `trg_expenses_paddock_same_property`, `trg_expenses_set_updated_by`; new columns `sanitary_applications.paddock_id`, `sanitary_applications.paddock_name`; replaced bodies (signatures unchanged) `register_sanitary_application(uuid,uuid,date,jsonb,text)`, `reverse_sanitary_application(uuid,text)`

**pgTAP:** `supabase/tests/07_expenses_test.sql`

**Dart — `lib/features/gastos/data/`:** `expense_model.dart` (`Expense`, `ExpenseListItem`, `ManualExpenseItem`, `SanitaryExpenseItem`, `dateOf`, `amountOf`, `categoryKeyOf`, `sortExpenseItemsDesc`); `expense_constants.dart` (`kExpenseCategories`, `kExpenseCategoryLabels`, `kExpenseCategoryIcons`, `kExpenseFilterCategories`, `kSanitaryPseudoCategory`, `kSanitaryPseudoCategoryLabel`, `kSanitaryCategoryIcon`); `expense_calculations.dart` (`totalAmount`, `itemCount`, `filterByDateRange`, `filterByCategory`, `currentMonthRange`, `rangeForPreset`, `ExpensePeriodPreset`); `expense_repository.dart` (`ExpenseRepository`, `expenseRepositoryProvider`, `expenseListByPaddockProvider`, `archivedExpenseListByPaddockProvider`, `unifiedExpenseListByPaddockProvider`, `paddockMonthExpenseTotalProvider`)

**Dart — `lib/core/auth/role_gates.dart`:** `canManageExpenses`

**Dart — `lib/features/gastos/presentation/`:** `gastos_screen.dart` (`GastosScreen`); `expense_form_dialog.dart` (`ExpenseFormDialog`, `confirmDeleteExpense`); `_expense_list_item_card.dart` (`ExpenseListItemCard`); `paddock_expense_summary_card.dart` (`PaddockExpenseSummaryCard`)

**Dart — modified files, new symbols only:** `AppRoutes.gastosById`, `AppRoutes.gastosPorPiquete` (`lib/core/router/routes.dart`); the `GoRoute(path: AppRoutes.gastosById, …)` registration (`lib/core/router/router.dart`); the `PaddockExpenseSummaryCard` insertion in `lib/features/piquetes/presentation/paddock_detail_screen.dart` (no new symbol declared there)

**Tests:** `test/features/gastos/expense_calculations_test.dart`, `test/features/gastos/role_gates_test.dart`, `test/features/gastos/expense_form_dialog_test.dart`

<output>
Create `.planning/phases/07-expenses-by-paddock/07-03-SUMMARY.md` when done
</output>
