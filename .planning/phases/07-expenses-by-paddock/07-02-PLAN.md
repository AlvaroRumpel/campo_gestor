---
phase: 07-expenses-by-paddock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/gastos/data/expense_model.dart
  - lib/features/gastos/data/expense_constants.dart
  - lib/features/gastos/data/expense_calculations.dart
  - test/features/gastos/expense_calculations_test.dart
autonomous: true
requirements: [GAST-01, GAST-02]

must_haves:
  truths:
    - "`Expense` round-trips through `fromJson`/`toJson` against a snake_case Supabase row, including a null `description` and a null `updated_by` (D-04, D-27)."
    - "`kExpenseCategories` holds exactly the 8 category keys of D-02 in the fixed order operação → estrutura → `outros` last (D-06), and `kExpenseCategoryLabels`/`kExpenseCategoryIcons` each have exactly one entry per key (D-05)."
    - "`totalAmount` over a mixed list of manual expenses and sanitary rows returns the arithmetic sum of every visible row's amount (D-18, D-29)."
    - "A sanitary row whose `totalCost` is null contributes exactly 0.0 to `totalAmount` and still increments `itemCount` — the total never silently shrinks without the row being present and counted (Claude's Discretion resolution)."
    - "`filterByDateRange` and `filterByCategory` are combinable in either order and produce the same subset (D-07)."
    - "`sortExpenseItemsDesc` orders by the item's own date descending and breaks ties on `createdAt` descending, so two items on the same date have a stable, repeatable order across reloads (D-19)."
    - "`expense_calculations.dart` and `expense_model.dart` import no `flutter/material.dart`, no `supabase_flutter` and no Riverpod, so `flutter test test/features/gastos/expense_calculations_test.dart` runs with no widget or network harness (D-18, D-36)."
    - "Currency display uses `formatCurrencyBrl` imported from `sanitary_calculations.dart`; `NumberFormat.currency` is never introduced (RESEARCH § Shared Patterns)."
  artifacts:
    - lib/features/gastos/data/expense_model.dart
    - lib/features/gastos/data/expense_constants.dart
    - lib/features/gastos/data/expense_calculations.dart
    - test/features/gastos/expense_calculations_test.dart
  key_links:
    - "`ExpenseListItem.sanitary` wraps the existing `SanitaryApplication` model unchanged — the unified list never defines a parallel sanitary type."
    - "`amountOf` is the single place where a null sanitary cost becomes 0.0; every total, count and rendered figure downstream reads through it."
    - "`kExpenseCategories` is the sole category vocabulary — the form dropdown (07-05) and the list filter (07-06) both read it, and no CHECK constraint mirrors it in the database (D-03)."
---

<objective>
Build the pure Dart core of the gastos feature: the `Expense` freezed model, the
`ExpenseListItem` sealed union that lets manual expenses and read-only sanitary rows live in one
list (D-32), the 8-category constant table (D-01, D-02, D-05), and the dependency-free
calculation module that owns the total, the count, the period/category filters and the sort
(D-07, D-18, D-19) — plus the Wave 0 unit test that proves them.

Purpose: this is the layer every other Dart plan in the phase imports, and it is where the
financial arithmetic that GAST-02 is graded on actually lives. Keeping it free of Flutter,
Riverpod and Supabase imports is what makes it testable in 15 seconds without a widget harness.

Output: 3 source files under `lib/features/gastos/data/` and the first test file under
`test/features/gastos/`.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-expenses-by-paddock/07-CONTEXT.md
@.planning/phases/07-expenses-by-paddock/07-RESEARCH.md
@.planning/phases/07-expenses-by-paddock/07-PATTERNS.md
@.planning/phases/07-expenses-by-paddock/07-VALIDATION.md
</context>

<planner_assumptions>
**Flagged, unresolved — surfaced deliberately, not dropped.** The deterministic edge probe
classified both phase requirements as `unclassified` and left them **`unresolved`**; neither was
auto-resolved to a `covered` or `backstop` edge. They are recorded here so the executor and the
verifier see them rather than inheriting a silent gap:

| Requirement | Probe status | Planner note |
|---|---|---|
| GAST-01 | `unclassified` / `unresolved` | No edge category was inferred from the requirement text. Boundary behaviour for expense creation is carried instead by explicit plan criteria: `amount > 0` at the table level (07-01, RESEARCH A3), a required-field validator on categoria/valor/data (07-05), and `description` nullable (D-04). No probe-derived edge criterion exists — treat this requirement's edge coverage as unverified by the probe. |
| GAST-02 | `unclassified` / `unresolved` | No edge category was inferred. Boundary behaviour for the total is carried by explicit plan criteria: zero items → "R$ 0,00" + "0 lançamentos" (07-06), null sanitary cost → 0 contribution but counted (this plan), inclusive date-range endpoints (this plan). No probe-derived edge criterion exists — treat this requirement's edge coverage as unverified by the probe. |

Two further planner assumptions carried from RESEARCH, both cheap to reverse:
- **A2 — "Ano" preset = calendar year** (Jan 1 of the current year → today), not a rolling
  12 months. One `DateTimeRange` literal; flag it at UAT if the user meant rolling.
- **A4 — feature folder is `lib/features/gastos/`**, matching the pt-BR route `/gastos/:paddockId`
  and the existing `sanitario`/`piquetes`/`animais` naming.
</planner_assumptions>

<!-- planner-discipline-allow: package:flutter/material.dart -->
<!-- planner-discipline-allow: supabase_flutter -->
<!-- planner-discipline-allow: flutter_riverpod -->
<!-- planner-discipline-allow: NumberFormat.currency -->

<tasks>

<task type="auto">
  <name>Task 1: Expense model and the ExpenseListItem sealed union</name>
  <files>lib/features/gastos/data/expense_model.dart</files>
  <read_first>
    - lib/features/sanitario/data/dose_model.dart — the freezed + `@JsonSerializable(fieldRename: FieldRename.snake)` idiom, the `// ignore: invalid_annotation_target` comment placement, and the `part` directives
    - lib/features/sanitario/data/sanitary_application_model.dart — the `SanitaryApplication` field list this union wraps (`id`, `appliedAt`, `createdAt`, `totalCost`, `doseName`, `lotName`, `reversesApplicationId`, `paddockId` after 07-01), plus `sortByAppliedAtDesc` for the tie-break idiom to mirror
    - lib/features/animais/data/animal_model.dart — a second freezed model for the nullable-column mapping idiom
  </read_first>
  <action>
Create `lib/features/gastos/data/expense_model.dart`.

Declare `@freezed sealed class Expense with _$Expense` carrying, in this order: `String id`,
`String propertyId`, `String paddockId`, `String category`, `double amount`, `DateTime
expenseDate`, `String? description`, `String createdBy`, `String? updatedBy`, `DateTime
createdAt`, `DateTime updatedAt`, `DateTime? deletedAt`. Annotate the factory with
`// ignore: invalid_annotation_target` followed by
`@JsonSerializable(fieldRename: FieldRename.snake)`, exactly as `Dose` and `SanitaryApplication`
do, and add `factory Expense.fromJson(Map<String, dynamic> json)`. Emit the `part
'expense_model.freezed.dart';` and `part 'expense_model.g.dart';` directives.

Declare the unified-list union (D-32, RESEARCH Pattern 4):
`@freezed sealed class ExpenseListItem with _$ExpenseListItem`, with
`const factory ExpenseListItem.manual(Expense expense) = ManualExpenseItem;` and
`const factory ExpenseListItem.sanitary(SanitaryApplication application) = SanitaryExpenseItem;`.
Import `SanitaryApplication` from `../../sanitario/data/sanitary_application_model.dart`; do not
redeclare a parallel sanitary type.

Add three top-level pure accessors over the union, each a Dart 3 `switch` expression:
- `DateTime dateOf(ExpenseListItem item)` → `expense.expenseDate` / `application.appliedAt`.
- `double amountOf(ExpenseListItem item)` → `expense.amount` / `application.totalCost?.abs() ??
  0.0`. Document in a doc comment that the `?? 0.0` here is the ONLY place a null sanitary cost
  becomes a number, that the row still counts toward `itemCount`, and that the UI renders "—"
  rather than a currency figure for it (Claude's Discretion resolution to D-11 Phase 6). The
  `.abs()` is deliberate: `reverse_sanitary_application` writes negated totals, and although
  reversed rows are filtered out upstream (D-33) the absolute value keeps this accessor correct
  in isolation.
- `String categoryKeyOf(ExpenseListItem item)` → `expense.category` for a manual row and the
  sanitary pseudo-category key for a sanitary row (imported from `expense_constants.dart`, D-32).

Add `DateTime _createdAtOf(ExpenseListItem item)` (private) and the public
`List<ExpenseListItem> sortExpenseItemsDesc(List<ExpenseListItem> items)` returning a NEW list
sorted by `dateOf` descending with `_createdAtOf` descending as the tie-break — mirror
`sortByAppliedAtDesc`'s shape and its doc comment referencing the G-05-4 lesson (D-19). Never
mutate the argument.

Import no `flutter/material.dart`, no `supabase_flutter`, no Riverpod in this file.

Run `dart run build_runner build --delete-conflicting-outputs` to generate the freezed and
json_serializable parts.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter analyze lib/features/gastos/data/expense_model.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/gastos/data/expense_model.dart` contains `sealed class Expense` and `sealed class ExpenseListItem`
    - `expense_model.freezed.dart` and `expense_model.g.dart` exist on disk after codegen
    - The file declares `ManualExpenseItem` and `SanitaryExpenseItem` as the two union variants
    - The file declares top-level `dateOf`, `amountOf`, `categoryKeyOf` and `sortExpenseItemsDesc`
    - `grep -c "package:flutter/material.dart\|supabase_flutter\|flutter_riverpod" lib/features/gastos/data/expense_model.dart` returns 0
    - `flutter analyze lib/features/gastos/data/expense_model.dart` exits 0 with no warnings
  </acceptance_criteria>
  <done>The Expense model and the two-variant unified-list union compile with generated parts, and the null-sanitary-cost rule lives in exactly one documented accessor.</done>
</task>

<task type="auto">
  <name>Task 2: Expense category constants, labels and icons</name>
  <files>lib/features/gastos/data/expense_constants.dart</files>
  <read_first>
    - lib/features/animais/data/animal_constants.dart — `kBreeds` (a plain `const List<String>` with no CHECK constraint behind it) and `BaixaReason`'s `dbValue`/`label` shape; the precedent for D-01/D-03
    - .planning/phases/07-expenses-by-paddock/07-PATTERNS.md § expense_constants.dart — the exact key/label pairs to use
    - .planning/phases/07-expenses-by-paddock/07-UI-SPEC.md § Color — category icons render in the default icon colour, never a per-category colour (D-05)
  </read_first>
  <action>
Create `lib/features/gastos/data/expense_constants.dart`.

Declare `const List<String> kExpenseCategories` with exactly these 8 snake_case keys in this
fixed order (D-02, D-06 — operação first, structural second, `outros` last):
`racao_suplementacao`, `sanidade_medicamentos`, `mao_de_obra`, `manutencao`,
`pastagem_adubacao`, `combustivel`, `arrendamento`, `outros`.

Declare `const Map<String, String> kExpenseCategoryLabels` with the pt-BR labels:
`racao_suplementacao` → `Ração/Suplementação`, `sanidade_medicamentos` →
`Sanidade/Medicamentos`, `mao_de_obra` → `Mão de obra`, `manutencao` → `Manutenção`,
`pastagem_adubacao` → `Pastagem/Adubação`, `combustivel` → `Combustível`, `arrendamento` →
`Arrendamento`, `outros` → `Outros`.

Declare `const Map<String, IconData> kExpenseCategoryIcons` with one entry per key (D-05), using
the RESEARCH Assumption A1 mapping: `Icons.grass`, `Icons.medical_services`, `Icons.engineering`,
`Icons.construction`, `Icons.eco`, `Icons.local_gas_station`, `Icons.description`,
`Icons.more_horiz` in the same order as `kExpenseCategories`. Note in a comment that these are
cosmetic and swappable with zero schema or logic impact. No per-category colour is defined
(D-05).

Declare `const String kSanitaryPseudoCategory = 'sanitario';` and
`const String kSanitaryPseudoCategoryLabel = 'Sanitário';` — the pseudo-category the unified list
filter treats alongside the 8 real ones (D-32). Keep it OUT of `kExpenseCategories` so the form
dropdown can never offer it; declare a separate `const List<String> kExpenseFilterCategories`
that is `kExpenseCategories` plus the pseudo-category, for the list filter only (D-07).

Declare `const IconData kSanitaryCategoryIcon = Icons.medical_services;` for the sanitary row.

This file needs `import 'package:flutter/material.dart';` for `IconData` — the one structural
deviation from `animal_constants.dart`, which is pure Dart. Leave a comment saying so.

Add a top-of-file comment recording D-01/D-03: the vocabulary is a Dart constant with no
database CHECK constraint behind it, so adding or renaming a category is a Flutter deploy with
zero migration — and the accepted cost is that a raw PATCH can store a value outside this list.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter analyze lib/features/gastos/data/expense_constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - `kExpenseCategories` contains exactly 8 entries, `outros` last and `racao_suplementacao` first
    - `kExpenseCategoryLabels` and `kExpenseCategoryIcons` each contain exactly 8 entries whose keys equal `kExpenseCategories`
    - `kSanitaryPseudoCategory` is NOT a member of `kExpenseCategories`
    - `kExpenseFilterCategories` has 9 entries (the 8 plus the pseudo-category)
    - The file contains no `Color` literal and no per-category colour map
    - `flutter analyze lib/features/gastos/data/expense_constants.dart` exits 0
  </acceptance_criteria>
  <done>The 8 categories, their pt-BR labels and their icons exist as compile-time constants, with the sanitary pseudo-category available to the filter but not to the form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure calculation module and its Wave 0 unit test</name>
  <files>lib/features/gastos/data/expense_calculations.dart, test/features/gastos/expense_calculations_test.dart</files>
  <read_first>
    - lib/features/sanitario/data/sanitary_calculations.dart — the "zero Flutter/Riverpod/Supabase imports" header comment to mirror and `formatCurrencyBrl` (lines 56-61), which this module imports rather than redefines
    - test/features/sanitario/sanitary_calculations_test.dart — the existing pure-function test file shape (grouping, naming, fixture construction)
    - lib/features/gastos/data/expense_model.dart — `ExpenseListItem`, `dateOf`, `amountOf`, `categoryKeyOf`, `sortExpenseItemsDesc` (task 1 of this plan)
    - lib/features/gastos/data/expense_constants.dart — `kSanitaryPseudoCategory` (task 2 of this plan)
  </read_first>
  <behavior>
    - `totalAmount([])` returns 0.0 and `itemCount([])` returns 0
    - `totalAmount` over 3 manual expenses of 100.50, 200.25 and 1000.00 returns 1300.75
    - `totalAmount` over 1 manual expense of 100.00 plus 1 sanitary row with `totalCost` 40.00 returns 140.00 (D-29)
    - `totalAmount` over 1 manual expense of 100.00 plus 1 sanitary row with `totalCost` null returns 100.00, and `itemCount` over that same list returns 2 — the null-cost row contributes 0 but is still counted
    - `filterByDateRange` includes items whose date equals the range start and items whose date equals the range end (both endpoints inclusive)
    - `filterByCategory(items, null)` returns every item; `filterByCategory(items, kSanitaryPseudoCategory)` returns only sanitary rows; `filterByCategory(items, 'combustivel')` returns only manual rows with that category
    - Applying `filterByDateRange` then `filterByCategory` yields the same list as applying them in the opposite order (D-07 combinability)
    - `sortExpenseItemsDesc` on two items with the same date returns the one with the later `createdAt` first (D-19 tie-break)
    - `rangeForPreset(ExpensePeriodPreset.mesAtual, now)` starts on day 1 of `now`'s month and ends on `now`
    - `rangeForPreset(ExpensePeriodPreset.ano, now)` starts on 1 January of `now`'s year (calendar year — planner assumption A2)
  </behavior>
  <action>
Create `test/features/gastos/expense_calculations_test.dart` FIRST, encoding every case in the
`<behavior>` block above with `flutter_test`'s `group`/`test`/`expect`. Build fixtures with plain
constructors — no mocks, no `ProviderContainer`, no widget pumping. Run it and confirm it fails
to compile against the not-yet-written module (the deliberate red state).

Then create `lib/features/gastos/data/expense_calculations.dart` with a header comment mirroring
`sanitary_calculations.dart`'s: dependency-free, no `flutter/material.dart`, no
`supabase_flutter`, no Riverpod, so it runs as a plain `flutter_test` unit test (D-36). Import
`formatCurrencyBrl` from `../../sanitario/data/sanitary_calculations.dart` and re-export or
re-reference it — do NOT define a second currency formatter and do NOT introduce
`NumberFormat.currency(locale: 'pt_BR')`; the existing helper avoids a non-breaking-space that
makes exact-string assertions environment-dependent, as its own comment records.

Implement:
- `double totalAmount(Iterable<ExpenseListItem> items)` folding `amountOf`. Add the D-18 ceiling
  comment verbatim in spirit: the sum runs over the already-loaded list, so if the list ever
  becomes paginated this silently becomes "total of this page" — upgrade to a server-side
  `SUM()` RPC at that point, not before.
- `int itemCount(Iterable<ExpenseListItem> items)`.
- `List<ExpenseListItem> filterByDateRange(List<ExpenseListItem> items, DateTimeRange range)` —
  both endpoints inclusive, comparing date-only values so a timestamp late on the end day is not
  dropped.
- `List<ExpenseListItem> filterByCategory(List<ExpenseListItem> items, String? categoryKey)` —
  a null key means no filter; `kSanitaryPseudoCategory` selects sanitary rows; any other key
  compares against `categoryKeyOf` (D-07, D-32).
- `enum ExpensePeriodPreset { mesAtual, mesPassado, ultimos3Meses, ano, personalizado }` with the
  pt-BR labels of D-16 ("Mês atual", "Mês passado", "Últimos 3 meses", "Ano", "Personalizado")
  exposed as a `label` getter or a const label map.
- `DateTimeRange rangeForPreset(ExpensePeriodPreset preset, DateTime now)` — `mesAtual` = day 1 of
  `now`'s month → `now` (D-15); `mesPassado` = day 1 → last day of the previous month;
  `ultimos3Meses` = day 1 of the month two months back → `now`; `ano` = 1 January of `now`'s year
  → `now` (calendar year, planner assumption A2 — leave a comment marking it as an assumption to
  confirm at UAT); `personalizado` throws `ArgumentError` because its range comes from
  `showDateRangePicker`, not from this function.
- `DateTimeRange currentMonthRange(DateTime now)` as the named shorthand the paddock summary card
  (07-07) and the screen default (07-06) both call, so the card's figure and the screen's opening
  figure cannot diverge (D-09, D-15).

Iterate until the test file passes.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter test test/features/gastos/expense_calculations_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/gastos/expense_calculations_test.dart` exits 0 with every `<behavior>` case present as a named test
    - `grep -c "package:flutter/material.dart\|supabase_flutter\|flutter_riverpod" lib/features/gastos/data/expense_calculations.dart` returns 0
    - `grep -c "NumberFormat.currency" lib/features/gastos/data/expense_calculations.dart` returns 0
    - `expense_calculations.dart` imports `formatCurrencyBrl` from `sanitary_calculations.dart`
    - `expense_calculations.dart` declares `totalAmount`, `itemCount`, `filterByDateRange`, `filterByCategory`, `rangeForPreset`, `currentMonthRange` and `enum ExpensePeriodPreset`
    - The test asserts a null-`totalCost` sanitary row contributes 0.0 to the total AND increments the count
    - `rangeForPreset` carries an inline comment marking the calendar-year interpretation of "Ano" as an assumption
  </acceptance_criteria>
  <done>The total, count, filters, sort helper and period presets are implemented as pure functions and proven green by a unit test that needs no widget or network harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database row → Dart model | A row read from `expenses` or `sanitary_applications` may carry a `category` value outside `kExpenseCategories` (D-03 declines a CHECK constraint) or a null cost |
| pure calculation → displayed financial figure | The number a user reads as "quanto este piquete custou" is produced entirely here; nothing downstream re-derives it |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08 | Tampering (data integrity) | `totalAmount` / `itemCount` | medium | mitigate | Both are pure functions with no I/O, proven by `expense_calculations_test.dart` against an explicit fixture including the null-cost case; the total cannot be produced by an untested code path (D-18) |
| T-07-09 | Information Disclosure | `Expense` model | low | mitigate | The model carries no credential, no token and no PII beyond a free-text description the user typed; `createdBy`/`updatedBy` hold uuids, never emails |
| T-07-10 | Tampering | out-of-list `category` value from the database | low | accept | D-03 declines the CHECK constraint. `kExpenseCategoryLabels[key] ?? key` degrades to showing the raw key rather than crashing — dirty data surfaces visibly instead of silently |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this plan — no `pubspec.yaml` change anywhere in Phase 7 (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `flutter test test/features/gastos/` exits 0
- `flutter analyze lib/features/gastos/` exits 0
- No file in `lib/features/gastos/data/` other than `expense_constants.dart` imports `flutter/material.dart`
</verification>

<success_criteria>
The gastos data core compiles, its arithmetic is proven by a 15-second unit test, and the
null-sanitary-cost rule and the date/category filters exist in exactly one place each.
</success_criteria>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 7 and does not exist in the codebase before this phase.

**SQL — `supabase/migrations/20260813_07_expenses_module.sql`:** table `expenses` (`id`, `property_id`, `paddock_id`, `category`, `amount`, `expense_date`, `description`, `created_by`, `updated_by`, `created_at`, `updated_at`, `deleted_at`); indexes `expenses_property_idx`, `expenses_paddock_date_idx`; policies `members_can_read_expenses`, `owner_vet_can_insert_expense`, `owner_vet_can_update_expense`; functions `enforce_expenses_paddock_same_property()`, `set_expenses_updated_by()`; triggers `trg_expenses_paddock_same_property`, `trg_expenses_set_updated_by`; new columns `sanitary_applications.paddock_id`, `sanitary_applications.paddock_name`; replaced bodies (signatures unchanged) `register_sanitary_application(uuid,uuid,date,jsonb,text)`, `reverse_sanitary_application(uuid,text)`

**pgTAP:** `supabase/tests/07_expenses_test.sql`

**Dart — `lib/features/gastos/data/`:** `expense_model.dart` (`Expense`, `ExpenseListItem`, `ManualExpenseItem`, `SanitaryExpenseItem`, `dateOf`, `amountOf`, `categoryKeyOf`, `sortExpenseItemsDesc`); `expense_constants.dart` (`kExpenseCategories`, `kExpenseCategoryLabels`, `kExpenseCategoryIcons`, `kExpenseFilterCategories`, `kSanitaryPseudoCategory`, `kSanitaryPseudoCategoryLabel`, `kSanitaryCategoryIcon`); `expense_calculations.dart` (`totalAmount`, `itemCount`, `filterByDateRange`, `filterByCategory`, `currentMonthRange`, `rangeForPreset`, `ExpensePeriodPreset`); `expense_repository.dart` (`ExpenseRepository`, `expenseRepositoryProvider`, `expenseListByPaddockProvider`, `archivedExpenseListByPaddockProvider`, `unifiedExpenseListByPaddockProvider`, `paddockMonthExpenseTotalProvider`)

**Dart — `lib/core/auth/role_gates.dart`:** `canManageExpenses`

**Dart — `lib/features/gastos/presentation/`:** `gastos_screen.dart` (`GastosScreen`); `expense_form_dialog.dart` (`ExpenseFormDialog`, `confirmDeleteExpense`); `_expense_list_item_card.dart` (`ExpenseListItemCard`); `paddock_expense_summary_card.dart` (`PaddockExpenseSummaryCard`)

**Dart — modified files, new symbols only:** `AppRoutes.gastosById`, `AppRoutes.gastosPorPiquete` (`lib/core/router/routes.dart`); the `GoRoute(path: AppRoutes.gastosById, …)` registration (`lib/core/router/router.dart`); the `PaddockExpenseSummaryCard` insertion in `lib/features/piquetes/presentation/paddock_detail_screen.dart` (no new symbol declared there)

**Tests:** `test/features/gastos/expense_calculations_test.dart`, `test/features/gastos/role_gates_test.dart`, `test/features/gastos/expense_form_dialog_test.dart`

<output>
Create `.planning/phases/07-expenses-by-paddock/07-02-SUMMARY.md` when done
</output>
