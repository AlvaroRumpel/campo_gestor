---
phase: 07-expenses-by-paddock
plan: 08
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07"]
files_modified: []
autonomous: false
requirements: [GAST-01, GAST-02]

must_haves:
  truths:
    - "`20260813_07_expenses_module.sql` is applied to the live PROD project `wrdwzychjhlpwpivfhhq`; the migration ledger moves from 17 to 18."
    - "A catalog read confirms the `expenses` table, both FKs, both triggers, exactly 3 RLS policies, zero DELETE policies, and both indexes."
    - "A catalog read confirms `sanitary_applications.paddock_id` and `paddock_name` exist, are NOT NULL, and that both pre-existing PROD rows carry non-null values (D-31)."
    - "`pg_trigger.tgenabled` for `trg_snapshot_immutable` on `sanitary_applications` is `'O'` after the apply — the backfill's DISABLE left no residue."
    - "`07_expenses_test.sql` replays against live PROD inside `BEGIN … ROLLBACK` with zero failures and leaves no fixture rows behind."
    - "The full Dart suite (`flutter test`) is green before the human UAT checkpoint opens."
    - "A human has exercised the real flow in the browser and confirmed GAST-01 and GAST-02 before the phase is considered done."
  artifacts:
    - .planning/phases/07-expenses-by-paddock/07-08-SUMMARY.md
  key_links:
    - "MCP `apply_migration` is the only path to the live database — the Supabase CLI is authenticated but unlinked with no TTY for a DB password, unchanged since Phase 3."
    - "MCP `execute_sql` inside `BEGIN … ROLLBACK` is the only pgTAP path — Docker is unreachable on this host, so `supabase test db` cannot start."
    - "Every Dart plan in this phase compiles and tests green against a schema that does not exist until this plan runs; nothing before this point has touched the database."
---

<objective>
Apply the Phase 7 migration to the live database, verify the resulting schema by catalog read,
replay the pgTAP suite in a rolled-back transaction, and hold a human UAT checkpoint.

Purpose: D-37 makes this a dedicated blocking plan for a specific reason. In Phases 4 and 5 the
schema push was task 3 of a plan that reported success, and the phase ended with code in master
and no database behind it. Isolating the apply means a failure here is visible as a failed plan,
not as a footnote in a passing one.

Output: no source files. A verified live schema, a green pgTAP replay, and a human sign-off.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-expenses-by-paddock/07-CONTEXT.md
@.planning/phases/07-expenses-by-paddock/07-RESEARCH.md
@.planning/phases/07-expenses-by-paddock/07-VALIDATION.md
@.planning/phases/07-expenses-by-paddock/07-01-SUMMARY.md
</context>

<schema_push_requirement>
**[BLOCKING] Schema push required.** This phase modifies `supabase/migrations/*.sql`. The
detected ORM is Supabase. The nominal push command is `supabase db push`, but the CLI on this
host is authenticated and **unlinked with no TTY for a DB password** (STATE.md § Blockers,
unchanged since Phase 3), so the established and only working path is MCP `apply_migration`
against project `wrdwzychjhlpwpivfhhq`. This plan is marked `autonomous: false` accordingly.

Build and analyze will pass without this push — Dart types come from hand-written models, not
from the live database — which is exactly the false-positive verification state this task exists
to prevent. The phase CANNOT pass verification until task 1 completes.
</schema_push_requirement>

<!-- planner-discipline-allow: supabase db push -->

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply the migration to live PROD and verify by catalog read</name>
  <files>supabase/migrations/20260813_07_expenses_module.sql</files>
  <read_first>
    - supabase/migrations/20260813_07_expenses_module.sql — the exact file being applied (07-01)
    - .planning/STATE.md § Blockers — the Supabase CLI unlinked / Docker unavailable constraints and the Phase 5/6 `apply_migration` precedent, including the migration ledger count of 17
    - .planning/phases/07-expenses-by-paddock/07-RESEARCH.md § Common Pitfalls #1 — the expected failure mode if the trigger disable is missing
  </read_first>
  <action>
Preflight first, via MCP `execute_sql` on project `wrdwzychjhlpwpivfhhq`:
- Confirm `expenses` does not already exist.
- Record `SELECT count(*) FROM sanitary_applications` and
  `SELECT count(*) FROM sanitary_applications sa JOIN lots l ON l.id = sa.lot_id WHERE
  l.paddock_id IS NOT NULL` — the second count is how many rows the backfill can resolve. If the
  two numbers differ, STOP and report: a row whose lot has no resolvable paddock will fail the
  `SET NOT NULL` and abort the whole migration, and the fix is a decision the user must make, not
  a silent `COALESCE`.

Then apply `supabase/migrations/20260813_07_expenses_module.sql` verbatim via MCP
`apply_migration`. Do not attempt `supabase db push` — the CLI is unlinked with no TTY. Do not
edit the migration file to make it apply; if it fails, report the exact SQLSTATE and message and
stop. The most likely failure is `prevent_snapshot_mutation()`'s exception, which would mean the
DISABLE/ENABLE pair around the backfill is missing or mis-ordered (RESEARCH Pitfall 1).

After a successful apply, verify by catalog read via MCP `execute_sql`, capturing each result in
the SUMMARY:
1. `expenses` exists with all 12 columns and the expected types, including `amount numeric(14,2)`.
2. Both foreign keys exist (`property_id → properties.id`, `paddock_id → paddocks.id`).
3. `pg_get_triggerdef` for `trg_expenses_paddock_same_property` and
   `trg_expenses_set_updated_by` both return non-null.
4. `SELECT count(*) FROM pg_policies WHERE tablename = 'expenses'` returns 3, and
   `SELECT count(*) FROM pg_policies WHERE tablename = 'expenses' AND cmd = 'DELETE'` returns 0.
5. Neither the UPDATE policy's `qual` nor its `with_check` contains `deleted_at`.
6. Both indexes exist: `expenses_property_idx` and `expenses_paddock_date_idx`.
7. `sanitary_applications.paddock_id` and `paddock_name` exist and are both `NOT NULL`.
8. `SELECT id, paddock_id, paddock_name FROM sanitary_applications` returns every pre-existing
   row with non-null values (D-31).
9. `SELECT tgenabled FROM pg_trigger WHERE tgname = 'trg_snapshot_immutable'` returns `O` — the
   backfill's DISABLE left no residue.
10. `pg_get_functiondef` for `register_sanitary_application` contains `paddock_id` in its INSERT
    column list, and the same for `reverse_sanitary_application`.

Update the migration ledger count in `.planning/STATE.md` § Blockers from 17 to 18 with a dated
line, matching the wording of the Phase 5/6 entries.
  </action>
  <verify>
    <automated>MISSING — no local database exists; verification is the MCP catalog read enumerated in the action, with each of the 10 results recorded verbatim in 07-08-SUMMARY.md</automated>
    <human-check>All 10 catalog checks return the stated result, and the STATE.md ledger line reads 18 migrations</human-check>
  </verify>
  <acceptance_criteria>
    - MCP `apply_migration` returned success for `20260813_07_expenses_module.sql`
    - All 10 catalog checks are recorded in `07-08-SUMMARY.md` with their actual returned values, not a summary sentence
    - `pg_policies` for `expenses` shows exactly 3 rows and 0 with `cmd = 'DELETE'`
    - The UPDATE policy's `qual` and `with_check` contain no `deleted_at` reference
    - Every pre-existing `sanitary_applications` row has a non-null `paddock_id` and `paddock_name`
    - `trg_snapshot_immutable`'s `tgenabled` is `O`
    - `.planning/STATE.md` records the ledger at 18 with a dated entry
    - No edit was made to `20260813_07_expenses_module.sql` in order to make it apply
  </acceptance_criteria>
  <done>The Phase 7 schema exists in the live database exactly as authored, the snapshot trigger is back on, and the backfilled rows carry their paddock attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Replay the pgTAP suite and the full Dart suite</name>
  <files>supabase/tests/07_expenses_test.sql</files>
  <read_first>
    - supabase/tests/07_expenses_test.sql — the suite authored in 07-01
    - .planning/STATE.md § Blockers — the Phase 4/5/6 MCP replay precedent, including the known environmental failure classes (a global-state `count(*)` assertion and the three-argument `has_index` overload)
    - .planning/phases/07-expenses-by-paddock/07-VALIDATION.md § Sampling Rate — the phase gate definition
  </read_first>
  <action>
Replay `supabase/tests/07_expenses_test.sql` verbatim against live PROD via MCP `execute_sql`,
wrapped in `BEGIN … ROLLBACK` so no fixture row survives. `supabase test db` is not available —
Docker is unreachable on this host — and this replay is the standing workaround used successfully
for `04_movements_test.sql`, `05_reproductive_test.sql` and `06_sanitary_test.sql`.

Record the pass/fail count in the SUMMARY. Target: zero failures. For any failure, classify it
before doing anything else:
- A **schema defect** — fix it with a forward-only corrective migration applied via
  `apply_migration`, never by editing `20260813_07_expenses_module.sql` in place. That is the
  exact mistake that caused G-06-2.
- A **test-file defect** — fix the assertion in `07_expenses_test.sql` and document the fix in
  the suite header, as `06_sanitary_test.sql` does for its own `like`/`alike` defect.
Do not weaken an assertion to make it pass, and do not report a suite as green while any
assertion is failing.

After the replay, confirm no fixture rows leaked: re-run the row counts from task 1's preflight
and assert they are unchanged.

Then run the full Dart suite: `flutter test`. It must be green in its entirety, not just
`test/features/gastos/`. Record the total test count in the SUMMARY — it should exceed the 259
recorded at the end of Phase 6 by the tests this phase added.
  </action>
  <verify>
    <automated>cd F:/_geral/Projetos/campo_gestor &amp;&amp; flutter test</automated>
    <human-check>The MCP pgTAP replay reports 0 failures and the post-replay row counts match the preflight counts</human-check>
  </verify>
  <acceptance_criteria>
    - The pgTAP replay ran inside `BEGIN … ROLLBACK` and its full pass/fail line is recorded verbatim in `07-08-SUMMARY.md`
    - Failures, if any, are classified as schema defect or test-file defect with the resolution recorded; no assertion was weakened
    - Post-replay row counts for `expenses` and `sanitary_applications` equal the preflight counts
    - `flutter test` exits 0 and the total test count is recorded in the SUMMARY
    - Any corrective migration applied is a new file, and `20260813_07_expenses_module.sql` is unchanged since task 1
  </acceptance_criteria>
  <done>The pgTAP suite passes against the real schema with no residue, and the whole Dart suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT of the expense flow</name>
  <files>none — verification only, no file is modified by this task</files>
  <read_first>
    - .planning/phases/07-expenses-by-paddock/07-CONTEXT.md — D-07..D-23 and D-28..D-33, the decisions the checklist below exercises
    - .planning/phases/07-expenses-by-paddock/07-UI-SPEC.md § Copywriting Contract — the exact strings the app must show
    - .planning/phases/07-expenses-by-paddock/07-08-SUMMARY.md — task 1's catalog results and task 2's suite results, which must both be green before this checkpoint opens
  </read_first>
  <action>
Pause and hand control to the human. Present the `<how-to-verify>` checklist below verbatim,
numbered, and wait for the resume signal. Do not modify any file, do not run any repair, and do
not mark the phase complete until the human answers. If the human reports issues, record each one
verbatim in `07-08-SUMMARY.md` against its checklist number and stop — gap closure is a separate
planning cycle, not something to attempt inline.
  </action>
  <acceptance_criteria>
    - The human has responded with either "approved" or a numbered list of issues
    - Any reported issue is recorded verbatim in `07-08-SUMMARY.md` against its checklist number
    - No source file was modified during this task
  </acceptance_criteria>
  <verify>
    <human-check>The human types "approved" against the 12-item checklist, or enumerates the issues found</human-check>
  </verify>
  <done>A human has exercised GAST-01 and GAST-02 in the running app against the live database and either approved or enumerated the gaps.</done>
  <what-built>
The complete Phase 7 surface: the `expenses` table with owner+veterinarian write policies, a
cross-property isolation trigger and audit columns; frozen `paddock_id`/`paddock_name` on
`sanitary_applications` with the 2 pre-existing rows backfilled; the `/gastos/:paddockId` screen
with period presets, a category filter, a live total header and a unified list of manual expenses
plus read-only sanitary rows; the create/edit dialog with a pt-BR comma-decimal valor field; the
delete confirmation naming the exact value and date; and the "Gastos" summary card on the paddock
detail screen.
  </what-built>
  <how-to-verify>
Run the app against PROD (`flutter run -d edge`, or open the deployed build) and walk this list.
Confirm each item or describe what you saw instead.

1. **Entry point (D-09).** Open a piquete's detail screen. A "Gastos" card sits directly below
   the info card showing a R$ figure and the words "este mês". Tap it — you land on the expense
   screen for that same piquete.
2. **AppBar (D-14).** The title reads `Gastos — {nome do piquete}` and the back button returns to
   the piquete, not to a dead end.
3. **Create (GAST-01).** Tap "Novo gasto". The dialog has categoria, valor, data and descrição —
   and **no** piquete field. Leave everything blank and tap "Salvar gasto": you get validation
   errors, not a save. Now pick a categoria, type `1240,50` in valor (with a comma), pick a date
   in the current month, leave descrição blank, and save. The row appears at the top of the list
   with R$ 1.240,50.
4. **Total (GAST-02).** The header above the list shows the R$ total and the count in pt-BR
   ("1 lançamento" / "N lançamentos"). Add a second gasto — both the total and the count change.
5. **Period filter (D-16).** Switch between "Mês atual", "Mês passado", "Últimos 3 meses" and
   "Ano". The list and the header total both change. Tap "Personalizado", then **cancel** the
   date picker without choosing: nothing should change and the previous preset stays selected.
   Then pick a real range and confirm the list narrows.
6. **Category filter (D-07).** Filter by the categoria you used. The total respects both the
   period and the category at once.
7. **Sanitary crossover (D-29/D-32).** Pick a piquete that has a sanitary application (from
   Phase 6 UAT). Its cost appears as a row in this list with a "Sanitário" badge, is included in
   the total, and cannot be edited or deleted. Tapping it opens the aplicação detail screen.
   If that application's dose had no cost, the row shows "—" instead of a value and still counts
   in the "N lançamentos" figure.
8. **Edit (D-12).** Tap a manual gasto. The same dialog opens pre-filled, titled "Editar gasto".
   Change the valor and save — the row and the total both update.
9. **Delete (D-28).** Delete a gasto. The confirmation names the exact value and date
   ("Excluir gasto de R$ 1.240,50 de 13/08?"). Cancel — nothing happens. Confirm — the row
   disappears and the total drops. Turn on "Mostrar excluídos": the row reappears marked as
   excluded, and the total does **not** go back up.
10. **Empty states (D-13).** Open a piquete with no gastos: "Nenhum gasto lançado neste piquete".
    Now on a piquete that does have gastos, filter to a period with none: "Nenhum gasto no
    período selecionado" with a "Limpar filtro" button, and the total reads R$ 0,00.
11. **Role gate (D-23).** If you can sign in as an `owner` (not veterinarian): the "Novo gasto"
    FAB is present and you can lançar a gasto. As a `reader`, the FAB is absent entirely — not
    greyed out — and you can still read the list and the total.
12. **Filter does not persist (D-21).** Change the period, leave the screen, come back. It opens
    on "Mês atual" again.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found, numbered against the list above</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration file → live production database | `apply_migration` writes directly to the project holding real UAT data; there is no staging environment |
| test fixtures → production rows | The pgTAP replay creates fixture rows in the production database inside a transaction that must roll back |
| "phase complete" claim → actual deployed state | Every prior phase's build and analyze passed without the schema existing |

## STRIDE Threat Register (ASVS L1; high severity blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-31 | Denial of Service (data availability) | migration aborting mid-way on live PROD | high | mitigate | `apply_migration` runs the file as one transaction, so a failure rolls back entirely; task 1's preflight counts detect the one condition (a lot with no resolvable paddock) that would make `SET NOT NULL` fail, before the apply runs |
| T-07-32 | Tampering | immutability trigger left disabled on PROD | high | mitigate | Task 1 check 9 reads `pg_trigger.tgenabled` and requires `O`; the DISABLE/ENABLE pair is inside the same transaction, so an abort cannot leave it off |
| T-07-33 | Tampering | pgTAP fixtures persisting in production | medium | mitigate | The replay is wrapped in `BEGIN … ROLLBACK` and task 2 re-checks the preflight row counts afterwards to prove nothing leaked |
| T-07-34 | Repudiation | a phase sealing green with no schema behind it | high | mitigate | This whole plan exists as a dedicated blocking gate for exactly that failure, observed in Phases 4 and 5 (D-37); `autonomous: false` and a blocking human checkpoint prevent an automatic seal |
| T-07-35 | Tampering | an in-place edit of an applied migration causing dev/prod drift | medium | mitigate | Tasks 1 and 2 forbid editing `20260813_07_expenses_module.sql` after apply; corrections are forward-only new files, the discipline that resolved G-06-2 |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this plan or anywhere in Phase 7 (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- MCP `apply_migration` succeeded and all 10 catalog checks returned the expected values
- `07_expenses_test.sql` replayed with 0 failures and 0 leaked rows
- `flutter test` exits 0 across the whole suite
- A human has typed "approved" against the 12-item UAT list
</verification>

<success_criteria>
GAST-01 and GAST-02 are demonstrably true against the live database and the running app, with the
schema verified by catalog read rather than by a build that would have passed either way.
</success_criteria>

## Artifacts this phase produces

Every symbol below is CREATED by Phase 7 and does not exist in the codebase before this phase.

**SQL — `supabase/migrations/20260813_07_expenses_module.sql`:** table `expenses` (`id`, `property_id`, `paddock_id`, `category`, `amount`, `expense_date`, `description`, `created_by`, `updated_by`, `created_at`, `updated_at`, `deleted_at`); indexes `expenses_property_idx`, `expenses_paddock_date_idx`; policies `members_can_read_expenses`, `owner_vet_can_insert_expense`, `owner_vet_can_update_expense`; functions `enforce_expenses_paddock_same_property()`, `set_expenses_updated_by()`; triggers `trg_expenses_paddock_same_property`, `trg_expenses_set_updated_by`; new columns `sanitary_applications.paddock_id`, `sanitary_applications.paddock_name`; replaced bodies (signatures unchanged) `register_sanitary_application(uuid,uuid,date,jsonb,text)`, `reverse_sanitary_application(uuid,text)`

**pgTAP:** `supabase/tests/07_expenses_test.sql`

**Dart — `lib/features/gastos/data/`:** `expense_model.dart` (`Expense`, `ExpenseListItem`, `ManualExpenseItem`, `SanitaryExpenseItem`, `dateOf`, `amountOf`, `categoryKeyOf`, `sortExpenseItemsDesc`); `expense_constants.dart` (`kExpenseCategories`, `kExpenseCategoryLabels`, `kExpenseCategoryIcons`, `kExpenseFilterCategories`, `kSanitaryPseudoCategory`, `kSanitaryPseudoCategoryLabel`, `kSanitaryCategoryIcon`); `expense_calculations.dart` (`totalAmount`, `itemCount`, `filterByDateRange`, `filterByCategory`, `currentMonthRange`, `rangeForPreset`, `ExpensePeriodPreset`); `expense_repository.dart` (`ExpenseRepository`, `buildUnifiedExpenseItems`, `expenseRepositoryProvider`, `unifiedExpenseListByPaddockProvider`, `unifiedExpenseListWithDeletedByPaddockProvider`, `paddockMonthExpenseTotalProvider`)

**Dart — `lib/core/auth/role_gates.dart`:** `canManageExpenses`

**Dart — `lib/features/gastos/presentation/`:** `gastos_screen.dart` (`GastosScreen`); `expense_form_dialog.dart` (`ExpenseFormDialog`, `confirmDeleteExpense`); `_expense_list_item_card.dart` (`ExpenseListItemCard`); `paddock_expense_summary_card.dart` (`PaddockExpenseSummaryCard`)

**Dart — modified files, new symbols only:** `AppRoutes.gastosById`, `AppRoutes.gastosPorPiquete` (`lib/core/router/routes.dart`); the `GoRoute(path: AppRoutes.gastosById, …)` registration (`lib/core/router/router.dart`); the `PaddockExpenseSummaryCard` insertion in `lib/features/piquetes/presentation/paddock_detail_screen.dart` (no new symbol declared there)

**Tests:** `test/features/gastos/expense_calculations_test.dart`, `test/features/gastos/role_gates_test.dart`, `test/features/gastos/expense_unified_list_test.dart`, `test/features/gastos/expense_form_dialog_test.dart`, `test/features/gastos/paddock_expense_card_test.dart`

<output>
Create `.planning/phases/07-expenses-by-paddock/07-08-SUMMARY.md` when done
</output>
