---
phase: 00-foundation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - pubspec.yaml
  - pubspec.lock
autonomous: true
requirements: []
nyquist_compliant: true
tags: [foundation, packages, codegen]

must_haves:
  truths:
    - "All Phase 0 stack packages installed with constraints that prevent accidental upgrade to Riverpod 3.x or Freezed 2.x"
    - "Codegen pipeline (build_runner + freezed + riverpod_generator) executes end-to-end without error"
    - "flutter pub get produces a deterministic pubspec.lock"
    - "flutter analyze remains clean after package changes"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Full Phase 0 dependency manifest per CONTEXT.md D-09"
      contains: "flutter_riverpod"
    - path: "pubspec.lock"
      provides: "Locked versions for reproducible builds"
  key_links:
    - from: "pubspec.yaml"
      to: "analysis_options.yaml"
      via: "custom_lint plugin reference activated by riverpod_lint dev dependency"
      pattern: "riverpod_lint"
---

<objective>
Install the complete Phase 0 dependency stack in pubspec.yaml per D-09, with version constraints that lock Riverpod to 2.x (D-09 + Pitfall 1) and Freezed to 3.x (RESEARCH.md correction to CLAUDE.md). Validate that codegen pipeline executes cleanly even though Phase 0 has no domain models yet (D-10).

Purpose: Future plans (04, 05, 06) and all later phases consume these packages. Getting versions right NOW prevents a painful retrofit. Locked Riverpod constraint `>=2.6.1 <3.0.0` (and matching `riverpod_annotation`, `riverpod_generator`) is the only protection against transitive upgrade to 3.x per Pitfall 1.

Output: Single updated pubspec.yaml file. `flutter pub get` resolves cleanly. `dart run build_runner build` exits 0 (no-op since no annotations exist yet, but pipeline proven). `flutter analyze` clean. Test scaffolds from Plan 01 still pass (and integration_test import can now be uncommented if desired).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@CLAUDE.md
@pubspec.yaml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update pubspec.yaml with full Phase 0 stack + run flutter pub get</name>
  <files>pubspec.yaml, pubspec.lock</files>
  <read_first>
    - .planning/phases/00-foundation/00-RESEARCH.md (Standard Stack section, lines 105-138)
    - .planning/phases/00-foundation/00-CONTEXT.md (D-09, D-10)
    - CLAUDE.md (Recommended Stack section — note: freezed cited as 2.4.4 here but RESEARCH.md confirms 3.x is current; we follow RESEARCH.md)
    - pubspec.yaml (current state — only cupertino_icons + flutter_lints)
  </read_first>
  <action>
    REPLACE the `dependencies:` and `dev_dependencies:` sections of pubspec.yaml with the following EXACT content (keep the rest of pubspec.yaml — name, description, environment, flutter section — intact):

```yaml
dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # Cupertino icons (Flutter template default — kept).
  cupertino_icons: ^1.0.8

  # ─── State Management (LOCKED to 2.x per CONTEXT.md D-09 + RESEARCH.md Pitfall 1) ───
  # DO NOT loosen these constraints. Riverpod 3.x is incompatible with our patterns.
  flutter_riverpod: ">=2.6.1 <3.0.0"
  riverpod_annotation: ">=2.6.1 <3.0.0"

  # ─── Navigation (web-friendly per CLAUDE.md; latest stable) ───
  go_router: ^17.2.0

  # ─── Backend client ───
  supabase_flutter: ^2.12.0

  # ─── Data classes & serialization (Freezed 3.x per RESEARCH.md Standard Stack) ───
  freezed_annotation: ^3.0.0
  json_annotation: ^4.11.0

  # ─── Local storage (Hive/Isar PROHIBITED in MVP — see CLAUDE.md What NOT to Use) ───
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.5.0

  # ─── UI helpers ───
  flutter_svg: ^2.2.0
  data_table_2: ^2.7.0

  # ─── Localization (pt-BR per D-13) ───
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # ─── Codegen (per D-10 — pipeline configured but unused until Phase 1) ───
  build_runner: ^2.14.0
  freezed: ^3.2.0
  json_serializable: ^6.13.0
  riverpod_generator: ">=2.6.5 <3.0.0"

  # ─── Lints (custom_lint enables riverpod_lint per analysis_options.yaml) ───
  custom_lint: ^0.6.0
  riverpod_lint: ^2.3.0

  # ─── Mocking ───
  mocktail: ^1.0.5
```

    After editing pubspec.yaml, run:
```bash
rtk flutter pub get
```

    If `flutter pub get` reports a resolver conflict that pushes Riverpod to 3.x, FAIL the task — do not loosen the constraint. The constraint `>=2.6.1 <3.0.0` is non-negotiable per CONTEXT.md D-09 and RESEARCH.md Pitfall 1. If conflict: read the resolver output, identify the offending transitive dependency, and either pin it tighter or report blocker.

    Inspect `pubspec.lock`: confirm `flutter_riverpod` version starts with `2.` (not `3.`) and `freezed` starts with `3.` (not `2.`).
  </action>
  <verify>
    <automated>flutter pub get 2>&1 | tail -20 && grep -A1 "  flutter_riverpod:" pubspec.lock | head -3 && grep -A1 "  freezed:" pubspec.lock | head -3 && flutter analyze --no-pub 2>&1 | (! grep -E "^\s*error")</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` contains exact constraint `flutter_riverpod: ">=2.6.1 <3.0.0"`.
    - `pubspec.yaml` contains exact constraint `riverpod_annotation: ">=2.6.1 <3.0.0"`.
    - `pubspec.yaml` contains exact constraint `riverpod_generator: ">=2.6.5 <3.0.0"`.
    - `pubspec.yaml` contains `freezed_annotation: ^3.0.0` and `freezed: ^3.2.0` (3.x — supersedes CLAUDE.md 2.4.4).
    - `pubspec.yaml` contains `go_router: ^17.2.0`.
    - `pubspec.yaml` contains `supabase_flutter: ^2.12.0`.
    - `pubspec.yaml` contains `intl: ^0.20.0`.
    - `pubspec.yaml` contains `flutter_localizations: sdk: flutter`.
    - `pubspec.yaml` contains `integration_test: sdk: flutter` in dev_dependencies.
    - `pubspec.yaml` contains `mocktail: ^1.0.5` in dev_dependencies.
    - `pubspec.yaml` contains `custom_lint: ^0.6.0` and `riverpod_lint: ^2.3.0` in dev_dependencies.
    - `flutter pub get` exits 0.
    - `pubspec.lock` exists; `grep "  flutter_riverpod:" pubspec.lock` followed by version line shows `version: "2.` (NOT `3.`).
    - `pubspec.lock` `freezed:` resolved version starts with `3.`.
    - `flutter analyze --no-pub` produces no `error` severity diagnostics (warnings/info acceptable).
    - `flutter test --no-pub` still exits 0 (Plan 01 scaffolds remain green).
  </acceptance_criteria>
  <done>pubspec.yaml has all 14 production deps + 9 dev deps with locked constraints; flutter pub get clean; analyze clean; test scaffolds still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate codegen pipeline (build_runner end-to-end no-op)</name>
  <files>(no production files; verifies pipeline only)</files>
  <read_first>
    - .planning/phases/00-foundation/00-CONTEXT.md (D-10: pipeline configured, NOT used in Phase 0)
    - .planning/phases/00-foundation/00-RESEARCH.md (Pitfall 5: --delete-conflicting-outputs flag)
  </read_first>
  <action>
    Run the codegen pipeline to confirm it executes cleanly even with zero `@freezed` / `@riverpod` annotations in the codebase. Per D-10, Phase 0 must prove the pipeline works; Phase 1 will add the first real annotations.

    Commands:
```bash
rtk dart run build_runner build --delete-conflicting-outputs
```

    Expected behavior: command exits 0 with output similar to "Succeeded after 1.2s with N outputs" where N may be 0 (since no annotated files exist). If the command reports MISSING package errors, re-run `flutter pub get` and try again. If it reports actual build failures, that indicates a package version conflict — investigate and report.

    DO NOT create any `.g.dart` or `.freezed.dart` files manually. Per .gitignore added in Plan 02, these are not committed; they are regenerated by future phases.

    DO NOT add domain models or annotated providers in this plan. That is Plan 04+ work.

    Run the static check chain to ensure nothing regressed:
```bash
rtk flutter analyze
rtk flutter test --no-pub
```
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&1 | grep -E "(Succeeded|Built|with [0-9]+ outputs)" && flutter analyze --no-pub 2>&1 | (! grep -E "^\s*error") && flutter test --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - `dart run build_runner build --delete-conflicting-outputs` exits 0.
    - Output of build_runner contains "Succeeded" OR "with 0 outputs" (zero generated files is acceptable since no annotations exist).
    - `flutter analyze --no-pub` exits 0 with no `error`-severity diagnostics.
    - `flutter test --no-pub` exits 0 (Plan 01 test scaffolds remain green).
    - No `.g.dart` or `.freezed.dart` files committed (`git status` does not show them as new tracked files; .gitignore covers them).
    - `git status --short pubspec.lock` shows pubspec.lock as modified/added (we DO commit pubspec.lock for reproducibility).
  </acceptance_criteria>
  <done>Codegen pipeline runs end-to-end clean; analyze and test still green; no codegen artifacts committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → local cache | Packages downloaded from pub.dev (signed); transitive deps may pull from any pub.dev source |
| pubspec.lock → CI/peer machines | Locked versions reproduce exact tree; lockfile drift = supply chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-08 | T (Tampering) | flutter_riverpod resolving to 3.x | mitigate | Hard upper bound `<3.0.0` in pubspec.yaml; verified via pubspec.lock inspection |
| T-00-09 | T (Tampering) | Transitive dependency pulling vulnerable code | accept | Scope is local dev; Phase 1+ adds dependency review gate |
| T-00-10 | I (Information Disclosure) | pubspec.lock containing private repo URLs | accept | We use only pub.dev sources; no git/path deps |
</threat_model>

<verification>
- `flutter pub get` exits 0.
- `flutter analyze --no-pub` exits 0 (no error-severity).
- `flutter test --no-pub` exits 0.
- `dart run build_runner build --delete-conflicting-outputs` exits 0.
- `pubspec.lock` shows `flutter_riverpod` version 2.x (not 3.x) and `freezed` version 3.x.
- No `.g.dart` / `.freezed.dart` files in git index.
</verification>

<success_criteria>
1. All Phase 0 packages from D-09 installed with locked constraints (Riverpod 2.x, Freezed 3.x).
2. Codegen pipeline runs end-to-end without error (D-10 satisfied).
3. analyze + test still pass — Plan 01 scaffolds didn't regress.
4. pubspec.lock committed for reproducibility.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md` documenting the resolved versions of flutter_riverpod, freezed, go_router, supabase_flutter (from pubspec.lock), and confirmation that codegen pipeline ran clean.
</output>
