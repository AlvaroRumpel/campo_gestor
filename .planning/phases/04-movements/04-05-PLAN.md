---
phase: 04-movements
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/lotes/presentation/mover_lote_dialog.dart
  - lib/features/animais/presentation/mover_animal_dialog.dart
  - test/widget/mover_lote_dialog_test.dart
  - test/widget/mover_animal_dialog_test.dart
autonomous: true
gap_closure: true
requirements: [MOV-01, MOV-02]
must_haves:
  truths:
    - "After a successful lot move, MoverLoteDialog invalidates animalListByPropertyProvider AND loteListByPropertyProvider (plus the existing loteByIdProvider + both loteListByPaddockProvider) so AnimaisScreen and the animal-move lot picker no longer show stale paddock/lot data (WR-01, WR-02)"
    - "Both move dialogs check `mounted` immediately after the awaited mutation before touching `ref`, so a dialog disposed mid-await cannot throw and mislabel a succeeded move as a failure (WR-03)"
    - "MoverLoteDialog info text reads '1 animal será transferido' when count == 1 and the plural form otherwise (WR-04)"
    - "A widget test taps 'Confirmar movimentação' in MoverAnimalDialog and in MoverLoteDialog and asserts the mutation fires, the success SnackBar copy shows, and (lote) the two new invalidations fire — closing the behavior_unverified items (IN-01)"
  artifacts:
    - lib/features/lotes/presentation/mover_lote_dialog.dart
    - lib/features/animais/presentation/mover_animal_dialog.dart
    - test/widget/mover_lote_dialog_test.dart
    - test/widget/mover_animal_dialog_test.dart
  key_links:
    - "MoverLoteDialog._submit -> ref.invalidate(animalListByPropertyProvider) + ref.invalidate(loteListByPropertyProvider)"
    - "MoverAnimalDialog._submit / MoverLoteDialog._submit -> `if (!mounted) return;` guard placed immediately after the awaited mutation, before any ref.invalidate"
---

<objective>
Close the four reviewer warnings + the untested-submit-flow gap from 04-REVIEW.md / 04-VERIFICATION.md without touching the RPC path (owned by 04-04). Fix MoverLoteDialog's missing invalidations (WR-01/WR-02), the invalidate-before-mounted ordering in both dialogs (WR-03), and the pt-BR plural bug (WR-04); then add submit-flow widget tests that actually tap Confirm for BOTH dialogs (IN-01), closing the three behavior_unverified items that automation can cover.

Purpose: Post-move UI shows correct paddock/lot state everywhere; a succeeded move is never reported as a failure; and the tap→confirm→success→SnackBar path is exercised by tests rather than assumed.
Output: Two hardened dialogs; two extended widget-test files with real submit-flow coverage.
</objective>

<execution_context>
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/workflows/execute-plan.md
@F:/_geral/Projetos/campo_gestor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-movements/04-REVIEW.md
@.planning/phases/04-movements/04-VERIFICATION.md
@.planning/phases/04-movements/04-UI-SPEC.md

# Files to fix:
@lib/features/lotes/presentation/mover_lote_dialog.dart
@lib/features/animais/presentation/mover_animal_dialog.dart
# Provider names to invalidate (plain FutureProviders, invalidate without args):
@lib/features/lotes/data/lote_repository.dart
@lib/features/animais/data/animal_repository.dart
# Existing test harnesses to extend (fake-repo + ProviderScope override pattern):
@test/widget/mover_lote_dialog_test.dart
@test/widget/mover_animal_dialog_test.dart
# Parent that shows the SnackBar from the pop result (mirror in the test host):
@lib/features/animais/presentation/animal_detail_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MoverLoteDialog — invalidations (WR-01/WR-02), mounted guard (WR-03), plural (WR-04)</name>
  <files>lib/features/lotes/presentation/mover_lote_dialog.dart</files>
  <read_first>
    - lib/features/lotes/presentation/mover_lote_dialog.dart (_submit lines 40-68; info Text line 86)
    - 04-REVIEW.md (WR-01, WR-02, WR-03, WR-04 — exact fixes)
    - lib/features/lotes/data/lote_repository.dart (loteListByPropertyProvider is a plain FutureProvider — invalidate with no arg)
    - lib/features/animais/data/animal_repository.dart (animalListByPropertyProvider is a plain FutureProvider — invalidate with no arg)
  </read_first>
  <action>
    Add an import for the animais repository so animalListByPropertyProvider is in scope: `import '../../animais/data/animal_repository.dart';` (loteListByPropertyProvider already comes from the existing lote_repository import).

    In _submit(), reorder + extend the success path. Immediately after `await ref.read(loteRepositoryProvider).moveLot(...)` returns, add `if (!mounted) return;` BEFORE any ref.invalidate call (WR-03). Then perform ALL invalidations: the existing three (loteByIdProvider(widget.lot.id), loteListByPaddockProvider(oldPaddockId), loteListByPaddockProvider(paddockId)) PLUS the two new ones — ref.invalidate(animalListByPropertyProvider) (WR-01) and ref.invalidate(loteListByPropertyProvider) (WR-02). Then call Navigator.pop(context, {'paddockName': selectedName}) unconditionally (mounted already checked). Leave the catch/finally blocks as-is.

    WR-04 (plural): replace the info Text child so it renders the singular form "1 animal será transferido para o novo piquete. A operação é atômica — ou todos movem ou nenhum." when widget.activeAnimalCount == 1, and the existing plural form "<count> animais serão transferidos para o novo piquete. A operação é atômica — ou todos movem ou nenhum." otherwise. Match 04-UI-SPEC.md copy.
  </action>
  <verify>
    <automated>flutter analyze lib/features/lotes/presentation/mover_lote_dialog.dart && flutter test test/widget/mover_lote_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - _submit invalidates all FIVE providers (3 existing + animalListByPropertyProvider + loteListByPropertyProvider).
    - `if (!mounted) return;` sits between the awaited moveLot and the first ref.invalidate; Navigator.pop is no longer wrapped in `if (mounted)`.
    - Info text is singular for count == 1, plural otherwise.
    - flutter analyze clean; existing lote-dialog tests still pass.
  </acceptance_criteria>
  <done>MoverLoteDialog invalidates all five providers, guards mounted before ref use, and renders grammatically-correct singular/plural info text.</done>
</task>

<task type="auto">
  <name>Task 2: Fix MoverAnimalDialog — mounted guard (WR-03)</name>
  <files>lib/features/animais/presentation/mover_animal_dialog.dart</files>
  <read_first>
    - lib/features/animais/presentation/mover_animal_dialog.dart (_submit lines 38-65; invalidations already correct at 52-55)
    - 04-REVIEW.md (WR-03)
  </read_first>
  <action>
    In _submit(), immediately after `await ref.read(animalRepositoryProvider).moveAnimal(...)` returns, add `if (!mounted) return;` BEFORE the four existing ref.invalidate calls (animalByIdProvider, animalListByLotProvider(oldLotId), animalListByLotProvider(lotId), animalListByPropertyProvider). Then call Navigator.pop(context, {'lotName': selectedName}) unconditionally, replacing the current `if (mounted) Navigator.pop(...)`. Do NOT change the invalidation set (this dialog already invalidates the correct four — WR-01/WR-02 apply only to the lote dialog). Leave catch/finally as-is.
  </action>
  <verify>
    <automated>flutter analyze lib/features/animais/presentation/mover_animal_dialog.dart && flutter test test/widget/mover_animal_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `if (!mounted) return;` sits between the awaited moveAnimal and the first ref.invalidate; Navigator.pop is unconditional afterwards.
    - Invalidation set unchanged (four providers); flutter analyze clean; existing animal-dialog tests still pass.
  </acceptance_criteria>
  <done>MoverAnimalDialog guards mounted immediately after the awaited move; invalidation set unchanged; analyze clean.</done>
</task>

<task type="auto">
  <name>Task 3: Submit-flow behavior tests for both dialogs (IN-01, closes behavior_unverified)</name>
  <files>test/widget/mover_animal_dialog_test.dart, test/widget/mover_lote_dialog_test.dart</files>
  <read_first>
    - test/widget/mover_animal_dialog_test.dart + test/widget/mover_lote_dialog_test.dart (existing fake-repo + ProviderScope override harness — extend, don't rewrite)
    - lib/features/animais/presentation/animal_detail_screen.dart (onMover: pops → parent shows SnackBar 'Animal movido para {lotName}' — mirror this in the test host)
  </read_first>
  <action>
    Extend both test files with a real tap-to-submit test. Launch each dialog from a host widget via showDialog so the pop result is capturable, and have the host reproduce the parent's behavior: on a non-null result map, show a SnackBar with the parent's copy ('Animal movido para {lotName}' for the animal dialog, 'Lote movido para {paddockName}' for the lote dialog). Each new test: pump the host, open the dialog, tap the destination ListTile ('Lote Destino' / 'Piquete Destino') to enable Confirm, tap find.widgetWithText(FilledButton, 'Confirmar movimentação'), pumpAndSettle.

    Animal dialog test — assert: the fake AnimalRepository recorded a moveAnimal call with id 'animal-7' and newLotId 'lot-target' (extend _FakeAnimalRepo to capture the args); the dialog closed; and find.text('Animal movido para Lote Destino') is found (SnackBar via the host).

    Lote dialog test — assert: the fake LoteRepository recorded a moveLot call (extend _FakeLoteRepo to capture lotId 'lot-7' + newPaddockId 'pad-target'); the dialog closed; find.text('Lote movido para Piquete Destino') is found; AND both new invalidations fired. Prove the invalidations with a mechanism that FAILS if either ref.invalidate is removed — e.g. override animalListByPropertyProvider and loteListByPropertyProvider with counting fakes, add an always-mounted probe Consumer that ref.watch()es both, and assert each fetch counter incremented after Confirm (initial build + post-invalidate refetch). A ProviderObserver capturing disposal is an acceptable alternative only if the providers are first read by a probe so disposal actually fires.

    Add one more lote test for WR-04: pump the dialog with activeAnimalCount: 1 and assert find.textContaining('1 animal será transferido') is found and find.textContaining('1 animais') findsNothing.

    Keep the existing static-render tests intact.
  </action>
  <verify>
    <automated>flutter test test/widget/mover_animal_dialog_test.dart test/widget/mover_lote_dialog_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Each dialog has a test that taps Confirm and asserts (a) the mutation fired with the expected params, (b) the dialog closed, (c) the correct SnackBar copy shows via the host.
    - The lote test additionally fails if either animalListByPropertyProvider or loteListByPropertyProvider invalidation is removed from _submit.
    - The WR-04 singular test passes (count == 1 renders '1 animal será transferido', not '1 animais').
    - Full `flutter test` suite green.
  </acceptance_criteria>
  <done>Both dialogs have a submit-flow test asserting mutation + SnackBar; the lote test fails if either new invalidation regresses; WR-04 singular test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog widget lifecycle → provider cache | A dialog disposed mid-await must not corrupt provider-cache state or misreport the move's outcome |

No new server/tenant boundary is introduced here. The multi-tenant isolation threat (T-4-01, cross-property animal move) is owned and mitigated server-side by plan 04-04's move_animal_to_lot RPC.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-07 | Tampering (stale cache / integrity) | MoverLoteDialog._submit invalidations | low | mitigate | Add animalListByPropertyProvider + loteListByPropertyProvider invalidations (Task 1) so post-move UI cannot display stale cross-feature paddock/lot data (WR-01/WR-02) |
| T-4-08 | Repudiation / false-failure | both dialogs' mounted ordering | low | mitigate | Check `mounted` before ref use post-await (Tasks 1–2) so a succeeded move is never surfaced as "Erro ao mover" or silently swallowed (WR-03) |
| T-4-SC | Tampering | package installs | low | accept | No new npm/pub/cargo packages added — dialog edits + test extensions only; nothing to install or audit |

No high/critical threats introduced by this plan. ASVS L1 block-on-high satisfied.
</threat_model>

<verification>
- `flutter analyze lib/features/lotes/presentation/mover_lote_dialog.dart lib/features/animais/presentation/mover_animal_dialog.dart` — no issues.
- `flutter test test/widget/mover_animal_dialog_test.dart test/widget/mover_lote_dialog_test.dart` — green, including the new submit-flow + WR-04 tests.
- `flutter test` — full suite green (regression).
</verification>

<success_criteria>
- MoverLoteDialog invalidates all five providers; both dialogs guard `mounted` before ref use; lote info text is grammatically correct for count == 1.
- Submit-flow tests for both dialogs tap Confirm and assert mutation + SnackBar; the lote test fails if either new invalidation regresses.
- All three automatable behavior_unverified items closed (the RPC-live item stays a human/UAT item, owned by 04-04's push).
</success_criteria>

<output>
Create `.planning/phases/04-movements/04-05-SUMMARY.md` when done. Record: the WR-01..04 fixes, the mounted-guard reorder in both dialogs, and the new submit-flow test coverage.
</output>
